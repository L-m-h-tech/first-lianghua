# 上下文摘要（2026-09-04，更新至**第64轮 G25续按H对齐非重叠再平衡+ATR14表达式化逐位parity + G22续可交易性掩码(涨跌停/交割日历) + G23续carry换手/容量(pytest 742全绿，VERSION 0.64.0/tag v0.64.0，研究侧零改动主链不跑双哈希即隔离证明，G5④确认第51轮已清账)**。G2双轨不删、push已全完成、git代理karing已配。
# （历史）上下文摘要（2026-09-04，更新至**第61轮 三线：①用户拍板把 PLUGIN_PARTS_ENABLED 默认翻 True——G2 真正切换到注册表取分、内联 9 part 完整保留做双轨观察、置 False 一键回退，默认on与强制off双哈希都=长期基线 c4da4cdf/50dcc80；②G25续：factor_expr DSL 新增 ts_ema/ts_rma 两个状态递推算子（SMA播种、因果无未来、可嵌套），MACD dif/dea/hist 三序列（嵌套 ts_ema）与 Wilder RSI14 全部表达式化并对 futures_data 过程式 float.hex 逐位 parity，LIBRARY+4、factors_catalog 34→38 条 research；③新增 tools/orthogonal_blend_oos.py 把 orthogonal_ic_blend 接真实 research_panel.db（64品种/6.1万行/4年）做月度扩展窗 walk-forward 样本外对照（正交IC vs 等权 vs 6单因子日度截面RankIC，负结果照实、reports+ledger、研究侧不进综合分）。pytest 713→718 全绿、代码 commit cacd0f8；VERSION 仍 0.52.0 未 bump/tag、仅本地未 push。以下最新块=第61轮，旧轮块依次存档**）
## 零、最新快速接续摘要（第64轮，先读这里）
- **第64轮（2026-09-04；pytest 738→742全绿 0fail/0err/0skip，junit=cache/r64_junit.xml；VERSION 0.63→0.64、tag v0.64.0；零新增运行依赖）**：用户点名一次推进四项并允许借鉴GitHub——**G25续（分层组合改进+ATR14表达式化）、G22续（涨跌停掩码/交割日历）、G23续（carry换手/容量）、G5④（risk_gate熔断动作层）**。先审计磁盘：**G5④ 在第48/50/51轮已完整落地（circuit_breaker 三动作模式 observe/paper_halt/paper_delever + paper_broker._delever_cut 自动减仓 + tools/circuit_review 阈值校准 + 18测试），本轮如实标注不重复开发**；真正做 G25续/G22续/G23续 三项（全研究侧/tools，main/analyzer/futures_data/portfolio/paper_broker 零 import 新模块=隔离合规，按铁律研究侧不改主链故不重跑双哈希）。
  - **① G25续（a）分层组合改进：orthogonal_blend_oos 新增 `evaluate_ls_books_aligned(books, score_key, n_q, cost_oneway, hold)`——按 H 对齐的非重叠再平衡**：每 hold 个交易日只在一个调仓日重算多空权重并记一笔 [调仓日,调仓日+hold) 持有期收益，期与期不重叠，**净收益=Σ期收益-Σ期换手成本可真实复利**（消除第63轮 H>1 前向收益日序重叠导致的"累计/回撤仅作相对比较"限制）；run() 对每个 H 同时输出 ls_orth_aligned/ls_equal_aligned 并在报告新增 "[按H=%d对齐·非重叠再平衡]" 行；selftest 增 hold=1/2 断言（期数约两倍、零成本净=毛、正成本净≤毛、hold=1 逐日=原逐日口径累计）。
    - 真实64品种面板（1017交易日）：**H5 对齐正交净年化-13.89%（等权+0.20%）**、调仓期175；**H20 对齐正交净年化+24.90%/净夏普0.99/日均换手0.627（等权+4.76%）**、调仓期43——消除重叠后比原逐日重叠口径（H20 等权+46.72%是重叠虚高）更真实，正交相对等权的优势在 H20 显现。
  - **② G25续（b）ATR14/TR 表达式化 + 逐位 parity**：factor_legacy_expr 新增 `ATR14_EXPR="ts_mean(max(max(high-low,abs(high-delay(close,1))),abs(low-delay(close,1))),14)"`（TR 非 close-only、吃 high/low/前收；DSL max 只接受二元故嵌套）+ `procedural_atr_series`（逐字复刻 compute_indicators 的 TR/ATR）+ `parity_atr`（t>=14 处同求和序 **float.hex 逐位相等**，400+对零 mismatch）；无未来断言只要求"改最后一根不影响 t<末根"（ATR 窗含当前根自身 TR，末根输出合法受影响）；factor_expr.LIBRARY +expr_atr14（22→23条）、factors_catalog 43→44条（introduced=64，全 research 不进综合分）；selftest 11→12组。
  - **③ G22续 可交易性掩码：新增 `tools/tradable_mask.py`（纯标准库零网络、只读 G21 面板、研究侧）**：纯函数 `locked_flags`（复用 backtest._locked_limit 口径：收盘贴最高/最低且涨跌幅达品种常态板幅 config.FUTURES_LIMIT_MOVE 判疑似锁板）、`nearest_delivery_days`（距交割月1号自然日，月首近似、纯函数可手算）、`mask_for_panel`（每 sym×date 计算 {locked, near_delivery, tradable}）、`summarize`（品种级/板块级占比）。输出 reports/tradable_mask.txt+.json。真实64品种 61353 样本：疑似锁板仅 3 例（日线贴板极罕见）、距交割月1号≤15天占 46.8%（15天窗口在月度交割结构下约一半自然日，符合预期）；结论=日线研究锁板剔除影响可忽略、看似高"临近交割占比"是月度窗口统计口径、真 HP/SP 仍待 G22 分类持仓。selftest 5组（手算锁板/交割天数/合成面板/汇总/报告结构）。
  - **④ G23续 carry 换手/容量**：carry_points_from_adjusted 每 point 增 v(成交量)/oi(总持仓)/vol_turn(换手率代理)/amount(成交额代理=收盘×量)；build_report 新增"七·补、换手与容量"章节——每调仓日多空腿成交额合计中位数、按参与率1%估单期容量、多空腿总成员数（数量级估算、**精确容量仍待 G14 一档盘口**，诚实标注）。真实24品种面板：中位成交额约151.5万元×日、1%参与率容量≈1.5万元、平均9品种/期。selftest 补字段后全过。
  - **验证**：pytest 738→742 全绿（新增 test_tradable_mask_selftest + compileall 收录新 tools 文件 + LIBRARY 编译循环覆盖 expr_atr14）；factor_expr/factor_legacy_expr/orthogonal_blend_oos/carry_eval/tradable_mask 各 selftest 全过；**隔离 grep：main/analyzer/futures_data/portfolio/paper_broker 零提及 factor_legacy_expr/tradable_mask**（研究侧增量铁律守）；双哈希不重跑（本轮零改动主链）。
  - **git**：本轮 6 文件改动+1 新文件（tools/tradable_mask.py）；VERSION=0.64.0、tag v0.64.0；push 走既有 karing 代理（127.0.0.1:3067，karing 运行中即自动）。
  - **下一站（第65轮候选）**：①G25续=更多过程式量表达式化（ATR 已清，可做日内/量仓类；表达式因子自动挖掘受红线门控）；②G22续=tradable_mask 接 carry_eval/xsmom 的截面剔除（掩码后重做多空对照，验证是否改变结论）；③G23续=carry 换手/容量接真实逐合约成交额（term_history 有合约级 vol/oi）、持仓侧容量（待G14一档盘口）；④G16 浅ML 等 build_ml_samples 积累；⑤G1 纸面影子≥4周后三方对账。旧阻塞不变：G14(一档盘口)/G16(ml_samples)/G13(LLM key)/G17(期权PCR·IV)/G18(Tushare积分)/G20(远期)；G5④ 已确认清账。
## 零.旧、最新快速接续摘要（第63轮，已被第64轮接续，保留存档）
- **第63轮（2026-09-04；pytest 733→738全绿 0fail/0err/0skip，junit=cache/r63_junit.xml；VERSION 0.62→0.63、tag v0.63.0；零新增运行依赖）**：用户三拍板=①G2**不删**analyzer内联冗余（维持注册表取分+内联双轨，回退 PLUGIN_PARTS_ENABLED=False；本轮不改G2代码只记决定）②确定git push（**但 git remote -v 为空、仓库从未配远程，缺远程URL无法push，需用户给地址/建仓，本轮只做本地commit+tag**）③VERSION每轮对齐轮次并bump/tag。续做=G1纸面引擎补OMS/成交回报/持仓对账 + G25续 KDJ/EMA表达式化 + OOS加成本/换手/分层多空；G16仍阻塞ml_samples；G3/G5摘要早已完成、G22续/G23续为研究边缘候选（真HP/SP待G22分类持仓），本轮如实标注不强造。
  - **① G1 paper_broker.py 补 OMS/成交回报/持仓对账（先审计只补缺口，既有委托状态机/三表/账户视图不动，默认撮合逐字节不变）**：
    - 纯函数 `reconcile_position_sets(internal,external,price_tol)`：逐品种对账，五类 break=missing_external(内部幽灵仓)/missing_internal(内部漏记)/direction(方向反)/lots(手数不符带lots_delta=外部-内部)/entry_price(开仓价差超容差，None=不比价)，返回 matched/breaks/n_matched/n_breaks/clean；`aggregate_fills(fills)` 成交回报聚合（笔数/手数/名义/费/滑点/已实现/多空开平/强平）。
    - PaperBroker：__init__ 加内存级 `_orders_by_id`(id→最新委托快照)/`fill_ledger`(全部成交)，_ins/_upd_order、_ins_trade 纯增量挂账（不改撮合返回值）；restore 末尾从 paper_orders_recent/paper_trades_recent(DESC→[::-1]升序)回填，跨进程连续。
    - 新方法：`orders_view(status,sym)` OMS全状态台账（不只pending；DB模式以三表为准、(ts,id)升序）、`cancel_order(sym=/order_id=,reason)` 主动撤在途挂单并返回撤单数（只动pending不碰已成交）、`fills_view(sym,side,since)`/`fill_report(since)` 成交回报流水与汇总、`reconcile_positions(external,price_tol)` 内部持仓vs外部台账、`reconcile_against_db()` 用三表未平开仓腿重建外部持仓做自洽对账（纯内存db=None返回None）。
    - selftest 11→14组；tests/test_paper_broker.py +5例（纯函数五类break/aggregate聚合/OMS台账+主动撤单/成交回报+broker对账/DB往返回填），27→32例。
  - **② G25续 KDJ/EMA 表达式化（factor_expr DSL 加算子，对过程式 float.hex 逐位 parity）**：
    - DSL `_TS_OPS` 加 `kdj_rsv(high,low,close,n)`（尾窗hh/ll、(c-ll)/(hh-ll)*100、平盘|hh-ll|<1e-12给50；非close-only吃high/low）与 `kdj_sm(x,n)`（K/D固定初值50、α=1/3递推，**必须 (2/3)*prev+(1/3)*x 结合序，写成(2prev+x)/3会差1ULP**；首个有限值即起算、区别于ts_ema的前n值SMA播种；对K序列再套一次即得D）。
    - LIBRARY +5：expr_ema12/ema26（ts_ema，MACD快/慢线EMA列）、expr_kdj_k/d/j（J=3K-2D）；factors_catalog 38→43条（introduced=63，全research不进综合分）。
    - factor_legacy_expr 加 parity_ema(12/26逐位)/parity_kdj（合成 high≥close≥low 的OHLC随机游走，K/D/J三序列逐位）、_synthetic_ohlc、KDJ_K/D/J_EXPR、EMA_EXPRS，parity_report 增 ema/kdj；selftest 10→11组，KDJ三序列各412对全部 float.hex 相等、无未来。expr_research 两个 series 装配补 high/low（缺则回退收盘），tests/test_factor_expr 库编译循环补 high/low。
  - **③ G25续 orthogonal_blend_oos 加 分层多空/换手/交易成本（tools，纯标准库、研究侧）**：
    - 纯函数 quantile_ls_day（按分5层、多顶层空底层、层内等权、gross=1权重±0.5腿；返回多/空腿平均收益/spread/gross_ret=0.5*spread）、turnover_between（traded=Σ|Δw|、one_sided=0.5*traded）、perf_of_returns（均值/年化/波动/夏普/累计/最大回撤/正比例）、evaluate_ls_books（逐日毛收益→换手→成本→净收益，cost_t=traded×单边费率）。
    - walk_forward 增第三返回值 books（每有效OOS日留 orth/equal 分数+y；IC路径数值不变），run 同时评 orth/equal 分层组合并写进 txt/json；CLI 加 --quantiles/--cost-oneway。**单边成本复用回测口径 BACKTEST_FEE_RATE+BACKTEST_SLIP_RATE=万1.5，不新造数字**。
    - 真实64品种面板（1017交易日/2022-06-28~2026-09-02）结果照实：H1正交分层毛年化+5.00%/净+0.88%/净夏普0.13/日均换手0.546（等权净-2.01%）；H5正交净-9.42%、等权净+6.31%；H20正交净+3.60%、等权净+46.72%（夏普1.57）；H1总成本拖累14.34%。**H>1前向收益日序重叠，累计/最大回撤仅作相对比较、非复利净值（报告已标注）**；日频高换手分层被成本吃掉大部分毛收益=负结果照实，不进综合分。selftest 4→5组（gross=1/完全换仓traded=2/零成本净=毛/正成本净≤毛）。
  - **主链双哈希（铁律）**：默认on与强制off分别跑 portfolio.main 默认8品种 period30，equity/trades 双 sha256 都=c4da4cdf61f3bcdc/50dcc800d326f8e9=长期基线（综合分逐字节不变；factor_expr 新增算子/库条目不被 main/analyzer import，纯研究侧增量）。跑完备份并还原 reports/。
  - **git**：代码commit + 文档commit 两个；VERSION=0.63.0、tag v0.63.0；**远程 origin 已配（https://github.com/L-m-h-tech/first-lianghua.git）、隐私xlsx已从git移除+commit 1af2445；**push 已成功**（GFW间歇封锁后网络短暂恢复窗口push，160个commit+全部tag已上GitHub；**后续已全局配置karing代理（git config --global http/https.proxy=http://127.0.0.1:3067），push不再需要手动操作**；远程原仅初始README，force-with-lease覆盖后同步；**最终全部push完成：远程main=f2f658e与本地零差异（161commit+34tag全部在GitHub）**；commit 1af2445为xlsx移除，f2efc57为摘要+gitignore更新，f2f658e为karing代理配置摘要更新）**。
  - **下一站（第64轮候选）**：①push已全部完成，远程main=f2f658e与本地完全同步（161commit+34tag）；后续新代码正常 git push 即可（已全局配置karing代理 http://127.0.0.1:3067；**只要karing运行中push即自动走代理，无需手动配置**；karing关闭时代理失效需手动连或重开karing）；②G1续=paper_broker 连续影子≥4周后与 signal_outcomes/回测三方对账（G30②仍硬阻塞 G14 一档盘口）；③G25续=分层组合改"按H对齐再平衡"消除前向重叠、降换手/稳健性检验，更多过程式量表达式化；④G16浅ML等 build_ml_samples 样本积累（可解释线性基线 orthogonal_ic_blend 已就位）；⑤G22续 tradable_mask涨跌停掩码/交割日历、G23续 carry换手与容量（研究边缘，真HP/SP待G22分类持仓）。
## 零.旧、最新快速接续摘要（第62轮，存档）
- **第62轮 G4续 回测严谨性补齐（2026-09-04；生产77py/35603行，pytest 718→733全绿约11s，零新增运行依赖）**：用户指令="继续完成G4回测严谨（walk-forward、手续费+滑点+冲击、样本外、多空分桶、对照基准），可用本机/借鉴GitHub，最后更新摘要"。
  - **先审计、只补缺口（不重复造）**：两次对话间项目已从第25轮推进到第61轮（718测试）。核对 backtest.py（902→约960行）确认第26轮 G4 已落地：`--fill close/next_open` 次根成交、交易级 iid bootstrap 1000 次 P5/中位/P95（总体+多空）、`--oos-ratio` 单次静态IS/OOS（总体+多空）、真实银河费率表+滑点+`--impact-rate` 冲击成本（往返分列、`--no-cost`）、3×3参数稳定性网格、storage backtest_runs 留档+历史百分位、抬头引 backtest_validation 的 DSR/PBO。**对照用户五要素：费+滑点+冲击✓、样本外(静态)✓、多空分桶✓（总体/分档/bootstrap/IS-OOS 都按多空拆）；真正缺 ①滚动walk-forward ②对照基准**，本轮只补这两项。
  - **新增 `backtest_rigor.py`（约200行，纯标准库零网络，模拟器以参数注入便于确定性单测，不 import backtest 避免循环）**：
    - `slice_prepared(prepared,a,b)`：切全局bar半开区间，series 的全局 i 重映射为窗内局部下标；**ind/score 沿用全局【因果】计算结果（只用a之前历史，无未来函数）**，closes/opens/highs/lows/bars 同步切片。
    - 基准：`buy_hold_window`（区间首有效价→末根，跳过前导0/非法价）、`benchmark_for_prepared`（从预热60到样本末，与策略最早可交易日对齐）、`pooled_buy_hold`（等权篮子=各品种B&H算术平均）、`excess`（算术差，缺值/NaN安全）、`beat_benchmark_pairs`（逐品种跑赢计数）。
    - 滚动WF：`wf_folds(n,warmup,train,test)` 首折OOS起点=warmup+train、之后每折推进test、**相邻折OOS互不重叠**、尾折不足2根丢弃；`select_best_param` 只在IS子窗对 (hold,entry) 网格按**净均收最高**选参，交易数<BACKTEST_WF_MIN_IS_TRADES(3)的参数不参选、严格大于保并列先者（确定性）；`walk_forward_symbol` 逐折"前段IS选参→紧邻后段OOS用该参数交易"，OOS交易标注 wf_fold/wf_hold/wf_entry，IS全不足则回退默认参数并标 fallback=True；`param_usage`/`is_vs_oos_avg` 出选参分布与折级IS→OOS衰减。
  - **接入 backtest.py（默认策略数值逐值不变）**：①fetch_and_run 每品种算 `result["buy_hold"]`；②build_report "一、总体"在IS/OOS块后加【对照基准】（默认开，`--no-benchmark`关）：等权N品种篮子买入持有累计 vs 策略净累计、超额百分点、逐品种跑赢x/N；③`--walk-forward`（默认关）+`--wf-train`(默认120)+`--wf-test`(默认40)：每品种用与稳定性同源的3×3网格做滚动选参，报告给OOS拼接总体+多空分桶、品种数/折数/兜底折数、折级IS均收→OOS均收、选参分布。config 增 BACKTEST_BENCHMARK(True)/BACKTEST_WF_ENABLED(False)/BACKTEST_WF_TRAIN_BARS120/BACKTEST_WF_TEST_BARS40/BACKTEST_WF_MIN_IS_TRADES3/BACKTEST_WARMUP_BARS60。
  - **关键教训（避免再犯）**：第26轮既有 G4 测试文件**就叫 tests/test_backtest_rigor.py（17例，测的是 backtest 模块）**，我起初用同名新文件把它覆盖、导致全量从718掉到716；已 `git checkout HEAD` 恢复原17例，并把本轮新测改名 **tests/test_wf_benchmark.py（14例）**。新增模块文件名 backtest_rigor.py 在 HEAD 不存在（untracked），无模块级冲突，仅测试文件名撞车。**新建测试文件前必须先 Glob/查 git 是否已有同名。**
  - **验证**：pytest **718→733全绿**（+14新例 + compileall覆盖新模块1例；stash核对HEAD净基线=718）；compileall递归全过；新文件纯LF无BOM。真实联网回测：默认8品种 净累计-25.2% vs 等权买入持有+8.5%、超额-33.7pp、仅1/8跑赢（诚实暴露纯技术规则该窗口跑不赢一直持有，多头+22.6%/空头-39.0%分桶清晰）；`--walk-forward` 8品种×2折=16折0兜底、OOS拼接103笔、折级IS均收+0.44%→OOS+0.01%（样本外衰减可见）、选参分布{20d/1.5:7,5d/1.5:5,...}；`--no-benchmark`确认可关；`--oos-ratio0.3 --impact-rate0.0002 --walk-forward` 三机制共存正常。
  - **git**：本轮提交 tag **v0.62.0**（VERSION 0.52→0.62 对齐轮次，53-61轮CHANGELOG未逐条补、以摘要+commit为准）。
  - **下一站（第63轮候选，融合总纲6.3）**：G1 纸面交易引擎仍是订单执行层补齐的关键（paper_broker 已有部分，可继续向 OMS/成交回报/持仓对账完善）；或 G2/G3/G5 因子IC流水线/特征存储/组合历史净值深化。**约束不变：不动实时主链与综合分口径，新能力默认影子/开关、缺省等价旧版；新增测试文件先查重名。**

## 零.旧、最新快速接续摘要（第61轮，存档）

### 本轮（第61轮）一句话
按用户拍板把 PLUGIN_PARTS_ENABLED 默认翻 True（G2 真正切换到注册表取分、内联完整保留可一键回退，默认on/强制off 双哈希都=长期基线）；G25 续：给 DSL 加 ts_ema/ts_rma 两个状态递推算子，把 MACD（dif/dea/hist，嵌套 ts_ema）与 Wilder RSI14 表达式化、对过程式逐位 parity；新增 tools/orthogonal_blend_oos.py 把 orthogonal_ic_blend 接真实 64 品种面板做 walk-forward 样本外对照。pytest 713→718 全绿，代码 commit cacd0f8。

### ① 默认开关翻 True（G2 真正切换，内联双轨保留）
- config.PLUGIN_PARTS_ENABLED False→True；analyzer 分支 getattr 默认 True；内联 9 part 计算及 fund_pack/inst_note 等下游局部量一行未删（置 False 即回退）。
- 双哈希：默认(on)与强制(off)分别跑 portfolio.main 默认8品种 period30，equity/trades 双 sha256 都=c4da4cdf61f3bcdc/50dcc800d326f8e9（跑完还原 reports 备份）。
- 配套：_parts_via_plugins 每次 register(replace=True)→assemble→finally clear，无注册表残留；factor_parts 三处 parity 驱动点（_drive_main / parity_against_analyzer / selftest 第11组）新增 _force_inline_analyzer 上下文或等价 save/restore，在驱动真 analyzer 取"内联真值"期间强制 flag=False（否则真值自引用注册表、且 helper 的 clear 会清掉测试预注册），结束还原；tests 两处（assemble 对照、switch 默认值断言）同步改。

### ② DSL 状态算子 ts_ema/ts_rma + MACD/RSI 表达式化（逐位 parity）
- factor_expr：_TS_OPS 加 ts_ema（标准EMA alpha=2/(n+1)，前n有限值SMA播种）/ts_rma（Wilder ((n-1)*prev+x)/n）；新增 _seeded_recurrence（跳过前导非有限值、前 n 个有限值 SMA 播种、其后全序列因果递推、无未来），时序算子实参支持嵌套（ts_ema(ts_ema(close,12)-ts_ema(close,26),9)）。
- 探针 500 点随机游走实测：ts_ema 对 futures_data._ema_series（p=9/12/26）、ts_rma 对 Wilder avg_gain/avg_loss、MACD dif/dea/hist、RSI 非平盘分支全部 float.hex 逐位相等 max_diff=0.0。
- factor_legacy_expr：MACD_DIF/DEA/HIST_EXPR、RSI14_EXPR；parity_macd（三序列要求逐位）、parity_rsi（非平盘逐位；过程式 avg_loss<=1e-12 强制返回100 的平盘/单边分支单独计 n_flat，是唯一已钉死口径差——表达式安全除法在该处给 None）；parity_report 加 macd/rsi；selftest 9→10 组（含单边上涨构造触发平盘分支、状态量无未来截断）。
- factor_expr.LIBRARY +4（expr_macd_dif/dea/hist、expr_rsi14，全 close-only 可被库自测编译）；factors_catalog 34→38 条 research（introduced=61），validate()==[]。

### ③ orthogonal_ic_blend 接真实面板样本外（tools/orthogonal_blend_oos.py，纯标准库）
- 读 cache/research_panel.db（64品种/61353行/2022-06-28~2026-09-02），默认因子 ret5/ret20/ret63/tsmom63/tsmom126/day_chg（可 --factors 覆盖），前向 H=1/5/20 交易日 y=c(t+H)/c(t)-1（严格未来）。
- 每个交易日先做截面均匀秩标准化到[-1,1]（cs_uniform，并列平均秩、只用当日截面）；扩展窗满 MIN_TRAIN=60 日后每 REFIT_EVERY=20 日（月度）用该日之前全部样本重估：各因子池化 Spearman IC→orthogonal_ic_blend 学顺序正交 OLS betas 与 |IC| 有符号归一权重；OOS 日用训练 betas 做三角递推残差化（apply_triangular：r0=f0，ri=fi−Σβ·rj）再按权重合成。
- 对照 orth_ic 正交合成 vs equal 等权 vs 6 个单因子的日度截面 RankIC（mean/ICIR=mean/std/正比例/天数），并算正交−等权配对日IC差与占优日比例。出 reports/orthogonal_blend_oos.txt/.json + experiment_ledger.safe_record 一条；缺库友好降级。
- 真实结果（负结果照实、不进综合分）：H1 正交 meanIC=0.0144/ICIR0.069/正比例54.0%（等权 -0.0049，正交−等权日均 +0.0193、占优日53.4%）；H5 近乎 0（-0.0038）；H20 单因子 tsmom126(0.0307)/ret20(0.0243) 反而更强、正交0.0078。结论：短周期正交去共线有微弱改善，中长周期单动量更直接，整体 |IC| 均很小、远未达上线阈值，仅研究侧留档。
- 自带零网络/零DB selftest（截面均匀秩/并列秩、三角残差化手算、IC汇总、合成面板 walk-forward 跑通且等长、截断末日无未来），纳入 tests/test_tools_selftest.py。

### 验证台账（本轮全部实跑）
- 全量 pytest 713→718 全绿（0 fail/0 err/0 skip，junit=cache/r61_junit.xml；新增 test_ts_ema_ts_rma_recurrence、test_macd_stateful_bit_exact、test_rsi_nonflat_bit_exact_and_flat_branch、test_orthogonal_blend_oos_selftest 共4例，test_compileall 参数化收录新 tools 文件 +1）；compileall 过；factor_expr/factor_legacy_expr/factor_parts/factors_catalog 模块自测分别 9组/10组/11组/38条全过。
- 双哈希 off=on=基线（见①）。隔离：main/portfolio/futures_data 仍零提及 factor_plugin/factor_parts/factor_legacy_expr/orthogonal；analyzer 仅函数内惰性 import 插件层；新工具仅标准库(argparse/json/math/os/sqlite3/pathlib/random)+内部模块，零新增第三方依赖。

### 边界 / 注意
- 默认虽翻 True，analyzer 内联路径一行未删（双轨观察期）；回退只需 config.PLUGIN_PARTS_ENABLED=False。是否下一轮删内联冗余只留注册表路径，仍需用户拍板。
- RSI 平盘/单边（avg_loss≈0）过程式强制 100、表达式给 None，是刻意保留并钉死的唯一口径差，真实连续行情极罕见（随机500点 n_flat=0）。
- OOS 工具只读自有 SQLite、只写 reports、台账旁路；研究侧不自动改 analyzer 权重、不进综合分；上线仍须 G29 体检+样本外+真实成本后≥现状。

### 下一站候选（第62轮）
- G25：KDJ（K/D 是对 RSV 的固定系数 EMA-like 递推，可再加专用算子或复用 ts_ema 变体）、把 EMA 列直接登记表达式；状态量表达式化已基本扫清。
- G16：ml_samples 积累后用 orthogonal_blend_oos 框架替换为浅 ML（可解释线性基线已就位）；可给 OOS 工具加交易成本/换手与分层多空组合回测。
- 待用户决定：是否删 analyzer 内联冗余只留注册表路径；VERSION 是否 bump/tag（第53~61轮刻意维持 0.52.0）；是否 push（一直仅本地）。
- 旧阻塞不变：G30②硬阻塞 G14 常驻一档盘口；真HP/SP待 G22 会员龙虎榜/COT 分类持仓；外部 G13(无LLM key)/G14(tick_snapshots)/G17(期权PCR·IV)/G18(Tushare积分)/G20(远期)。


## 零-旧、第60轮快速接续摘要（已被第61轮接续，保留存档）

### 本轮（第60轮）一句话
按用户点名完成 **G2 第四切片 + 最后一切片（G2 插件化主线收官）** 与 **G25 续**：①把最后一个、context 最复杂的「基本面」part 适配器化（库存/龙虎/期限carry/基差四子项 + term，全部走 fundamental_factors 原函数、同运算序），对真实 analyzer 166 开门/8 关门 float.hex 逐位，**9 个 live part 至此 9/9 全齐**；随后落地**最后一切片**——analyzer 新增默认关开关 `config.PLUGIN_PARTS_ENABLED`，开启时经 `factor_parts.assemble_live_parts` 按 PART_KEYS 序从注册表重建 parts（原油动态键、插入序与内联完全一致，故 sum 顺序相同），helper 惰性 import、任何异常回退内联；**off（内联）与 on（注册表）两条路跑默认 8 品种组合，双哈希都等于长期基线 c4da4cdf/50dcc80，逐字节一致**，旧内联路径完整保留、随时可回退。②G25 续：ma5/20/60 表达式化（末位容差）、boll_std20 与 hv20 因**求和顺序与过程式完全一致而逐字节镜像**、新增 `orthogonal_ic_blend`（顺序正交去共线 + |IC| 有符号归一加权，G16 浅ML 样本未齐前的可解释线性合成基线，纯研究侧）。pytest **706→713 全绿**、compileall 过、隔离 grep 仅 analyzer 函数内惰性接入；代码 commit **7b7d701**、VERSION 仍 0.52.0 未 bump/tag。

### ① G2 第四切片：「基本面」适配器（9/9，最复杂 part）
- factor_parts 新增 `fundamental_compute`：context={term（宿主已用 contracts.term_structure 算好）, fund_raw:{inv,rank,basis}}，**函数内惰性 import fundamental_factors**，逐行复刻 analyzer 第80~90行：inv 序列→inventory_factor、rank 元组 `rank_factor(*rank_in)`、term→carry_factor、basis→basis_factor，再 build_fundamental，`pack 且 |score|>0.01` 才返回分数。
- 用例 `part_parity_cases("基本面")`：合成 contracts info.list（≥TERM_MIN_MONTHS=2 个月）同时喂主链 contract 与插件 term（同源 term_structure）；覆盖四子项全缺/单 carry(back·contango·平)/单基差/单龙虎(含无昨日)/单库存(≥FUND_INV_MIN_SAMPLES=15)/四项齐/160 随机。`_drive_main` 加 contract、fund_raw 映射。结果 **n_open=166/n_closed=8、max_diff=0.0 零 mismatch**。
- builtin_part_plugins 由 8 扩到 **9=list(PART_KEYS)**；新增 FOURTH_SLICE_KEY；parity_all_against_analyzer=三切片7+基本面（日线动量单列）。

### ② G2 最后一切片：analyzer 可从注册表取分（**默认关、双路逐字节等价、可回退**）
- config 新增 `PLUGIN_PARTS_ENABLED=False`（紧邻 PRS_* 风险 sizing 开关，注释写明默认走内联基线）。
- analyzer 新增模块级 `_parts_via_plugins(fallback,...)`：**惰性** import factor_parts/factor_plugin（顶层零依赖），register_builtin_parts(replace=True)→assemble_live_parts→finally clear；**任何 Exception 都 return fallback 内联 parts**，主链永不因插件层故障受损。
- analyze_variety 在 9 个内联 part 全部算完、`score=sum(parts)` 之前插入唯一分支：`if getattr(config,"PLUGIN_PARTS_ENABLED",False): parts=_parts_via_plugins(...)`。默认关时只多一次 getattr 判断，**输出零变化**；fund_pack/inst_note 等下游局部量仍由内联计算（result["fundamental"] 等不受影响）。
- factor_parts.assemble_live_parts：按 **catalog.PART_KEYS 序**（=analyzer 内联插入序，已逐行核对：新闻→原油→机构→日线→技术共振→分钟共振→盘中动量→量仓→基本面）逐 part 造 context、fp.evaluate，None 不入 dict，原油发成动态键 `"原油联动(w=%.2f)"%oil_w`。
- **等价证明（三层，全绿）**：(a) factor_parts selftest 第11组 240 例 9-part 同时在场，assemble 结果与真实 analyzer parts **逐键 float.hex 相等**且 score 逐位；(b) 新增单测 200 例 assemble vs 内联、120 例 analyzer off/on 切换逐键逐位+score 相等；(c) **端到端**：portfolio.main 分别在 flag=False/True 下跑默认 8 品种 period30，equity/trades 双 sha256 **off=on=c4da4cdf61f3bcdc / 50dcc800d326f8e9**（跑完已还原 reports）。
- 源码边界改判（已更新两处钉死测试）：**main.py 永不接插件层**；analyzer 只允许**函数内惰性** import（测试逐行断言无顶层 import）且开关默认 False；factor_legacy_expr 仍禁止 main/analyzer/futures_data import。

### ③ G25 续：更多过程式技术量表达式化 + 正交 IC 加权（研究侧、不进综合分）
- 探针先证：`ts_std(close,20)` 与 `_sample_std(close[-20:])`、`ts_std(log(close/delay(close,1)),20)*sqrt252` 与 `_hv_at(.,20)` 因**均值/平方和的遍历与求和顺序完全一致**而 **float.hex 逐位相等（max_diff=0）**；sqrt252 用精确字面量 15.874507866387544==math.sqrt(252)。factor_expr.LIBRARY +5（expr_ma5/ma20/ma60/boll_std20/hv20），factors_catalog 29→**34 条** research。
- factor_legacy_expr 新增 parity_boll_std/parity_hv20（要求逐位）、HV20_EXPR、_SQRT252；parity_report 加 boll_std/hv20；selftest 7→**9 组**（boll 401/hv 400 逐位、正交IC）。
- 新增 `orthogonal_ic_blend(matrix,ics)`：顺序 Schmidt 式（每因子只对前面"已残差化"因子 OLS 取残差，复用 factor_expr.orthogonalize）→ ic_weights（|IC| 有符号归一、和=1、全零退化等权）→ combine 缺失归一；G16 浅 ML 样本未齐前的可解释线性合成基线。selftest/单测构造共线因子验证残差对基底协方差≈0、权重和=1、单因子退化、维度不符报错。

### 验证台账（本轮全部实跑）
- factor_parts selftest **11组 ALL PASS**（日线501/3；三+四切片 1171 开门/231 关门逐位；宿主装配 240 例逐键逐位）；factor_legacy_expr **9组**（ret逐字节、SMA容差非逐位、日线动量757逐位、boll/hv各400+逐位、正交IC）；factor_expr 9组、factors_catalog 34条 均过。
- 全量 pytest **706→713 全绿**（0 failed/0 error/0 skipped，cache/r60_junit.xml；+7=factor_parts 第四切片2+assemble+switch 共4、factor_legacy_expr boll/hv/正交 共3；本轮无新增根文件故 compileall 参数化不增例）。compileall 根+tools 过。
- **主链等价**：git diff 生产侧仅 analyzer（+29行，唯一分支默认关+惰性helper）、config（+5行开关）；main/portfolio/futures_data/storage 零改动；off/on 端到端双哈希均=基线（见上）；隔离 grep 确认插件层只被 analyzer 函数内惰性、插件层自身与 tests 引用。运行依赖仍仅 requests/uiautomation/websocket-client，纯标准库实现、未抄外部代码。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump/tag/CHANGELOG**（延续第53~60轮研究/重构侧增量不发版）。代码 commit **7b7d701**（9 文件 +537/−44），文档（本摘要）随后单独 commit；reports/、cache/*.db 在 .gitignore。
- **G2 状态=插件化主线收官**：r57 宿主✅ r58 日线动量✅ r59 再搬7✅ **r60 基本面✅(9/9)+最后一切片可切换✅**。当前默认仍走内联（最稳）；注册表路径已被证明逐字节等价，后续若要"默认切注册表"只需把 PLUGIN_PARTS_ENABLED 默认改 True 并再跑一次双哈希即可（本轮刻意不翻默认值，保留一个完整稳定版本的内联基线）。
- 坑（同前轮，仍有效）：解释器 **D:\Python\python.exe(3.12.4)**，PATH 上 python 是缺依赖 3.14；git 在 `C:\Program Files\Git\cmd`；PowerShell 复杂校验写 cache/*.py 跑完即删；浮点"代数等价≠逐位"，boll/hv 能逐位是因为连遍历求和序都一致，SMA 增量累加 vs 窗内重算则只到 1e-15；analyzer 里 fund_pack 同时供 parts 与 result["fundamental"]，最后一切片只替换 parts、不删内联局部量计算。

### 下一站候选（第61轮）
G2 已无剩余 part；可选：①把 PLUGIN_PARTS_ENABLED 默认翻 True 并全量双哈希后删除内联冗余（更彻底但降低可回退性，需用户拍板，本轮默认保留双轨）；②G25/G16：orthogonal_ic_blend 接真实 research_panel 做样本外 IC 加权对照（仍受 G16 ml_samples 积累约束）、把 EMA/RSI/MACD 等状态量表达式化（需先给 DSL 增 ts_ema 算子）；③旧排队 G30②（硬阻塞 G14 常驻一档盘口）、G24 真HP/SP（待 G22 分类持仓）、G22续/G23续；外部阻塞 G13(无LLM key)/G14(常驻盘口)/G16(等ml_samples)/G17(期权PCR·IV)/G18(Tushare积分)/G20(远期)。

---

## 零-旧、第59轮快速接续摘要（已被第60轮接续，保留存档）

### 本轮（第59轮）一句话
按用户点名两线并进并回写摘要：**①G2 第三切片**——factor_parts 在第二切片「日线动量」之外，再把**其余 7 个门控简单的 live part**（新闻消息面/原油联动/机构动向/技术共振/分钟共振/盘中动量/量仓资金）全部适配器化，每个都对**真实 analyzer.analyze_variety 主链**做 float.hex 逐位 parity（合计 1005 开门+223 关门用例，max_diff=0.0），**9 个 part 已搬 8 个、仅余「基本面」**，main/analyzer 仍零 import 插件层、**不切主链**，默认8品种双哈希 c4da4cdf/50dcc80 复算逐字节一致；**②G25 续（旧因子过程式→表达式）**——新增根模块 factor_legacy_expr：ret1/5/20 用与过程式**同运算序**的 `close/delay(close,n)-1` 做到**逐字节镜像**，ma5/10/20/60 的 ts_mean 对增量式 _sma_series 仅 ~1e-15 末位舍入差（容差并**钉死非逐位**、故主链保留过程式），给 factor_expr 引擎补 **tanh 逐元素算子**后把整条日线动量 part 声明式复刻、757 例逐位相等。pytest **691→706 全绿**、compileall 过、隔离 grep 生产链零 import；代码 commit **f580242**、VERSION 仍 0.52.0 未 bump 未 tag。

### ① G2 第三切片：factor_parts.py 再搬 7 个 live part（9 搬 8，仅余基本面，**仍不接主链**）
- 7 个 part 的 context 契约/门控/公式（逐行对应 analyzer 第47~77行）：**新闻消息面**=news_score 透传、**无门控恒在 parts**；**原油联动**门控 `oil_w>0`、值 `oil_score*oil_w`（主链键名动态"原油联动(w=..)"，规范键=原油联动，提取按 startswith 归一）；**机构动向**门控 `inst 且 total>=3`、`tanh((bull-bear)/total*2)*2`（开门用例必须带 volatile 否则主链 f-string 取 inst['volatile'] 会 KeyError）；**技术共振**门控 `kline_ok&price>0 且 |resonance|>0.01`（开门时主链同时算日线动量，桩需补 ret5/ret20）；**分钟共振**门控 `intraday.ok 且 |r|>0.01`；**盘中动量**门控 `|tick_mom|>0.01`；**量仓资金**门控 `|flow.score|>0.01`（复刻 `flow.get("score") or 0`）。
- 通用框架：`_part_plugin` 由 **factors_catalog 派生 direction/bound/layer**（元数据不可能漂移）；`_COMPUTE/_PLUGINS/THIRD_SLICE_KEYS`；`part_parity_cases(key)` 各 part 确定性用例（门控关闭/阈值恰 0.01 边界/正负/固定种子随机，seed=20260904+key 散列）；`_drive_main(key,ctx)` 把 context 映射成 analyze_variety 最小桩、隔离出目标 part（其余 part 置空/关门）；`_main_value` 提取（原油动态键前缀归一）；`parity_part_against_analyzer/parity_all_against_analyzer` 要求**插件有值⇔主链有该 part 且 float.hex 逐位相等**。builtin_part_plugins 现按 PART_KEYS 序返回 8 个（=PART_KEYS 去掉基本面）。
- **结果**：第三切片 7 part 合计 **n_open=1005/n_closed=223、全部 max_diff=0.0、零 mismatch**；新闻无门控 n_closed=0、原油 oil_w<=0 关门。selftest 由 8 组扩到 **10 组**。日线动量第二切片 parity 维持 n_open=501/n_closed=3。

### ② G25 续：factor_legacy_expr.py（新根模块）+ factor_expr 加 tanh（**不切主链、回退铁律**）
- **关键认知（探针先验证再定口径）**：过程式 ret 是 `close/close[-n]-1`；表达式若写 `delta/delay` 虽代数等价但**运算顺序不同、末位不逐位**，改写成同运算序 `close/delay(close,n)-1` 后在 420 点随机游走上 **float.hex 全相等 max_diff=0**；过程式 _sma_series 是"增量累加+滑窗扣减"、ts_mean 每窗 `sum` 重算，二者**只在末位舍入不同（实测最大相对差 1.9e-15）、非逐位**——所以 ret 敢逐字节镜像、SMA 只做 1ULP 容差并让主链继续用过程式。
- factor_legacy_expr 内容：`procedural_ret_series`（独立铺序列）、`parity_ret`（逐位）、`parity_sma`（惰性 import 真 futures_data._sma_series，容差 SMA_REL_TOL=1e-12 并断言 n_hex<n_pair 钉死非逐位）、`DAILY_MOMENTUM_EXPR_FULL/NOMA`、`daily_momentum_expr_value`（标量包长度1序列，ma10 假值走两项式）、`parity_daily_momentum`（惰性 import factor_parts.legacy，网格+512随机共757例逐位）、`parity_report`、合成序列零DB selftest **7组返回0**。
- factor_expr 引擎：`_EL_OPS` 与 ts/cs 两个逐元素求值器各加 **tanh**（非有限安全降级 None），selftest 加 tanh 手算；LIBRARY 加 expr_ret5_exact/expr_ret20_exact/expr_ma10 三条（只用 close 可被库自测编译）；factors_catalog CATALOG 25→**29 条**，新增 expr_ret5_exact/expr_ret20_exact/expr_ma10/expr_part_momentum_decl 四条 **research**（不进综合分）。

### 验证台账（本轮全部实跑）
- factor_parts selftest **10组 ALL PASS**（日线501/3 + 第三切片1005/223 全逐位）；factor_legacy_expr selftest **7组 ALL PASS**（ret逐字节、SMA容差且非逐位、日线动量声明式757逐位、无未来、ma假值、catalog）；factor_expr/factors_catalog selftest 过（catalog 29条）。
- 新增 tests/test_factor_legacy_expr.py **8例**；test_factor_parts 加 4 例第三切片（元数据/门控/确定性/真analyzer逐位）并把内置列表断言从['日线动量']改为8个；test_factor_expr 加 tanh 1例；test_tools_selftest 登记 factor_legacy_expr +1。全量 pytest **691→706 全绿**（0 failed/0 error/0 skipped，cache/r59_junit.xml；+15=factor_parts4+factor_expr1+legacy8+selftest1+compileall参数化新根文件1）。
- **主链零改动等价证明**：git diff 仅 factor_expr/factor_parts/factors_catalog（研究/插件层）+factor_legacy_expr（新）+4测试，**main/analyzer/portfolio/futures_data 零改动**；默认8品种 `portfolio.py --codes I,MA,RB,SA,TA,AU,AG,CU --period 30` 复算 equity=**c4da4cdf61f3bcdc**/trades=**50dcc800d326f8e9** 双 sha256 **逐字节一致**（跑完已还原 reports 原产物）；隔离 grep：factor_parts/factor_plugin/factor_legacy_expr 仅被插件层自身（factor_legacy_expr 函数内惰性 import factor_parts）与 tests import，生产链零命中；源码钉死测试扩到 main/analyzer/futures_data 不得提 factor_legacy_expr。运行依赖仍仅 requests/uiautomation/websocket-client。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump、未 tag、未改 CHANGELOG/README**（延续第53~58轮研究/重构侧增量不发版）。代码 commit **f580242**（4改+2新共8文件，+756/−22），文档（本摘要）随后单独 commit；reports/ 与 cache/*.db 在 .gitignore。
- **G2 进度**：r57 宿主扩展点✅、r58 日线动量✅、**r59 再搬7个✅→9 搬 8，仅剩「基本面」**（依赖 fundamental_factors 四子项+term 期限结构，context 最复杂，留第四切片）；**最后一切片**=基本面 parity 通过后，才让 analyzer 从注册表取分、并再做一次全量双哈希回归，旧内联路径始终可回退。
- 坑：①解释器 **D:\Python\python.exe（3.12.4）**，PATH 上 python 是缺依赖的 3.14 沙箱；git 在 `C:\Program Files\Git\cmd`、node 在 `C:\Program Files\nodejs`，均不在 PATH；②PowerShell 内联 python 含括号/转义双引号必错，复杂校验写 cache/*.py 跑完即删；③浮点"代数等价≠逐位等价"，表达式化必须复刻**运算顺序与字面量**（ret 用 close/delay-1 而非 delta/delay；日线动量 ma 项是 tanh((price/ma10-1)*220)*1.0，不是 tanh(price/ma10-1)*220）；④机构 part 开门即拼 inst['volatile']，parity 用例三家数要自洽。

### 下一站候选（第60轮）
**G2 第四切片**=搬最后一个 live part「基本面」（库存/龙虎/期限carry/基差四子项，context 带 term/fund_raw）进 factor_parts 做双层逐位 parity；通过后即可评估**最后一切片**（analyzer 从注册表取分+全量双哈希回归）。G25 后续=正交 IC 加权接 G16、把更多过程式技术量按"同运算序"表达式化。旧排队：G30②（硬阻塞 G14 常驻一档盘口）、G24 真HP/SP（待 G22 分类持仓）、G22续/G23续；外部阻塞 G13(无LLM key)/G14(常驻盘口)/G16(等ml_samples)/G17(期权PCR·IV)/G18(Tushare积分)/G20(远期)。

---

## 零-旧、第58轮快速接续摘要（已被第59轮接续，保留存档）

### 本轮（第58轮）一句话
按上轮"下一站"做两件并回写摘要：**①G2 第二切片 parity**——新增根模块 factor_parts.py，把第一个 live part「日线动量」用适配器包成 FactorPlugin 平行实现，对**独立 legacy 公式**与**真实 analyzer.analyze_variety 主链**各跑 501 开门+3 关门用例 **float.hex 逐位相等（max_diff=0.0）**，**main/analyzer 仍零 import 插件层、主链不切**（默认8品种双哈希 c4da4cdf/50dcc80 复算逐字节一致）；**②wf_cost_lab/factor_health 接看板**——charts 新增 wf_cost_payload/factor_health_payload 与 5 张卡（IS/OOS 夏普、OOS为正占比、费率×滑点成本热力图、事件层三周期9因子RankIC、日频IC衰减），看板 **29→34 图**。pytest **679→691 全绿**、内联 JS node --check 过、真实浏览器加载/图例交互/reload 三通道 console 零错；代码 commit **4e7d8e6**、VERSION 仍 0.52.0 未 bump 未 tag。

### ① G2 第二切片：factor_parts.py（根模块，295行，纯标准库，**仍不接主链**）
- 路线（总纲 G2 + r57 第一切片既定）：第一切片只建"不可变契约+有序注册表+异常隔离+catalog 一致性"扩展点；本切片把**第一个 live part「日线动量」**用适配器搬进注册表做 parity，**analyzer 仍以内联公式计算、绝不从注册表取分**；要等全部 9 个 part 逐切片 parity 通过后，最后一切片才让 analyzer 从注册表取分，任一切片 parity 不达标就不切、旧内联路径永远可回退。
- context 契约与 analyzer 入参一一对应 `{kline_ok,price,ind{ret5,ret20,ma10}}`；`daily_momentum_compute` 逐行对应 analyzer 第61~66行（门控 `kline_ok and price>0`、`tanh(ret5*160)*2.5+tanh(ret20*70)*2.0`、ma10 为真值才加 `tanh(price/ma10-1)*220`），门控关闭返 None=主链不加入该 part；`legacy_daily_momentum` **独立重写**同一公式（刻意不互调，否则 parity 变成同义反复）。
- `daily_momentum_plugin()` 元数据钉死 catalog（status=live、direction=+1、bound=(-4.5,4.5)、layer=技术）；`builtin_part_plugins/register_builtin_parts`；`parity_cases`=3 关门+7×7×5 规则网格+256 固定种子随机（seed=20260903，含 ma10 缺失/0 假值/正负/极端值）=504 例；**两层 parity**：`parity_against_formula`（vs 独立 legacy、float.hex 逐位）与 `parity_against_analyzer`（**函数内惰性 import analyzer**、用最小桩驱动真实 analyze_variety 取 parts["日线动量"] 逐位比对，关门用例要求主链无该 part 且插件返 None）；selftest **8 组**。
- 分层：顶层只 import 标准库+factor_plugin/factors_catalog（零网络零DB），analyzer 仅在 parity 函数内惰性 import；test_factor_plugin 与 test_factor_parts **读源码钉死** main.py/analyzer.py 不得出现 factor_plugin/factor_parts 字样。

### ② wf_cost_lab / factor_health 接看板（charts.py 1761→2076，29→34图，纯增量展示）
- `wf_cost_payload` 读 reports/wf_cost_lab.json：4 品种 IS/OOS 夏普、稳定评级/选参切换率/IS→OOS 衰减/选参遗憾/最优参数，5×5（每腿费率bp×单边滑点bp）逐笔复利净收益%曲面（每格带胜率/笔数/成本占毛利），缺/坏/空 results 返 None；`_bp_label` 小数费率→bp 紧凑文本。
- `factor_health_payload` 读 reports/factor_health.json：事件层 30/120/1440 三周期×9 part RankIC（带块自助 p5/p95 CI、同号率、裁决、翻窗；ci=None 如样本不足安全降级 lo/hi/same=None），日频层 9 因子对未来 H=1..60 交易日池化 IC 衰减+半衰期；周期按数值升序、因子保工具写入(=PART_KEYS)顺序；缺/坏/空 event 返 None。
- build_payload 各以独立 try 挂 `payload["wf_cost"]/["fhealth"]`（缺 sidecar 只 None、不拖垮其余图）；DOM 加 5 卡：**c-wf-stab**（IS蓝/OOS金分组柱+0轴，tooltip 评级/切换率/衰减/遗憾/最优参数）、**c-wf-pos**（OOS为正段占比柱、≥50%红否则绿、50%虚线）、**c-wf-surface**（4 品种 4 个 heatmap series 共享对称0轴 visualMap 红盈绿亏、legend 点选切换、默认只显首个）、**c-fh-event**（三周期×9因子分组柱、0轴、tooltip CI/同号率/翻窗/裁决）、**c-fh-daily**（9 因子 IC 衰减多线、±0.1 弱信号虚线、legend 名标半衰期）；renderWf/renderFh 全 ES5（无箭头/const/let/模板串）接 loadAndRender 成功+onerror 两路径，CHART_IDS 29→34。
- 真实产物（不美化、忠实复现第37/45轮负结果）：wf 4 品种 RB/MA/I/TA 评级全"漂移"（IS/OOS 夏普 0.021/−0.116、0.104/0.084、−0.022/−0.172、0.079/0.072，曲面 vmin −20.5%/vmax +21.4%）；fh 次日仅 技术共振+0.160/机构−0.230/原油−0.136 显著、盘中动量/量仓资金样本不足，日频 9 因子 |IC| 全<0.1=日频回看收益无稳定截面预测力、仅 ret5 半衰期41.3。

### 验证台账（本轮全部实跑）
- factor_parts selftest **8组 ALL PASS**（501 开门+3 关门，对独立公式与真 analyzer 均逐位 max_diff=0.0）；新增 tests/test_factor_parts.py **8 例**；test_charts 29→**31 例**（两 payload 合成映射+缺/坏/空返 None、build_payload 用例把 PC_JSON 指 tmp 保 hermetic 并断言两新键）；test_factor_plugin 源码钉死扩到 factor_parts；test_tools_selftest +1。
- 全量 pytest **679→691 全绿**（0 failed/0 error/0 skipped，cache/r58_junit.xml；+12=test_factor_parts 8、test_charts 2、tools_selftest 1、compileall 参数化新文件 1）；compileall 根+tools 过；charts 内联 JS（50241 字符）node --check 过、DOM 34 卡与 CHART_IDS 34 完全对账。
- **主链零改动等价证明**：git diff 仅 charts.py(纯展示)+factor_parts.py(新)+4 测试文件，main/analyzer/portfolio 零改动；默认8品种 `portfolio.py --codes I,MA,RB,SA,TA,AU,AG,CU --period 30` 复算 equity=**c4da4cdf61f3bcdc**/trades=**50dcc800d326f8e9** 双 sha256 **逐字节一致**（跑完已还原 reports 原产物）；源码钉死测试保证主链不 import 插件层。运行依赖仍仅 requests/uiautomation/websocket-client。
- 真实浏览器（file:// 图表看板.html）：5 新卡均出真实 canvas（wf-stab 2系列×4品种、wf-pos 1×4、wf-surface 4 heatmap×25格、fh-event 3×9、fh-daily 9线×8点），加载后 console=[]、图例切换品种/周期无错、reload 后 console=[]。注：本机 bu 整页截图管线对 8600px 超长页持续 COMMAND_TIMEOUT（与本次代码无关），改用 echarts.getInstanceByDom 程序化读 series 类型/数据点/canvas 尺寸 + console 三通道验证（比截图更确定）。
- 真实入口已跑：charts --rebuild 刷新 reports/chart_data.js（16 个 payload 键含 wf_cost/fhealth）/图表看板.html（34 卡；reports 在 .gitignore 不入库）。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump、未 tag、未改 CHANGELOG/README**（延续第53~57轮研究/看板侧增量不发版）。代码 commit **4e7d8e6**（4 改+2 新共 6 文件），文档（本摘要）随后单独 commit；reports/ 与 cache/*.db 在 .gitignore。
- **G2 进度**：第一切片 r57=宿主扩展点✅；第二切片本轮=首个 live part「日线动量」适配器+双层逐字节 parity✅、**仍不切主链**；剩余 8 个 part（新闻消息面/原油联动/机构动向/技术共振/分钟共振/盘中动量/量仓资金/基本面）后续逐切片同法搬入并 parity，全部逐位绿后最后一切片才让 analyzer 从注册表取分、并再做一次全量双哈希回归。
- 坑：①项目解释器是 **D:\Python\python.exe（3.12.4，依赖齐全）**，PATH 上的 python 是 3.14.7 沙箱解释器、缺 requests/pytest，跑测试/工具必须用 D:\Python；git 在 `C:\Program Files\Git\cmd\git.exe`、node 在 `C:\Program Files\nodejs\node.exe`，均不在 PATH 要写全路径；②PowerShell 内联 python 含方括号/转义双引号必解析错误，复杂校验写 cache/*.py 跑；③ECharts 多 heatmap 共享 visualMap 要用 legend.selected 默认只显首个、色带按全品种 vmin/vmax 对称 0 轴才跨品种可比；④bu 截图对超长页持续超时，程序化读 ECharts 实例更稳。

### 下一站候选（第59轮）
**G2 第三切片**=搬第 2 个 live part（建议挑门控简单的「原油联动」或「新闻消息面」）进 factor_parts 并同法双层 parity、仍不切主链；或继续把剩余研究 sidecar 接看板（已剩不多）。旧排队：G30②（硬阻塞 G14 常驻一档盘口）、G24续真HP/SP（待 G22 分类持仓）、G25续旧因子过程式→表达式（须综合分逐字节回归）、G22续/G23续；外部阻塞 G13(无LLM key)/G14(常驻盘口)/G16(等ml_samples)/G17(期权PCR·IV)/G18(Tushare积分)/G20(远期)。

---

## 零-旧、第57轮快速接续摘要（已被第58轮接续，保留存档）

### 本轮（第57轮）一句话
按上轮"下一站"一次推进四条线并回写摘要：**①portfolio_risk_lab 接看板**（charts 26→**29**图）；**②G24续** 新增 tools/spec_pressure_lab.py（真HP/SP须G22分类持仓、当前没有，诚实只做成交活跃度/量仓行为三代理）；**③G8** 新增 tools/web_dashboard.py（纯标准库只读Web/手机看板）；**④G2第一切片** 新增 factor_plugin.py（插件契约/注册表/隔离/catalog一致性，**刻意不重接analyzer主链**，综合分逐字节不变）。pytest **650→679全绿**、compileall过、内联JS node--check过、真实浏览器3卡console零错、Web服务器真实起服冒烟全对；代码 commit **3bca3ed**、VERSION 仍 0.52.0 未 bump/tag。

### ① portfolio_risk_lab 接看板（charts.py，26→29图，纯增量）
- **portfolio_risk_payload()** 读 reports/portfolio_risk_lab.json（缺/坏返None）：per_method 四 sizing（equal等名义/inv_vol逆波动/erc风险平价/gmv全局最小方差）的 eff_n/div_benefit/ann_vol/avg_abs_corr/port_param_var；oil_stress 三情景 total；equal 的 strongest/weakest_pairs 各6。
- 三卡：**c-prl-method**（年化波动柱+有效持仓N金线双轴，tooltip分散化收益/平均相关/参数VaR）、**c-prl-stress**（原油−10/−5/+5%情景四方法分组柱）、**c-prl-pairs**（最强/最弱相关对横向条红正绿负，升序使最大值落顶）。
- 坑：情景键是字符串"−0.1/−0.05/0.05"，**必须 sorted(key=float)**（字典序会把−0.05排到−0.1前）；方法卡中文类目 rotate:16 防挤。真实：年化波动等名义9.3%→GMV4.2%、有效N 61→12.3（降风险即集中的权衡一目了然），原油−10%等名义−1.22%/GMV−0.15%，最强PX/TA 0.81、最弱AU/PF−0.40。看板 HTML 补 `<meta viewport>`（配合G8手机）。

### ② G24续 tools/spec_pressure_lab.py（纯标准库/只读DB/只写reports/只监控）
- **诚实边界**：真套保压力HP/投机压力SP须**分类持仓**（交易所会员龙虎榜多空/COT商业非商业），本项目只有总持仓（panel.oi=主力、ckline.p=各合约），**无多空/套保-投机分类（G22缺口仍在）**，故不冒充真HP/SP，只给三行为代理，render【四】明确标注。
- 三代理：**(1)投机度=成交量/持仓量**（turnover，尾窗120 z/分位/均值，跨品种只比z不比原始倍数）；**(2)量仓四象限** quadrant(近5日收益×近5日ΔOI)→增仓上行多头主动/增仓下行空头主动/减仓上行空头回补/减仓下行多头离场；**(3)近月持仓集中度**（ckline最近200交易日各合约p→主力占比/活跃合约数/HHI，**换月信号只看主力占比自身尾窗分位<10%，弃绝对阈值**——有色常态挂10+合约、主力天然仅40%，绝对0.55阈值曾误报31只，改后仅4只）。
- **真实64品种（2026-09-02）**：投机度z中位+0.47、|z|≥1.5活跃12只/清淡0（SC+3.30、EG+3.10、Y+2.55最活跃）；四象限 增仓上行26(40.6%)/增仓下行12/减仓上行18/减仓下行8；换月分散仅 AO/BU/LU/AP 4只。selftest **9组**；已纳入 research_review **SOURCES**（注意 research_review **无参=selftest，生成必须带参数**如 `--reports-dir reports`）+ experiment_ledger 台账。

### ③ G8 tools/web_dashboard.py（纯标准库只读静态服务器，零第三方）
- `python tools/web_dashboard.py` 默认绑 **127.0.0.1:8765** 自动开浏览器；`--lan` 才绑0.0.0.0（打印局域网IP+风险提示）、`--port/--root/--no-open`。只允许 **GET/HEAD**（POST/PUT/DELETE/PATCH 全405）；**safe_join** percent-decode后 normpath+abspath+commonpath 强制落在 reports/ 内（`..`/编码%2f穿越/盘符/空字节全403）；暗色手机自适应索引页（含viewport、入口图表看板/实时报告优先、文件名HTML转义防XSS）。
- 真实起服冒烟（urllib）：GET / 200含viewport、chart_data.js 200(294KB)、`../上下文摘要.md`与`..%2f..%2fconfig.py`均**403**、不存在404、POST**405**、HEAD无body，全对。selftest **6组**。

### ④ G2第一切片 factor_plugin.py（根模块，纯增量，**不接主链**）
- 为什么不一次重写analyzer：综合分9 part实时装配散在analyzer/factors/fundamental_factors，直接改插件调度是高风险纯重构，须保双哈希基线（equity=c4da4cdf/trades=50dcc80）。本轮只做**扩展点**：`FactorPlugin`（__slots__不可变、key/compute/direction∈−1,0,1/status五态/bound(lo,hi)构造期校验）、有序注册表 register/get/names/unregister/clear（重复key须replace）、**evaluate/evaluate_all 单插件异常隔离**（boom不拖垮ok、context=None安全、missing标记）、wrap_function适配器、**check_registry_vs_catalog**（live插件key必须∈PART_KEYS、非external须在CATALOG登记）、ordered_live_keys按PART_KEYS规范序、example_plugins两个研究态示例、import零副作用。
- **铁律**：main.py/analyzer.py **不得 import factor_plugin**（test_factor_plugin 读源码钉死），主链逐字节不变；后续切片再逐个把live part用适配器搬进注册表做parity、最后才让analyzer从注册表取分，任一步不达标不切。selftest **8组**。

### 验证台账（本轮全部实跑）
- 新增测试：test_spec_pressure_lab(6)/test_web_dashboard(7)/test_factor_plugin(9)，test_charts +1（29例），test_tools_selftest +3（spec/web/factor_plugin selftest），加 test_compileall 对3个新文件参数化+3；全量 **650→679 全绿**（0fail/0err/0skip，cache/r57_junit.xml）；compileall 根+tools 过；charts内联JS node--check过。
- 隔离：grep证明 main/analyzer/portfolio/portfolio_constructor/risk_gate/circuit_breaker/storage/futures_data/config 对 spec_pressure_lab/web_dashboard/factor_plugin **零import**；新模块仅被tests import（research_review对spec是字符串命令非import）。运行依赖仍仅 requests/uiautomation/websocket-client。
- 真实入口已跑：spec_pressure_lab 出 reports/spec_pressure_lab.txt|.json+台账；research_review 带参重生成（含新sidecar行）；charts --rebuild（29卡+viewport）；web_dashboard 起停一次冒烟。

### 边界 / 注意
- VERSION 仍 **0.52.0 未bump/未tag/未改CHANGELOG/README**；代码 commit **3bca3ed**（10文件：4新模块+3新测试+charts/research_review/test_tools_selftest），文档（本摘要）随后单独commit；reports/、cache/*.db在.gitignore。
- 坑：①PowerShell内联python带双引号必SyntaxError、`python -m compileall *.py` 不展开通配（要用compile_dir/glob）；②research_review无参跑selftest、生成要带参；③情景/版本类**字符串数字排序要用float key**；④Windows文件名禁`<>`，XSS转义测试用`&`；⑤"换月/分散"类信号要用自身历史分位而非跨品种绝对阈值。

### 下一站候选（第58轮）
本轮清掉 G24续（代理版，真HP仍待G22分类持仓）、G8（只读Web看板已交付）、G2（第一切片扩展点已落地，**后续=逐part适配器parity切片**）。旧排队：**G30②**（硬阻塞G14常驻一档盘口）、**G2续第二切片**（把1个live part如"日线动量"用适配器搬进factor_plugin并做逐字节parity，仍不切主链）、G25续（旧因子表达式化须综合分逐字节回归）、G22续/G23续；看板还可接 wf_cost_lab/factor_health（纯展示同套路）。外部阻塞 G13(无LLM key)/G14(常驻盘口)/G16(等ml_samples)/G17(期权)/G18(Tushare积分)/G20(远期)；真HP/SP待G22会员/COT分类持仓采集。

---

## 零-旧、第56轮快速接续摘要（已被第57轮接续，保留存档）

### 本轮（第56轮）一句话
按上轮"下一站"续做两块并回写摘要：**①G12续**——(a) 给 spread_lab 补投料/出成系数+加工费算**产业链盘面虚拟利润额（元/吨）**，(b) 把 spread_lab 接进看板（3卡）；**②G30续复盘页签**——把 trade_journal 接进看板（3卡，②纸面-回测一致性仍待 G14 盘口、本轮诚实不做）。看板 CHART_IDS **20→26**；pytest **646→650 全绿**、compileall 过、内联 JS node --check 过、真实浏览器6卡出 canvas/console零错/修两处轴重叠；代码 commit **36cbfa2**、VERSION 仍 0.52.0 未 bump 未 tag。

### ① G12续-a 盘面虚拟利润额（tools/spread_lab.py）
- 新增 **MARGINS 系数表**：这些腿报价都是元/吨，故只需投料/出成权重+加工费、**不需合约乘数**（乘数只在真实配手数时用，本工具只监控）。5条：盘面压榨利润 `0.785*M+0.185*Y-A-110`（1吨大豆≈0.785豆粕+0.185豆油，加工费约110；DCE一号A为食用国产豆、与进口压榨口径有偏差已注明）、盘面焦化利润 `J-1.3*JM-150`（1.3吨焦煤→1吨焦炭）、虚拟钢厂毛利 `RB-1.6*I-0.5*J`（不计废钢/合金/能源/人工故 fee=0）、卷螺差 `HC-RB`、豆棕价差 `Y-P`。
- 纯函数 `aligned_margin(close_map,weights,fee)`（公共日对齐、缺腿/非正跳过）+ `margin_stat`（尾窗z/分位/近60日**绝对**元差）；render 加【三】利润额块、原边界改【四】并诚实标注"经验系数随工艺/地区/时间浮动、非精确财务利润"；JSON 加 margins、台账 metrics 加 margin_n/margin_latest。
- **真实值（2026-09-02）**：盘面压榨 **1911 元/吨（z=+2.84 处自身历史极高）**、焦化 287（z+0.65）、虚拟钢厂毛利 965（+0.31）、卷螺差 +11、豆棕价差 −476。selftest **8→10组**（新增手算压榨公式/缺腿/非正、利润额z与60日绝对差）。

### ① G12续-b + ② G30续：看板新增6卡（charts.py，20→26图）
- **spread_payload()** 读 spread_lab.json：breadth(back/n/pct)、term_ext（跨期 |z|≥1 取16只按z升序）、chains(11比价z)、margins(5利润z)；缺/坏返 None。三卡：**c-spread-term**（⑩跨期价差z横向条+spread-chips"backwardation 29/64(45%)"）、**c-spread-chain**（产业链比价尾窗z，tooltip最新比价/60日）、**c-spread-margin**（盘面利润z，tooltip元/吨）。通用 `_hzZBar`（横向z条红正绿负、splitNumber:4 防窄值域刻度挤叠）。
- **journal_payload()** 读 trade_journal.json（2563笔）：overall + by_hold_band（按键首数字1→4排序）/by_score_band（弱→轻仓→分批）/by_reason/daily（逐日net+前端累计）。三卡：**c-jr-hold / c-jr-reason**（`_bucketDual` 双轴：净盈亏柱红正绿负左轴+胜率金线右轴，类目轴 rotate18° 防中文挤叠）、**c-jr-daily**（当日净盈亏柱+累计金折线+jr-chips"成交2563/胜率39.7%/PF0.90/期望-31/盈亏比1.36"）。
- build_payload 加⑩spread/⑪journal 两独立 try；renderSpread/renderJournal（ES5）接 loadAndRender 成功+onerror 两路径；test_charts CHART_IDS 补到26、新增 test_spread_payload/test_journal_payload（缺文件None/升序/分档排序/累计/坏JSON）。
- **真实浏览器核验**：6卡均出 canvas（3横向单系列+3双轴），目视无重叠无裁切；复盘卡复现第42轮文字结论——**极短(1-2bar)净−19.7万/胜率30%，≥3bar转正且胜率随持仓档单调升到71%；止损−61.7万(胜率0)/止盈+53.3万；日度累计漂移到−7.9万**；console=[]零错。

### G30 现状（本轮后）
①trade_journal(42轮)✅、③research_review(43轮)✅、**看板复盘页签本轮✅（G30 除②外全清）**；**仅剩 ②纸面-回测一致性**（同信号回测 vs 纸面成交价差/滑点/成交率，须 G14 自采一档盘口 tick_snapshots 校准滑点假设）——G14 仍阻塞，本轮不硬做、不编造。

### 验证台账（本轮全部实跑）
- spread_lab selftest **10组 ALL PASS**；test_spread_lab 8→**10例**；test_charts 26→**28例**。
- 全量 pytest **646→650 全绿**（0 failed/0 error/0 skipped，cache/r56_junit.xml；+4=test_charts+2、test_spread_lab+2）；compileall 根+tools 过；charts 内联 JS（36647字符）node --check 过。
- 隔离：grep 证明 main/analyzer/portfolio/risk_gate/circuit_breaker 对 spread_lab/trade_journal/journal_payload/spread_payload **零 import**；charts.py 为纯增量展示（缺 sidecar 即 None 空态、其余20图与主链行为不变）。运行依赖仍仅 requests/uiautomation/websocket-client。
- 真实入口已跑：spread_lab 重生成 reports/spread_lab.txt/.json（含5利润）+台账；charts --rebuild 刷新 chart_data.js/图表看板.html（26卡）。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump、未 tag、未改 CHANGELOG/README**（延续研究/看板侧增量不发版）。代码 commit **36cbfa2**（5文件+459/−21），文档（本摘要）随后单独 commit；reports/ 与 cache/*.db 在 .gitignore。
- 坑：①PowerShell 内联 python 带转义双引号必 SyntaxError、且无 `%` 格式化，复杂校验写 cache/*.py 跑完即删；②bu 截图在26卡重页面偶发连续 COMMAND_TIMEOUT，bu.resync() 后即恢复（js 1+1 证明页面活着、非崩溃）；③ECharts 窄值域横向条值轴要显式 splitNumber:4、中文类目轴 rotate:18° 防标签挤叠。

### 下一站候选（第57轮）
G12/G28/G30(除②) 均已清。旧排队：**G30②**（唯一剩项，硬阻塞于 G14 常驻一档盘口采集）、G24续（HP/SP 待 G22 分类持仓）、G2策略插件化（纯重构行为不变、风险高）、G8只读Web看板（可选）、G25续（旧因子表达式化须综合分逐字节回归）、G22续/G23续；也可把 portfolio_risk_lab / wf_cost_lab / factor_health 等研究 sidecar 继续接看板（纯展示同套路、低风险）。外部阻塞 G13(无LLM key)/G14(常驻盘口)/G16(等ml_samples)/G17(期权PCR·IV)/G18(Tushare积分)/G20(远期)。

---

## 零-旧、第55轮快速接续摘要（已被第56轮接续，保留存档）

### 本轮（第55轮）一句话
用户点名做两件并回写摘要：**①G28续** 把已有 reports/attribution.json 接进看板做"归因"页签（完全复刻第52轮接 pnav/creview 的套路：离线 sidecar→payload 读取函数→独立 try 进 build_payload→DOM 卡→ES5 render→loadAndRender 两路径→node --check+真实浏览器+test_charts）；**②G12** 新增研究侧 tools/spread_lab.py 产业链/跨期价差监控（纯标准库只读、只监控不下单、不接 main）。pytest **635→646 全绿**、compileall 过、代码 commit **3ea2406**、VERSION 仍 0.52.0 未 bump 未 tag。

### ① G28续 看板"归因"两卡（charts.py，18→20图）
- 新增 `attribution_payload(path=None)`：读 reports/attribution.json，取主周期 **main_h=1440** 块——9 因子的日均收益贡献 contrib（+beta/tstat/ic/share 供 tooltip），按 contrib **升序**排（ECharts 横向类目轴首项在底、升序喂入最大值落顶）；BHB 板块 5 个的 alloc/select/inter/effect；bhb 三分解与 alpha/r2/n/horizons；main_h 缺失回退最大 horizon；缺文件/坏 JSON/horizons 非 dict 一律返 **None**。
- build_payload 第⑨块独立 try 加 `payload["attribution"]`（缺数据只 None 不拖垮其余19图）。
- `_PANEL_DOM` 在⑧熔断两卡后加两张 full 卡：**c-attr-factor**（⑨因子收益归因横向条，红 UP 正/绿 DOWN 负，右标数值、attr-chips 显示"主周期1440/n/α/R²"）与 **c-attr-bhb**（5板块 配置蓝/选择金/交互灰 分组柱）；JS CHART_IDS 18→**20**。
- `renderAttr(d)`（ES5 function 无箭头，复用 signedColor/baseGrid/pct）：无数据 empty 两卡；接入 loadAndRender 成功路径 `renderAttr(D.attribution)` 与 sc.onerror `renderAttr(null)`。
- 验证：抽 _PANEL_JS `node --check` 过（29380字符）；`python charts.py --rebuild` 后 chart_data.js 含 attribution（9因子5板块）；**真实浏览器 file:// 打开，两卡均出 canvas（因子图1系列9点、BHB3系列）、chips="主周期1440/n=235/α=0.0708%/R²=0.193"、console=[]零错、截图目视横向条/BHB柱无重叠无裁切、clear+reload 容错正常**。
- tests/test_charts.py：CHART_IDS 元组补两新卡（该元组此前只到15、第52轮漏更，本轮一并补到20，fragment 与外层 _dashboard_html 逐卡断言）；新增 test_attribution_payload（缺文件None/升序/main_h回退/空horizons/坏JSON）。

### ② G12 产业链/跨期价差监控（tools/spread_lab.py，约430行）
- **跨期（期限结构）**：复用 G22 `term_history.TermHistoryStore().load_contract_bars(sym)`（离线读 cache/term_history.db，ckline 96.6万行/64品种/2017-11~2026-09，字段 sym/code/d/o/h/l/c/s结算/v/p持仓）+ `th.build_term_series` 重建每品种近/次/远月，**不重写**；纯函数 `spread_pct_series`(近/次−1，>0=backwardation)、`rolling_z/rolling_percentile`（120日 PIT 尾窗、min40、零方差/不足返None）、`term_symbol_stat`（最新近次月、价差%、年化carry_nn、level/slope/curv、形态 back/contango、价差与carry的z/分位、OI）。
- **产业链（跨品种）**：读 G21 面板收盘价，`aligned_ratio` 按公共日对齐算**无量纲比价 A/B**（不同合约单位不可直接相减），`chain_stat` 给尾窗 z/分位/近60日变化；固定 **11 对**产业链腿（黑色 卷螺HC/RB·矿螺I/RB·焦化J/JM；油脂 豆棕Y/P·豆菜粕M/RM·油粕Y/M；能化 醇碱MA/SA·聚酯TA/EG·聚烯烃L/PP；有色铜铝CU/AL；贵金属金银AU/AG），两腿都在面板才启用。
- 出 reports/spread_lab.txt|.json + 台账 experiment=spread_lab；research_review.SOURCES 加一行纳入新鲜度表；CLI `--term-db/--panel-db/--selftest`，selftest **8组**。
- **真实64品种诚实结果（2026-09-02）**：**backwardation 广度 29/64=45%**；跨期按年化carry排序，FU/EG 深度 back（+96%/+66% 年化）、MA/BU/SC/LU/JD/PG/PP 等 back，EC 深度 contango（−117% 年化，近交割月年化放大、数据忠实）、LH/UR/CJ contango；|价差z|≥1.5 异常 12 只（SM−5.36/SR−5.26 极端压缩，EG+2.18/CU+2.07/OI+2.08/MA+1.80 等拉伸）。产业链比价极端：**I/RB z=−2.15（铁矿相对螺纹极弱=钢厂利润扩张代理）、M/RM +2.11、MA/SA +2.14、TA/EG −2.09**，余者 |z|<1.5。
- **诚实缺口**：精确加工/压榨/焦化"利润额"需合约乘数·出成率·加工费系数（如压榨利润=0.18豆粕+0.80豆油−大豆−加工费），本轮不硬编易过期系数、只用稳健无量纲比价做偏离监控；跨期为日频结算价、未计手续费/滑点/保证金/换月；固定面板有幸存者偏差；**只监控不下单、不进综合分、不接 main**。

### 验证台账（本轮全部实跑）
- spread_lab selftest **8组 ALL PASS**；新增 tests/test_spread_lab.py **8例**；test_tools_selftest 登记 import+1例；test_charts 25→**26例**。
- 全量 pytest **635→646 全绿**（0 failed/0 error/0 skipped，junit=cache/r55_junit.xml；+11=test_charts+1、test_spread_lab 8、tools_selftest+1、compileall参数化新文件+1）；compileall 根+tools 过；charts 内联 JS node --check 过。
- 隔离：grep 证明 main/analyzer/portfolio/risk_gate/circuit_breaker 对 spread_lab/microstructure_lab **零 import**；charts.py 本轮确有改动但是**纯增量**（新增 key/卡/render，attribution.json 缺失即 None 空态、其余19图与主链逐行为不变，与第52轮同型，全套测试+浏览器兜底）。运行依赖仍仅 requests/uiautomation/websocket-client。
- 真实入口已跑：spread_lab 生成 reports/spread_lab.txt(8763B)/.json(30926B)+台账（term64/back29/chain11/z极端12）；charts --rebuild 刷新 chart_data.js/图表看板.html；research_review.run([]) 重生成、spread_lab/microstructure_lab 两行均 [新鲜] state=ok。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump、未 tag、未改 CHANGELOG/README**（延续第53/54轮研究侧增量不发版）。代码 commit **3ea2406**（6文件+620/−3），文档（本摘要）随后单独 commit；reports/ 与 cache/*.db 在 .gitignore。
- 坑：①PowerShell 无 `%` 字符串格式化、内联 python 含转义双引号易 SyntaxError，复杂校验写 cache/*.py 跑完即删；②research_review 无参跑 selftest、生成要 run([])；③Get-ChildItem 通配列举偶发空、以 python os.path.exists 为准；④bu 截图单独成 cell（合在一个 cell 易 COMMAND_TIMEOUT，重试一次即好）。

### 下一站候选（第56轮）
G28 研究侧归因已在第35轮做、看板本轮补齐=**G28 全清**；G12 本轮交付跨期+无量纲产业链比价，**G12续**=补合约乘数/出成率/加工费系数表后算精确产业利润额、或把 spread_lab 也接看板（期限 carry 热力/backwardation 广度/比价z条形，纯展示同套路）。其余旧排队：G30续看板复盘页签(②纸面-回测一致性须等G14)、G2策略插件化(纯重构行为不变、风险高)、G8只读Web看板(可选)、G25续旧因子表达式化(须综合分逐字节回归)、G22续/G23续；外部阻塞 G13(无key)/G14(常驻盘口采集)/G16(等样本)/G17/G18(积分)/G20(远期)。

---

## 零-旧、第54轮快速接续摘要（已被第55轮接续，保留存档）

### 本轮（第54轮）一句话
用户开放授权"检查还有哪些项目未完成→继续完成→最后更新摘要"。先做只读盘点（G主状态表+各轮下一站+代码TODO扫描），确认 G3/G7 已在第29/37轮完成、G1纸面影子属运行期、G16等样本/G18要积分/G20远期/G14要常驻采集/G2纯重构风险高，**历史"旧排队"反复排最前、且现在纯标准库可独立做完的研究侧项=G24 微结构/持仓/季节因子族**；本轮落地其第一块 `tools/microstructure_lab.py`（研究侧、只读面板、不接 main、生产链路零改动，git diff 为证），负结果诚实呈现、不进综合分。

### 未完成项盘点结论（权威口径=总纲G主状态表+本摘要，不是较旧的WP/P路线图）
- **已完成**：G1✅27-28纸面引擎+**63补OMS全状态台账/主动撤单/成交回报fill_report/持仓对账reconcile（仅余运行期连续影子≥4周后三方对账）**、G3✅29（metrics.py+tear三卡）、G4✅26+62滚动WF/基准、G5全✅47-51、G6/G9/G10/G11✅25、G7 TSMOM研究侧✅37、G19✅46、G21✅36-37、G25✅38表达式引擎+59/60/61续+**63续 KDJ(K/D/J)·EMA12/26表达式化逐位parity(catalog43条)、OOS分层多空/换手/真实成本(万1.5)**、G26✅40-41+49+52-53、G27✅44-45、G28✅35+55（全清）、G29✅37+39、G30①③✅42-43+56（仅②待G14）、G12✅55-56（全清）、G24✅54+57代理（真HP/SP待G22）、G8✅57只读Web看板、G2✅57-60 9/9全适配器化+61默认注册表取分（**63用户拍板保留内联双轨不删、可PLUGIN_PARTS_ENABLED=False回退，on/off双哈希=基线**）；G15分钟库=⏳持续常驻。
- **本轮做掉**：**G1 paper_broker 补 OMS全状态台账/主动撤单/成交回报/持仓对账（纯函数+方法+5测试）**；**G25续 KDJ(K/D/J)/EMA(12/26)表达式化并对过程式逐位parity、orthogonal_blend_oos 加分层多空/换手/真实成本**。
- **仍开放、纯标准库可续做**：G1 运行期连续影子≥4周后与 signal_outcomes/回测三方对账（G30②纸面-回测一致性仍唯一硬阻塞 G14 常驻一档盘口 tick_snapshots）、G25续（分层组合按H对齐再平衡消重叠/降换手检验、更多过程式量表达式化、orthogonal_ic_blend 待 G16 样本）、G24真HP/SP（待G22会员龙虎榜/COT分类持仓采集，代理版已交付）、G22续(tradable_mask涨跌停掩码/交割日历/面板增量)/G23续(carry换手容量/更长样本)；研究sidecar接看板已基本接齐。
- **外部/样本阻塞、暂不可做**：G13 LLM第二意见（无key休眠、无llm_reviewer.py）、G16浅ML（须ml_samples积累+成本后≥规则才上线、无ml_inference.py）、G17期权PCR/官方IV、G18 Tushare（需2000/5000积分、无tushare_client.py）、G14一档盘口快照自采（需常驻采集线程，是G30②/精确容量前置）、G20实盘闭环（纸面满3月+合规穿透，远期）。
- 代码全树 TODO 扫描几乎为空（仅 factor_expr.py:372 未知算子报错、report.py:917 空态文案，均非待办）。

### 新增模块与纯函数（tools/microstructure_lab.py，约450行，合成可断言）
- 复用：`pb.PanelStore` 读 cache/research_panel.db（面板列含 sym/date/sector/o/h/l/c/**v量/oi持仓**/ret1d/hv/ret5..252/tsmom*/fund_*，61353行全64品种2022-06-28~2026-09-02）、`fh.rows_by_symbol/forward_map`、`feval.spearman/quantile_buckets/monotonic_score`、`el.safe_record` 旁路台账；**不 import config/main/analyzer**，路径自管（LAB_TXT/JSON=reports/）。
- 五个因子（全部PIT尾窗、对齐序列、缺失/非正安全降级None）：`pct_change(series,win)`=OI的win期变化率(doi1/doi5)；`rolling_amihud(rets,c,v,20)`=尾窗mean(|ret|/(c*v))非流动性（展示×1e9、正线性缩放不影响秩）；`rolling_idiovol(rets,mkt,60)`=尾窗把ret_i对**全市场等权收益**（market_by_date逐日截面均值）做一元OLS取残差标准差（纯市场驱动→≈0）；`rolling_skew(_skew)`=三阶标准化矩滚动偏度；`calendar_seasonality(bysym,'month'/'weekday')`=分自然月/星期池化均值/中位/上涨占比（坏日期跳过）。
- 检验：`factor_forward_curve` 跨品种池化对H=1/5/20给 n/RankIC/Q5-Q1/单调度/五档均值与上涨率（n<40不给IC不编造）；`render` 三板块（①连续因子前向检验②日历季节③数据缺口/诚实边界）；`run` 出 reports/microstructure_lab.txt|.json + 台账 experiment=microstructure_lab（两次运行 config_hash 相同自动 repeat_of 串联）；CLI `--db/--selftest`，selftest **8组**。
- research_review.py SOURCES +1 行（factor_regime后）把 microstructure_lab.json 纳入一键复盘新鲜度表（通用load_sidecar、缺/坏安全降级）；**注意 research_review 无参 `python tools/research_review.py` 默认跑 selftest，真正生成要带参数或 run([])/run(sys.argv[1:])**。

### 真实数据诚实结论（全64品种，固定面板，未计成本；research不进综合分）
- **五因子都没有稳定截面预测力**：doi1 H1/5/20 IC −0.004/+0.012/−0.012、doi5 +0.008/+0.023/−0.021、amihud20 −0.008/−0.013/−0.038、idiovol60 −0.005/−0.027/−0.062（H20最弱负向、|IC|仍<0.07）、skew60 −0.002/−0.006/−0.010；Q5-Q1 全在 ±0.45% 内、单调度0.25~1.0不稳。按既有门槛（|IC|≥0.05弱信号/≥0.10较稳）**全部不达**，与 tsmom/expr 研究侧负结果一致→只归档、不挂影子、不进综合分。
- **日历季节近平**：12个月池化日均收益在 −0.11%(10月)~+0.07%(1月)、上涨占比46%~51%；周一~周五 −0.06%~+0.06%、约47%~52%；无经济显著的"旺/淡季"或星期效应（样本仅4.1年、有幸存者偏差，仅历史描述不外推）。
- **诚实缺口**：HP/SP（hedging/speculative pressure 套保/投机压力）需交易者分类持仓（类CFTC COT），本面板只有总持仓量oi→本轮不做不编造；日频微结构无法替代逐笔/盘口，精确流动性/滑点待 G14 一档快照自采；Amihud 用 c*v 估成交额、品种间单位不统一，仅时序/截面秩可比。

### 验证台账（本轮全部实跑）
- 模块 selftest **8组 ALL PASS**；新增 tests/test_microstructure_lab.py **9例**（pct_change边界/Amihud常数与零量/偏度对称与左右尾/特异波动纯市场≈0与含噪保留/市场等权/build对齐与PIT无前缀/完美预见IC≈1与样本不足护栏/季节注入与坏日期/render三板块）；test_tools_selftest.py 登记 import+1例。
- 全量 pytest **624→635 全绿**（0 failed/0 error/0 skipped，junit=cache/r54_junit.xml；+11=9专属+1 selftest登记+1 compileall参数化新文件）；compileall 根+tools 全过。
- **生产零改动等价证明**：git status/diff --stat 变更面仅 tools/microstructure_lab.py(新)、tests/test_microstructure_lab.py(新)、tests/test_tools_selftest.py(+6)、tools/research_review.py(+1)；**无任何根生产模块改动**（main/analyzer/portfolio/综合分/risk_gate/circuit_breaker/storage表结构全部未碰，grep microstructure 在生产链零命中），故默认8品种生产输出逐字节不变（git层面已证；基线 equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 仍为权威参照）。运行依赖仍仅 requests/uiautomation/websocket-client。
- 真实入口已跑：python tools/microstructure_lab.py 生成 reports/microstructure_lab.txt(3051B)/.json(9228B)+台账；research_review.run([]) 重生成 research_review.txt/.json，G24 行显示"[新鲜]…state=ok"。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump、未打 tag、未改 CHANGELOG/README**（沿用第53轮做法：研究侧增量不发版；如需 v0.54.0 可再补文档同步）。代码已 commit **df51ee0**（4文件+602行），文档（本摘要）随后单独 commit；reports/ 在 .gitignore 不跟踪。
- PowerShell 坑复现：`Get-ChildItem reports\x.*` 通配在多命令行里偶发列举为空（文件其实在），核对落盘用 python os.path.exists 更可靠；research_review 无参=selftest 别误以为已生成报告。

### 下一站候选（第55轮）
优先**G28续**（把已在的 reports/attribution.json 接看板做"归因"页签/卡，纯展示侧、和第52轮接pnav/creview同套路，风险最低）或 **G30续 看板复盘页签**；研究侧可做 **G12 产业链/跨期价差监控**（term_history 已有期限/OI底座，只监控不下单）；G24若要继续=等G22分类持仓补HP/SP。旧排队：G1纸面影子(运行期)/G2/G8/G14/G13/G16/G17/G18/G20（阻塞原因见上）。

---

## 零-旧、第53轮快速接续摘要（已被第54轮接续，保留存档）

### 本轮（第53轮）一句话
跨会话接续，先按"教训3 以磁盘为准"复验第52轮三项，发现**①只做了一半**（稠密61跑了三档 gross，但全64口径仍只跑 gross=1），本轮把**全64品种也跑满 gross(1.0/1.2/1.5)×四sizing×段首日换手成本**（报告/CSV/JSON/台账/selftest/pytest 全链路补齐）；②③复验确认已完成，仅修掉看板⑧熔断图两处只有浏览器可见的轴名重叠/裁切。pytest **623→624 全绿**、稠密61数值逐点不变（默认等价）、纯标准库/只读/不接 main。

### 为什么会有这一轮（关键：摘要叙事≠磁盘）
- 用户重发"G26续二①全64×多gross网格 ②净值/熔断接看板新页签 ③wf多周期/purged+embargo"。第52轮摘要**首行**声称①已做"全64×多gross"，但其详细行与代码一致地写着"全64只跑 gross=1 对照（←续二①要把全64跑满三档）"。
- 逐文件核磁盘结论：`git log` 589fd2e(功能)+ca2d610(文档)、工作区干净、VERSION=v0.52.0；③机制与实跑齐全（wf_cost_lab.txt 头部"WF窗口20训10测/40训20测｜purge=2 embargo=1"，每品种都有"多周期/防前视对照"双窗块）；②charts.py（**在仓库根目录、不在 web/，摘要里 web/charts.py 是路径笔误**）18 图卡齐全、c-pnav/c-creview-sweep/c-creview-fwd 均渲染；**唯独①的 tools/portfolio_lab.py run() 第288行 `gross_cost_grid(proxy_all,(1.0,),…)` 全64只传了 (1.0,)**。

### ① portfolio_lab 全64三档 gross×换手成本（本轮真正补的开发）
- run()：全64口径 `gross_grid_all = gross_cost_grid(proxy_all, gross_list, …)`（gross_list=DEFAULT_GROSS_GRID=(1,1.2,1.5)，与稠密主宇宙同轨迹同成本1.5bp），不再只跑1档。
- 报告【三】：删掉原"全64只报 gross=1 净夏普"单行，改为与稠密表**同列**的全64三档子表（"─ 全64品种口径…三档 gross 全跑："，四方法×三档共12行）。
- `reports/portfolio_gross_grid.csv` 长表**12→13列**：新增首列 `universe`（fixed61=稠密主口径12行 / all64=全品种12行，各=4方法×3档，合计24数据行）；稠密 fixed61 行数值与旧版逐点一致（只多了首列标签）。
- JSON `all_universe`：新增 `gross_grid`（四方法×三档完整），**保留 gross1**（=三档中 gross=1 那档）向后兼容；实验台账 params 补 gross_grid/one_way_cost/all_universe_n。
- selftest 11→**12组**（新增"全品种三档网格"：fill_missing 含稀疏S4的5品种 proxy 也三档齐全、波动随gross单调、净夏普≤毛）；tests/test_portfolio_nav.py **+1例** test_all_universe_gross_grid_runs_three_levels。
- **全64三档真实数值（样本外，单边1.5bp，缺失日补0）**：gross=1 净夏普延续旧记录 等权0.35/逆波动0.48/ERC0.38/gmv−0.18；放大到1.2/1.5后——等权 年化3.31%→3.88%→4.66%、波动9.46%→11.35%→14.18%、夏普0.35→0.34→0.33、回撤9.78%→11.65%→14.40%；逆波动 4.01%/0.48→5.79%/0.46；ERC 2.71%/0.38→3.90%/0.37；**gmv 全档为负 −0.90%/−0.18→−1.43%/−0.19（杠杆放大其亏损）**。结论与稠密61一致：杠杆等比放大收益与波动/回撤、毛夏普几乎不变、成本拖累极小（等权/逆波动≈0、ERC≈0.01%、gmv 0.03→0.04%/年），**杠杆改风险预算不产生alpha；全64口径因稀疏品种补0稀释，夏普整体低于稠密61，主结论仍以稠密61为准（诚实标注覆盖偏差）**。

### ② 看板"图表看板"页签（复验已完成 + 修2处显示缺陷，教训8双关）
- 架构：外层 report.py `_DASHBOARD_TABS` 的 ("__charts__","图表看板") 页签**同页内嵌** charts.dashboard_embed_parts()（非iframe）；独立直链页=reports/图表看板.html（charts.ensure_charts_page/--rebuild 幂等生成）。⑦组合历史净值(c-pnav)+⑧熔断网格(c-creview-sweep)+⑧远期对照(c-creview-fwd) 三新卡已在，CHART_IDS 15→18。
- 本轮浏览器实测发现并修复（charts.py 仅4行、纯显示串、零数据改动）：⑧左图是半宽 `card`（非 card full），左对齐图例与 y 轴名"触发次数"（nameLocation=end 在左上）**重叠**、x 轴名"单日损失阈值"在右端**被裁切**；而右侧兄弟图本就无轴名。改法=去掉 sweep 图这两处冗余轴名（h3标题与0.5%/1%/2%刻度已自解释），与兄弟图及 c-ic/c-mono 实际被 Object.assign 覆盖为空轴名的既成行为一致。
- 双关证据：抽内联脚本 `node --check` 通过（2段26207字符）；真实浏览器 file:// 打开，18卡中13张出 canvas（c-sector/c-xs/c-paper×3 为离线无实时/纸面数据的**预期空态**、占位文案优雅非崩溃），**console 零报错**，⑦四净值曲线+⑧两图渲染正常，修复后 xName/yName 均为空、图例4项独立成行无重叠。

### ③ wf_cost_lab 多周期 / purge+embargo（复验：机制与实跑均已完成，本轮未改码）
- tools/backtest_validation.py walk_forward(matrix,train,test,step,purge,embargo)：IS=[s,s+train-purge)、OOS=[s+train+embargo,…)、滚动条件 s+train+embargo+test<=t、train-purge<2安全返空、回传purge/embargo；另有 purged_kfold_splits（AFML ch7）。
- tools/wf_cost_lab.py run_symbol 收 wf_multi（多组 train/test 各配 purge/embargo），CLI --wf-train/--wf-test 逗号多值配对 + --purge/--embargo；render"多周期/防前视对照"块、报告头标窗口与隔离带、selftest 9组全过。实跑产物 reports/wf_cost_lab.{txt,json} 已用 20训10测+40训20测、purge=2/embargo=1 生成（RB/MA/I/TA）。

### 验证台账（本轮全部实跑）
- selftest：portfolio_lab **12组** ALL PASS、wf_cost_lab 9组、backtest_validation 全过；`python -m compileall` OK。
- 全量 pytest：**624 passed / 0 failed / 0 error / 0 skipped**（junit=cache/r53_junit.xml；第52轮为623，本轮+1）；定向 test_portfolio_nav 10例、test_charts 25例全过。
- 真实入口：python tools/portfolio_lab.py 重跑刷新 reports/portfolio_lab.{txt,json}/portfolio_nav.csv/portfolio_gross_grid.csv 与台账；python charts.py --rebuild 刷新 chart_data.js/图表看板.html；node --check + 浏览器截图/console 双关。
- 改动范围（git diff --stat）：仅 charts.py(4行)、tests/test_portfolio_nav.py(+18)、tools/portfolio_lab.py(+50/−15)；**未碰 main/analyzer/综合分/portfolio 默认 CSV/storage 表结构**；reports/ 为 .gitignore 研究产物。稠密61 gross 数值与第52轮逐点一致（等权1.0=年化3.97%/波9.48%/夏普0.42/回撤9.54% 等）。

### 边界 / 注意
- VERSION 仍 **0.52.0 未 bump**（本轮是 v0.52.0 之上的研究侧补全+复验，未改 CHANGELOG/README、未打 tag；如需按惯例发 v0.53.0 文档同步可再做）。代码改动在工作区（3文件），是否 commit/tag 待用户定。
- 外层 reports/实时报告.html 是监控轮次产物（18:38 早于研究改动），其"图表看板"页签内嵌片段会在**下一轮 main 监控自动刷新**；独立 图表看板.html 已为最新，直接打开即可见全部18图。
- 教训3再验证一次：跨会话务必"git log/status + 读源码函数体 + 跑测试"三对账，摘要首行的完成度宣称可能超前于磁盘。

### 下一站候选
G29 因子体检（路线图优先级上升）/ G26组合构建宜在G2后；本轮 gross 网格结论已稳，可考虑把全64三档也接进看板⑦附近做稠密vs全品种对照小图（非必须）。

---

## 零-旧、第52轮快速接续摘要（已被第53轮补全/复验，保留存档）

### 本轮（第52轮）一句话
三项全是**研究工具+看板展示侧**增强，不碰 main/analyzer/综合分/portfolio默认CSV/storage表结构：①portfolio_lab 把四方法 sizing 延伸到 gross(1/1.2/1.5) 杠杆网格×段首日换手成本，并补全64品种 fill_missing 覆盖率对照；②charts 看板接入第49轮组合净值+第50轮熔断校准两份离线产物（新增3卡，15→18图）；③backtest_validation.walk_forward 加 AFML ch7 purge/embargo 防前视隔离带、wf_cost_lab 支持多窗口周期对照。默认参数全部等价旧版（双哈希逐字节一致为证）。

### 改了哪六个文件（4生产+2测试）
1. **tools/portfolio_lab.py**：rolling_proxy 每方法出参加 `seg_bounds=[{start,length,entry_turnover}]`（首段 entry_turnover=None）；新增 DEFAULT_GROSS_GRID=(1,1.2,1.5)/DEFAULT_ONEWAY_COST=1.5e-4(1.5bp)、纯函数 `gross_net_daily(daily,bounds,gross,cost)->(net,charges)`（先整体×gross，再按段pos偏移把成本落段首点；gross=1/cost=0逐点恒等）、`gross_cost_grid(proxy,gross_list,cost,methods)`（净=扣费/毛=零成本对照，净夏普≤毛夏普、波动随gross单调）；dense_matrix 加 fill_missing（False=旧稠密61宇宙默认等价，True=全64缺失ret补0）；run() 主宇宙跑三档网格、全64只跑gross=1对照，报告加【三】块、落 reports/portfolio_gross_grid.csv(长表12列)、json加gross_grid/gross_one_way_cost/gross_grid_csv/all_universe、台账artifacts加gross_path；selftest 8→11组。
2. **tools/backtest_validation.py**：walk_forward(matrix,train,test,step=None,purge=0,embargo=0)，IS=[start,start+train-purge)、OOS=[start+train+embargo, start+train+embargo+test)、滚动条件 start+train+embargo+test<=t、train-purge<2安全返空、返回dict与每段回传purge/embargo；**purge=embargo=0时切窗与旧版逐段一致**；selftest加隔离带断言。
3. **tools/wf_cost_lab.py**：run_symbol 加 purge/embargo/wf_presets，对多组(train,test)各跑walk_forward+wf_stability收集 `wf_multi`，**默认组(20/10/0/0)仍填stability/wf_summary向后兼容**；CLI --wf-train/--wf-test 接受逗号多值配对、新增--purge/--embargo；render加"多周期/防前视对照"小段、报告头标窗口与隔离带、台账params同步；selftest 8→9组。
4. **charts.py（根，被main每轮write_chart_data调用）**：新增 `portfolio_nav_payload`（读reports/portfolio_nav.csv四方法净值抽稀+期末/回撤摘要，缺/坏返None）与 `circuit_review_payload`（读reports/circuit_review.json抽阈值网格六档四方法触发数+1%校准档T+1/3/5/10条件远期vs基准+counts，路径=os.path.dirname(config.PC_JSON)拼）；build_payload 两键各独立try；_PANEL_DOM 加 c-pnav(四方法净值full)/c-creview-sweep(阈值触发柱)/c-creview-fwd(条件远期柱)三卡；CHART_IDS 15→18；renderPnav/renderCreview（ES5 function禁箭头、复用BLUE/GOLD/UP/baseGrid/axisStyle）接入loadAndRender与onerror空态。
5. tests/test_portfolio_nav.py +3例（gross_net_daily段首日收费/首段None不收费手算、gross网格波动单调且净≤毛、fill_missing保全部品种）；6. tests/test_charts.py +2例（两payload合成解析+缺文件/坏表None）。

### 真实数据诚实结论
- **①gross网格（固定宇宙61品种、样本外378日）**：gross1→1.5等比放大收益与波动、毛夏普几乎不变（等权年化3.97%→5.66%/波动9.48%→14.22%/夏普0.42→0.40/回撤9.54%→14.06%；逆波动0.55→0.53仍最优；gmv0.35→0.33仍垫底）；1.5bp+20日再平衡下**换手成本拖累极小**（等权/逆波动≈0.00%、ERC0.01%、gmv最高0.03~0.04%/年）=杠杆放大风险而非风险调整收益。**全64 fill_missing口径夏普整体下移**（等权0.42→0.35/逆波动0.55→0.48/ERC0.53→0.38/gmv0.34→−0.18）=稠密筛选有轻微覆盖偏差、稀疏品种补0稀释gmv，主结论仍以稠密61宇宙为准。诚实标注：gross为线性多头杠杆近似、未计保证金/强平/融资成本。
- **③隔离带（RB/MA/I/TA 30m、purge=2 embargo=1、20训10测 vs 40训20测）**：RB/I两窗口均漂移（短窗OOS夏普−0.116/−0.172、锚定33%、切换4次），MA长窗略好(+0.141/锚定50%)，TA短窗+0.072转长窗−0.029；**无一品种在两窗口+隔离带下都稳定占优**（诚实负结果，CTA参数样本外优势经不起防前视收紧，不宜据此上杠杆）。
- **②看板真实取数**：nav 378点四方法期末1.0624/1.0723/1.0597/1.0268；review等权阈值触发72/32/18/2/1/0、T+1条件−0.16%vs基准+0.01%；charts --rebuild后chart_data.js含两键、图表看板.html含三新卡。

### 证据（可复跑）
- pytest **618→623全绿**（0失败/错误/跳过，cache/r52_junit.xml，约8s）；compileall全项目过；portfolio_lab selftest 11组、wf_cost_lab 9组、backtest_validation selftest含隔离带组。
- 默认8品种回归 `python portfolio.py --codes I,MA,RB,SA,TA,AU,AG,CU --period 30`：equity=**c4da4cdf61f3bcdc**、trades=**50dcc800d326f8e9** 双sha256**逐字节一致**（本轮没动主链）；grep证明main/analyzer/portfolio对portfolio_lab/wf_cost_lab/backtest_validation零import（仅tools互引+tests）。
- 规模：生产 **68 py / 30797 行**（根47/20606、tools21/10191）；tests **39个test_*.py（+conftest=40）/6301行/623用例**；看板15→18图；storage仍14业务表；运行依赖仍仅requests/uiautomation/websocket-client。
- git：代码commit **589fd2e**，文档commit后打 **tag v0.52.0**；VERSION 0.51.0→0.52.0（有BOM）。

### 关键坑（本轮新增，下轮勿踩）
- charts 被 main 每轮调 write_chart_data，新增payload**必须独立try、文件缺失返None**，绝不能让研究产物缺失拖垮既有15图或主链；JS内嵌_PANEL_JS禁箭头函数用function、配色复用常量。
- walk_forward 加隔离带后滚动终止条件别忘加 embargo（start+train+embargo+test<=t），否则末段会越界；purge只缩IS不后移、embargo只后移OOS，两者正交。
- PowerShell here-string(@'...'@)往测试文件追加**深层嵌套dict括号会被吞导致'{' never closed**，复杂payload改成分步构造（先建子dict再组装）；内联python含双引号串也易SyntaxError，校验写cache/*.py。
- portfolio_nav/circuit_review 在 reports 下无config常量，用 os.path.join(os.path.dirname(config.PC_JSON),'xxx') 定位；看板页文件名是 reports/图表看板.html（不是dashboard.html）。

### 下一站候选（第53轮）
G24/G28续/G1纸面影子/G2/G8/G14（盘口容量，wf_cost_lab estimate_turnover_capacity已留"精确待G14"钩子）/G13/G3/G7/G12；或把 portfolio_risk_lab 也接看板。G5已全清，本轮研究/看板侧增强闭环。

## 零.旧、最新快速接续摘要（第51轮，已被第52轮接续，存档）

### 本轮（第51轮）一句话
G5④ 最后一个增强欠账清账：第48轮熔断在 delever 档只给"建议减仓50%"文字，本轮新增 `paper_delever` 模式把它变成**显式开关下的纸面自动减仓执行**；默认 `CIRCUIT_ACTION='observe'` 时 self.breaker=None、on_cycle 完全不进减仓分支，**不传逐字节等价（双哈希为证）**。代码 commit 7a20548、文档 commit 见 git log、tag **v0.51.0**，VERSION=0.51.0。

### 改了哪四个生产文件（全部默认关闭/不传等价）
1. **portfolio.py `Portfolio.close(sym,price,dt,reason,leg=,reduce_lots=None)`**：None 或 ≥持仓=整仓全平原路径（**全平开仓费必须直取 pos.open_fee_yuan，禁止 x*held/held——本轮双哈希一度失配（15.6302608176 vs …817599998）就是它引入的浮点末位，已修**）；0<reduce_lots<持仓=部分减仓：close_lots=rl、按手数比例分摊开仓费（仅 partial 分支做乘法）、pos.lots 扣减并保留不 pop、只减不反向；rl≤0 返 None。rec 增 `partial/remaining`（lots=本次平仓手数），closed 两种都记。
2. **circuit_breaker.py**：加 `PAPER_DELEVER="paper_delever"`、`ACTION_MODES=(observe,paper_halt,paper_delever)`、`_HALT_MODES=(PAPER_HALT,PAPER_DELEVER)`；纯函数 `reduce_lots_of(held,ratio)=math.floor(held*ratio)`（不足1手/非法返0=绝不把减半变清仓）、`delever_plan(brief,ratio,done=None)`（只收 1≤rl<held、跳过 done、按 sym 排序、返[{sym,direction,held_lots,reduce_lots}]、不改入参）；__init__ 加 `self._delever_done=set()`、_reset_day 清空；open_allowed 改 `action_mode in _HALT_MODES`；新增方法 `delever_targets(brief)`（仅 paper_delever 且 level==DELEVER 出计划否则[]）、`mark_delevered(sym)`；decision 增 `auto_delever=(mode==PAPER_DELEVER and level==DELEVER)`、delever 文案三模式区分；selftest 11→**15组**（`python circuit_breaker.py`）。
3. **paper_broker.py**：①构造挂载条件 `==PAPER_HALT`→`in (PAPER_HALT,PAPER_DELEVER)`；②`_fill_leg` 平仓分支 pf.close 透传 `reduce_lots=order.get('reduce_lots')`、close_reason 可由 order 覆盖（="熔断自动减仓"）、**仅 rec.remaining≤0 才 pos_ref.pop**（部分减仓保留持仓）；③新增 `_delever_cut(ts,by_sym,by_quote)` 并在 **on_cycle 阶段A _process_pending 后、阶段B信号前**调用（返回 trades/orders，summary 增 `n_delever`）：breaker None/非 paper_delever/plan 空→[]；brief 现取 pf.positions；逐项取价（行价→pf._last_prices→entry_price）、锁板 locked_at_quote（平多 sell/平空 buy，用模块级 `_side_of(dir,'close')`）、无价/锁板**不 mark 下轮重试**、_make_order(action='close')+order['reduce_lots']/['close_reason']、**当轮立即 _fill_leg 成交**（照 _liquidate 不走 next，因决策来自上一轮阶段D=晚一轮无未来、风控减仓不再等），成交才 mark_delevered。
4. **config.py 565-571 仅注释**：CIRCUIT_ACTION 允许值补 paper_delever、DELEVER 段注明自动执行；**值全不变**（ENABLED=True、ACTION='observe' 默认、WARN2/HALT3/DELEVER5/RATIO0.5）。

### 纪律（时序/安全）
断路器在 on_cycle 阶段D快照后 update→供**下一**轮用（自动减仓严格晚一轮、无未来）；当日各品种只减一次（_delever_done，日切 _reset_day 清空=新一天再到 delever 可再减）；floor 保证只减不清、只平不反向；observe 默认 breaker=None 逐字节等价；PAPER_ENABLED 默认 False 不触真实账户。

### 证据（可复跑）
- pytest **610→618 全绿**（0失败/错误/跳过，cache/r51_junit.xml，约8s）：test_circuit_breaker +4（reduce_lots_of/delever_plan/paper_delever停开+计划+当日一次+日切/observe·paper_halt不出计划，含参数化）、test_paper_broker +3（内核部分平仓剩余+分批净盈亏==一次全平+0手不减/超持仓全平、paper_delever晚一轮减一半当日一次、observe/paper_halt零减仓）。
- 默认8品种回归 `python portfolio.py --codes I,MA,RB,SA,TA,AU,AG,CU --period 30`：equity=**c4da4cdf61f3bcdc**、trades=**50dcc800d326f8e9** 双 sha256 逐字节一致（修复全平开仓费浮点末位后复算一致）；compileall 全过；circuit_breaker selftest 15组。
- 规模：生产 **68 py / 30418 行**（根47/20438、tools21/9980）；tests **39 个 test_*.py（+conftest=40）/6210 行/618 用例**；运行依赖仍仅 requests/uiautomation/websocket-client；台账仍7类。

### 关键坑（本轮新增，下轮勿踩）
- **默认等价的浮点陷阱**：全平路径做 `open_fee*held/held` 会产生 x.xx…998 末位误差污染 trades CSV（equity 不受影响），全平必须直取原值、只在 partial 分支按比例算。
- 合成冒烟里喂 on_cycle 的 row 若 score 落在中性带（|score|<PAPER_EXIT_SCORE=2）会先被信号平掉导致 brief 空；要"只持有不交易"给 score=3.0（entry4/exit2 迟滞带）。
- PowerShell 内联多行中文+反引号长串会 SyntaxError，复杂补丁一律写 cache/*.py 跑完即删；文档 utf-8-sig 读、按原 BOM 写（VERSION/上下文摘要有BOM，CHANGELOG/README/tests-README/总纲无BOM）、newline 用 \n。

### 下一站候选（第52轮，G5 已全清，转其他 G）
①G26续二 日线全64+多 gross(1.0/1.2/1.5)网格影子/换手成本（四 sizing 已齐，最连贯）；②portfolio_nav.csv/circuit_review 接看板 charts 增净值/回撤/阈值页签（须先 Read 重读 doubao-visualization SKILL.md 与 references）；③wf_cost_lab 多周期/purged+embargo。旧排队：G24/G28续/G1纸面影子/G2/G8/G14/G13/G3/G7/G12。
# （历史）# 上下文摘要（2026-09-03，更新至**第51轮 G5④ delever 纸面自动减仓执行：新增第三种动作模式 paper_delever（默认 observe 完全不动），Portfolio.close 加 reduce_lots 部分平仓、circuit_breaker 加减仓计划纯函数与当日一次/日切、paper_broker 新增 _delever_cut 阶段A后晚一轮无未来执行；默认8品种双哈希逐字节一致、pytest 610→618全绿、tag v0.51.0；至此 G5 五项①~⑤及④两项增强欠账（阈值校准+自动减仓）全部清账**）
# （历史）# 上下文摘要（2026-09-03，更新至**第50轮 G5④熔断阈值历史校准：新增 tools/circuit_review.py 日频逐日代理只读面板，把第48轮熔断阈值放真实历史净值回放；结论=61品种分散组合最差单日仅-2.15%、默认3%halt日频0触发、1%观察档仅次日微弱续跌，维持默认阈值不改；610用例全绿、双哈希一致、tag v0.50.0**）
# （历史）# 上下文摘要（2026-09-03，更新至**第49轮 G5⑤ 第四种风险型sizing=gmv最小方差接入回测内核 + 多品种组合历史净值曲线（G5五项收尾）：①gmv 补入 RISK_SIZING_METHODS/--risk-sizing/--compare-risk（三法→四法影子对照，复用 trailing_risk_weights 严格PIT，默认留空=旧等名义不传等价）；②tools/portfolio_lab.py 增 nav_curve/drawdown_window 纯函数 + rolling_proxy 记日期对齐idx，落逐日组合历史净值 reports/portfolio_nav.csv（四方法date+日收益+复利净值，379日）、json增nav_summary、报告增期末净值/最深回撤两行；真实61品种504日：期末净值 等权1.0624/逆波动1.0723/ERC1.0597/gmv1.0268，年化波动9.48→8.35→7.11→gmv4.93%(较等权-48%)、回撤9.54→7.79→6.46→5.26%单调降，但gmv夏普0.35反降(等权0.42/逆波动0.55/ERC0.53)、有效N仅11.4、年换手1.77最高=降波动最猛却牺牲收益且集中，只宜对照不默认启用；pytest593→600全绿、默认8品种双哈希逐字节一致、main编译过、台账6类各1条、tag v0.49.0；G5①~⑤全部完成，仅余G5④ delever自动减仓执行留增强**）
# （历史）# 上下文摘要（2026-09-03，更新至**第48轮 G5④ 组合层单日浮亏熔断（risk_gate 由只标注升级为可配置动作）：新增根模块 circuit_breaker.py（315行纯标准库、零网络、纯决策不直接下单/不改持仓），与 risk_gate 的单品种信号级 veto 正交、补组合账户级断路器——盯当日日初权益→当前权益浮亏，warn≥2%只提示/halt≥3%停开新仓(只平不开)/delever≥5%加建议减仓50%文字(不自动砍仓)，保证金风险度≥95%第二触发抬到halt；当日粘性(峰值浮亏定档、触发后不回落解锁防抖动)、跨交易日自动重置重计日初基准；动作模式 CIRCUIT_ACTION 默认 observe 只标注(PaperBroker 不挂断路器 self.breaker=None、委托逐字节等价旧版)，显式 paper_halt 才在 on_cycle 阶段B filter_orders 剔 open/reverse_open 腿保留 close/reverse_close(反手只平不反向开)，阶段D快照后更新供下一轮无未来函数；config 增 CIRCUIT_*7常量；pytest572→593全绿、默认8品种双哈希逐字节一致、main --once exit0、tag v0.48.0；自动减仓执行/G5⑤风险平价sizing+组合历史净值回测留续**）
# （历史）# 上下文摘要（2026-09-03，更新至**第47轮 G5 组合层风险（研究侧①②③）：新增根模块 portfolio_risk.py（347行纯标准库纯函数，协方差复用 portfolio_constructor）+ tools/portfolio_risk_lab.py（241行只读G21面板、出 reports/portfolio_risk_lab.txt|.json、台账登记）；①相关矩阵/平均绝对相关/板块块/最强最弱对 ②组合VaR双口径（历史模拟VaR+ES含肥尾、参数z·σ与√h多日）+分散化收益，对equal/inv_vol/erc/gmv对照 ③原油SC经OLS beta的±5%/-10%线性一阶压力；真实61品种126日窗：平均绝对相关0.161、板块内贵金属0.60/能化0.35、等权历史VaR95=1.06%/ES1.21%/参数0.98%(肥尾溢价+8%)/10日3.11%、erc0.66%/gmv0.40%(有效N 61→34.8/12.3代价)、原油-5%等权-0.61%；只读研究不接main不改risk_gate/综合分/sizing（grep零引用），pytest549→572全绿、默认8品种双哈希逐字节一致、tag v0.47.0；G5④熔断动作/⑤风险平价sizing+组合净值回测留续**）
# （历史）上下文摘要（2026-09-03，更新至**第46轮 G19 数据库在线热备份+开机自启导出+灾备恢复+main只读--version：新增根模块 db_backup.py（533行纯标准库sqlite、只读源库只写backup/、不接主循环不改综合分），Online Backup API一致性热备（源只读URI、WAL安全、常驻写库不用停、360MB约10s）+副本quick_check防假备份+滚动保留30份且只认自命名绝不误删+--list/--verify/--restore（现有库改名.before_restore留存不覆盖）+导出run_backup.bat与任务计划XML（每日16:30+登录、只导出不自动注册）+《灾备恢复Runbook》+main/db_backup只读--version；真实359.8MB/14表467695行备份+恢复演练通过；pytest526→549全绿、默认8品种双哈希逐字节一致、main --once零异常、tag v0.46.0**）
# （历史）上下文摘要（2026-09-03，更新至**第45轮 G27②③ walk-forward参数稳定性+fee/slip成本敏感性曲面/换手容量：新增 tools/wf_cost_lab.py（601行纯标准库、只读monitor.db，复用 backtest_validation 的参数网格/WF引擎不重写）；②wf_stability 输出选中参数轨迹/锚定率/切换率/IS→OOS衰减并按稳定·一般·漂移评级，③fee×slip 5×5净收益曲面+成本安全垫倍数+分钟量换手/参与率容量数量级（精确待G14）；真实RB·I漂移且基准档已亏安全垫0、MA一般最扛成本(+17.8%双轴∞)、TA漂移但+6.5%滑点4倍垫；pytest 510→526全绿、默认8品种双哈希逐字节一致、main/analyzer零import、main --once零异常、tag v0.45.0；至此G27①②③全部完成**）
# （历史）上下文摘要（2026-09-03，更新至**第44轮 G27① 统一实验台账 experiment_ledger：新增根模块 experiment_ledger.py（626行纯标准库），追加式 reports/experiment_runs.jsonl，config_hash=类型+参数+输入内容身份(排mtime)、同配置两次hash一致/repeat_of漂移串联，4宿主旁路钩子，pytest 486→510，默认8品种双哈希逐字节一致、main/analyzer零import、main --once零异常，tag v0.44.0**）
# （历史）上下文摘要（2026-09-03，更新至**第39轮 G29续 因子regime分层+换手稳定性+幂律/指数衰减形态：新增 tools/factor_regime.py（牛熊震荡用面板PIT的ret126、高中低波用hv60过去120日G25引擎ts_rank分桶算前向RankIC；因子滚动分位1/5/20日秩自相关与隐含换手；IC(H)同时拟合指数lnIC=a-H/τ与幂律lnIC=a-βlnH按R2择优、不硬套半衰期；复用factor_health日频层/factor_eval.spearman/factor_expr.compute_ts，纯标准库只读G21面板不接main不改综合分）；真实定位'长周期动量ret252/tsmom252仅低波regime有效(H20低波IC约+0.10、熊低波+0.11)、高波regime转负(动量高波崩溃)，信号lag1自相关0.72~0.85/月度隐含换手约0.39，近零IC指数幂律都不贴合'；config增REGIME_*簇；生产57py/24604行、tests30文件440用例全绿、tag v0.39.0**）
# （历史）上下文摘要（2026-09-03，更新至**第38轮 G25 纯标准库表达式因子引擎+因子治理：新增根模块 factor_expr.py（白名单DSL递归下降解析，禁eval/exec/属性/dunder/语句拼接，时序 delay/delta/ts_mean/ts_std/ts_rank/ts_minmax/decay_linear/corr 尾窗无未来+截面 cross_rank/scale/zscore 同一棵AST双态求值；治理 pearson/spearman/高斯OLS正交残差/等权·IC·ICIR加权），新增 tools/expr_research.py 纯离线读G21面板（面板列vs bar回读同表达式全64品种303309点 parity maxAbsDiff=0、表达式5日动量对齐实时ret5仅1.1e-16、5个research因子前向H1/5/20 |IC|均<0.06无稳定预测负结果诚实）；5个表达式因子登记 factors_catalog（20→25、research不进综合分、main零import旧实现逐字节不变）；生产56py/24226行、tests30文件438用例全绿、commit 见git、tag v0.38.0**）
# （历史）上下文摘要（2026-09-03，更新至**第37轮 G21续（研究工具统一读标准面板）+G29 因子体检：panel_builder 增 panel_rows_to_bars/load_adjusted_bars 回读层，tsmom/xsmom/carry 加 --panel 读已复权面板（实证"已复权序列二次复权会误判换月"故禁止再复权，真实联网vs面板重叠点 maxAbsDiff=0 逐值等价，缺省网络路径不变），面板补齐全64品种61353行；新增 tools/factor_health.py（事件层 RankIC+滚动IC失效预警+块bootstrap CI+多空/档位regime，日频层读G21面板算IC期限指数半衰期），体检卡回写 factors_catalog，真实定位'机构动向次日IC-0.230健康反向/原油联动次日+0.276健康/日频单因子|IC|<0.10无稳定预测'；生产54py/23178行、tests29文件397用例全绿、两次commit 9dca232+8e33dae、tag v0.37.0**）
# （历史）上下文摘要（2026-09-03，更新至**第34轮 G22+G23 落地：新增 term_history.py 多合约期限结构/持仓OI/近月连续净值数据底座 + tools/carry_eval.py 商品carry截面双样本（两口径对照——主连复权不含roll时carry对价格方向为负t=-1.52，近月连续含展期口径长9.9年t=+2.55成立、近4.1年t=+1.39差0.11未达门槛1.5，机制结论=carry赚展期roll的钱不是价格择时的钱，按铁律不进综合分不挂影子、归档'长样本成立/近窗边缘'待跟踪候选，区别于动量双样本全负的彻底证伪）；生产47→49py、19945→21002行，pytest342→353全绿，main冒烟0失败，commit 4a4a095，tag v0.34.0；下一站=G22补tradable_mask/季节日历/常驻采集、carry更长样本与板块条件化/换手容量，或并行G21面板/G28归因**）
# （历史）上下文摘要（2026-09-02，更新至**第29轮 G3 完整绩效指标包/tear sheet：新增 metrics.py（纯标准库统一绩效口径：累计/年化/CAGR/波动/夏普/Sortino/Calmar/Omega/Ulcer/VaR·CVaR/水下回撤/滚动夏普/月度矩阵 + 逐笔胜率/PF/盈亏比/连胜连亏 + MFE/MAE，--selftest 47项手算断言）；portfolio Position 增 MFE/MAE 跟踪、performance() 增补G3键（旧键逐值不变），report 纸面账户补风险调整/连胜连亏/月度行，backtest 总体段补G3扩展行；charts 看板12→15图（⑥水下回撤曲线/滚动60日夏普/月度收益热力，tear_payload 优先纸面影子否则组合回测CSV，休眠也能用既有曲线出图），config 增4常量；pytest 293→304全绿（24测试文件）、node --check+真实浏览器目视三图零控制台错误、真实64品种 --once 冲烟exit0 且休眠零纸面块；生产44→45个py、17460→18178行、零新依赖；tag v0.29.0；下一站第30轮=G7 多窗口TSMOM(63/126/252) 先 factor_eval 评IC再影子）**。开工先读本文件『零、第29轮快速接续摘要』。
# （历史）上下文摘要（2026-09-02，更新至**第28轮 G1纸面交易引擎（二）账户+看板：PaperBroker 接入 main 主循环（State 按 PAPER_ENABLED 持有、默认 False 完全休眠，run_cycle 5.5 段独立成段不动主链）；平今/平昨按交易所结算交易日 owner 实时判定（夜盘算下一交易日、判不了保守平昨）；reports/paper_account.txt 全量账户文本+实时报告紧凑纸面块+看板页签（休眠隐藏）；charts 9→12 图（纸面净值/回撤/风险度三图，权益轴两位小数）；storage 修 paper_equity_series 最近N升序+委托状态计数；pytest 274→295 全绿、selftest 29 断言、真实64品种两轮集成+休眠 --once 双冒烟、浏览器双态目视；commit e53e4fc、tag v0.28.0；44 个 py/17326 行；下一站 G3 绩效指标包或 G7 多窗口动量，G1 剩运行期影子≥4周对照**）
# （历史）上下文摘要（2026-09-02，更新至**第27轮 G1纸面交易引擎（一）表+撮合：新增 paper_broker.py（PaperBroker 701行，纯标准库零网络，账户内核直接复用 portfolio.Portfolio、不写第二套资金逻辑；close/next 两档成交、next 严格晚于信号；三阈值迟滞开/平/反手、实时锁板、滑点入价+真实费率、临时约束挂单顺延/信号消失撤单/同向不重挂、进程重启由三表恢复）；storage 业务表 11→14（paper_orders 委托流水/paper_trades 开平仓成交/paper_equity 每轮权益快照）+9方法+table_counts/prune；config 增 PAPER_* 20项、PAPER_ENABLED 默认 False 休眠（本轮不接 main、不动实时主链与综合分口径）。生产 43→44个py、15938→16832行，tests 255→274全绿约3s（新增 test_paper_broker 18例 + --selftest 25断言），零新增运行依赖；真实64品种5轮 next 撮合（42挂→12成交30排队→不重挂→信号归零撤遗留+12平仓单→全平无遗留，orders54/trades24 真实费率全命中零兜底保证金、持仓中+平仓后恢复逐值一致）、main --once exit0、monitor.db 自动向后兼容建14表 integrity ok（paper三表空）。本轮提交 tag v0.27.0。下一站=第28轮 G1（二）账户+看板：main 接入 State/run_cycle、reports/paper_account.txt、report 纸面账户块、charts 净值图、连续影子≥4周后对照 signal_outcomes、评估 PAPER_ENABLED 是否默认开**）
# （历史）上下文摘要（2026-09-02，更新至**第26轮 G4回测严谨性：backtest.py 增 --fill {close,next_open} 成交时点双档（close默认=信号根收盘旧口径逐值不变；next_open=次根开盘保守对照，锁板顺延/末根无次根信号不虚构/反手先平后开）、固定种子交易级 iid bootstrap 1000次给累计收益与最大回撤 P5-中位-P95（总体+多空，<20笔不给）、--oos-ratio 样本内外分段、--impact-rate 冲击成本单列；storage 增第11张业务表 backtest_runs 逐次留档+报告抬头历史百分位；tools/backtest_validation.py 同步出 backtest_validation.json sidecar（DSR/PBO）供回测抬头交叉引用。生产仍43个py、15560→15938行，tests 238→255全绿约3s，零新增运行依赖；真实全64品种 close 1713笔净-24.4% vs next_open 1571笔净-79.2%（保守档更差、负结果诚实）、main --once exit0、DB integrity ok（11业务表）。本轮提交tag v0.26.0。下一站=第27-28轮 G1纸面交易引擎（paper_broker/三表/影子账户，详见融合总纲6.3）。——以下为历史——）
# （历史）上下文摘要（2026-09-02，更新至**第25轮 数据底座一组：G9 git工程化 + G10配置外置 + G11数据源熔断降级链 + G6数据质量监控**。G9=git init基线tag v0.24.0/.gitignore/.gitattributes统一LF/requirements锁==/CHANGELOG/VERSION；G10=新增config_loader.py（.env+config.json深合并，缺配置与历史逐字节一致）+config.template.json+.env.example；G11=新增data_router.py（SourceHealth熔断CLOSED/OPEN/HALF_OPEN+DataRouter有序主备+REGISTRY健康总账）并接入futures_data/intraday_bars；G6=新增data_health.py（缺数/陈旧/跳变体检+跨轮连续异常追踪）+storage第11张表data_health+main落表告警+report【数据源健康】块+tools/db_archive.py零依赖按年归档。生产39→43个py、14663→15560行，tests 191→238全绿约2.7s，零新增运行依赖；真实main --once exit0、data_health落4行、DB integrity ok（11表）。本轮提交tag v0.25.0。下一站=第26轮 G4回测严谨（walk-forward/手续费滑点/样本外/多空分桶，详见融合总纲6.3）。——以下为历史——）
# （历史）上下文摘要（2026-09-02，更新至**第24轮 第三份全网/GitHub对标+六文档融合：先全网调研量化系统组成与GitHub主流量化项目并与本项目对比，产出《量化系统对标与改进报告.html》《对标能力可视化.html》（均真实浏览器验证）；再把这2份HTML与《统一改进路线图》《未完成项落地方案》《AI量化对标与改进方案》《GitHub对标与改进清单》4份md融合为单一总入口《总体对标与统一改进总纲（融合版）.md》，未实现项统一G1-G20编号并按P0-P3优先级分类；本轮纯文档、零改生产代码（仍39个py/14663行、tests191全绿），新增3个文档文件；前序第23轮=看板两页合并（实时报告内嵌图表页签、charts.py拆style/dom/js三段IIFE、191用例）。——以下为历史——
# （历史）第23轮=看板两页合并（实时报告"图表看板"页签由iframe改同页内嵌9图、charts.py拆_PANEL_STYLE/DOM/JS三段并IIFE导出window.ChartPanel、独立图表看板.html直链保留、191用例）；第22轮=P1-3 看板图表化（charts.py/ECharts9图/189用例）；第18轮 WP-F1（P0）=多空双面卡+横截面cross_section.py+独立风控闸门risk_gate.py+五维情绪；第19轮 WP-F2=signal_calibrator/factor_eval/build_ml_samples；第20轮=backtest_validation(DSR/CSCV-PBO/PurgedKFold/WF/参数高原)；第21轮=tests/ pytest回归网（174用例）。

> 用途：新开会话时把本文件发给助手即可无缝接续，无需复述历史。

## 零.旧、第50轮快速接续摘要（已被第51轮接续，存档）
- **一句话**：本轮补 **G5④ 的阈值校准欠账**（第48轮熔断落地时明确留的"阈值只用合成断言、未在真实长序列回放"）——新增**研究侧工具 `tools/circuit_review.py`（约323行纯标准库、零网络、只读 G21 面板 cache/research_panel.db，经 portfolio_lab 复现四方法滚动样本外日收益）**，把 warn2%/halt3%/delever5% 放真实历史日频净值上回放校准。**不接 main、不改 circuit_breaker 默认值/paper_broker/综合分/持仓**，默认8品种 CSV 双哈希不变。代码 commit **e728215**、文档 commit 见 git log、tag **v0.50.0**。
### 一、模块与纯函数（tools/circuit_review.py，合成可断言）
- `loss_of(ret)=-ret`（日收益→单日损失，损失为正）；`forward_compound(daily,i,h)`=触发日 i 后 1..h 日**累计复利**收益（不含触发当日、后续不足/非法下标返 None）。
- `threshold_events(daily,th)`=逐日损失≥th 的下标（日频每日独立=每日日切重置，含等号）；`level_counts(daily,{warn,halt,delever})`=用 **circuit_breaker.classify_level 同口径**分三档计数+下标。
- `conditional_forwards(daily,trigger_idx,horizons)`=触发点集合在 T+1/3/5/10 的条件远期收益分布（n/均值/中位/下跌占比，`_dist`）**对照全样本无条件基准**（所有后面还有 max_h 日的时点），回答"大跌后续跌=停开避险有价值 / 反弹=停开误杀"；`sweep_halt`=halt 阈值网格(0.5/0.8/1/1.5/2/3%)触发数/占比/条件收益（阈值越高触发越少=单调）。
- `analyze_method`=单方法端到端（三档计数+halt_forward+**calib 校准观察档=1%** 的条件远期+网格+最差单日）；`load_method_series` 复用 pl.load_return_map/dense_matrix/rolling_proxy（取 out[m]['idx']/['daily'] 对齐 dates）；`render` 三段文本；`run` 出 reports/circuit_review.txt|.json + 台账旁路 kind=**circuit_review（台账由6类增至7类）**；selftest 8组、CLI `--selftest/--db`。
### 二、真实数据诚实结论（固定宇宙61/64、稠密2024-08-06~2026-09-02共504日、样本外曲线378日、满仓多头无成本）
- **默认阈值在日频分散组合几乎永不触发**：四方法378日 halt3%/delever5% **全0次**、warn2% 仅等权/逆波动各1次；最差单日仅 等权-2.15%/逆波动-2.16%/ERC-1.91%/gmv-1.70%（3%约为组合日σ的5倍）→**这套阈值真正保护日内/集中持仓/加杠杆账户，日频全分散组合天然难触发**。
- 下移到 **1%校准观察档**才有样本（等权18/逆波动10/ERC7/gmv1）：等权 T+1 条件-0.16% vs 基准+0.01%（差-0.18%、下跌占比61%vs49%）=**仅次日有微弱续跌惯性**，T+3 转正+0.22%、T+10 +0.07%=3日后均值回归；逆波动同形；ERC/gmv 样本太少不稳。
- 阈值网格：触发数随阈值单调降（0.5%72→0.8%32→1%18→1.5%2→2%1→3%0），各档 T+5 条件均值在零附近、**无"越跌越续跌"强信号**。
- **总结论：熔断在分散组合层面以尾部保险/纪律为主、不产生统计显著避险 alpha；默认 2/3/5% 维持不动（为日内/杠杆场景保留），若要日频组合层保护需 halt 下移约1~1.5%，但样本太少需更长历史——本轮只校准、不改 config 默认。**
- **口径诚实边界（报告已写明）**：circuit_breaker 实盘是日内状态机（日初权益→当前、当日粘性、一天多快照），本台只有日频收盘净值、每日一根=每天重置，**无法复现当日锁定、真实日内触发只会更多**，属保守逐日代理；固定宇宙有幸存者偏差、未计手续费/滑点/保证金/换月；不构成投资建议。
### 三、验证与规模
- 新增 tests/test_circuit_review.py **9例零网络/零DB**（损失与远期复利手算/事件穿越含等号/三档分档/续跌vs反弹条件分布/_dist统计/网格单调与占比有界/analyze结构与空序列安全/render三段/selftest）；模块自带8组 selftest。
- 全量 **pytest 600→610 全绿**（0失败/错误/跳过，cache/r50_junit.xml，约8s）；compileall（含main）过。
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 双 sha256 逐字节一致**（跑 `portfolio.py --codes ... --period 30` 后比 cache/base_*）；grep 证明 main/analyzer/paper_broker 零 import circuit_review；运行依赖仍仅 requests/uiautomation/websocket-client。
- 生产 py **67→68（根47不变20257行、tools20→21：9657→9980）合计29914→30237行**；tests **38→39 个 test_*.py（+conftest=40）/5982→6092行/610用例**；storage14表、看板15图15页签不变。
### 纪律与下一站（第51轮）
- **G5 五项①~⑤早在第49轮已全落地；本轮补齐④的阈值校准**。G5 唯一剩余增强欠账=**④ delever 自动减仓执行**（当前 circuit_breaker 只停开新仓+"建议减仓约50%"文字，不自动砍仓）。
- **第51轮优先候选**：①**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本（四方法已齐，最连贯）；②把 portfolio_nav.csv / circuit_review 接看板 charts 增四方法净值/回撤页签（**须先 Read 重读 doubao-visualization skill 的 mode-echarts/option-spec/shared-quality**）；③wf_cost_lab 多周期/purged+embargo；④G5④ delever 自动减仓执行（paper_halt 下真正按比例下平仓腿，默认 observe 仍不动）。
- 旧排队：G24微结构季节、G28续逐笔持仓归因、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14盘口深度、G13、G3、G7、G12。
### 复用提示与踩坑
- **研究侧校准类工具照本轮/portfolio_risk_lab 范式**：sys.path 插 _ROOT/_HERE→import 根模块与 portfolio_lab；读面板只走 pl.load_return_map/dense_matrix/rolling_proxy（mode=ro、不重算网络）；产物 reports/xxx.txt|.json、末尾 el.safe_record 旁路 try 包住；CLI 带 --selftest。
- **PowerShell 坑**：`python tools/xxx.py | Select-Object -First N` 会因提前关管道触发 **broken pipe（exit -1）且中断在 print 阶段、导致 print 之后的写文件没执行**——真实跑要落盘就重定向 `1>cache/x.log 2>&1` 别接 Select。
- 历轮通用坑：不支持 `&&`（用 `;`）、复杂中文内联走 cache/*.py 跑完即删、文档 utf-8-sig 读保持编码写 newline='
'、git PATH 前置 `C:\Program Files\Git\cmd`；compare-risk 会覆盖 reports 默认 CSV，收尾重跑 --period 30 恢复基线再比双哈希。

## 零.旧、最新快速接续摘要（第49轮，已被第50轮接续，存档）
- **一句话**：本轮做 **G5⑤ 收尾**——总纲 G5 五项（①相关矩阵②组合VaR③原油压力④单日浮亏熔断⑤第四种sizing+组合历史净值回测）的最后一项。第40轮 portfolio_constructor 已有 equal/inv_vol/erc/gmv 四方法、第41轮只把 inv_vol/erc 接进 portfolio 回测内核，本轮**补入第四种 gmv**，并把 portfolio_lab 滚动代理的日收益落成**逐日组合历史净值曲线**。默认 `--risk-sizing` 留空=旧等名义、不传完全等价（双哈希为证）；main/analyzer/综合分零改动。
### 一、gmv 接入回测内核（默认关闭，5处小改 portfolio.py）
- `RISK_SIZING_METHODS=("inv_vol","erc")→("inv_vol","erc","gmv")`（Portfolio.__init__ 白名单，非法值仍安全回退 None）；`--risk-sizing choices` 加 gmv；报告 rname 加 gmv→"最小方差"；`--compare-risk` 对照循环 ("","inv_vol","erc")→四法、labels/method_key 同步。
- gmv **不写第二套逻辑**：复用 trailing_risk_weights（只用 t 之前收盘价估协方差=严格PIT、公共时间戳稠密对齐、可估品种<2/历史不足安全回退等名义）与 decide_lots 第250行风险权重覆盖通道（risk_weights[sym]>0 才覆盖目标名义，宇宙外回退等名义）。pc.construct 第346行本就支持 gmv（投影梯度+capped-simplex）。
### 二、多品种组合历史净值曲线（tools/portfolio_lab.py，研究侧只读 G21 面板）
- 新增纯函数 **nav_curve(daily,start=1.0)**（逐日复利净值、不改入参、空序列返[]）与 **drawdown_window(nav,idxs,dates)**（净值→最大回撤分数+峰值/谷底下标与日期、单调上涨/单点/空安全）。
- rolling_proxy 的 out[m] 增 **idx**（每个日收益对应的全局 mat 行号，供与 dates 对齐；四方法共用同一再平衡日历故 idx 全等、严格递增、首值=lookback=无未来）。
- run()：navs=各法 nav_curve；nav_summary=期末/最高/最低净值+最深回撤区间；落 **reports/portfolio_nav.csv（表头 date+四法_ret+四法_nav，379行=2025-02-18起，因lookback126）**；json payload 增 nav_summary/nav_csv；报告【一】表格后增"期末净值(初始1.0)"与"净值最深回撤(峰→谷日期)"两行；台账 artifacts 加 nav 路径。selftest 5→8组。
### 三、真实数据诚实结论（固定宇宙61/64、稠密2024-08-06~2026-09-02共504日、窗126/再平衡20/收缩0.10/单票上限20%/满仓多头无成本）
- **期末净值(初始1.0)**：等权1.0624 / 逆波动1.0723 / ERC1.0597 / **gmv1.0268**。
- **年化波动**：等权9.48% → 逆波动8.35%(−11.9%) → ERC7.11%(−25.0%) → **gmv4.93%(−48.0%)**；**最大回撤** 9.54/7.79/6.46/**5.26%** 单调降；**夏普** 等权0.42/逆波动0.55/ERC0.53/**gmv0.35（降波动最猛但收益牺牲更多、夏普反降）**；平均有效N 61→54.8→39.4→**11.4（gmv高度集中）**、年化换手 0/0.27/0.64/**1.77（gmv调仓最频繁）**。
- 8品种日线60日 `--compare-risk` 四法影子对照 gmv 正常求解（有效N6.2、平仓105笔略少于基线109=集中致部分目标名义不足1手）。结论与第40轮一致：**风险型分配价值在降波动/回撤非增收益；gmv 只宜作对照、不默认启用**。
### 验证与规模
- 新增 tests/test_portfolio_nav.py **6例零网络**（nav复利与不改入参/回撤窗口手算与安全退化/rolling的idx日期对齐四方法等长/玩具面板gmv波动≤等权且净值有限）；test_portfolio 补 gmv 进 trailing 属性参数化 + 白名单/默认关闭用例；portfolio_lab selftest 5→8组。**全量 pytest 593→600 全绿 0失败0错误0跳过（cache/r49_junit.xml）**、compileall（含 main.py）过。
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 双 sha256 逐字节一致**（compare-risk 用 --daily 60 会覆盖 reports CSV，收尾已重跑默认 --period 30 恢复基线再比对）；实验台账仍6类各1条（portfolio_lab 同hash走repeat_of未新增）；零新增依赖。
- 生产 py **仍67（根47不变20257行、tools20不变但 portfolio_lab 380行→9657）合计29914行**；tests 37→38文件 5900→5982行（新增 test_portfolio_nav）；用例593→600；storage/看板不动；代码 commit 6446d76、文档 commit 见 git log、**tag v0.49.0**。
- **诚实边界**：净值曲线为日频已比例复权主连面板、固定宇宙有幸存者偏差、未计手续费/滑点/保证金/换月（报告已标注），仅决定"风险型分配是否值得默认接入"，不直接上线。
### 纪律与下一站（第50轮）
- **G5 已全部完成（①~⑤）**；唯一增强欠账=**G5④ delever 自动减仓执行**（第48轮 circuit_breaker 只到 halt 停开新仓+delever 建议文字，真要自动砍仓须在 PaperBroker 阶段C旁加减仓腿，仍默认关闭、observe等价）。
- **第50轮优先候选**：①**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本（G26 主线，四方法现已齐）；②**熔断历史回放校准**（paper 影子积累后读 paper_equity 按交易日重放 breaker 统计 halt/delever 触发频次；当前 paper_equity 0行，可先用 portfolio_nav 日净值做组合层回撤熔断的阈值校准研究）；③wf_cost_lab 多周期/purged+embargo 增强；④把 portfolio_nav.csv 接看板画四方法净值/回撤图（charts 增页签，休眠可读CSV）。
- 旧排队：G24微结构季节、G28续逐笔持仓归因/看板页签、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12产业链价差。
- 复用提示：**权重/sizing 类照本轮——pc.construct 已支持的方法接内核只需放开白名单/CLI/对照循环/报告映射四处、不写第二套；任何接入默认留空走旧口径、双哈希复算；研究侧净值=日收益逐日复利 nav_curve、回撤用 drawdown_window 记峰谷日期；rolling 类代理必须同步记全局行号 idx 才能与 dates 对齐画曲线；compare-risk 会覆盖 reports 默认CSV，收尾必须重跑默认 --period 30 恢复基线再比哈希。**

## 零.旧、最新快速接续摘要（第48轮，已被第49轮接续，存档）
- **一句话**：本轮做 **G5④ 组合层单日浮亏熔断**——总纲 G5 五项里第④7项"risk_gate 单日浮亏熔断由只标注升级为可配置动作（纸面层停开/减仓、建议层文字，默认仍只标注）"。现有 `risk_gate.py`（112行）是**单品种/信号级** pass/warn/veto（无有效价/量不足/信号与涨跌背离/HV极端），本轮补的是与之**正交的组合账户级**维度。新增**根模块 `circuit_breaker.py`（315行纯标准库、零网络、纯决策不直接下单/不改持仓）**，并在 `paper_broker.py` 加**默认旁路**挂钩（默认 observe 时 self.breaker=None、阶段B不过滤任何委托、成交逐字节等价旧版）。main/analyzer/综合分**零改动**。
### 一、circuit_breaker.py（11组零网络自测；纯函数+状态机，不 import config 之外的宿主状态，from_config 延迟导入且缺项回退默认）
1. **口径纯函数**：day_of（'YYYY-MM-DD HH:MM:SS'→日期前缀，非法/空安全返None）；daily_loss_pct=(日初权益−当前权益)/日初权益（**损失为正**、盈利为负、基准非正/非法返0）；classify_level 按 (warn,halt,delever) 阈值定档（边界含等号）；max_level 级别取高；filter_orders（allow_open=True原样；False剔 open/reverse_open、保留 close/reverse_close，不改入参）。
2. **CircuitBreaker 状态机**：构造注入 action_mode/三阈值/risk_halt/delever_ratio，校验 0<warn≤halt≤delever 与合法动作模式，否则 ValueError。update(ts,equity,risk_degree,n_positions)：跨日（日期前缀变化）_reset_day 重置日初基准/峰值/级别/events；当日 peak_loss=max(历史,当前) 只升不降=**当日粘性**（触发后浮亏回落不解锁，防阈值附近反复抖动）；风险度≥risk_halt 作为第二触发源抬到 halt（非法风险度不崩）；级别 max(浮亏档,风险档,旧级别) 粘性，升档记 events。
3. **decision 输出**：{level,action_mode,allow_open,daily_loss,peak_loss,day_open_equity,risk_trigger,suggest_reduce_ratio(delever档=0.5否则0),messages[],day_changed}；**open_allowed()=observe恒True、paper_halt 仅 level≥halt 才False**；render 一行人类可读；from_config 读 CIRCUIT_*。
4. **两档动作（真实账户永不自动操作）**：observe（默认）只计算/标注，即便 delever 也 allow_open=True；paper_halt 在 halt/delever 真正停开新仓（平仓照常），**delever 的"减仓"只给比例建议文字、不自动砍仓（自动减仓执行留后续）**。
### 二、paper_broker 挂钩（默认旁路、无未来函数）
- `__init__` 加 `circuit=None` 参数与 self.breaker：显式传入优先；否则仅当 config.CIRCUIT_ENABLED 且 CIRCUIT_ACTION=='paper_halt' 才 from_config 构造，**默认 observe→self.breaker=None**。
- on_cycle **阶段B**：`orders=self._decide(...)` 后若 self.breaker 非空，`orders=circuit_breaker.filter_orders(orders, self.breaker.open_allowed())`——停开时 open 整单剔除、reverse 只留 reverse_close 平仓腿（next档过滤后无 orders 会自然撤遗留挂单）。
- on_cycle **阶段D**：_snapshot 后用本轮最新 snap.equity/risk_degree/n_positions 调 breaker.update，结果存 self._last_circuit 并放进 summary["circuit"]（**用本轮权益供下一轮阶段B，严格无未来函数**；第一轮无状态默认 allow_open）。
- config 新增7常量（RISK_GATE 段后）：CIRCUIT_ENABLED=True、CIRCUIT_ACTION="observe"、CIRCUIT_WARN_LOSS=0.02、CIRCUIT_HALT_LOSS=0.03、CIRCUIT_DELEVER_LOSS=0.05、CIRCUIT_RISK_HALT=0.95、CIRCUIT_DELEVER_RATIO=0.50。
### 三、验证与规模
- tests/test_circuit_breaker.py **新增19例零网络**（日期解析参数化/浮亏口径/三档边界/级别取高/委托过滤参数化/非法参数、observe即使delever恒可开、当日粘性不回落+日切重置、warn仍可开、风险度第二触发与非法安全、from_config桩，及与 PaperBroker 的 close 档集成：paper_halt 下新仓被拦+反手只留平仓腿、默认 observe 无断路器照常开新仓）+ test_tools_selftest 注册 circuit_breaker 11组 selftest；**全量 pytest 572→593 全绿 0失败0错误0跳过（cache/r48_junit.xml）**、compileall 过。**坑：paper 成交字段是 side(open/close) 不是 action（action 在 order 上）。**
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 双 sha256 逐字节一致**；真实 main --once exit0、报告正常（cache/r48_main.log）；零新增依赖。
- 生产 py **66→67（根46→47新增 circuit_breaker 315行、tools20不变）29485→29829行**；tests 36→37文件 5748→5900行（test_circuit_breaker 146行）；用例572→593；storage 14业务表/看板15图15页签不动；代码 commit 057107a、文档 commit 见 git log、**tag v0.48.0**。
- **诚实边界**：PAPER_ENABLED 默认 False、paper_equity 表当前0行无历史曲线，本轮三阈值用合成断言验证状态机正确性，**未在真实纸面长序列上回放统计触发频次/校准阈值**（待 paper 影子积累后补历史回放工具）；本轮是动作层不是研究实验、**不挂 experiment_ledger 台账**。
### 纪律与下一站（第49轮）
- **G5 剩余**：⑤第四种 sizing=相关性/风险平价约束接入 portfolio.decide_lots（portfolio_constructor 已有 erc/gmv，须默认 equal 影子对照、不传等价）；多品种组合历史净值回测（替代 backtest 自陈"交易序列复利近似"，可复用 portfolio_lab.rolling_proxy/perf_stats）；**delever 自动减仓执行**（本轮只到停开+建议文字，真要自动砍仓须在 PaperBroker 阶段C旁加减仓腿、仍默认关闭）。
- **第49轮优先候选**：①**G5⑤ 风险平价 sizing 影子/组合历史净值回测**（顺 G5 收尾）；②**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本；③熔断历史回放校准（paper 影子积累后，读 paper_equity 按交易日重放 breaker 统计 halt/delever 触发频次与最长持续）；④wf_cost_lab 多周期/purged 增强。
- 旧排队：G24微结构季节、G28续逐笔持仓归因/看板页签、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12产业链价差。
- 复用提示：**风控动作类照本轮——决策(纯函数根模块、可零网络selftest)与执行(宿主挂钩)分离、动作模式默认 observe 只标注、显式才动作且不传等价旧版；当日粘性+日切重置防抖动；断路器状态用上一轮快照更新供本轮决策=无未来函数；反手先平后开两条腿在停开时只留平仓腿；paper 成交字段 side、委托字段 action 别混。**

## 零.旧、最新快速接续摘要（第47轮，已被第48轮接续，存档）
- **一句话**：本轮做 **G5 组合层风险的研究侧①②③**（总纲 G5 共五项，④risk_gate 熔断可配置动作、⑤第四种风险平价 sizing+多品种组合历史净值回测留后续）。新增**根模块 `portfolio_risk.py`（347行纯标准库纯函数、零网络，协方差/收缩复用 portfolio_constructor）**与**研究工具 `tools/portfolio_risk_lab.py`（241行，只读 G21 面板 cache/research_panel.db，复用 portfolio_lab 的 load_return_map/dense_matrix 稠密对齐，出 reports/portfolio_risk_lab.txt|.json，末尾经 experiment_ledger 旁路登记一条）**。main/analyzer/portfolio/risk_gate **grep 证明零 import**，纯增量只读。
### 一、portfolio_risk.py（11组零网络自测；纯函数不读库）
1. **相关结构**：correlation_matrix（协方差→Pearson，零方差行列安全记0、裁剪[-1,1]、对角1对称）；avg_abs_offdiag（平均绝对相关=系统性联动强度）/avg_signed_offdiag；sector_corr_block（板块×板块平均相关，板块内为品种对均值）；top_pairs 最强/最弱对；percentile（C=1线性插值，等价 numpy linear）。
2. **组合 VaR 双口径（VaR/ES 一律损失为正）**：portfolio_return_series（行式 mat×w）；historical_var 历史模拟法（VaR_q=−经验(1−q)分位、ES=超过 VaR 尾部条件均值、worst/best，不假设正态含真实肥尾，全正收益时 VaR 为负=该分位仍盈利）；parametric_var 参数法（σ_p=sqrt(w'Σw)、VaR=z·σ，z 表 Z_QUANTILE 95=1.644854/99=2.326348，多日 ×√h）；standalone_var 加权单体（假设完全相关无分散）、diversification_benefit=1−组合参数VaR/加权单体。
3. **原油压力（线性一阶）**：oil_betas 从协方差算 β_i=C(i,oil)/C(oil,oil) 与 R²=ρ²（零方差原油安全）；stress_oil 组合损益=Σ w_iβ_i·shock、贡献按绝对值排序。明确忽略非线性/危机时相关突变，属数量级情景。
4. **统一入口 risk_snapshot**：传入"按资产"收益序列+权重+levels+horizons+oil_idx+sector_of+syms，一次出 C/R/平均相关/历史&参数VaR/分散化/最强最弱对/板块块/原油三情景(−5%/−10%/+5%)。
### 二、portfolio_risk_lab.py（3组自测；固定宇宙=portfolio_lab 同口径 504日窗覆盖率≥95%→61/64品种，风险度量取最近 PC_LOOKBACK=126 日）
- 对 METHODS=equal/inv_vol/erc/gmv 各用 pc.construct（shrink=PC_SHRINK0.10、cap=PC_MAX_WEIGHT0.20）定权，再调 risk_snapshot；报告三段：【一】相关结构（等权宇宙的客观结构）【二】四权重 VaR/ES/年化波动/肥尾溢价(历史/参数−1)/有效N/分散化收益对照【三】原油压力（|beta|最强品种+四权重三情景+等权−5%主要拖累）。OIL_SYM='SC'（INE原油，固定宇宙含）。
### 三、真实数据诚实结论（61品种、窗2026-03-04~09-02共126日、满仓多头权重和=1未加杠杆）
- **相关**：平均绝对相关仅 **0.161**（商品篮子整体易分散）、带符号+0.125；板块内联动 贵金属0.598 > 能源化工0.346 > 黑色0.300 > 有色0.279 > 农产品0.143；贵金属对能源化工 −0.160（跨板块对冲）；最强对=产业链 PX-TA0.81/BC-CU0.78/L-PP0.77/PF-TA0.73/J-JM0.72/NR-RU0.72；最弱=黄金对化工 AU-PF−0.40/AU-L−0.36（避险对冲）。
- **VaR**：等权单日 历史VaR95=1.06%/ES95=1.21%/VaR99=1.31%、参数VaR95=0.98%/VaR99=1.39%/**10日VaR95=3.11%**、肥尾溢价+8%（正态略低估左尾）；风险型权重降尾部：逆波动0.78%、erc0.66%、gmv0.40%（历史VaR95），**代价是有效N 61→53.4/34.8/12.3 越来越集中**；分散化收益 等权59.7%→逆波动62.7%→erc69.4%→gmv71.8%。
- **原油压力**：|beta|最强 SC=1/FU0.71/LU0.69/EB0.50/PG0.49/PX0.47；原油−5% 组合损益 等权−0.61%/逆波动−0.38%/erc−0.25%/gmv−0.07%，−10%翻倍、+5%对称；等权−5%主要拖累 SC/FU/LU。
### 验证与规模
- tests/test_portfolio_risk.py **新增19例零网络/零面板**（相关正负零方差/板块块/分位数参数化/组合收益/历史VaR-ES与全正VaR为负/参数VaR精确值与√h及未知分位报错/原油beta精确线性/压力方向/分散化/端到端与零方差退化）+ test_tools_selftest 注册 portfolio_risk 11组与 portfolio_risk_lab 3组 selftest；**全量 pytest 549→572 全绿 0失败0错误0跳过（cache/r47_junit.xml）**、compileall 过。
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 双 sha256 逐字节一致**；main/analyzer/portfolio/risk_gate grep 零引用（本轮未改 main，故未跑 main --once）；零新增依赖。
- 生产 py **64→66（根45→46新增 portfolio_risk 347行、tools19→20新增 portfolio_risk_lab 241行）28897→29485行**；tests 35→36文件 5597→5748行（test_portfolio_risk 139行）；用例549→572；storage/看板不动；代码 commit d87983f、文档 commit 见 git log、**tag v0.47.0**。
- reports 最终恢复**16品种口径**（trade_journal 2563笔，恢复时 FUTURES_EXPERIMENT_LEDGER=0 关台账）；统一实验台账去重后**6类各1条**（新增 portfolio_risk_lab=4720b6b4f9），可读视图 reports/experiment_ledger_view.txt 已用 `experiment_ledger.py --list` 重生成。
### 纪律与下一站（第48轮）
- **G5 剩余**：④risk_gate 单日浮亏熔断由"只标注"升级为"可配置动作"（纸面层停开/减仓、建议层文字，**默认仍只标注、不传等价旧版**）；⑤第四种 sizing=相关性/风险平价约束（portfolio_constructor 已有 erc/gmv，接入 portfolio.decide_lots 须默认 equal 影子对照）；多品种组合历史净值回测（替代 backtest 自陈"交易序列复利近似"，可复用 portfolio_lab.rolling_proxy/perf_stats）。
- **第48轮优先候选**：①**G5④ risk_gate 可配置熔断动作**（顺着本轮 VaR 数字接动作层，边界小、默认关闭）；②**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本；③G5⑤ 风险平价 sizing 影子；④wf_cost_lab 多周期/purged 增强。
- 旧排队：G24微结构季节、G28续逐笔持仓归因/看板页签、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12产业链价差。
- 复用提示：**风险度量类照本轮——VaR/ES 损失为正口径统一、历史法不假设正态用于暴露肥尾、参数法标注正态与√h(i.i.d.)假设、压力只做线性一阶并显式声明忽略相关突变；纯算法放根模块(可零网络selftest)、取数/渲染/CLI/台账放 tools；研究工具一律只读面板(mode=ro)、grep 证明 main/analyzer/portfolio 零引用、默认CSV双哈希每轮复算；台账探查跑会留重复条，收尾按 experiment 去重只留1条并用 --list 重刷 view。**

## 零.旧、最新快速接续摘要（第46轮，已被第47轮接续，存档）
- **一句话**：本轮做运维安全网 **G19**——monitor.db（WAL、约360MB，含 quotes/minute_bars/signals/signal_outcomes/news/options/paper_*/backtest_runs 全部家当）此前没有任何自动备份，磁盘损坏/误写即全损。新增**根模块 `db_backup.py`（533行纯标准库 sqlite3/os/shutil、零网络、只读源库只写 backup/、不接 main 主循环、不改综合分/生产数据）**。区别于 tools/db_archive.py（按年导出归档）：本工具做高频在线热备+滚动保留+一键恢复+自启导出。
### 一、db_backup.py（533行，13组零网络自测；纯函数层不碰生产库，tmp 造库演练）
1. **在线热备 online_backup（不用停程序）**：用 SQLite 官方 **Online Backup API** `src.backup(dst,pages=-1)`，源库以**只读 URI（file:...?mode=ro）**打开，main 常驻写库也得到事务一致性快照、对 WAL 安全、不持长锁；360MB 实测约10秒。**坑（已修）**：不要写分页循环（backup() 返回 None、易只拷部分），直接 pages=-1 一步全量。
2. **副本自检防假备份**：备份后对**副本** PRAGMA quick_check，非 ok 立即删坏副本并 raise；每份配同名 `.db.json` sidecar（源/副本字节、双 quick_check、各用户表行数 table_row_counts、VERSION、时间）。
3. **滚动保留 prune_plan/list_backup_files（只认自己命名）**：`backup/monitor_YYYYMMDD-HHMMSS.db`，默认保留最近 **30 份**（--keep，<=0全保留），按时间戳删最旧、sidecar 同步删；parse_backup_stamp 严格校验，目录里 notes.txt/别的.db **绝不误删**；同秒碰撞加序号不覆盖。
4. **校验/恢复**：--list（大小/副本qc/版本）、--verify（全部或--latest-n，异常退出码1）；restore_backup=先校验备份非坏→现有 monitor.db 及 -wal/-shm **改名 .before_restore_时间戳留存（绝不直接覆盖丢现场）**→反向 Online Backup 写回→新库再 quick_check；交互输 yes、--yes 供脚本。
5. **自启只导出不改系统**：--emit-bat 生成 run_backup.bat（chcp65001+cd /d+--once，注释用英文避免 chcp 前乱码）；--emit-task-xml 生成 `backup/futures_monitor_db_backup_task.xml`（**每日16:30 CalendarTrigger + LogonTrigger**、LeastPrivilege、StartWhenAvailable 错过补跑、PT30M 超时、IgnoreNew 防重入，UTF-16）；**不执行 schtasks /register**，导入步骤与异地保管/无备份兜底写进《灾备恢复Runbook.md》。
6. **main 只读 --version（G19④，默认等价）**：main.py parse 后最早分支读 VERSION 打印即 return（不 setup_environment/不连库/不启动）；**坑：main 没 import io，用内置 open**；不传 --version 路径完全不变。db_backup 自身也有 --version（注意它与 --once 同传会被 version 先短路，真实不同时传）。
### 二、真实冒烟诚实结论
- 对真实 monitor.db：源359.8MB→副本359.8MB、**14张用户表467,695行**、源/副本 quick_check 均 ok、约10秒；--list/--verify 正常、只认本工具命名；selftest 在 tmp 库完整演练"备份→改坏现场(改行数)→restore 回到备份点、旧库留存、坏备份拒绝恢复、坏库 quick_check 返 OPEN_ERROR 不抛"。
- `.gitignore` 忽略 backup/*.db、*.db.json、*.before_restore_*（二进制不入库），任务 XML 与 run_backup.bat 入库；最终 backup/ 留一份最新备份+XML 模板。
### 验证与规模
- tests/test_db_backup.py **新增22例零网络/零生产库**（全 tmp_path 造临时 sqlite，绝不碰 data/monitor.db：文件名解析5个非法反例参数化、prune_plan、只认前缀、online_backup一致性+源只读、sidecar行数、滚动删最旧连同sidecar、同秒碰撞、缺源报错、restore留存旧库、坏备份拒绝、坏库OPEN_ERROR、XML/bat内容、版本缺失安全）+ test_tools_selftest 注册13组 selftest；**全量 pytest 526→549 全绿 0失败0错误0跳过（cache/r46_junit.xml）**、compileall 过。
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 与 cache/base 双 sha256 逐字节一致**（main 仅加只读 --version 早返回、不传零变化）；真实 main --once exit0、stderr 0行零异常（cache/r46_main_*.log）；main --version 秒退打印 0.46.0。
- 生产 py **63→64（根44→45新增 db_backup 533行、tools19不变）28352→28897 行**；tests 34→35文件 5430→5597行（test_db_backup 161行）；用例526→549；storage 14业务表不变、看板15图15页签不动；零新增依赖；代码 commit 2f86514、文档 commit 见 git log、**tag v0.46.0**。
- reports 最终恢复**16品种口径**（trade_journal 2563笔，恢复时 FUTURES_EXPERIMENT_LEDGER=0 关台账避免重复登记）；备份不属研究实验、**不挂 experiment_ledger 台账**。
### 纪律与下一站（第47轮）
- **备份使用纪律**：db_backup 只备份 monitor.db；backup/ 与源同盘防误删不防物理盘损，须按 Runbook 每周异地拷1~2份；要启用每日自动备份需用户手动导入任务 XML（工具不擅自注册系统任务）；恢复前先 --verify 选 qc=ok 的备份、恢复后旧库在 .before_restore_* 确认无误再删。
- **第47轮优先候选**：①**G5 组合层风险（研究侧先行）** 64品种日收益相关矩阵+组合VaR(历史法/参数法)+原油±5%压力传导，吃 portfolio_constructor 协方差/风险贡献；**先做只读研究工具、risk_gate 可配置动作与第四种sizing留后续、默认仍只标注**；②**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本；③wf_cost_lab 增强（曲面扩多参数/多周期、WF扩purged+embargo）。
- 旧排队：G24微结构季节、G28续逐笔持仓归因、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照(G30②与精确容量前置)、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12产业链价差。
- 复用提示：**运维/备份类照本轮——Online Backup API pages=-1 一次性全量、源用 mode=ro 只读、副本必须 quick_check 才认、删除只认严格自命名、恢复先留存不覆盖、任何改系统(schtasks/注册表/自启)只导出文件由用户手动执行；cache 脚本 sys.path 要 insert 项目根（脚本所在 cache/ 才是 sys.path[0]）；Windows 补丁字符串里路径用正斜杠避免 \f 变换页符。**

## 零.旧、最新快速接续摘要（第45轮，已被第46轮接续，存档）
- **一句话**：补齐总纲 G27 剩余的②③（①统一实验台账第44轮已落地），新增**研究侧工具 `tools/wf_cost_lab.py`（601行纯标准库、零网络、只读 monitor.db、只写 reports/wf_cost_lab.txt|.json，不被 main/analyzer import）**，回答三问：最优参数随滚动窗是锚定还是漂移、成本加到多少策略由盈转亏、策略能承载多大资金。**关键工程决策=复用而非重写**：第34轮 backtest_validation 已有 build_param_grid_matrix（同信号/撮合的 entry/stop/target 18组参数网格回放成 T×N 日收益矩阵）与 walk_forward（滚动IS窗按夏普选参、下一OOS窗验证），本轮只在其上补"跨品种批量组织+稳定度评级+成本曲面+换手容量+sidecar/台账登记"，不重写 WF 引擎。
### 一、tools/wf_cost_lab.py（601行，8组零网络自测；纯函数层不 import 项目模块、零DB可测，真实回放层惰性 import）
1. **②wf_stability（参数稳定性轨迹+评级）**：输入 walk_forward 的 segments（每段 chosen候选idx/is·oos夏普/oos_best/median/beat_median）+候选名，输出选中参数名轨迹、各候选被选次数、最高频占比=**锚定率**、切换次数/率、IS→OOS夏普衰减、选参遗憾(事后最优−实际选)、OOS为正段占比、跑赢OOS中位数比例；评级 **稳定(锚定率≥60% 且 OOS正段≥60%)/一般(锚定率≥40%)/漂移(<40%)**，0段=样本不足，chosen 越界防御成"?"。
2. **③build_cost_surface（成本敏感性曲面）**：固定全样本**笔夏普最高参数**，在 每腿费率(0/0.25/0.5/1/2bp)×单边滑点(0/0.5/1/2/4bp) 5×5 网格，用 ib.simulate 的**兜底比例费率模式**(use_real_fees=False、每腿收 fee_rate、滑点按方向不利偏移成交价，便于精确扫档；与真实费率表口径不同→结果只用于相对敏感度/安全垫、非绝对盈亏)重放；每格 summarize_trades=笔数/胜率/笔夏普(均值/标准差不年化)/逐笔复利/成本占毛利比。breakeven_cost 沿基准行/列找首个复利≤0档，给**成本安全垫倍数=首个转负档/基准**（全程不转负=None即∞、基准已亏=0）。
3. **estimate_turnover_capacity（换手+容量，数量级、诚实标注）**：按交易日聚合分钟bar Σ(v×收盘×乘数)=市场日均名义；策略侧日均笔数/单手名义(均入场价×乘数)/1手年换手(×243)；**参与率上限默认10%**反推每笔可承载手数=市场日均名义×cap/(单手名义×日均笔数)、对应名义资金。明确线性、忽略冲击、库内bar覆盖口径，**精确容量待 G14 一档盘口自采**。坑：兜底费率模式 simulate 内 multiplier=0，乘数必须单独 load_fee_schedule 取真实值。
4. **编排/CLI/台账**：run_symbol(单品种)=网格WF+选最优参+独立装载bars跑曲面+容量；run(--codes默认RB,MA,I,TA/--all/--period/--wf-train20/--wf-test10/--participation)，出 reports/wf_cost_lab.txt+.json(allow_nan=False)；末尾经 experiment_ledger.safe_record 旁路登记 kind=wf_cost_lab（全try、失败不影响本工具）。**无参=selftest、带参才 run()（老坑再确认）**。
### 二、真实冒烟诚实结论（默认 RB,MA,I,TA，30m 各1047bar/83交易日/18组网格，WF=20训10测=6段）
- **RB 漂移**：最优 e2/s2/t1.5，OOS夏普-0.10，基准复利-3.6%，成本/毛利64.3%，**基准档已不盈利→双轴安全垫0**；**I 漂移**：e1.5/s2/t2，OOS-0.14，-10.1%，基准已亏→安全垫0。这两个连零成本附近都难盈利，**绝不应进默认**。
- **MA 一般**：e2/s1.2/t3，OOS+0.04，基准复利+17.8%，成本/毛利仅6.9%，fee/slip 扫到最高档全程不转负=**∞最扛成本**；**TA 漂移但正收益**：e1.5/s1.2/t3，OOS+0.06，+6.5%，费率全程扛住、滑点到4bp才转负≈**基准4倍安全垫**。
- 容量数量级：RB 市场日均名义约154亿、单手3.1万、10%参与率每笔约3.1万手/名义约9.7亿；TA 约204亿、单手3.0万、约4.2万手/12.3亿（低频策略容量本就大，仅数量级）。结论与第34轮一致：30m入场参数近窗样本外优势弱、参数普遍漂移，MA/TA 有成本缓冲但仍须双样本+影子，调参不覆盖默认。
### 验证与规模
- tests/test_wf_cost_lab.py **新增14例零网络/零DB**（笔统计复利、曲面沿fee/slip单调+基准格定位、break-even转负/全正/基准已亏三态、WF稳定-漂移-越界防御-空、combo名解析、容量手算/空/零乘数/字符串日期、成稿、JSON无NaN；曲面用注入假runner不碰回测内核）+ test_tools_selftest 注册模块8组selftest；**全量 pytest 510→526 全绿 0失败0错误0跳过（cache/r45_junit.xml）**、compileall 过。
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) equity/trades 与 cache/base 双 sha256 逐字节一致**（equity c4da4cdf61f3bcdc/trades 50dcc800d326f8e9）；grep 证明 main.py/analyzer.py 零 import wf_cost_lab；真实 main --once exit0、stderr 0行零异常（cache/r45_main_*.log）。
- 生产 py **62→63（根44不变、tools18→19新增 wf_cost_lab 601行）27751→28352 行**；tests 33→34文件 5245→5430行（test_wf_cost_lab 179行）；用例510→526；storage仍14表、看板15图15页签（不动）；零新增依赖；代码 commit 36f9068、文档 commit 见 git log、**tag v0.45.0**。
- reports 最终恢复**16品种口径**（RB,HC,I,JM,CU,AL,MA,TA,SA,FG,AU,AG,M,Y,SR,CF；trade_journal 2563笔，恢复时用 FUTURES_EXPERIMENT_LEDGER=0 关台账避免重复登记）；台账最终**5类各1条**干净（trade_journal/research_review/portfolio_lab/portfolio.compare_risk/wf_cost_lab）。
### 纪律与下一站（第46轮）
- **wf_cost_lab 使用纪律**：成本曲面是兜底比例费率口径，看"相对敏感度/安全垫倍数"而非绝对盈亏；要绝对盈亏须用真实费率表(load_fee_schedule)再跑；容量是线性数量级、非盘口深度，G14 一档快照前不得当精确容量；评级阈值(60%/40%)是经验规则、报告已注明，可调但要双样本。
- **至此 G27 ①台账(44)②WF稳定性③成本曲面/容量(45)全部落地**。第46轮优先候选：①**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本；②**G19** 备份/自启/runbook（成本最低安全项）；③**G5** 组合风控VaR/压力（吃 portfolio_constructor 协方差/风险贡献）；④wf_cost_lab 增强=曲面扩多参数/多周期、WF 扩 purged+embargo；⑤看板加 research_review/experiment_ledger/wf_cost_lab 页签（低优先）。
- 旧排队：G24微结构季节、G28续逐笔持仓归因、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照(G30②与精确容量前置)、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12价
- 复用提示：**研究侧新工具一律"纯函数层零项目import可单测 + 真实层惰性import"；复用既有引擎(build_param_grid_matrix/walk_forward)前先读其返回结构，别重写；Windows 复杂中文补丁写 cache/*.py 跑完即删、勿 python -c 内联；研究工具无参=selftest带参才run()；读CSV/TXT用 utf-8-sig。**

## 零.旧、最新快速接续摘要（第44轮，已被第45轮接续，存档）
- **一句话**：本轮落地总纲 G27①「统一实验台账」——新增**根模块 `experiment_ledger.py`（626行纯标准库、零网络、不被 main/analyzer import）**，给分散的研究/回测实验（portfolio_lab/trade_journal/research_review/portfolio --compare-risk，后续 WF/成本曲面）建 MLflow 式登记处：追加式 `reports/experiment_runs.jsonl`（gitignore 运行日志，绝不覆盖既有报告/CSV，不碰 storage.backtest_runs 主链表）。**重要过程**：开工时发现工作区已有另一路未提交的同题成果（根模块 experiment_ledger.py+test+4宿主钩子，比我自建的 tools/experiment_runs 更严谨），遂弃重复方案、完整接管并验证收尾。代码 commit d9d2a6e，tag v0.44.0。
### 一、experiment_ledger.py（626行，13组零网络自测；只登记与查询，不重跑任何实验）
1. **config_hash（G27 验收点=同配置两次 hash 一致）**：canonical_bytes 排序键+紧凑分隔+UTF-8（键序无关、中文稳定）；canonical_hash=sha256(实验类型+规范化参数+**输入数据内容身份**)取16位，**刻意不含运行时刻/文件 mtime/产物**——输入被逐字节重写（mtime变内容不变）hash 不变；json_safe 前置清洗（非有限浮点→None、datetime→串、set/tuple→list、键转str），落库前 json.dumps(allow_nan=False) 预检。
2. **数据指纹**：file_fingerprint（exists/size/mtime，≤2MB 算全量 sha256、超限只记 size 以免对分钟库/大CSV/DB全量哈希）、build_manifest 批量去重、data_identity_from_manifest 有 sha 用 sha 否则退化 size 且**排除 mtime**。
3. **LedgerStore 追加式台账**：每行紧凑JSON（utf-8、LF），读时宽容（空/坏行跳过计 bad_lines），写时**原子替换**(同目录tmp+os.replace)+进程内 RLock；同 config_hash 已存在写 `repeat_of=上一条run_id`（两条都留、串联看漂移、不覆盖），同秒同hash撞 run_id 自动 -r2/-r3；filter 按实验/limit。
4. **safe_record 宿主统一入口**：构造+追加、任何异常全吞返 None（台账是旁路绝不拖垮宿主）；环境变量 `FUTURES_EXPERIMENT_LEDGER` 重定向路径，置 0/off/false/none 关闭。CLI：`--list/--show run_id/--repeats/--export/--experiment/--limit/--ledger`，无参=selftest。
5. **4 宿主旁路钩子（全 try 包裹、不改任何产物口径）**：portfolio.py **仅 --compare-risk 时**登记三法指标+sizing参数（普通回测路径不触发）；tools/portfolio_lab.py 登记四方法滚动指标+构建设置；tools/trade_journal.py 登记笔数/胜率/PF/期望/连亏/由盈转亏（payload 构造提到 json 分支外复用）；tools/research_review.py **惰性 import**（守其模块级不 import 项目模块纪律，导入前补 _ROOT 到 sys.path）登记数据源新鲜度计数+待办WARN/INFO计数。conftest 启动把台账重定向系统临时文件并清空（测试隔离，不污染真实 reports）；.gitignore 增 reports/*.jsonl。
### 二、真实冒烟诚实结论（reports 最终为16品种口径，台账4类4条）
- 4 宿主钩子全部真实登记成功；**真实演示同配置重跑 config_hash 完全一致**：trade_journal=fc30b0d5ce、research_review=3fe4f997a1，第二次跑写 repeat_of 指回首跑（--repeats 可见指标漂移轨迹）；compare-risk 手动补登第41轮WP-F2真实三法结果（equal 92.09万/回撤5.91%、inv_vol 94.10万/7.07%、erc 92.08万/7.90%，三法差异小、风险sizing默认仍关）。
- **本轮踩坑（重要、避免再犯）**：research_review/trade_journal 等工具 `__main__` 是"**无参=selftest、带参才 run()**"；冒烟时若不带参数，宿主走自测、钩子根本不触发（一度误判为钩子静默失败，逐层插桩后确认是命令漏参，非 bug，插桩已全部还原）。
### 验证与规模
- tests/test_experiment_ledger.py **新增22例零网络确定性测试**（规范哈希键序无关/同配置一致/敏感、json_safe、指纹排mtime、记录字段、追加与repeat串联、碰撞-r2、坏行宽容、原子LF、safe_record成功/吞错、渲染、CLI、环境变量关闭重定向）+ test_tools_selftest 注册1 + test_compileall 随新根模块自动1 → 全量 **pytest 486→510 全绿（0失败0错误0跳过）**、模块13组自测过、compileall 过。
- **默认8品种(I,MA,RB,SA,TA,AU,AG,CU) portfolio_equity/trades 与 cache/base 双 sha256 逐字节一致**（证明 portfolio.py 主链改动零影响，普通回测路径不触发钩子）；grep 证明 main.py/analyzer.py 零 import experiment_ledger；真实 main --once exit0、stderr 空、零 ERROR/Traceback。
- 生产 py **61→62（根43→44新增 experiment_ledger 626行、tools18不变）27021→27751 行**；tests 32→33文件 5011→5245行（test_experiment_ledger 228行）；storage 仍14表、看板15图（本轮不动）；运行依赖仍仅 requests/uiautomation/websocket-client 零新增。
### 纪律与下一站（第45轮）
- **台账使用纪律**：实验台账是 append-only 运行日志（gitignore 不入库），宿主自动登记、也可手动补登无独立文件的实验（如控制台 compare-risk）；config_hash 同=可复现配置、不同=参数或输入内容变了，--repeats 看漂移；台账只读登记、绝不反向改参/改主链，任何调参仍走双样本+影子+默认回退。
- **G27 留续**：②walk-forward 滚动参数稳定性（滚动窗定参下窗验证，复用 backtest_validation 已有 WF/PurgedKFold）、③fee/slip 成本敏感性曲面×净收益/夏普+换手容量估计；台账已预留其 kind 登记位。
- **第45轮优先候选**：①**G27②③**（WF滚动+成本曲面，顺着台账做完整）；②**G26续二** 日线全64+多gross(1.0/1.2/1.5)网格影子/换手成本；③**G19** 备份/自启/runbook（成本最低安全项）；④**G5** 组合风控VaR/压力（吃 portfolio_constructor 协方差/风险贡献）；⑤看板加 research_review/experiment_ledger 页签（低优先）；⑥G25续/G22续/G23续。
- 旧排队：G24微结构季节、G28续逐笔持仓归因、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照(G30②前置)、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12价差；regime/失效因子采纳前仍须双样本+事件层互证。
- 复用提示：**宿主旁路钩子一律 safe_record+全 try，登记失败不得影响宿主；测试里凡调宿主 run() 必须靠 conftest 的 FUTURES_EXPERIMENT_LEDGER 重定向临时文件，别污染真实 reports；冒烟研究工具记得带参数（无参=selftest 不触发 run/钩子）；Windows 读项目 CSV/TXT 仍 utf-8-sig；复杂中文/引号补丁走 cache/*.py 脚本跑完即删**；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`，物理行数以 Python glob、测试数以 --junitxml 解析为准。

## 零.旧、第43轮快速接续摘要（已被第44轮接续，存档）
- **一句话**：承接第42轮 G30① journal，本轮落地总纲 G30③「一键日/周复盘」——新增 `tools/research_review.py`（777行纯标准库、零网络、只读）。**核心设计：不 subprocess 重跑任何工具**，只把各研究工具已落盘的 reports/*.json sidecar（factor_health/factor_regime/attribution/trade_journal/portfolio_lab/backtest_validation）+ portfolio_equity.csv + 主链 signal_tracking.txt 聚合成一份"收盘研究简报+规则化待办"，秒级出 reports/research_review.txt+.json。**命名坑（重要）**：主链已有 reports/daily_review.txt（report.build_daily_review 实时轮动+新闻、永久保留，属 main 铁律不动），故研究侧不叫 daily_review 而命名 research_review，绝不覆盖主链。守三铁律：不 import 任何生产模块、不被 main import、不改主链/综合分/默认CSV。代码 commit 5d3fa06+文档 commit，tag v0.43.0。
### 一、tools/research_review.py（777行，11组零网络自测；SOURCES 台账登记9数据源与刷新命令）
1. **装载与新鲜度**：load_sidecar 坏JSON返(None,mtime)以区分 missing/broken；freshness_state 三态 ok/stale(默认>168h)/missing + age_label；**读 CSV/TXT 必须用 utf-8-sig**（真实 portfolio_equity.csv 带 BOM，否则首列键变 `\ufeffdt`、DictReader 末尾空行还会覆盖 last——本轮两个真实坑，均已修并加断言：期初取首行/期末取末行、跳过 dt 为空的记录、全表扫最大回撤）；load_signal_tracking 正则提主链信号各周期样本/胜率/方向收益。
2. **六段提取器（缺字段安全返{}）**：sec_factor_health（G29 event[30] verdict 收"失效/预警"、daily 5日RankIC排序、半衰期）、sec_attribution（G28 指定周期 alpha/R²、因子贡献与板块BHB effect 排序、缺周期返{}）、sec_journal（G30① 总览+持仓/信号弱势桶[n≥10且PF<0.7]+由盈转亏比例）、sec_lab（G26 四方法滚动年化/波动/夏普/回撤/换手+快照有效N）、sec_validation（WP-F4 DSR/SR0/裁决+网格PBO）。
3. **规则化待办引擎 build_actions**：纯规则产出(WARN/INFO/OK,文本)并按级排序（WARN在前）；规则=sidecar缺失（**expr_research 可选、signal_tracking 主链自动产出，这俩缺失只 INFO**，其余缺失 WARN）、陈旧、因子失效预警、journal整体PF<1/弱势持仓桶/弱势信号档/由盈转亏≥50%、组合回撤>15%或风险度>80%、DSR<0.95、归因alpha<-1e-3、ERC夏普比等权高>0.08（决策素材，不自动改sizing）；全绿给 OK。**待办是提示不是调参令**。
4. **成稿/CLI**：build_report 七段（抬头+〇新鲜度总表+一信号+二G29+三G28+四journal+五风险+六防过拟合+七待办+固定声明）；build_json_payload allow_nan=False；CLI `--reports-dir/--stale-hours/--out/--json-out`，空目录也能出全降级报告。无参=selftest、带参=run（与其它 tools 一致）。
### 二、真实聚合诚实结论（reports 9数据源全新鲜，秒级成稿）
- **信号命中**：30m 464样本胜率49.3%/方向-0.01%、2h 441/50.9%/+0.02%、**次日(约24h)315/55.2%/+0.25%——方向优势随持有期拉长才显现，分钟级≈掷硬币**（与 journal"1-2bar极短单失血"互证）。
- **G29 事件因子30m 4个失效预警**：新闻消息面(IC+0.711%/最长连失6/失败占比62.5%)、机构动向(+2.355%/连失3/60%)、日线动量(-4.519%/连失5/56.2%)、技术共振(+7.656%/连失5/54.5%)；日频5日RankIC最弱 ret20 -2.673%、最强 tsmom252 +1.118%（长周期动量最稳，与第39轮 regime 一致）。
- **G28 30m** n=376 alpha=-0.00021/根、R²仅2.7%，最负日线动量、最正技术共振，板块农产品 effect+0.00099 最正、能化/有色偏负。
- **组合风险**：默认8品种(I,MA,RB,SA,TA,AU,AG,CU)回测100万→92.09万(-7.91%)/最大回撤10.5%；G26滚动504日61品种 equal夏普0.42/回撤9.5% vs inv_vol0.55/7.8%、erc0.53/6.5%、gmv0.35/5.3%(年换手177%过高)；WP-F4 组合DSR=0.0001(试18组)无法排除多重试验偶然性、2品种网格0个样本外为正。一键待办 6WARN+3INFO，免去人翻7份报告。
### 验证与规模
- tests/test_research_review.py **新增15例零网络**（缺/损sidecar、新鲜度三态与age文案、equity的BOM+末尾空记录+全表回撤+期初末口径、信号正则、各段提取排序与弱势桶n门槛、待办WARN优先/可选降级/全OK、collect空目录与合成sidecar端到端、七段成稿与allow_nan、格式化器）；test_tools_selftest 注册(+1)、test_compileall 随新生产py自动+1 → 全量 **pytest 469→486 全绿（约7.3s/32文件+conftest）0失败0错误0跳过**、compileall 过。
- **默认8品种基线宇宙已查明=I,MA,RB,SA,TA,AU,AG,CU**（其中仅 I/MA/RB/SA/TA 有成交，AU/AG/CU 无成交但贡献时间网格；用时间网格集合反推法确定，已写进commit，下次直接用）：该宇宙 equity/trades CSV 与 cache/base_portfolio_*.csv **双哈希逐字节一致**；git 证明本轮仅新增 tools/research_review.py+两 tests、主链零改动；真实 main --once 43s exit0 无 ERROR/Traceback。
- 生产 py **60→61（根43不变、tools17→18含 research_review）、26244→27021 行**（research_review 777行）；tests 31→32文件、4806→5011行（test_research_review 199行）；storage 仍14表、看板15图（本轮不动）；运行依赖仍仅 requests/uiautomation/websocket-client。
### 纪律与下一站（第44轮）
- **编排器使用纪律**：research_review 只读已落盘 sidecar，跑前先确保各研究工具产物是新的（看〇新鲜度表）；它聚合的是"研究/回测侧"，主链实时复盘仍看 daily_review.txt，两者不混；待办只提示，任何改动走双样本+影子+默认回退。
- **优先候选**：①**G27** 统一实验台账 experiment_runs（登记 compare-risk/portfolio_lab/journal/research_review 快照，现在各实验分散、靠文件名和时间戳）；②**G26续二** 日线全64+多gross网格影子/换手成本；③**G19** 备份/自启/runbook（成本1.5最低安全项）；④**G5** 组合风控VaR/压力（吃 portfolio_constructor 协方差）；⑤看板加 research_review 页签（G30③尾巴，低优先）；⑥G25续/G22续/G23续。
- 旧排队：G24微结构季节、G28续逐笔持仓归因、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照(G30②前置)、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12价差；regime/失效因子采纳前仍须双样本+事件层互证。
- 复用提示：**聚合/编排类工具照本轮模式——不重跑只读sidecar最解耦；Windows 下读任何项目产出的 CSV/TXT 一律 utf-8-sig（BOM 是常态）；DictReader 记得跳过 dt 为空的末尾记录；JSON sidecar 提取器全部 .get 链式防御、缺字段返{}；规则引擎把"可选/自动产物缺失"降 INFO 避免告警泛滥；测试用 tmp_path 造合成 sidecar、绝不读真实 reports**；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`、复杂引号/中文补丁走 cache 脚本跑完即删，物理行数以 Python glob、测试数以 --junitxml 解析为准。

## 零.旧、第42轮快速接续摘要（已被第43轮接续，存档）
- **一句话**：复盘三件套（G28归因✅第35轮/G29体检✅37-39轮）之后，本轮补 G30 第一块——**交易复盘 journal**：新增 `tools/trade_journal.py`（710行纯标准库、零网络、只读不写主链），把 portfolio.py 回测产出的 portfolio_trades.csv（每行=一笔完整开平 round-trip，净盈亏已含开平费与平今腿）一键转成"钱亏在哪类单上"的分桶/节奏/MFE-MAE/典型单复盘报告。守三铁律：不碰 main/analyzer/综合分、**不改 portfolio 默认 CSV 输出（8品种基线哈希仍逐字节一致）**、零新依赖；G30②纸面vs真实成交一致性（需 G14 自采盘口）与③daily_review 全链路编排器本轮不做、留续。一次代码 commit(202ac51)+一次文档 commit，tag v0.42.0。
### 一、tools/trade_journal.py（710行，17组零网络自测，复用 metrics.trade_stats/excursion 同口径）
1. **装载与安全降级**：load_trades 宽容解析两种时间格式/坏值/空头负分/缺文件返[]，按 exit_dt 升序；load_equity 读逐bar权益；**空文件/0笔/缺equity/缺分钟库全部出降级报告绝不抛错**（总纲验收硬要求）。
2. **七维分桶 bucket_table**（每桶：笔数/胜率/净盈亏/均笔期望/盈亏比payoff/利润因子PF=总盈/|总亏|/费用/费用占|毛|/均持仓/最长连胜连亏，按净盈亏升序最差在前）：品种、板块、多空、**平仓原因组**（止盈/止损/日终·样本强平/反向信号归并）、平今平昨、**信号强度按|入场分|**（空头入场分为负、方向已独立成桶，多空同档，阈值沿用 config SCORE_NEUTRAL2/LIGHT4/MID6.5）、持仓时长档（1-2/3-6/7-12/13+ bar）。period_pnl 出日/ISO周盈亏节奏、cumulative_curve 累计净值、equity_drawdown_days 权益回撤对照。
3. **盘中 MFE/MAE（可选 --bars）**：attach_excursions 与 portfolio.load_minute_feed 同口径装载（storage+intraday_backtest.load_minute_bars+backtest.ratio_adjusted_bars 比例复权，保证与成交价同序列），bisect 取 [entry_dt,exit_dt] 闭区间，用每根 bar **盘中 h/l**（多:MFE看h/MAE看l，空镜像；含入场根全根极值=保守上界，报告标注），按品种只装载一次、覆盖率透明、缺区间笔安全跳过；excursion_summary 分全体/盈/亏，并算"亏损单盘中曾浮盈>0.1%仍亏损平仓=由盈转亏/回吐"比例。
4. **一键成稿与CLI**：build_report 七段（总览/七维分桶/日节奏/周节奏/MFE-MAE/最佳最差5单/规则化观察）；观察=期望拆解 wr×均盈−败率×均亏、n≥10且PF<0.7弱势桶（**全胜桶PF=None不误报、原因组定义性全赢全亏不参与扫描**——这两条是本轮踩出的口径坑）、费用占比、强平结构、由盈转亏。CLI `--trades/--equity/--bars/--period/--review {none,daily,weekly,both}/--out/--json-out`，出 reports/trade_journal.txt+.json（allow_nan=False）。
### 二、真实复盘诚实结论（16品种30m、2026-05-11~09-01、2563笔，--bars覆盖率100%，1.8s）
- **总览**：胜率39.7%、盈亏比1.36、**PF0.90（成本后期望为负 −31元/笔）**、净−79,101（毛−42,768、费用36,334占|毛|85.0%）、均持3.9根30m、最长连胜13/连亏15；多1279笔PF0.83亏64,033、空1284笔PF0.96亏15,068（样本偏震荡）；17周仅5周盈利（29.4%）；权益100万→92.09万、回撤10.5%。
- **最有行动价值——亏损高度集中在极短持仓噪声单**：持1-2bar **1289笔(50.3%)胜率30.3%/PF0.57/净−196,770**；3-6bar 981笔 PF1.26/+63,851；7-12bar 286笔 胜率59.4%/**PF2.16/+53,177**；13bar+ 7笔 PF3.07。**拿得住的单才赚、1-2根被噪声扫出的单系统性失血**（与第41轮风险型sizing互为印证：问题在离场/持仓耐心而非横截面分配）。
- **平仓结构**：止损970笔−616,730 vs 止盈530笔+532,993（均笔+1,006）；**日终强平870笔反而胜率54.5%/PF1.78/+61,736**（日终不留仓不是亏损源）；反向信号193笔 PF0.03/−57,099（这批样本里反向即走代价高）。**信号强度单调改善**：弱(|分|<2)526笔胜率34.2%/PF0.82 → 轻仓[2,4)1546笔40.1%/0.89 → 分批[4,6)491笔44.2%/**1.00打平**（分钟入场分到不了≥6.5强档，无强信号桶）；品种仅 JM 为正(+6,996/PF1.09)，RB/FG/CF 最弱。
- **盘中MFE/MAE**：全体均MFE0.46%/MAE0.45%；盈利单 MFE0.84%/MAE0.20%（浮盈路径顺），亏损单 MFE0.22%/MAE0.61%；**亏损单1546笔中912笔(59.0%)盘中曾浮盈>0.1%仍以亏损平仓**=离场/止盈纪律改进线索。本轮只出证据、不改任何策略参数（改参须另开轮次走双样本+影子）。
### 验证与规模
- tests/test_trade_journal.py **新增17例零网络**（CSV往返类型/排序、原因归并、|分|档位多空同档、分桶手算PF、日周ISO聚合、MFE-MAE多空镜像与闭区间/区间外安全、monkeypatch注入bars不碰生产库、全胜桶不误报/弱桶命中、run端到端txt+json、空数据降级）；test_tools_selftest 注册 selftest(+1)、test_compileall 随新生产py自动+1 → 全量 **pytest 450→469 全绿（约8.9s/31文件+conftest）0失败0错误0跳过**、compileall 过；**默认8品种组合回测 equity/trades CSV 与 cache 基线哈希逐字节一致**（主链零改动证据）；真实 main --once 42.6s exit0 无 ERROR/Traceback。
- 生产 py **59→60（根43不变、tools16→17含 trade_journal）、25534→26244 行**（trade_journal 710行）；tests 30→31文件、4580→4806行（test_trade_journal 220行）；storage 仍14表、看板15图（本轮不动）；运行依赖仍仅 requests/uiautomation/websocket-client。
### 纪律与下一站（第43轮）
- **journal 的使用纪律**：它是"诊断镜"不是"调参令"——极短单失血/由盈转亏等发现要变成改动，必须另开轮次走时间双样本+事件层互证+默认回退，禁止直接照单改止损/持仓规则。
- **优先候选**：①**G30续** ③tools/daily_review.py 全链路编排（串 G29因子→信号命中→G28归因→本轮journal→G3/G5风险→待办，收盘一键）；②**G27** 统一实验台账 experiment_runs（登记 compare-risk/portfolio_lab/journal 快照）；③**G26续二** 日线全64+多gross网格影子/换手成本；④**G19** 备份/自启/runbook（成本1.5最低安全项）；⑤**G5** 组合风控（可吃 portfolio_constructor 协方差/风险贡献，实际成本低于表估2.5）；⑥G25续/G22续/G23续。
- 旧排队：G24微结构季节、G28续(逐笔持仓归因+看板页签)、G1纸面影子对照(PAPER默认False)、G2插件化、G8只读web、G14一档快照(G30②前置)、G13无key休眠、G3绩效tear sheet、G7 TSMOM生产化、G12价差；regime条件动量采纳前仍须双样本+事件层互证。
- 复用提示：复盘/报告类工具照本轮模式——**统计口径直接复用 metrics.trade_stats 不重造、分桶key函数收标量时记得在外层包 lambda(t:...)（本轮两次踩 key_fn 收到整笔 dict 的坑）、老式 % 格式化不支持千分位逗号要用 str.format、读生产库只在显式 --bars 时做且测试用 monkeypatch 注入合成bars、空数据降级是验收项不是可选项**；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`、含|分|等特殊字符的中文补丁不要 -c 内联（会被管道解析）走 cache 补丁脚本跑完即删，物理行数以 Python glob、测试数以 --junitxml 解析为准。

## 零.旧、第41轮快速接续摘要（已被第42轮接续，存档）
- **一句话**：承接第40轮 G26（离线 portfolio_constructor + portfolio_lab 已证 ERC/逆波动的降险价值），本轮按总纲 G26「实施顺序②」把**逆波动/ERC 风险型目标权重接入 `portfolio.py` 共享账户内核**（组合回测与 paper_broker 都走它）。守三铁律：纯标准库零新依赖、**默认 risk_sizing=None 时手数决策逐字节等价旧等名义（equity/trades CSV 哈希级回归一致）**、不碰 main/analyzer/综合分；GMV 第40轮已证过集中（有效N仅11）**不接入**；实时/paper 协方差权重源本轮**不接线**（先回测影子、未注入权重时内核自动回退等名义）。一次代码 commit(12755ec)+一次文档 commit，tag v0.41.0。
### 一、portfolio.py 共享内核增量（1018→1191行）
1. **Portfolio 状态/手数（纯增量、缺省不变）**：`__init__` 增 `risk_sizing(None/inv_vol/erc，非法值归None)`、`risk_gross=1.0`，状态 `risk_weights{sym:目标名义占比}/risk_meta/risk_meta_log`，方法 `set_risk_weights(wmap,meta)` 注入、`avg_risk_eff_n()` 历次重估平均有效N。`decide_lots` 在旧三 sizing 分支（equal_notional/equal_risk/score）与校准乘子之间插入横截面覆盖：risk_sizing 开启且 sym 在最新权重宇宙→`raw=eq*w/per_lot_notional`（记 `_last_target_weight`），**宇宙外/未估出保留等名义 raw 安全回退**；下游单品种 max_symbol_weight、板块 max_sector_weight、现金、max_concurrent 上限链与 WP-F2 校准乘子全部原样复用。risk_sizing=None 时整段不进入。
2. **`trailing_risk_weights(feeds,t,method,*,window,min_hist,shrink,cap,gross)` 纯函数（严格PIT核心）**：对每品种 `bisect_left(dts,t)-1` 取最后一根 dt<t 的bar（**t当根及以后一律不看**，入场发生在t开盘），窗口内收盘价 {dt:c}；先按自身长度≥min_hist+1 筛、再取各品种**公共时间戳稠密对齐**（协方差必须同时刻配对）算简单收益、收益长度≥min_hist 才入宇宙；调 `portfolio_constructor.construct(R,method,shrink,cap,target_annual=0)` 出权重×gross；meta 带 n/T/asof/eff_n/ann_vol/div_ratio/gross_base/excluded。**可估品种<2 或公共历史不足 → 返回 ({},meta带reason)，绝不抛错**（调用方回退等名义）。
3. **引擎 `run_portfolio(...,risk_cfg=None)` + `_reset_feeds(feeds)`**：时间轴每根 t 按 risk_step（首根+每 rebalance 根）调 trailing_risk_weights 并 `pf.set_risk_weights`；risk_cfg=None 时零行为变化。`_reset_feeds` 清每个 feed 的 pos/pending/blocked_entry/blocked_exit，使同一批 bars 可被确定性重复回放（影子对照前提，已测两次曲线/成交逐值一致）。
4. **CLI 与同宇宙影子对照**：新增 `--risk-sizing {inv_vol,erc}`（留空=关）、`--risk-window(默认PRS_WINDOW=126)`、`--risk-rebalance(20)`、`--risk-min-hist(40)`、`--risk-gross(1.0)`、`--risk-cap(0.20)`、`--compare-risk`。main 内 `_run_once(method)` 重置 feeds→建新 Portfolio→回放；`--compare-risk` 把 等名义/逆波动/ERC **各确定性回放一次**，`build_compare_block` 出对照表（期末权益/总收益/年化/夏普/最大回撤/平均风险度/最大持仓/平仓笔数/平均有效N），**写出的 equity/trades CSV 永远是基线等名义、逐字节不变**；报告抬头标注风险型口径（窗口/重估/总敞口/上限/严格无未来）。
### 二、paper_broker.py（830→840行）与 config.py（766→775行）
- PaperBroker `__init__` 增 risk_sizing/risk_gross 能力位透传给 Portfolio + `set_risk_weights()` 透传方法；**默认 None 逐字节等价旧版**；实时协方差权重源（需K线历史）本轮刻意不接线，注释明示"先回测影子达标后再议"。
- config 在 PC_JSON 后增量插入 **PRS_* 簇（纯新增）**：PRS_ENABLED=False/PRS_METHOD="erc"/PRS_WINDOW=PC_LOOKBACK(126)/PRS_REBAL=20/PRS_MIN_HIST=40/PRS_GROSS=1.00/PRS_CAP=PC_MAX_WEIGHT(0.20)。
### 三、真实数据影子证据（本地分钟库 30m、严格无未来，三法仅目标名义不同、撮合/成本/约束完全一致）
- **8品种小样本**：三法平仓同为995笔（证明只改手数、不改信号路径）；等名义 −3.91%/回撤5.91%/平均风险度6.7%，逆波动 −6.00%/回撤7.30%/有效N6.5，ERC −6.45%/回撤7.90%/有效N6.6（gross1.0 在小宇宙偏保守）。
- **16品种宽样本（RB/HC/I/JM/CU/AL/MA/TA/SA/FG/AU/AG/M/Y/SR/CF）**：等名义 −7.91%/年化−19.81%/夏普−2.20/**回撤10.50%/平均风险度15.2%**/2563笔；**逆波动 −2.55%/回撤3.37%/风险度5.0%/1791笔/有效N13.2；ERC −2.67%/回撤3.29%/风险度5.0%/1828笔/有效N12.8**。笔数减少是"低权重高价品种目标名义不足1手→不开仓"的约束链一致结果（非未来函数，已写进对照表说明）。
- **判读**：宽宇宙下风险型显著降回撤/降风险度（其设计目标，方向与第40轮日级 portfolio_lab 一致），**价值在降险而非增收益**；小宇宙/gross1.0 下反而更保守，说明效果依赖宇宙宽度与总敞口选择。**本轮不默认启用**，是否默认开/paper 接实时源须更长样本（日线全64、多 gross 网格）与换手成本评估。
### 验证与规模
- tests/test_portfolio.py **+6 组零网络断言（223→343行）**：①默认 risk_sizing=None 关注入权重也逐值不变；②权重定目标手数（RB 0.30→10手 vs 等名义5手）+宇宙外CU回退等名义10手；③**严格PIT：篡改 t 当根及以后收盘价权重逐位不变**、过早 t 返空map带reason；④inv_vol/erc 权重非负/和=gross/单票≤cap/缺历史品种进excluded；⑤_reset_feeds 两次回放曲线+成交逐值一致；⑥引擎 risk_cfg=None 与不传逐值一致、开启后 risk_meta_log 留痕且权重和=1。
- 全量 **pytest 444→450 全绿（约8s/30文件+conftest）0失败0错误0跳过**、compileall 全过；**默认组合回测 portfolio_equity.csv/portfolio_trades.csv 与改动前基线哈希逐字节一致**（报告仅标题行变化）；真实 main --once 48s exit0 无 ERROR/Traceback，证明主链/综合分零改动。
- 生产 py **仍59（根43/tools16）、25341→25534 行**（portfolio 1018→1191 +173、paper_broker 830→840 +10、config 766→775 +9）；tests 仍30文件（+conftest31）/450用例/4580行；storage 仍14张业务表、看板15图（本轮不动）；运行依赖仍仅 requests/uiautomation/websocket-client。
### 纪律与下一站（第42轮）
- **风险型 sizing 的后续门槛（勿抢跑默认开）**：先在**日线全64品种**与多 gross（1.0/1.2/1.5）网格跑 --compare-risk 看跨周期稳健性；评估"不足1手不开仓"是否需要最小手数兜底；paper 接实时协方差源前须证明 K线历史 PIT 可得且与回测同口径；这些达标前默认保持 risk_sizing=None。
- **优先候选**：①**G26续二** 日线全样本+gross网格影子/权重重估换手成本计量；②**G30** 交易 journal/一键复盘（tools/trade_journal.py 分桶盈亏+MFE/MAE+daily_review，只读不改主链，P1高性价比）；③**G27** 统一实验台账 experiment_runs（把本轮 compare-risk 与 portfolio_lab 滚动代理登记进去）；④**G25续** 旧因子表达式改写（须综合分逐字节回归）、正交/IC·ICIR加权接G16；⑤**G22续** tradable_mask/季节交割日历/面板常驻增量；⑥**G23续** 跟踪carry近窗含roll t。
- 旧排队：G24微结构/季节因子、G28续(逐笔持仓归因+看板归因页签)、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控(可吃portfolio_constructor协方差/风险贡献)、G2插件化、G8只读web、G14一档快照、G13无key休眠；regime条件动量（第39轮）采纳前仍须时间双样本+事件层互证。
- 复用提示：任何"接入型"改动照本轮模式——**先留存默认口径输出做哈希级基线、新能力全走可选开关且缺省路径不进入新分支、PIT用bisect_left(t)−1并写"篡改未来bar不变"测试、重复回放靠_reset_feeds、对照实验只改一个维度（本轮只改目标名义）**；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`、Edit 超长中文间歇故障走带assert的cache补丁脚本跑完即删，物理行数以Python glob、测试数以--junitxml解析为准。

## 零.旧、第40轮快速接续摘要（已被第41轮接续，存档）
- **一句话**：G1 纸面/G4 回测严谨/G21-G25-G29 研究链就位后，本轮补"模拟整合"环节的资本分配层 **G26 组合构建器**：现有 portfolio.decide_lots 是逐品种独立定手数，缺一个横截面一次性回答"同一篮子谁分多少"的权重优化器。新增根模块 `portfolio_constructor.py`（纯标准库、只用协方差**不预测预期收益**：等权/逆波动/ERC风险平价/长仓GMV+目标波动）与研究工具 `tools/portfolio_lab.py`（读 G21 面板做快照+滚动样本外代理回测）。守三铁律：**默认 equal 等价旧等名义口径、不接 main、不改综合分与既有 sizing**（全树 grep 仅 portfolio_lab 引用 portfolio_constructor）。一次代码 commit + 一次文档 commit，tag v0.40.0。
### 一、portfolio_constructor.py（根模块，约430行，10组手算自测，不 import tools、不被 main import）
1. **协方差与线性代数（自包含纯Py）**：covariance 样本协方差（输入按资产的等长序列）、shrink_diagonal 向"平均方差对角阵"收缩（Ledoit-Wolf 简化、保正定改善条件数）、largest_eigenvalue 幂迭代（定投影梯度步长1/L）、is_psd Cholesky 判定、_gauss_solve 自含高斯消元（ERC Newton 用，不依赖 factor_expr 以保持模块独立）。
2. **四方法**：equal_weights 等权（基线/回退）；inverse_vol_weights w∝1/σ；**risk_parity ERC**（最小化 F(w)=½w'Σw−Σln w_i，KKT 条件 w_i(Σw)_i=常数=各品种风险贡献相等）；**min_variance/quadratic_long_only 长仓GMV**（min ½w'Σw−q'w，FISTA加速投影梯度，凸问题全局最优，q=0即GMV）。
3. **ERC 算法关键踩坑（务必沿用、勿回退到乘性定点）**：乘性定点 w*√(目标/RC)、对数障碍普通梯度、对角Newton 三种写法在真实61资产上**都停滞**（风险贡献相对偏差卡在0.019、effN虚高到53）；根因是**迭代内归一化破坏 F(w) 的自然尺度**（该目标最优解有特定尺度、w_i(Σw)_i=1，归一化会把它推离最优点再被拉回=振荡）。最终用**全 Newton**：Hessian H=Σ+diag(1/w_i²)，每步 _gauss_solve 解 H·d=g + Armijo式回溯线搜索保 w>0 且 F 单调降，**迭代中绝不归一、仅末尾归一+上限投影**；真实61资产约30次迭代收敛到各品种 RC 精确=1/n（maxRC=0.0164=1/61）、effN=34.8（相关矩阵下的真实ERC，比停滞版更集中、正确）。
4. **约束与诊断**：project_capped_simplex 投影到{w≥0,Σw=1,w_i≤cap}（二分平移量τ使Σclamp(w−τ,0,cap)=1，GMV/ERC求解后套单票上限）；target_vol_scale 按目标年化波动等比缩放、max_gross 总敞口上限防低波期过度加杠杆；risk_contributions（绝对RC+占比，占比和=1）、diversification_ratio（Σwσ/组合σ）、effective_n=1/Σw²、gross_exposure、turnover=½Σ|Δw|（支持不同标的并集）。统一入口 construct(returns,method,shrink,cap,target_annual,...)。
### 二、tools/portfolio_lab.py（约300行，5组自测，纯离线/零网络/只读面板）
- load_return_map 取面板 ret1d；dense_matrix 选**固定宇宙**（最近504日有效收益覆盖≥95%，真实入选61/64）并按公共日稠密对齐（有幸存者偏差、已在报告标注）；latest_snapshot 用过去126日协方差出四方法权重/年化波动/有效N/分散化度/最大单品种RC/目标波动杠杆+前六大权重；**rolling_proxy 滚动样本外代理**：每20日用"仅当时可得的过去126日"估协方差定权、持有到下一再平衡（严格无未来），perf_stats 算几何年化/波动/夏普/最大回撤/Calmar 与平均有效N、年化换手。出 reports/portfolio_lab.txt/.json（allow_nan=False）。
### 真实数据诚实结论（61商品、2024-08-06~2026-09-02 共504日、每20日再平衡、无成本多头代理，equal为基线）
| 方法 | 年化收益 | 年化波动 | 夏普 | 最大回撤 | 平均有效N | 年化换手 |
| --- | --- | --- | --- | --- | --- | --- |
| 等权 | +3.97% | 9.48% | 0.42 | 9.54% | 61.0 | 0.00 |
| 逆波动 | +4.59% | 8.35%(−11.9%) | 0.55 | 7.79% | 54.8 | 0.27（最省） |
| ERC风险平价 | +3.80% | 7.11%(−25.0%) | 0.53 | 6.46%(−32%) | 39.4 | 0.64 |
| GMV最小方差 | +1.72% | 4.93%(−48.0%) | 0.35 | 5.26% | 11.4（集中） | 1.77 |
- **判读**：风险型分配确实降波动/回撤（其设计目标，非预测涨跌）；**ERC 在降险与分散/成本间最平衡（夏普0.42→0.53、maxRC仅1.6%）、逆波动最廉价（换手0.27）**，二者值得后续以"默认equal、可切换影子、缺省逐字节等价旧版"接入 paper/backtest；**GMV 降波动最猛但顶到单票20%上限、有效N仅11、年化换手1.77、夏普反降到0.35，过集中不直用**。目标年化波动15%在当前低波商品上四方法都触1.5倍总敞口上限（说明该目标偏激进、杠杆须谨慎）。代理有幸存者偏差、未计费用/保证金/换月，**本轮只出证据、不改线上**。
### 验证与规模
- pytest **440→444 全绿（约7.9s/30文件+conftest）0失败0错误0跳过**（账目：test_tools_selftest 注册 portfolio_constructor/portfolio_lab +2、test_compileall 随2个新生产py自动+2）；两模块 --selftest 全过（对角协方差ERC=逆波动手算、等相关等波动ERC=等权、GMV波动≤等权凸最优不变量、capped-simplex、目标波动精确缩放+杠杆上限、协方差手算、滚动无未来且GMV≤等权、覆盖率筛选、短序列安全）、compileall 过；全树 grep 确认仅 portfolio_lab 引用、main/analyzer/portfolio 零 import；真实全61品种 portfolio_lab 14.9s exit0；真实 main --once 一轮冲烟 exit0 无 ERROR/Traceback。
- 生产 py **57→59（根42→43含 portfolio_constructor、tools15→16含 portfolio_lab）、24604→25341 行**；tests 仍30文件（+conftest31）/444用例；storage 仍14张业务表、看板15图；运行依赖仍仅 requests/uiautomation/websocket-client。config 在 PORTFOLIO 块后增量插入 PC_* 常量簇（纯新增）。
### 纪律与下一站（第41轮）
- **G26 续（接入须谨慎）**：把 ERC/逆波动以**可选 sizing 模式**接入 paper_broker/backtest（默认仍 equal_notional、缺省逐字节等价旧版、先影子对照≥一段样本）；板块上限复用 portfolio 现有 max_sector_weight；目标波动杠杆默认关、总敞口硬上限。接入前先过既有回测逐字节回归。
- **优先候选**：①**G25续** 旧因子表达式改写（须先过综合分逐字节回归）、orthogonalize/IC·ICIR加权接G16；②**G22续** tradable_mask涨跌停掩码/季节交割日历/面板常驻增量；③**G23续** 跟踪carry近窗含roll t 是否站上1.5；④**G30** 交易journal/一键复盘；⑤**G27** 统一实验台账（本轮 portfolio_lab 的滚动代理可登记进 experiment_runs）。
- 旧排队：G24微结构/季节因子、G28续(逐笔持仓归因+看板归因页签)、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控(可直接吃本轮协方差/风险贡献)、G2插件化、G8只读web、G14一档快照、G13无key休眠；regime条件动量（第39轮）采纳前仍须时间双样本+事件层互证。
- 复用提示：组合/风险类纯算法照 portfolio_constructor 模式——根模块自包含（不import tools、自带高斯消元不耦合factor_expr）、纯函数先于IO、迭代优化器警惕"迭代内归一化破坏自然尺度"这类定点停滞（用全Newton+回溯、末尾才归一）、凸优化用"最优值不劣于基线"做不变量自测；研究工具照 portfolio_lab 模式固定宇宙+严格无未来滚动+诚实标注幸存者偏差/未计成本；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`、Edit 超长中文间歇故障走带assert的cache补丁脚本跑完即删，物理行数以Python glob、测试数以--junitxml解析为准，真实研究工具耗时以 Stopwatch 实测、勿被后台轮询超时误导。
## 零.旧、第39轮快速接续摘要（已被第40轮接续，存档）
- **一句话**：G25 表达式引擎（第38轮）与 G29 因子体检（第37轮）就位后，本轮补齐 G29 体检刻意留下的三块增量，新增独立研究工具 `tools/factor_regime.py`（不膨胀已被 selftest 锁定的 factor_health.py）：①**regime 分层 IC**——因子是否只在特定市场状态有效（牛/熊/震荡 × 高/中/低波分桶）；②**持续性/隐含换手**——信号多稳、不同调仓间隔换掉多少；③**幂律 vs 指数衰减形态**——IC 随持有期到底怎么衰减（第37轮只拟合了指数）。守三铁律：纯标准库零新依赖、只读 G21 面板、**不接 main、不改任何线上权重与综合分**（全树 grep 证实 factor_regime 不被任何根/tools 生产模块 import）；复用 G25 引擎 ts_rank、factor_health 日频层、factor_eval.spearman，不重造轮子。一次代码 commit + 一次文档 commit，tag v0.39.0。
### 一、tools/factor_regime.py（约360行，6组零网络/零DB自测，纯标准库）
1. **regime 标签（PIT、只用过去）**：`trend_labels` 用面板已 PIT 落库的 ret126 判牛/熊/震荡（|ret126|<REGIME_TREND_FLAT=0.02=flat，否则 up/down）；`vol_labels` 用 hv60 序列经 `factor_expr.compute_ts("ts_rank(hv60,120)")` 得过去120日分位（<1/3 low、>2/3 high、余 mid，尾窗只用过去=PIT）。`compute_labels` 每品种只算一次标签并缓存复用。
2. **`regime_stratified_ic(rows_by_sym,factor,H,labels)`**：复用 factor_health.forward_map 取严格未来收益，跨品种池化，按 trend(up/down/flat)、vol(low/mid/high) 及牛熊×低高波四组合(up_low/up_high/down_low/down_high)分桶，各桶 factor_eval.spearman 算前向 RankIC 与 n，单桶 n<REGIME_MIN_N=40 不给 IC（ic=None 但保留 n）。
3. **`factor_persistence`**：对因子值用 G25 引擎 ts_rank(REGIME_RANK_WIN=20) 得滚动分位，对再平衡间隔 k∈REGIME_TURNOVER_LAGS=(1,5,20) 算分位**秩自相关** spearman(r_t,r_{t+k}) 与**隐含换手** mean|r_t−r_{t+k}|（0~1，越小越稳/调仓越省），跨品种池化。
4. **`fit_decay_shapes` + 自写 `_ols`**：对同一因子的 |IC(H)| 曲线（复用 factor_health.daily_factor_curve over REGIME_DECAY_H=(1,2,3,5,10,20,40,60)）同时拟合**指数** ln|IC|=a−H/τ（半衰期 τ·ln2，OLS 斜率须<0）与**幂律** ln|IC|=a−β·lnH（OLS 斜率为负、β=−slope 须>0），各给 log 空间 R²，按 R² 高者选 prefer；需≥3有效点且|IC|>1e-4，不衰减/点不足如实返 None。analyze_factor 串三块、run 读 PanelStore 出 reports/factor_regime.txt/.json（allow_nan=False）。
5. **性能踩坑（务必沿用）**：纯 Py 滚动 ts_rank 是 O(n·窗)，最初每因子×每期限都重算 252 窗 vol 标签导致运行数分钟；重构为 compute_labels 每品种只算一次 + REGIME_VOL_LOOKBACK 252→120、REGIME_RANK_WIN=20 后正常（研究工具分钟级内可接受）。幂律 β 符号坑：lnIC 对 lnH 的 OLS 斜率为负、衰减指数 β=−slope（曾写反成 slope>0 导致幂律永不成立，已修勿回退）。
### 二、真实数据诚实结论（全64品种面板61353行，九因子=ret5/20/63/126/252+tsmom63/126/252/blend）
- **长周期动量是"低波专用、高波反转"的 regime 条件因子（本轮最有价值的结构）**：ret252/tsmom252 在 H=20 的**低波桶 RankIC≈+0.10（+0.100/+0.107）、熊低波 +0.112/+0.100**，而**高波桶转负（−0.037/−0.040）、熊高波 −0.022/−0.048**；短周期（ret5/tsmom63）各 regime 全在0附近。方向与"动量在高波动/危机期崩溃（momentum crash）"经典结论一致——比"全样本IC≈0"更细、可操作，但按三铁律**本轮只记录、不改分不挂影子**，须再做双样本/事件层互证。
- **持续性/换手**：九因子 lag1 秩自相关 0.72~0.85（信号平滑）、lag5 降到约0.47、lag20≈0；隐含换手 0.13(lag1)→0.27(lag5)→0.39(lag20)——月度调仓已换掉约四成分位、周度调仓能留住大部分信号，为后续 G26 组合调仓频率提供成本侧依据。
- **衰减形态**：除 ret5 勉强指数（R²仅0.057、半衰期41日但几乎不解释）外，其余因子"指数不衰减/幂律不成立"——IC 在0附近变号、两形态都不贴合，再次印证第37/38轮"单因子无稳定期限衰减"负结果，不硬套半衰期。
### 验证与规模
- pytest **438→440 全绿（约6.8s/30文件+conftest）0失败0错误0跳过**（账目：test_tools_selftest 注册 factor_regime +1、test_compileall 随新生产 py 自动+1）；factor_regime `--selftest` 6组全过（trend/vol标签PIT边界、构造"只在牛桶有效"合成因子验证分层IC、平滑序列换手可手算、指数/幂律/常数三曲线钉死择优与样本不足安全降级、analyze_factor端到端）、compileall 过；全树 grep 确认 factor_regime 不被任何根/tools 生产模块 import；真实全64面板 factor_regime exit0 出 txt/json；真实 main --once 一轮冲烟 exit0、无 ERROR/Traceback，证明主链与综合分零改动。
- 生产 py **56→57（根42不变、tools14→15含 factor_regime）、24226→24604 行**；tests 仍30文件（+conftest31）/440用例；storage 仍14张业务表、看板15图；运行依赖仍仅 requests/uiautomation/websocket-client。config 仅在 HEALTH_JSON 后增量插入 REGIME_* 常量簇（纯新增无删除）。
### 纪律与下一站（第40轮）
- **regime 条件动量的处置（重要、勿抢跑）**：长周期动量"低波有效/高波反转"本轮只是池化描述，**不进综合分、不挂实时影子**；若后续要采纳，须先做时间双样本（如前后各半/滚动）与事件层（factor_health 事件层按 regime 分桶）互证、并设计 PIT 的 regime 状态开关（默认关、缺省等价旧版），符合"双样本显著才采纳"。
- **优先候选**：①**G25 续** 旧因子表达式改写（须先过综合分逐字节回归、默认不换）、orthogonalize/IC·ICIR加权接 G16 浅ML特征合成；②**G22 续** tradable_mask涨跌停掩码/季节交割日历/面板常驻增量；③**G23 续** 跟踪carry近窗含roll t 是否站上1.5；④**G30** 交易journal/一键复盘；⑤**G26** 组合构建器（风险平价/最小方差/目标波动，默认equal旧sizing，本轮换手结论可供调仓频率参考）。
- 旧排队：G24微结构/季节因子、G27实验台账、G28续(逐笔持仓归因+看板归因页签)、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控、G2插件化、G8只读web、G14一档快照、G13无key休眠。
- 复用提示：研究工具照 factor_regime/factor_health/expr_research 模式——纯标准库、只读独立缓存/DB(mode=ro)、纯函数先于IO、--selftest含构造有效桶/衰减形态的正反用例、真实数据出诚实结论、不接main不改分；滚动时序量优先复用 factor_expr.compute_ts（窗口写正整数字面量、缺值None别造0），但注意纯Py ts_rank是O(n·窗)、长窗×多因子要像本轮一样缓存/取短窗；读面板已复权bar走 panel_builder、绝不再 ratio_adjust；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`/内联正则引号易被转义、Edit 超长中文间歇"Native execution failed"（原样重试一次，仍失败落 cache 带assert补丁脚本跑完即删），物理行数以 Python glob、测试数以 --junitxml 解析为准。
## 零.旧、第38轮快速接续摘要（已被第39轮接续，存档）
- **一句话**：G21/G21续/G29 三地基就位后，本轮落地第37轮排定的首选 **G25 表达式因子引擎 + 因子治理**：新增根模块 `factor_expr.py`，把因子定义成**表达式字符串+元数据**，用一个**白名单、无 eval/exec/属性/导入**的小型 DSL 统一求值，实时 bar 与离线面板喂同一棵语法树、逐值一致（training-serving parity）；新增 `tools/expr_research.py` 纯离线读 G21 面板做 parity 证明与前向 IC 体检。守 G25 回退铁律：**引擎只承载新 research 因子，旧技术/基本面因子保持过程式原实现、综合分逐字节不变，factor_expr 不被 main/analyzer import**（全树 grep 仅 expr_research 引用）。一次代码 commit + 一次文档 commit，tag v0.38.0。
### 一、factor_expr.py（根模块，约480行，9组自测，纯标准库、不 import tools）
1. **安全解析器（递归下降）**：词法只认数字/标识符/四则/括号/逗号；全局禁 `__`(dunder)、`;`(语句)，非数字一部分的属性点 `.` 由分词器按非法字符拒绝，import/eval/exec/lambda/globals/getattr 即便写成名字也只是输入字段名、作函数调用则过不了白名单；函数调用**必须命中算子白名单**，未知算子/错误元数/窗口非常量正整数/括号不匹配一律 ExprError（test_factor_expr 21个危险/畸形参数化反向用例钉死）。窗口参数强制**正整数字面量**，从源头杜绝数据相关未来函数。
2. **时序算子（尾窗严格无未来、支持任意嵌套）**：delay/delta/ts_sum/ts_mean/ts_std(样本std)/ts_min/ts_max/ts_rank(窗内平均秩→0..1)/ts_minmax/decay_linear(线性衰减加权 Qlib同式)/corr(滚动Pearson≥3对)；单一递归 `_eval_ts`（早期写过 eval_ts/_eval_ts_with_windows 双路径会在嵌套时序算子处把窗口当序列，已重写为单一递归，勿回退）；暖机/除零/零方差/非正log 一律 None 不崩；扰动末根断言此前所有位置不变=无未来。
3. **截面算子（同一时点跨品种）**：cross_rank(平均秩→0..1)/scale(Σ|w|=1)/zscore；时序算子进截面、截面算子进时序均显式报错，两套上下文共用同一 AST（eval_cs）。
4. **因子治理（自含、不依赖 tools）**：pearson/spearman(并列平均秩)、高斯消元 solve、**orthogonalize(target,bases) 正交残差**（只用同时有限下标估β、返回残差与β、共线/样本不足安全降级，合成用例恢复 β=2,3）、equal_weights/ic_weights(|IC|归一保留方向)/icir_weights(滚动IC均值/std)、combine 逐点加权（缺失按可得权重归一）。
5. **LIBRARY 5个 research 因子**：expr_ma_bias5=`delta(close,5)/delay(close,5)`（等价ret5作parity基准）、expr_ma_ratio 短长均线比、expr_trend_per_vol 单位波动趋势、expr_price_accel 二阶加速度（嵌套delta）、expr_illiq 非流动性代理（Amihud|收益|/量，面板无成交额用volume，方向−1）；同步登记 factors_catalog（20→25，layer=表达式研究、status=research、不进综合分），validate 仍零问题、selftest 过。
### 二、tools/expr_research.py（约290行，5组自测，纯离线/零网络/只读面板）
- `series_from_rows`（面板列直读=离线）vs `series_from_bars`（panel_rows_to_bars 回读成bar再取c/v=实时形状）喂同表达式做 parity；`indicator_crosscheck` 把表达式5日动量对齐面板里实时管线 compute_indicators 落库的 ret5（ret5=c/closes[-6]-1 与表达式同口径，futures_data.py:474）；`symbol_factor_ic/forward_return` **严格只向未来**取 H=1/5/20 收益算 spearman（合成加速序列 t² 验证 delta递增而fwd递减→秩相关−1）；最近交易日 cross_rank 跨品种演示；出 reports/expr_research.txt/.json（allow_nan=False）。
### 真实数据诚实结论（全64品种面板61353行）
- **parity 完美坐实**：面板列 vs bar回读同表达式 **303309 个有限点 maxAbsDiff=0.000e+00、零不一致**；表达式动量 vs 实时 ret5 **61033 比对点 maxAbsDiff=1.11e-16（纯浮点ε）**。
- **5个表达式因子前向 |IC| 全部<0.06、无稳定预测力**：最强 expr_ma_ratio 的 H=5 逐品种均值+0.056 但池化仅+0.029，其余在0附近、H=20 多转负；与 tsmom/xsmom 双样本证伪、G29日频九因子|IC|<0.10 一致——简单价量表达式在4年池化样本上同样无边际，**全部维持 research、不挂影子不进分**（负结果诚实）。截面演示最近日短长均线比最低 JD/CJ/SS、最高 J/JM/BU。
### 验证与规模
- pytest **397→438 全绿（约6.7s/30文件+conftest）0失败0错误0跳过**（账目：新 test_factor_expr.py 37例、test_tools_selftest 注册 factor_expr/expr_research +2、test_compileall 随2个新生产py自动+2）；factor_expr/expr_research/factors_catalog 的 --selftest 全过、compileall 过；全树 grep 确认仅 expr_research 引用 factor_expr、main/analyzer 零 import；真实 main --once 一轮冲烟证明主链与综合分零改动。
- 生产 py **54→56（根41→42含factor_expr、tools13→14含expr_research）、23178→24226 行**；tests 29→30文件；storage 仍14张业务表、看板15图；运行依赖仍仅 requests/uiautomation/websocket-client。
### 纪律与下一站（第39轮）
- **G25 续（可选）**：旧技术/基本面因子的表达式改写（**必须先做综合分逐字节回归**、默认不换）、把 orthogonalize/IC·ICIR加权接到 G16 浅ML 特征合成；表达式自动批量挖掘受总纲第16条红线门控（只在tools产候选、人工+G23/G29双样本、禁止端到端上线）。
- **优先候选**：①**G29 续** 换手衰减/牛熊·波动率regime状态机分层/幂律vs指数半衰期/常驻体检（机构动向反向仍须更长样本双样本才动analyzer）；②**G22 续** tradable_mask涨跌停掩码/季节交割日历/面板常驻增量；③**G23 续** 跟踪carry近窗含roll t 是否站上1.5；④**G30** 交易journal/一键复盘；⑤**G26** 组合构建器（风险平价/最小方差/目标波动，默认equal旧sizing）。
- 旧排队：G24微结构/季节因子、G27实验台账、G28续(逐笔持仓归因+看板归因页签)、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控、G2插件化、G8只读web、G14一档快照、G13无key休眠。
- 复用提示：新因子优先用 factor_expr 表达式定义（时序用 compute_ts、截面用 eval_cs、窗口写正整数字面量、缺值为None别造0）；研究工具照 expr_research/factor_health 模式——纯标准库、只读独立缓存/DB(mode=ro)、纯函数先于IO、--selftest含危险反向用例与无未来扰动、真实数据出诚实结论、不接main不改分；读面板已复权bar走 panel_builder.load_adjusted_bars(prefer_panel=True)、绝不再 ratio_adjust；解释器 `D:\Python\python.exe`，PowerShell 不支持 `&&`/内联正则引号易被转义，复杂中文落 cache 临时 .py 跑完即删，物理行数以 Python glob、测试数以 --junitxml 解析为准。
## 零.旧、第37轮快速接续摘要（已被第38轮接续，存档）
- **一句话**：本轮分两段。**G21续**补齐第36轮 G21 最后一条验收——让真正消费日K的三个研究工具 tsmom/xsmom/carry 能改读 G21 标准面板，并以"改前改后逐值一致"钉死，面板补齐到全64品种；**G29 因子体检**新增 `tools/factor_health.py`，给每个因子一张可复算的健康卡（现在还有没有力/稳不稳/衰减多快），回应第35轮发现的"机构动向次日反向"。两段都守三铁律：纯标准库零新依赖、研究/监控记录层、**不接 main、不改交易、不改综合分**；新能力默认开关、缺省等价旧版。两次提交（G21续 9dca232 / G29 8e33dae）。
### 第一段 G21续：研究工具统一读标准面板（commit 9dca232）
1. **踩坑与定法（关键、务必沿用）**：G21 面板存的是**已比例复权** OHLC。临时联网实证（RB/MA/CU/AU/SC/J 六品种）发现：若回读后再喂一次 `backtest.ratio_adjusted_bars`（backtest.py:87，阈值=max(绝对缺口, MAD×倍数)），因首次复权已把真换月跳空置0→全序列 MAD 变小→换月阈值降低，会把真实大波动**二次误判为换月**（SC 价位偏6.31%、J 偏12.7%，换月计数31→3/12→2；RB/MA/CU/AU 因幂等偏0）。**结论：面板回读路径禁止再复权，直接返回已复权 bar。**
2. **panel_builder.py 增回读/统一装载层**：`panel_rows_to_bars(rows)`（面板行→bar-dict：d/o/h/l/c/v、p=oi、s 以 c 代）；`load_adjusted_bars(code,days,prefer_panel=False,db_path=None)`（prefer_panel 且库内有→回读已复权 bar[-days:]、source="panel"、**不再 ratio_adjust**；否则旧网络路径 fetch[-days:]→ratio_adjusted_bars、source="network"）。selftest 7→8 组（回读==建面板已复权 c / 回读再复权 roll=0 价位不变 / 临时库面板路径==网络复权末值）。
3. **三工具加 `--panel`、网络路径逐字节不变**：把"复权之后的纯计算"抽成内部纯函数——tsmom `records_from_adjusted`、xsmom `points_from_adjusted`、carry `carry_points_from_adjusted`；公开 build_symbol_records/build_symbol_points/build_carry_points 仍走"网络现拉→ratio_adjust→内部"旧路径；`--panel` 时 worker 用 load_adjusted_bars(prefer_panel=True) 取已复权 bar 直调内部（carry 期限序列仍走 term_history，面板只替代主连价；面板约1023日，长2500样本请用缺省网络）。run() 各加 --panel（缺省 False=旧版）。factor_eval/attribution 只读 monitor.db 不拉日K，**无需**读面板。
4. **全64面板 + 等价回归**：面板从46补到**全64品种/61353行/0失败/区间2022-06-28~2026-09-02**（多数1014行，新品种按上市日较短）。真实联网等价（RB/MA/CU/SC/J/AU/M/TA，days=1023）：xsmom 6096、tsmom 5616 个重叠(品种,日)点，面板 vs 网络的 z/ret/fwd **maxAbsDiff=0.000e+00、零 None 不一致**（面板仅少 PANEL_WARMUP-1=9 根暖机 bar）；carry --panel 四品种 exit0。test_research_panel 14→18（回读 roundtrip、xsmom/tsmom 面板路径==网络路径、load_adjusted_bars 面板源且再复权 roll=0）。
### 第二段 G29：因子体检 factor_health（commit 8e33dae）
1. **新增 `tools/factor_health.py`（约430行，8组自测，纯标准库，只读不写生产库），两层**：
   - **事件层**（复用 attribution.load_events 只读 monitor.db、方向化暴露 x=part×dir 同 factor_eval，三周期30/120/1440）：整体 **RankIC**（复用 factor_eval.spearman）；**滚动IC** rolling_ic（窗60/步20事件）统计弱窗(|IC|<HEALTH_IC_EPS=0.03)/翻窗(与整体异号)/最长连续失效窗，连续≥HEALTH_FAIL_WINDOWS=3 判**失效预警**；**block bootstrap CI** block_bootstrap_ic（块长20保留时序自相关、500次、确定性种子20260903，给 p5/p50/p95/同号概率）；裁决 健康 / **健康(反向)**（稳定非零但IC为负=反转信号）/ 走弱·不稳定 / 失效预警 / 样本不足——"健康"要求 **CI 朝0保守边也越过0.03 且同号率≥0.95**（纯噪声小样本不会误判，合成噪声用例钉死）；regime_proxy 给多/空×轻仓/分批/强信号的 IC 矩阵（可得代理，牛熊/波动率 regime 待后续）。
   - **日频层**（读 G21 面板，已PIT/复权）：forward_map 严格只向未来取收益（无未来函数）；对 HEALTH_DAILY_FACTORS=ret5/20/63/126/252、tsmom63/126/252/blend 九因子，算未来 H=1/2/3/5/10/20/40/60 交易日的**池化 RankIC**（daily_factor_curve，跨品种池化）与 Q5-Q1 价差（复用 factor_eval.quantile_buckets）；fit_exp_halflife 拟合 **IC(H)=A·exp(−H/τ)，半衰期=τ·ln2**（对 ln|IC| 做 OLS，需≥3有效点且斜率<0；不衰减/增强如实返回 None，不编造）。输出 reports/factor_health.txt + .json（allow_nan=False）。
2. **体检卡回写注册表**：`factors_catalog.py` 增静态 `HEALTH_SNAPSHOT`（asof/方法/9 part 的 ic·CI·裁决·note + 日频层总结）与 `get_health(key)`；validate 钉死"体检卡只能回写已登记因子"，selftest 锁定机构动向反向结论（20条登记不变）。config 增 HEALTH_* 常量簇（本轮开工时已在工作树，G29 commit 一并纳入；G21续 commit 已刻意排除它）。
### 真实数据诚实结论（monitor.db 2026-08以来事件 + 全64面板4.1年）
- **机构动向次日(1440) RankIC=−0.230、CI[−0.341,−0.118] 不跨零、同号率1.00=健康(反向)**：统计稳定显著但方向为负，与第35轮 t=−2.77/IC−0.203 **两轮互证**——当前方向化口径下它是**反转信号而非确认**（多头侧IC−0.297，轻仓/分批/强信号各档一致为负）；后续应研究反向用或降权，**本轮不改线上权重**。
- **原油联动次日 +0.276、CI[+0.045,+0.407] 健康**（同号0.98、强信号档+0.584）；基本面 2h +0.123 健康、次日跨零；30分钟短周期多数 part 失效预警/走弱（样本噪声、滚动翻转多）；盘中动量/量仓资金 n=9 样本不足。
- **日频层九因子对未来1~60日池化 |IC| 均<0.10、多数随 H 变号不构成单调衰减**——单独日频回看收益在4年池化样本上无稳定横截面预测力，与 tsmom/xsmom 双样本证伪一致（负结果诚实呈现）。
### 验证与规模
- pytest **391→397 全绿（约4.7s/29文件+conftest）0失败0错误**（账目：G21续 test_research_panel +4；G29 factor_health 自测注册 +1、test_compileall 随新生产 py 自动 +1）；panel_builder/三研究工具/factor_health/factors_catalog 的 --selftest 全过；真实 main --once 一轮冲烟证明主链与综合分零改动。
- 生产 py **53→54（根41不变、tools12→13含新 factor_health）、22603→23178 行**；tests 仍29文件（+conftest30）/397用例/4338行；storage 仍14张业务表、看板15图；运行依赖仍仅 requests/uiautomation/websocket-client。
### 纪律与下一站（第38轮）
- **G29 续（可选）**：换手率随调仓间隔曲线、牛熊/波动率 regime 状态机分层、幂律 vs 指数半衰期对比、定期常驻体检；**机构动向反向**的处置（反向用/降权/剔除）须再过一轮更长样本双样本才动 analyzer，现阶段只记录。
- **优先候选**：①**G25 表达式因子引擎**（G21 面板+factors_catalog 地基已就位、G29 体检可把关，可坐实 training-serving parity，G16 前置）；②**G22 续** tradable_mask 涨跌停掩码/季节交割日历/面板常驻增量；③**G23 续** 跟踪 carry 近窗含roll t 是否站上1.5；④**G30 交易 journal/一键复盘**（G28/G29 已供因子侧证据）。
- 旧排队：G24微结构/季节因子、G26组合构建(宜G2后)、G27实验台账、G28续(纸面/组合逐笔归因+看板归因页签)、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控、G2插件化、G8只读web、G14一档快照、G13无key休眠。
- 复用提示：研究工具读面板一律走 `panel_builder.load_adjusted_bars(prefer_panel=True)` 拿**已复权**bar、**绝不再 ratio_adjust**（二次复权误判换月坑见上）；新增研究/体检工具照 factor_health/attribution 模式——纯标准库、只读DB(mode=ro)/独立缓存、纯函数先于IO、--selftest 合成断言（含噪声不误判/构造失效触发的反向用例）、真实数据出诚实结论、不接 main 不改分；解释器 `D:\Python\python.exe`，Edit 超长中文间歇故障走带 assert 的 cache 补丁脚本、跑完即删，物理行数/测试数以 Python glob 与 --junitxml 解析为准。
## 零.旧、第36轮快速接续摘要（已被第37轮接续，存档）
- **一句话**：本轮落地第33轮规划的剩余 P1 地基项 **G21 标准研究面板 + 特征注册表 + PIT/训练-服务一致性校验**，新增根模块 `factors_catalog.py`（全项目因子唯一登记处）与两个纯标准库研究工具 `tools/panel_builder.py`（品种×交易日×30字段标准面板，落**独立**缓存库 cache/research_panel.db）、`tools/pit_audit.py`（未来函数扫描+扰动法+实时/离线 parity）；不碰生产 monitor.db 表结构、不接 main、不改综合分（守三铁律）。两条硬口径被合成断言与真实数据双重钉死：**PIT 无未来函数**（每行只用≤当日bar前缀、基本面严格取前一交易日 as-of）与**训练-服务一致性**（面板行==对同一前缀走实时同款 compute_indicators，真实3品种×25时点零不一致）。
### 本轮新增（2026-09-03，tag v0.36.0）
1. **`factors_catalog.py`（根模块，195行，纯数据零行为，1组自测）**：20条因子登记（综合分9part=live、基本面4子项、4个技术指标原料、tsmom影子/xsmom已archived/carry待tracking），每条 key/中文名/层级/方向(±1/0)/贡献界/status/引入轮次/实时计算处/公式/档案指针；`validate()` 自检（key唯一/字段齐/方向·状态合法/9part必live）、动态原油键“原油联动(w=..)”归一、catalog_text()；测试钉死 **PART_KEYS==config.ATTR_FACTOR_ORDER**。后续 G25 表达式引擎/G29 体检按 key 回写。
2. **`tools/panel_builder.py`（430行，7组自测，纯标准库）**：主连比例复权行情(复用 backtest.ratio_adjusted_bars/resolve_codes)+compute_indicators 17个扁平标量+基本面3字段=标准长表（30列：sym/date/sector+OHLC/v/oi(p持仓)+ret1d+17特征+fund_score/carry/basis）。纯函数 `asof_before/asof_pick`（strict=严格早于）、`build_symbol_rows`（逐 t 只用 bars[:t+1]、基本面严格 trade_date<当日、取不到NULL不编造）；`PanelStore` 独立 SQLite（research_panel 主键(sym,date)、整品种 DELETE+INSERT 事务**幂等重建逐值一致**、research_runs manifest）；联网 build_items 失败软降级不编造；config 增 PANEL_* 簇（库路径/1023日/暖机10/17特征键/抽样24/manifest）。
3. **`tools/pit_audit.py`（291行，5组自测，通用审计器）**：①timestamp_leaks/asof_join_check 时间戳越界扫描（feature_ts>event_ts、缺失跳过）；②assert_no_future 扰动法（篡改 t 之后全部OHLC、t 行逐值不变，含“故意偷看最后一根”反向用例确保抓得到泄漏）；③parity_one/parity_for_symbol 训练-服务一致性（面板行 vs 同前缀实时 compute_indicators，均匀抽样逐字段）；④audit_panel_db 缓存结构审计（每品种日期唯一严格递增、ret1d 可由相邻收盘价复算、无NaN/Inf）。
4. **测试**：新增 tests/test_research_panel.py **14** 零网络用例；test_tools_selftest 纳 factors_catalog/panel_builder/pit_audit（+3）；test_compileall 随3个新生产py自动+3。
### 真实数据验证（联网，非合成）
- 构建覆盖五板块11品种（RB/I/J黑色、MA/TA/SC能化、CU/AL有色、M/Y农产品、AU贵金属）各1014行=**11154行、0失败、2022-07-01~2026-09-02（约4.1年与研究主样本对齐）、累计换月跳空处理86次**；真实基本面 PIT as-of 生效（如 RB 末日取到前一日 fund_score）。
- `pit_audit --db` 对真实缓存**结构审计全过**（日期唯一递增/ret1d自洽/无NaN）；`pit_audit --codes RB0,MA0,CU0` 联网 parity 每品种25抽样时点**全部OK零不一致**；RB 真实**幂等重建**：前后均1014行、库总数稳定11154不翻倍、回读与内存逐值一致。
- **刻意不做**：本轮不把 tsmom/xsmom/factor/carry/attribution 改读面板（避免大改既有研究工具），仅证明面板与实时同函数逐值一致；既有工具切换读面板、面板全64品种常驻增量留后续。
### 验证与规模
- pytest **371→391 全绿（约4.7s/29文件）0失败0错误**（账目：+14新文件、+3 selftest注册、+3编译参数化=+20）；三模块 --selftest 全过、compileall 全树过；真实 `main.py --once --no-launch` exit0、1899行日志 ERROR/Traceback=0、分钟K全周期64合约正常、正常退出，主链与综合分零改动。
- 生产 py **50→53（根41含新factors_catalog、tools12含新panel_builder/pit_audit）、21672→22603 行**；tests 28→29文件/3997→4267行；运行依赖仍仅 requests/uiautomation/websocket-client；面板库 cache/research_panel.db 与 reports/research_panel_manifest.txt 均 gitignore/研究侧。
### 纪律与下一站（第37轮）
- **G21 续（地基收尾）**：把 tsmom/xsmom/factor_eval/carry_eval/attribution 由各自现拉改为“优先读 research_panel、缺则构建缓存”，并做改前改后结果逐值一致回归（G21 验收最后一条）；面板 --all 全64品种建一次、常驻增量。
- **优先候选**：①**G29 因子体检**（第35轮发现机构动向次日反向 IC−0.203，优先级仍高：IC衰减半衰期/block bootstrap/滚动60-120日失效预警/regime分层，体检卡回写 factors_catalog）；②**G22 续** tradable_mask涨跌停掩码/季节交割日历；③**G23 续**跟踪 carry 近窗含roll t 是否站上1.5；④**G25 表达式因子引擎**（地基G21已就位，可承载新研究因子并坐实parity，G16前置）。
- 旧排队：G24微结构/季节因子、G26组合构建(宜G2后)、G27实验台账、G30交易journal、G28续(纸面/组合逐笔归因+看板归因页签)、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控、G2插件化、G8只读web、G14一档快照、G13无key休眠。
- 复用提示：新增研究侧工具照 panel_builder/pit_audit 模式——纯标准库、独立缓存库不碰生产表、纯函数先于IO、PIT只用≤t前缀+严格asof、--selftest 合成断言（含“故意泄漏”反向用例）、真实数据出诚实结论、不接 main 不改分；面板字段键统一从 config.PANEL_FEATURE_KEYS / factors_catalog 取，勿散落硬编码。
## 零.旧、第35轮快速接续摘要（已被第36轮接续，存档）
- **一句话**：本轮落地第33轮规划的 P1 复盘项 **G28 因子收益归因 + BHB 板块归因**，新增纯标准库研究工具 `tools/attribution.py`（只读 monitor.db、不接 main、不改综合分，守三铁律），第一次回答“已实现盈亏该记到哪个因子、哪个板块头上”；加法归因 mean(y)=α+Σβ·mean(x) 与 BHB 三效应 AR+SR+IR=超额 两条恒等式严格闭合（真实数据闭合误差≈1e-19、200组随机 fuzz 钉死）。
### 本轮新增（2026-09-03，tag v0.35.0）
1. **`tools/attribution.py`（654行，8组自测，纯标准库、只读DB）**：样本=signals.parts_json ⨝ signal_outcomes 有效事件(hit/miss/flat)；方向化暴露=part×信号方向(meta-labeling 同 factor_eval)、y=方向收益(storage 口径 dir×(评估价/入场价−1))。①**多因子加法归因**：带截距 OLS（复用 tsmom_eval._solve 高斯消元、零方差/共线列自动剔除、奇异安全降级），β=每单位方向化暴露的边际收益、贡献=β×平均暴露，mean(y)=α+Σβ·mean(x) 严格闭合；另给 β 的 t 值((X'X)^-1对角)、IC、因子支持时胜率、支持 vs 反对均收、IS70/OOS30 β方向一致性；②**BHB(Brinson-Hood-Beebower 1986)板块三效应**：基准=全市场64品种板块只数占比(config.VARIETIES 在有事件板块集内归一)×板块“无方向”均涨(=y/dir)，组合=实际信号板块事件占比×方向化均收，配置 AR=Σ(wp−wb)Rb、选择 SR=Σwb(Rp−Rb)、交互 IR=Σ(wp−wb)(Rp−Rb)，AR+SR+IR=组合−基准恒等闭合；另出评估月月度三效应序列；③**累计归因曲线**：事件按 ts 逐笔累计总收益/残差/各因子 β·x（末端=Σy 闭合）落 CSV。复用 factor_eval.pearson/spearman/_canon（动态“原油联动(w=..)”归一）不重造轮子。config 增 ATTR_* 常量簇（三周期/主周期次日/最小样本40/OOS30%/支持阈值0.05/9因子规范序/三输出路径）。输出 reports/attribution.txt + .json(sidecar 无NaN) + attribution_curve.csv。
2. **测试**：新增 tests/test_attribution.py **16** 个零网络用例（方向化暴露与做空翻转/动态键归一/坏行、OLS 精确恢复与奇异、加法闭合与零方差剔除/空样本降级、BHB 两板块手算+200组随机 fuzz 恒等、板块统计 rb 无方向、基准权重归一、累计曲线末端闭合、IS-OOS 有序不重叠、端到端报告与 sidecar JSON 安全）；test_tools_selftest 纳 attribution(+1)；test_compileall 随新生产 py 自动+1。
### 真实结论（monitor.db：30分 n=370 / 2时 n=368 / 次日 n=235，均为2026-08起实盘信号事件）
- **因子层面**：短周期靠**技术共振**——30m 贡献+0.123%(β t=2.35/IC0.095，是平均+0.022%主来源)、2h +0.230%(t=3.69/IC0.154，平均+0.061%主来源)，新闻消息面 2h 也显著(t=3.44)；**次日(主周期)97% 靠原油联动**+0.341%(β t=6.04/IC0.393/支持时胜率70.1%/支持均收+1.148% vs 反对−0.015%，n=98 油系事件)，技术共振次之+0.132%；**机构动向次日为负贡献 −0.126%(t=−2.77/IC=−0.203，支持均收仅+0.039%、反对反+0.765%)，与第28轮 factor_eval“机构预测力弱”互证，列为 G29 重点复核对象**；日线动量暴露最大(均值≈4)但 β≈0，只做门槛不贡献边际；OLS R²=0.027/0.078/0.193（次日最高，线性可解释部分仍有限）；**IS/OOS β 方向一致仅 4~5/8**，多数因子样本外翻转（样本仅约1个月），现阶段只定位结构、不调权重。
- **板块层面(BHB)**：三周期一致 **“选择效应 ≫ 配置效应(AR≈0)”**——超额主要来自板块内方向/择时，而非把信号押到板块结构更优处；次日选择+0.113%/配置+0.082%/交互−0.041%=超额+0.153%，农产品板块内选择+0.199%、能化−0.163%。分组交叉验证：30m/2h 空头均收(+0.15%/+0.16%)略强多头、次日多头(+0.399%)强于空头(+0.168%)；轻仓档短周期为负、强信号/分批档为正（与信号校准器方向一致）。
- **诚实边界**：事件条件样本（只在|分|≥2发信号时观测、非连续组合、时段/品种有偏）；OLS 为线性加法、不刻画交互/非线性、β 是相关非因果；BHB 基准是事件条件下板块无方向均涨、非逐日连续基准；**本轮不改任何线上权重，只产出可复算的复盘归因**。
### 验证与规模
- pytest **353→371 全绿（约4.6s/28文件）0失败0错误**；attribution `--selftest` 8组全过、compileall 全树过；真实 monitor.db 三周期归因 exit0、两恒等式闭合误差≈1e-19、数字可复现；真实 `main.py --once --no-launch` exit0、ERROR/Traceback=0、分钟K 320任务空/失败0、数据源全成，主链与综合分零改动（本轮仅新增离线工具+config 常量簇）。
- 生产 py **49→50（根40+tools10）、21002→21672 行**；tests 27→28 文件/3997 行；运行依赖仍仅 requests/uiautomation/websocket-client。
### 纪律与下一站（第36轮）
- **G28 续（可选）**：接入纸面/组合回测的**逐笔持仓归因**（本轮仅信号事件层）、看板加“归因”页签（改前端须过 node --check+真实浏览器目视，教训8）。
- **优先候选**：①P1 **G21 标准研究面板+特征注册表+PIT/训练-服务一致性校验**（地基，所有研究工具共用统一面板）；②**G22 续** tradable_mask 涨跌停掩码/季节交割日历/常驻增量采集；③**G23 续**跟踪 carry（近窗含roll t 稳定≥1.5 才谈影子）；④本轮发现机构动向次日反向 → **G29 因子体检（IC衰减半衰期/滚动失效预警）优先级上升**。
- 旧排队：G24微结构/季节因子、G25表达式引擎(→G16)、G26组合构建(宜G2后)、G27实验台账、G30交易journal、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控、G2插件化、G8只读web、G14一档快照、G13无key休眠。
- 复用提示：新增研究/复盘工具照 attribution/carry_eval 模式——纯标准库、只读DB(mode=ro)、纯函数先于IO、--selftest 合成断言钉死闭合恒等式、真实数据出诚实结论、不接 main 不改分；跨工具复用 factor_eval(pearson/spearman/_canon)、tsmom_eval(_solve 高斯消元OLS)、xsmom_eval(分档/绩效/robust_verdict)。
## 零.旧、第34轮快速接续摘要（已被第35轮接续，存档）
- **一句话**：本轮把第33轮规划的 P1 首选链 **G22（期限结构/OI 数据底座）→ G23（carry 截面双样本）真正落地为代码并跑出真实结论**。纯标准库零新增依赖、研究侧先行、不接入 main 常驻、不改综合分（守三铁律）。
### 本轮新增（2026-09-03，commit 4a4a095 / tag v0.34.0）
1. **G22 根模块 `term_history.py`（469行，8组自测）**：用新浪**逐合约日K**重建历史期限结构。关键实测：日K接口四所统一"大写品种+4位年月"（RB2501/TA2501/M2501/SI2501 均可取完整生命周期；郑商所三位年 TA501 取不到），字段 **p=持仓量、s=结算价**。纯函数：`month_iter/kline_symbol/parse_yymm/delivery_first/month_gap_days`、`select_curve`（换月缓冲 ROLL_BUFFER_DAYS=3 剔除临交割/无量、选近/次/远月）、`annual_carry`（近高远低Back为正，与 contracts.term_structure 同号）、`curve_loadings`（Nelson-Siegel level/slope/curv 固定载荷=期限PCA三主成分零依赖近似）、`moving_mean/basis_change`、`build_term_series`（{code:bars}→逐日 近/次/远结算价+carry_far/carry_nn/载荷/oi_sum/oi_near）、**`near_roll_nav`（近月连续净值：同近月内吃结算价变动以保留展期roll、换月不跨合约计盈亏——学术carry正确收益口径，关键）**；`TermHistoryStore` SQLite 缓存到 cache/term_history.db（gitignore、增量跳过、cempty空合约标记、多线程 build_symbol_range）。已真实缓存全64品种约5千合约/数十万K线（一次性、之后秒读）。
2. **G23 研究工具 `tools/carry_eval.py`（588行，5组自测）**：**复用 xsmom_eval 的 cross_section_periods/perf_stats/bands_profile/sector_*/truncate_dates/conditional_scan/robust_verdict/gate_verdict 全套**（不重造轮子）；6因子族 carry/carry_nn/carry_mom/slope/curv/doi；每点同时算两套未来H收益 `fwd`（主连复权、不含roll）与 `fwdn`（near_roll_nav、含roll），`retarget` 切换；报告七章（分档/H网格/因子族两口径t/板块与LO-SO/双样本/近月口径双样本补/机制判读）。
### 真实结论（64品种、近4.1年1023 + 长9.9年2500双样本、真实成本两腿6e-4，reports/carry_eval.txt）
- **口径A 主连复权（不含roll，测价格方向）：carry 截面多空为负**——近窗净t=-1.52、净均-0.82%/20日、最深Back档Q5未来20日反而最差(-1.06%)、RankIC=-0.3；carry_mom/slope/curv 同为负或不显著。
- **口径B 近月连续含roll（学术carry全收益）：长9.9年净t=+2.55、净均+1.21%/20日、年化+12.4%、夏普0.68、胜率62%、长窗不衰减反增强（95期≥短窗52×1.5，非同源小样本）；近4.1年净t=+1.39（差0.11未达门槛1.5）**；carry_nn近月t=+1.45、doi+1.35。
- **机制结论（最重要）：carry 赚的是展期 roll 的钱、不是价格方向择时的钱**——主连把换月roll抹掉后信号消失甚至反转。按三铁律近窗t未达1.5 → **不进综合分、不挂实时影子**，归档"长样本成立/近窗边际减弱"**待跟踪候选**（≠TSMOM/XSMOM双样本全负的彻底证伪）；有色板块内主连口径t=2.33/1.85但被"长窗期数≥短窗×1.5"同源小样本判据拦下（氧化铝/碳酸锂2024才上市），留待样本积累。
### 验证与规模
- 新增 tests/test_term_history.py（8用例）、test_tools_selftest 加 carry_eval；**pytest 342→353 全绿（约4.6s）**；两模块 --selftest 全过；真实 `main.py --once --no-launch` 一轮完成、ERROR/Traceback=0、分钟K空/失败0。
- 生产 py **47→49（根40含新term_history、tools9含新carry_eval）、19945→21002 行**；storage仍14表、看板15图；运行依赖仍仅 requests/uiautomation/websocket-client；.gitignore 补 reports/*.log（运行日志不入库，cache/term_history.db 研究缓存也不入库）。
### 纪律与下一站（第35轮）
- **G22 续**：涨跌停板幅 tradable_mask（本轮只做交割/流动性筛选）、季节交割日历表、常驻增量采集、与 G21 标准面板对接。
- **G23 续（跟踪边缘候选）**：更长样本/不同平滑与H、板块条件化（有色单独跟踪）、换手与容量检验、carry_nn vs carry_far 择优；**在近窗含roll t 稳定站上1.5且长窗保持前，不进综合分、不挂影子**。
- 或按第33轮 P1 并行 **G21 标准研究面板/特征注册/PIT校验（地基）**、**G28 因子+BHB板块归因（让综合分看得懂）**。
- 复用提示：carry_eval 直接 import xsmom_eval 纯函数；新增研究因子照 carry_eval 模式（主连/近月两口径+双样本 robust_verdict）；解释器 `D:\Python\python.exe`；Edit 超长中文故障走带 assert 的 cache 补丁脚本、跑完即删；物理行数用 Python glob 数。
## 零.旧、第33轮快速接续摘要（已被第34轮接续，存档）
- **一句话**：第33轮是一次**对标补路的纯文档轮**（不改任何生产代码）——重做全网+GitHub 的 2026 AI量化调研，聚焦用户点名的**数据收集、数据整理分析、因子生成、模拟数据整合分析、结束后复盘**五个环节，先以磁盘代码为准盘清现状，再对标 Qlib/RD-Agent/FactorEngine/vectorbt/pyfolio 与商品期货学术成果，识别出 G1–G20 没覆盖的 **10 个改进项 G21–G30**，按优先级写进《总体对标与统一改进总纲（融合版）.md》（新增 3.5 全章+一页纸总览+不做清单+回退表+排期+7.5 来源，404→517 行）。生产代码零改动，pytest 仍 **342 全绿**，tag **v0.33.0**。
### 本轮已完成（2026-09-03，commit 36241d6 / tag v0.33.0，纯文档）
1. **现状盘点（以磁盘为准）**：采集=futures_data/intraday_bars/data_router/data_health + 基本面/舆情/期权多源；因子=analyzer 综合分+factors/fundamental_factors(库存/龙虎榜/carry/basis 单品种打分)+cross_section 稳健z+iv_surface+TSMOM/XSMOM 研究(已证伪归档)；模拟=backtest/intraday_backtest/portfolio/paper_broker/backtest_validation(DSR/PBO/WF)；复盘=metrics(tear sheet)/signal_calibrator(hit-miss)/charts15图。
2. **五环节差距 → G21–G30（每项在总纲 3.5 都有"现状→对标来源→文件级改法→验收→默认/回退"）**：
   - **数据收集**：G22 多合约近/次/远月期限结构历史落库（carry 必需，主连比例复权算不了展期）+ 持仓量OI/成交额连续序列 + 统一 tradable_mask（涨跌停/停牌/临交割/夜盘）+ 季节交割日历。
   - **数据整理/治理**：G21 统一"品种×交易日×字段"标准研究面板（缓存落 SQLite、数据 manifest 血缘）+ 特征注册表（因子目录/版本/IC档案）+ 通用 PIT 未来函数扫描器 + **实时analyzer与离线面板同名因子逐值一致**的 training-serving parity 测试（现在各研究工具重复造面板、audit_pit 仅一次性）。
   - **因子生成**：**G23（P1，最高价值）** 把现有只做单品种打分的 carry/basis 升级为跨品种截面排序+近4年/10年双样本（直接复用 xsmom_eval 框架与 robust_verdict），补 basis momentum、期限结构 PCA（平移/斜率/曲率）；G24 补学术九族缺项（ΔOI、Amihud 非流动、特异波动、偏度、套保/投机压力、季节哑变量、跨品种比价）；G25 纯标准库**表达式因子引擎**（白名单算子、实时离线同一引擎、因子正交/IC加权，是 G16 浅ML 前置）。
   - **模拟整合**：G26 组合构建器（风险平价ERC/最小方差GMV/目标波动，纯Python求协方差与ERC、默认等权旧口径不变）；G27 统一实验台账 experiment_runs（回测/纸面/组合/因子/WF 配置hash+数据版本+结论可复现）+ walk-forward 滚动稳定性 + 成本敏感性曲面（**只学 vectorbt 的实验组织、不引 numba**）。
   - **复盘**：**G28（P1，高性价比）** 多因子收益归因（盈亏拆到综合分各 part）+ BHB 板块归因（配置/选择/交互，分量闭合=总收益）；G29 因子体检（IC 指数/幂律衰减半衰期+block bootstrap 区间、换手/Q5-Q1 spread 衰减、滚动IC失效预警落 factor_health、regime 分层）；G30 交易 journal（按信号档位/主导因子/板块/方向/持时分桶+典型单回放+纸面-回测一致性+一键日/周复盘报告）。
3. **关键对标判断（决定第34轮起路线）**：①**carry/展期收益是商品期货最持久稳健的 alpha、强于已被第30-32轮证伪的动量**（湘财证券因子筛选、清华动量+近远月价差+持仓三因子样本外印证、高盛期限结构、Janardanan-Qiao-Rouwenhorst 155年商品收益=现货+carry、Bayes-CID 2026 GBDT 九族且族间低相关）；项目 carry_factor/basis_factor **早就存在但只是单品种当前时点 tanh 进综合分，从没做截面排序/分层/双样本**——所以 G23 不是从零造因子，而是"把已有因子用 XSMOM 同款严谨框架验证一遍"，性价比最高。②数据/复盘是两块共性地基：G21 面板+特征注册、G28 归因能同时让现有综合分"看得懂、可复算"。
4. **新增明确不做项（总纲第四篇 15/16）**：不引 numba/vectorbt/numpy/pandas 重依赖做向量化回测（64品种日频事件驱动性能够用、守零依赖）；不让 LLM/遗传规划(RD-Agent/AlphaGen/gplearn)自动挖的因子直接上线（最多 tools 出候选，仍须人工+G23/G29 双样本与体检、受 G13/G16 门控）。
### 验证（纯文档轮）
- **零生产代码改动**：仍 47 个 py、19945 行；全量 **pytest 342 全绿（约4.6s）** 确认无回归；本轮无需 main --once（未碰运行代码）。
- 总纲改动逐项回读核对：一页纸总览 G21–G30 共10行、3.5 全章10小节、不做清单15/16、6.2 回退表一行、6.3 排期两行、7.5 来源五组（商品因子/AI因子工程/数据治理/组合构建/复盘归因），文件 404→517 行。
### 纪律与下一站（第34轮）
- **P1 落地顺序**：G22（期限结构/OI 采集，carry 的数据前置）→ **G23（carry 截面双样本，复用 tools/xsmom_eval.py 的 truncate_dates/conditional_scan/robust_verdict，新建 tools/carry_eval.py，负结果照样诚实归档）**；G21（面板/特征注册地基）与 G28（因子/板块归因，让现有综合分看得懂）可与之并行。
- **P2 排队**：G24 微结构/季节因子 → G25 表达式引擎（→G16）；G26 组合构建宜在 G2 插件化后；G27/G29/G30 复盘三件可并行。
- **三铁律与本轮新增纪律不变**：纯标准库零新依赖；研究/采集/报告侧先行、默认等价旧版、组合默认 equal 旧 sizing；任何新因子须过近4+10年双样本（含"长窗期数≥短窗×1.5"防伪、窄板块先查品种凑齐门槛日期）才谈影子、绝不自动进综合分；解释器 `D:\Python\python.exe`；Edit 超长中文故障走带 assert 的补丁脚本；物理行数用 Python 数。
## 零.旧、第32轮快速接续摘要（已被第33轮接续，存档）
- **一句话**：第32轮针对第31轮暴露的三条线索（①板块内有色/农产品为正、能化反向；②长样本全靠多头腿、做空腿失效；③显著性必须随样本量增大而稳定）做**截面动量条件化**——给 `tools/xsmom_eval.py` 加板块池过滤、多头腿（多头超额/纯多头）与"一次拉满2500根、内存截最近1023交易日"的**同源双样本**，对 **8 个条件化候选 × 近4.1年/长9.9年两窗口**做稳健硬检验。**诚实负结果：8 候选无一通过双样本稳健判据**，截面动量经条件化仍不达标，与已证伪的时序动量一并**归档为边缘效应**；本轮仍纯离线，未改任何生产主链/analyzer/cross_section/综合分，tag **v0.32.0**，pytest **336→342 全绿**。
### 本轮已完成（G7 截面动量条件化，2026-09-02，commit 9ee8661 / tag v0.32.0）
1. **`tools/xsmom_eval.py` 766→977 行（纯标准库、缺省等价旧版）**：
   - `cross_section_periods` 增 `sector_scope`（成员收集时按板块集合过滤，池内等权基准 mkt 随之限定在池内），逐期增 `long_excess=long-mkt`（只做多最强档但扣池内等权基准的纯选股 alpha）。
   - **三种腿模式 `LEG_KEY`**：`ls`=多空价差(两腿成本)、`lex`=多头超额 long-mkt(单腿成本)、`long`=纯多头含beta(单腿)；perf_stats 既有 `legs=2 if key=="ls" else 1` 直接兼容。
   - `truncate_dates(dates,n)` 取全局日历最近 n 个交易日（by_date 共享不重建）→ run 一次拉 XSMOM_ROBUST_DAYS=2500 根，主样本=截最近 EVAL_DAYS=1023，两窗口**同源、因子值完全一致只差区间**。
   - `conditional_scan` 对 config 的 8 候选×双窗批量算净绩效；`robust_verdict` 双样本稳健=两窗净均>0 且净t≥1.5、长窗t不短窗衰减超 XSMOM_DECAY_TOL=0.5、**且长窗非重叠期数≥短窗×XSMOM_LONG_N_RATIO=1.5（本轮新增的关键防伪判据）**。
   - `build_report` 增"**五、条件化增强×双样本对照**"章（每候选列两窗净均/t/夏普/期数与否决原因；robust_panel=None 时整章不输出=旧行为），主组合支持 main_scope/main_leg；run 新增 --scope/--leg/--main-days/--long-n-ratio/--no-conditional，--days 默认改2500。
   - `--selftest` 10→**14 组**（板块池过滤+long_excess手算+单/双腿成本、窗口截断不改原序列、conditional_scan结构、robust_verdict 达标/衰减/为负/同源小样本/窗口不足五分支、第五章端到端与缺省无第五章）。
2. **config.py 增第32轮常量簇**（XSMOM_EVAL_JSON 之后）：XSMOM_ROBUST_DAYS=2500、XSMOM_COND_MIN_NAMES=8、XSMOM_DECAY_TOL=0.5、**XSMOM_LONG_N_RATIO=1.5**、XSMOM_COND_CANDIDATES=8 候选三元组（名称,板块池None或元组,腿）：全市场多空基线/有色内多空/农产品内多空/有色+农产品池/剔除能化/全市场多头超额/有色+农产品多头超额/全市场纯多头。
3. **测试**：tests/test_xsmom.py 15→**21 例**（板块池+long_excess手算、单/双腿成本、窗口截断、robust_verdict五分支含同源小样本、conditional_scan结构与样本不足、第五章与主组合腿标注）；test_tools_selftest 的 xsmom selftest 同步14组。
### 验证（全部实跑、负结果诚实）
- pytest **336→342 全绿约4.4s/26个 test 文件**；xsmom_eval `--selftest` 14组全过；compileall 全过。
- **真实64品种、59404个(品种×交易日)点、一次拉2500根双样本评估 exit0、数字可复现**；并专门写诊断脚本核实"有色两窗 n 都=25"的根因=品种上市日期（见下）。
- 真实 `main --once` **exit0**：分钟K 320任务空/失败0、行情覆盖64/64、缺数0/陈旧价0/跳变0、零 Traceback/ERROR；主链与综合分零改动（本轮仅改离线工具+config）。
- 规模：生产仍 **47个py**、19718→**19945行**（xsmom_eval 766→977 +211、config +16、test +113）；运行依赖仍仅 requests/uiautomation/websocket-client，**零新增依赖**。
### 真实结论（关键：8候选×双样本无一达标，条件化也救不回）
- **一个重要口径修正（先看）**：本轮拉满2500根再截最近1023交易日，z252 用足252日历史、主样本是**真正4.1年（n=51个非重叠期）**；而第31轮直接拉1023根会扣掉252日暖机、实际只有最近约**3年（n=37）**。更正确口径下**全市场基线近4年净t只有+0.45（非第31轮暖机截断口径的1.43）、净夏普0.22、IS t=-0.06**——证明"差一点达标"本身是样本窗口敏感的脆弱结果。
- **8候选两窗净t（近4.1年/长9.9年）与裁决**：全市场多空基线 0.45/1.37✗；**有色内多空 2.01/2.01（两窗n都=25完全相同）✗同源小样本**；农产品内 0.79/1.95✗近窗弱；有色+农产品池 1.21/1.50✗；**剔除能化 1.48/2.54✗（最接近、近窗仍差0.02）**；全市场多头超额 0.64/1.17✗；有色+农产品多头超额 1.18/0.53✗；全市场纯多头 0.19/1.73✗。
- **两个"最像有戏"的为何被否**：①有色 t=2.01 最强，但有色要凑齐分5档所需≥10只（CU/AL/ZN/PB/NI/SN 六老 + BC国际铜 + SI工业硅 + AO氧化铝/LC碳酸锂，**AO/LC 2024-08才上市**），有效截面历史仅约2年/25期，近4年与10年窗口对它是**同一段小样本、期数零增量**，被 LONG_N_RATIO 拦下（t高只是小样本+板块内每档仅2只的高集中度，不可外推）；②"剔除能化"双窗为正、长窗更强且样本充足，但近4年 t=1.48 仍差0.02，且"剔除能化"是**事后按已知结果选板块(data snooping)**，不为此放宽门槛。
- **多头超额/纯多头两候选双窗都弱** → 第31轮"长样本靠多头腿"只是 beta 现象，扣掉市场基准后的选股 alpha 很薄，该线索也不成立。
- **最终裁决❌**：板块池/只做多/板块合并等条件化都无法把截面动量增强到双样本稳健达标。**G7 动量因子探索到此闭环：国内商品期货 2017–2026 样本内，经典时序动量(TSMOM,第30轮证伪)与截面动量(XSMOM,第31-32轮)均不构成"确定不更差"的可交易/可并入信号，统一归档边缘效应，不并入、不挂实时影子、不改综合分。** 报告 reports/xsmom_eval.txt/.json（一份含主样本四章+条件化第五章）留档可复算。
### 纪律与本轮边界（务必延续）
- 本轮**没有、也不允许**把任何条件化 XSMOM 并入 analyzer/cross_section/综合分或挂实时；"剔除能化近t1.48/长t2.54"仅作**未来观察项**，须等积累更多 OOS、且不再靠事后选板块才再议，禁止现在据此放宽 tmin。
- **双样本稳健的新防伪教训**：只比"长窗t是否衰减"不够——板块/窄池品种上市晚会让长窗拿不到增量历史、两窗实为同一段小样本（有色两窗n都=25即此坑），必须叠加"长窗非重叠期数≥短窗×1.5"（XSMOM_LONG_N_RATIO）才算真双样本；评估窄板块先查成员凑齐门槛的起始日期。
- **暖机口径教训**：算长窗因子(z252)的研究样本必须"多拉历史再截主窗口"，直接按主窗口天数拉数据会因暖机丢掉早期点、使样本变短且区间漂移（第31轮1023根实际仅n=37即此坑）；跨轮比较数字前先对齐 n（非重叠期数）。
### 下一站（第33轮）
1. **G12 跨品种价差/产业链联动（首选，G7动量已闭环）**：黑色/能化内部联动强，做产业链对（螺纹-热卷、矿-螺、豆粕-豆油/油-粕、PTA-PX-聚酯）价差异常与比值回归的**离线研究**（先 tools 评估、同守双样本+成本+不进综合分）。
2. **carry/基差/持仓/库存等基本面因子**：动量在国内商品弱，可换 carry（近远月/基差）或持仓结构方向做截面排序，仍走"离线双样本硬检验→确定不更差才影子"。
3. 其余按融合总纲排队：G1纸面运行期影子对照（PAPER仍默认False、连续≥4周再对 signal_outcomes、成本后为负诚实回退）/G19备份灾备/G5组合风控/G2插件化/G8只读web/G14一档快照/G13 LLM(无key休眠)/G16浅ML(样本不成熟不上)。
- **三铁律不变**：①不动实时主链与综合分口径；②新能力默认影子/开关、缺省等价旧版、确定不更差才采纳（显著性须随样本量稳定、且双样本须有真实增量历史）；③生产零新增依赖、每改动合成断言+真实冒烟+负结果诚实。解释器一律 `D:\Python\python.exe`；物理行数用 Python 数；Edit 间歇故障用带 assert 的 Python 补丁脚本；PowerShell 复杂中文处理走 `python -X utf8`；改前端必须 node --check+真实浏览器目视。
## 零.旧、第31轮快速接续摘要（已被第32轮接续，存档）
- **一句话**：第31轮按第30轮留档的**首选方向**，做 **G7 截面动量多空 XSMOM（已被证伪的"时序动量"的截面替代）**——新建离线研究工具 `tools/xsmom_eval.py`（766行、纯标准库零新依赖），在每个调仓日跨64品种按长窗波动调整动量 z 排序、多最强一档/空最弱一档构建**市场中性多空组合**（Jegadeesh-Titman / AQR 截面动量口径），真实64品种跑了**近4.1年(1023根)与9.9年(2500根)两个样本**：长窗(126/252)弱正、短窗(20/63)偏负，主组合 z252/H20 近4年净t=+1.43、10年净t=+1.37，**样本翻3倍显著性不升反降、夏普降、回撤放大、做空腿长样本失效、能化板块反向**，裁决**❌不达标不并入、维持纯离线研究**；本轮**未改任何生产主链/analyzer/cross_section/综合分**（截面排序需全市场同时点数据，证据不足前连实时影子都不加），tag **v0.31.0**，pytest **319→336 全绿**。
### 本轮已完成（G7 截面替代，2026-09-02，commit 7a9f8ae / tag v0.31.0）
1. **新建 `tools/xsmom_eval.py`（766行，离线可联网、纯标准库零新增依赖，第47个生产py）**：与 tsmom_eval 的本质区别——TSMOM 是品种内"自己过去预测自己未来"（已证伪、品种内偏反转），XSMOM 是**同一时点跨品种排序、多强空弱赚相对排序的钱、对冲全市场beta**（正是第30轮 pooled 弱正里截面成分的纯净提取）。复用 backtest.resolve_codes/fetch_daily_kline/ratio_adjusted_bars（主连换月跳空置0比例复权）、futures_data.tsmom_series（排序因子 z{L}=ret÷窗口日收益std×√252，实时离线同一函数）、factor_eval.pearson/spearman。
   - **面板** `build_symbol_points/build_panel`：全市场(品种×交易日)面板，暖机后入面板、未来H日收益未成熟保留 None 由组合层过滤（**无未来函数**，测试钉死）；`forward_returns` 手算远期收益、尾部 None。
   - **非重叠组合** `cross_section_periods`：每 H 个交易日调一次仓、持有 H 日（期与期不重叠、可复利算夏普/回撤）；当日按因子升序分 N=5 档（与 factor_eval 同切法、不重不漏），多 Q5/空 Q1；支持**等权 equal 与反波动率加权 ivol**（AQR口径，低波动权重高、波动率缺失安全退回等权）；当日可得品种 < min_names(16) 或 < 2N 则跳过不硬凑。
   - **绩效** `perf_stats`：期数/毛净均收/**t=期均÷(期std/√n)**/胜率/复利累计/年化/夏普(√252/H)/最大回撤；净=毛-两腿往返成本（默认 fee万0.5+滑点万1、两腿万6/期，CLI可覆盖）。
   - **诊断**：`bands_profile` 分档单调性+档位列序Spearman+多空价差；两腿拆"多头最强档/做空最弱档/全市场等权基准"+多空与市场基准相关（验市场中性）；IS70/OOS30分段；日频重叠调仓稳健对照（明确标注重叠使t偏乐观）；原始ret因子对照；`sector_breakdown/sector_internal` 板块净敞口+留一板块LOSO+板块内截面；L∈{20,63,126,252}×H∈{5,20,60}参数网格；`gate_verdict` 截面专属判据（净t≥1.5、净均>0、OOS不转负、分档单调≥75%且价差>0、至少一条腿方向对、单一板块净敞口≤60%）。产出 reports/xsmom_eval.txt+.json；`--selftest` 10组零网络断言。
2. **config.py 650→672**：增 XSMOM_* 常量簇（LOOKBACKS=(20,63,126,252)/HORIZONS=(5,20,60)/MAIN_L=252/MAIN_H=20/N_Q=5/MIN_NAMES=16/MIN_SECTOR_NAMES=6/OOS_RATIO=0.3/TMIN=1.5/MONO_GATE=0.75/MAX_SECTOR_DRIVE=0.60/EVAL_DAYS=1023/EVAL_FILE·JSON，G10深合并对tuple安全）。
3. **测试**：新建 tests/test_xsmom.py **15例**（远期无泄漏/分档不重不漏/等权·ivol手算/非重叠步长/多空价差手算/成本与t手算/回撤/分档单调/IS-OOS有序/板块敞口与板块内/裁决六道否决门/暖机无未来/合成趋势面板端到端为正/空样本安全降级）；test_tools_selftest 纳入 xsmom_eval +1。
### 验证（全部实跑、边缘结果诚实）
- pytest **319→336 全绿约4.3s/26个 test 文件**；xsmom_eval `--selftest` 10组全过；compile_dir 全项目编译 True。
- **真实64品种两档样本均 exit0、数字可复现**（重生成一致）：近4.1年=45801个(品种×日)/774调仓日；9.9年=111174点/2263调仓日。
- 真实 `main --once --no-launch` **exit0**（1920行日志零真实错误、64品种×5周期分钟K自采320任务失败0、第1轮报告正常保存退出）；本轮无任何生产主链文件改动，综合分/信号/建议逐字节不受影响。
- 规模：生产 46→**47个py**、18930→**19718行**（新增 xsmom_eval 766、config +22）；tests 26文件；运行依赖仍仅 requests/uiautomation/websocket-client，**零新增依赖**。
### 真实结论（关键：弱正但不达标，明显强于时序、仍不够并入）
- **近4.1年主组合 z252/H20**：毛多空价差+1.01%/期、净+0.95%、**净t=+1.43（差一点未过1.5）**、净夏普0.83、胜率65%、累计+37.9%、回撤12%；分档 Q1..Q5=-0.41/-0.20/-0.35/-0.03/+0.60%（单调性75%、档位列序Spearman=0.90）；**两腿方向都对**（多头+0.60%、做空最弱档+0.41%）；多空与市场基准相关-0.20（确实市场中性、没吃beta）；但 IS 段很弱(t=0.54)、正收益主要靠 OOS(t=1.44)。
- **决定性稳健性=9.9年长样本（112个非重叠期）**：净t不升反降**+1.37**、净夏普降到0.46、回撤放大到22.7%、**做空腿转亏**（最弱档绝对涨幅+0.16%，多空全靠多头腿）、IS/OOS仅0.91/1.12；反波动率加权t=1.31、原始ret因子t=1.25 均不更优。→ 近4年 t=1.43 含 regime 偶然性，样本拉长显著性不增强=不满足"确定不更差"。
- **参数网格**：仅长窗(126/252)为正且t都在1左右、短窗(20/63)偏负（与第30轮"长窗才弱正、短窗反转"一致），没有一格 t≥1.5。
- **板块（两样本稳健复现）**：全市场组合净敞口很分散（最大仅±16%、**无单一板块偏置**）；板块内截面多空**有色+2.11%(10年+2.75%)、农产品+1.07%(+0.88%)为正，能源化工-0.66%(-0.29%)为负，2/3板块为正**。
- **裁决❌不并入、维持纯离线研究**：国内商品截面动量是"统计弱正、经济偏薄（扣费年化6~12%、长样本回撤20%+）、主要靠多头腿、能化反向、拉长样本显著性不增"的**边缘效应**，强于被证伪的时序动量（时序品种内是反转；截面至少长窗弱正、两腿与市场中性形态健康），但达不到稳健并入门槛；与"中国商品截面动量存在但弱于股指、时强时弱"的已知经验一致。两份样本报告 reports/xsmom_eval.txt（1023根，对齐第30轮口径）与 xsmom_eval_long.txt（2500根）全部留档可复算。
### 纪律与本轮边界（务必延续）
- **本轮没有、也不允许把 XSMOM 并入 analyzer/cross_section 或综合分**；截面排序需全市场同时点数据，本就不适合挂单品种分析行，证据不足前连实时影子都不加（与第30轮时序影子性质不同）。
- 评截面策略**必须同时报非重叠主口径 + 长样本稳健性**：单看近4年 t=1.43 会误判"差一点就成"，拉长到10年 t 反降、做空腿失效才暴露 regime 偶然——**显著性必须随样本量上升而稳定，否则不采纳**（新教训，已在 gate 与报告体现）。
- 时序(TSMOM)与截面(XSMOM)是两类策略，勿混：TSMOM 看品种内时间序列、XSMOM 看同时点横截面排序；评估复权口径统一=backtest.ratio_adjusted_bars，EVAL_DAYS=1023 对齐回测/第30轮。
### 下一站（第32轮）
1. **截面动量条件化（首选，基于本轮板块证据）**：只在**有色/农产品**板块内做截面多空（两样本都为正、且有色t较强），或构建"多头腿-only"组合（长样本全靠多头腿、做空腿失效）——先用 xsmom_eval 加 `--sector-scope`/`--long-only` 离线验证条件化后净t能否稳定≥1.5且长样本不衰减，达标再谈影子。
2. **G12 跨品种价差/产业链联动**：本轮板块证据显示黑色/能化内部联动强，可做产业链（螺纹-热卷/豆粕-豆油/油-粕）价差异常与截面排序结合的离线研究。
3. 其余按融合总纲排队：G1纸面运行期对照（PAPER仍默认False、≥4周影子）/G19备份/G5组合风控/G2插件化/G8只读web/G14一档快照/G13 LLM(无key休眠)/G16浅ML(样本不成熟不上)。
- **三铁律不变**：①不动实时主链与综合分口径；②新能力默认影子/开关、缺省等价旧版、确定不更差才采纳（且显著性要随样本量稳定）；③生产零新增依赖、每改动合成断言+真实冒烟+负结果诚实。解释器一律 `D:\Python\python.exe`；物理行数用 Python 数（教训6）；Edit 间歇故障用带 assert 的 Python 补丁脚本（教训5）；改前端必须 node --check+真实浏览器目视（教训8）。
## 零.旧、第30轮快速接续摘要（已被第31轮接续，存档）
- **一句话**：第30轮完成融合总纲 **G7 多窗口时序动量 TSMOM(63/126/252) 的"影子评估阶段"**——生产侧补长窗特征与影子字段（绝不进综合分），新建离线时序 IC 评估工具并用真实64品种检验，**结论为诚实负结果：三窗口在国内商品"品种内时序"维度不成立（偏反转），全部不并入、维持纯影子**，综合分与实时主链零改动。生产 45→46个py、18178→18930行，pytest 304→319全绿（25个测试文件约4s），tag v0.30.0。
- **用户指令**："先理解上下文摘要 md、继续完成项目、可用本机查找任何需要的东西/GitHub 借鉴、最后更新上下文摘要"。

### 本轮已完成（G7 影子阶段，2026-09-02，tag v0.30.0）
- **① futures_data.py 621→733（纯增量、旧值逐字节不变）**：新增纯函数簇 `_lookback_return/_window_std/tsmom_at/tsmom_features/tsmom_series/_tsmom_empty`（零网络纯标准库，实时与离线共用同一口径，杜绝两套算法）。ret{L}=过去L日累计简单收益（与 ret5/ret20 同口径）；tsmom{L}=z=ret÷(过去L日日收益样本std×√TSMOM_ANN252) 的波动调整趋势（跨品种量纲可比）；blend=mean(tanh(clip(z,±TSMOM_Z_CLIP=3)))，按"可得窗口"聚合（历史不足/零波动的窗口给 None 不编造；故 t=63 起 blend 即可用、t=252 三窗齐全）。compute_indicators 在 **max_bars=140 截断之前**用完整有效序列算长窗（保证 ret252 有≥253根；新浪日K固定1023根足够），新增 ret63/ret126/ret252/tsmom63/tsmom126/tsmom252/tsmom_blend/tsmom_n_valid 八键；**截断后的旧 RSI/MACD/KDJ/MA/HV 输入序列逐字节不变（test_tsmom 逐字段回归断言）**；KlineCache 取数失败 fallback 同步补键。
- **② analyzer.py 332→344（影子只记录不进分）**：TSMOM_SHADOW(默认True)下把 `tsmom_shadow`（八键）挂到 analyze_variety 返回行，随 signals.raw_json 自动落库长期跟踪（**不改表结构、不进 parts_json**）；绝不加入 parts、score 不参与——test_tsmom 断言开关两态 score/parts 逐值相等；TSMOM_SHADOW=False 即与本轮前逐字节等价。
- **③ config.py 633→650**：TSMOM_LOOKBACKS=(63,126,252)/TSMOM_ANN=252/TSMOM_Z_CLIP=3.0/TSMOM_SHADOW=True，研究侧 TSMOM_FORECAST_HORIZONS=(5,20,60)/EVAL_DAYS=1023/EVAL_WORKERS=6/EVAL_MIN_SAMPLE=120/EVAL_OOS_RATIO=0.3/EVAL_RIC_GATE=0.02/EVAL_FILE/EVAL_JSON；tuple 经 G10 config_loader 深合并安全（list→tuple）。
- **④ 新建 tools/tsmom_eval.py（622行，离线研究侧可联网、零新增依赖）**：复用 backtest.resolve_codes/fetch_daily_kline/ratio_adjusted_bars（主连换月跳空置0比例复权）与 factor_eval 的 pearson/spearman/monthly_ic/icir/quantile_buckets；对全市场构造(品种×交易日) pooled 面板（暖机 t≥252、t+H<n 才入样，无未来函数），输出六张表：Ⅰ各窗口原始ret/波动z/等权blend × 未来5/20/60日 的 IC/RankIC/ICIR 与期限衰减；Ⅱ主地平线分档单调/多空价差/方向命中；Ⅲ因子两两 Spearman 互相关；Ⅳ剔除现有 ret5/ret20 后的 OLS 残差边际增量（自带高斯消元多元OLS、奇异降级减均值）；Ⅴ IS前70%估IC权重/OOS后30%验证的等权 vs IC加权合成；Ⅵ 分品种一致性 + "确定不更差"自动裁决。产出 reports/tsmom_eval.txt + tsmom_eval.json（reports 按 .gitignore 不入库）。CLI：--codes/--limit/--days/--lookbacks/--horizons/--main-h/--gate/--oos-ratio/--selftest；`--selftest` 10 组零网络断言。
- **⑤ 测试**：新建 tests/test_tsmom.py 13 例（手算 ret/z、零波动与历史不足降级、序列对齐与暖机、compute 旧 tech 逐字节回归、影子开关 score 相等铁律、OLS 残差正交、远期收益无泄漏、IS/OOS 切分、伪相关裁决等）；test_tools_selftest 纳入 tsmom_eval +1；compileall +1。

### 验证（全部实跑、负结果诚实）
- pytest **304→319 全绿约4s/25个 test 文件**；tsmom_eval `--selftest` 10组全过；4个改动文件 py_compile 通过。
- **真实64品种、41961个(品种×交易日)、近约4年1023根日K** tsmom_eval exit0：pooled 层面 z252 对未来20日 RankIC 仅 +0.023、60日 +0.036（随期限增强、ICIR+0.58）、OOS(后30%)+0.046 不塌、剔除 ret5/ret20 残差 +0.011（一度触发 pooled 判据）；**但加入分品种一致性后仅 9/64 品种各自 RankIC 为正、品种内中位 -0.163**；63/126 更弱（仅12%~19%品种为正、中位 -0.13~-0.14，pooled 亦为负），等权 blend 被63窗拖累、残差 -0.015。**结论：pooled 的弱正主要来自跨品种价格水平混合的截面伪相关，真正的"品种内时序动量"在国内商品近4年不成立（品种内反而偏反转），故 63/126/252 三窗及等权/IC加权合成全部裁决 ❌ 不并入、维持纯影子**——这正是"影子先行、确定不更差才采纳、负结果诚实"避免把伪相关因子加进综合分的价值。留档事实：z252 与现有短窗相关性低（0.14/0.32，信息不重复）、三长窗彼此相关 0.58~0.73；现有 ret20 在20日地平线上表现最好（RankIC+0.048、分档100%单调）。
- 真实 `main --once` exit0（1830行日志零真实错误、64品种×5周期分钟K自采失败0）；DB 核验最新8条信号全部带 tsmom_shadow（n_valid=3、数值合理），parts_json 零 TSMOM 泄漏；主链/综合分/看板零改动（本轮未动前端，无需 node --check/浏览器目视）。

### 关键决定 / 口径（勿踩坑）
- **本轮严格只做"评估+影子"，没有、也不允许把 TSMOM 并入 analyzer 日线动量**；负结果已固化在 reports/tsmom_eval.txt 与 sidecar verdict。
- pooled IC 混合了时序+截面成分，会被跨品种价格水平差误导；评 TSMOM 必须同时看**品种内(per-symbol)一致性**（过半为正、中位非负）——_gate_ok 已把它列为必要条件，selftest 有防回退断言。
- compute_indicators 长窗必须在 max_bars 截断"前"算（否则只有140根算不出252）；但直接扩大 max_bars 会改变 RSI/MACD 递推初值→扰动综合分，禁止，故采用"截断前单算长窗、截断后走旧流程"。
- 实时影子值随 signals.raw_json 落库（raw_json=整行 json 自动带新字段，无需改表）；**绝不能塞进 parts 或 parts_json**（那会改综合分并污染 factor_eval 九因子口径）。
- 评估与未来任何并入必须同一复权口径=backtest.ratio_adjusted_bars（主连换月跳空置0）；TSMOM_EVAL_DAYS=1023（回测默认 BACKTEST_LOOKBACK_DAYS=250 不足以算252窗，研究工具取全量）。
- 教训复用：Edit 间歇 "Native execution failed" → 改用带 assert 计数的 Python 补丁脚本；PowerShell 复杂引号用临时脚本文件；唯一验证解释器 `D:\Python\python.exe`；物理行数用 Python 数。

### 下一站（第31轮）
- G7 本轮已证伪"品种内多窗口时序动量(5/20/60日预测)"这一具体设定，**不要简单重试等权三窗并入**。可选方向（按融合总纲与本轮证据）：
  1. **截面动量多空替代时序动量（首选）**：pooled 弱正主要来自截面成分，可研究"每一时点跨64品种按 z252 排序做多Top/空Bottom"的截面多空收益（cross_section.py 已有稳健 z/MAD 与多空Top5框架，可扩展为长窗动量排序因子），截面与时序是两类策略，可能成立；
  2. 更长历史/换样本与**分板块条件化**：当前仅约4年1023根、单一regime，可接更长主连或滚动窗口看动量是否只在特定板块（黑色/有色 vs 农产品/能化）成立；
  3. 结合持仓/库存/carry 给动量做条件过滤（基本面确认的动量）后再评残差增量；
  4. 或按融合总纲推进其它 G/P 项（P2-3 产业链价差并入 cross_section、WP-F4 浅ML 等样本成熟；G20 实盘门槛为远期）。
- 影子数据已开始随 signals.raw_json 积累，≥4周后可用 tools/tsmom_eval 同口径对"信号点事件样本"再确认一次（信号点是事件样本、非连续时序，两类口径勿混）。运行回归：项目根 `D:\Python\python.exe -m pytest`；重跑评估 `D:\Python\python.exe tools	smom_eval.py`。

## 零.旧、第29轮快速接续摘要（已被第30轮接续，存档）
> 一句话：第29轮完成融合总纲 **G3 完整绩效指标包/tear sheet**——新增纯标准库 `metrics.py` 统一全项目绩效口径，portfolio/paper/backtest 报告增补风险调整指标（旧口径逐值不变），看板新增水下曲线/滚动夏普/月度热力三图；默认 PAPER_ENABLED=False 主链与综合分零改动，tag **v0.29.0**，pytest **293→304 全绿**。

### 本轮已完成（G3，2026-09-02，commit 2920e2e / tag v0.29.0）
1. **新增 `metrics.py`（592行，纯标准库/零网络/零第三方依赖，第45个生产py）**，对标 pyfolio/empyrical，两类输入严格分开：
   - **周期收益序列**（如日度收益率小数）：`cumulative_return/mean_return/annualized_return(算术,与portfolio旧口径一致)/cagr(几何)/volatility/sharpe_ratio/sortino_ratio(全样本下行偏差empyrical口径)/drawdown_series(水下序列)/max_drawdown/calmar_ratio(CAGR÷maxDD)/omega_ratio(盈亏概率加权比)/ulcer_index(回撤深度×持续)/value_at_risk·conditional_var(历史法线性插值分位,默认alpha=0.05)/rolling_sharpe(暖机期None等长列表)/monthly_returns(自然月复利矩阵 years·cells·matrix)`；
   - **逐笔盈亏序列**：`trade_stats`（n/胜率/平均盈亏/期望/gross盈亏/**profit_factor=总盈÷|总亏|/payoff_ratio=平均盈÷|平均亏|/最大连胜·连亏（按交易顺序，0盈亏不中断）/best·worst**）；
   - **持仓过程 MFE/MAE**：`excursion(direction,entry,path)` 多空对称算最大有利/不利偏移（正小数）、`mae_mfe_summary` 汇总（含盈亏分组、MFE/MAE比）；
   - 工具：`clean_returns(滤NaN/脏值)/quantile_linear(R type7)/returns_from_equity/_date_key/daily_last_equity(一天多轮取最后一点收敛成日度)`；一键 `tear_sheet()` 返回固定键集合，**任一子指标样本不足即该键 None、绝不抛异常**；`--selftest` **47项手算可复核断言**（rs=[+1,-2,+3,-1,+2]% 推导 Omega=2、Ulcer=0.01、VaR5%=-0.018、CVaR=-0.02、Calmar、滚动首值等）。
2. **portfolio.py（账户内核，纯增量、旧值逐字节不变）**：Position `__slots__` 增 mfe/mae；`record()` 盯市时对每仓累计相对开仓价的有利/不利偏移；`close()` 成交流水带 mfe/mae；`performance()` 用日度收益（与归属日严格对齐，修掉 rets 比 dates 短1的 off-by-one）调 metrics，在**旧键（total_ret/ann_ret/sharpe/sortino/max_dd/win_rate…）全部保留原值**前提下新增 calmar/omega/ulcer/var95/cvar95/monthly/profit_factor/max_win_streak/max_loss_streak/mae_mfe。
3. **report.py（仅 PAPER_ENABLED 路径，休眠零影响）**：paper_account.txt【组合绩效】在原2行后加"风险调整(G3)"行（Calmar/Omega/Ulcer/VaR95/CVaR95/PF）、"交易连续性"行（最大连胜连亏+持仓过程平均MFE/MAE）、按自然月输出月度收益行；新增 `_num`（None/NaN 统一显"-"）、`_paper_monthly_lines`、`_paper_excursion_tail`。
4. **backtest.py 日线回测**：总体表现段旧指标之后追加一行"G3扩展绩效"（PF/盈亏比/最大连胜连亏/Omega/Ulcer/Calmar，逐笔非重叠交易为观测、年化按 252/hold 与既有夏普同口径），新增 `_fmt_g3_extended`，样本不足显"样本不足"，**旧指标逐值不变**。
5. **charts.py 看板 12→15 图（第⑥块 G3 绩效）**：新增 `_tear_from_series/tear_payload`——**优先纸面影子 paper_equity 全量快照、否则读 portfolio_equity.csv 组合回测**（故 PAPER 休眠时也能立刻用既有回测曲线出图）；水下曲线按原始逐点序列（首点0、反转轴+面积），滚动夏普/月度先 `daily_last_equity` 收敛日度；前端 `renderTear` 三图=水下回撤（chips 展示年化夏普/Sortino/Calmar/Omega/Ulcer/VaR95/CVaR95+数据来源）、滚动60交易日夏普（零线、暖机显空）、月度收益热力图（年×12月、红涨绿跌、整行宽度、悬停精确值）；成功/onerror 两路径都渲染、全 null 显空态、allow_nan=False。CHART_IDS/DOM/测试容器清单同步15。
6. **config.py** 增 `METRICS_BARS_PER_YEAR=243 / METRICS_ROLLING_WINDOW=60 / METRICS_VAR_ALPHA=0.05 / TEAR_MAX_POINTS=1200`（受 G10 config.json 深合并覆盖机制统一管理）。

### 验证（全部实跑、负结果诚实）
- **pytest 293→304 全绿（约3.5s，24个测试文件）**：新增 `tests/test_metrics.py` 6 用例（selftest全绿且≥40断言防误删/tear固定键与独立降级/分位手算/日度收敛/多空MFE-MAE对称/portfolio performance 新旧键集成）；`tests/test_charts.py` +4（三系列对齐与 off-by-one 回归/单日样本不足/paper优先再回退CSV/都没有显None/build_payload含tear块）并同步 CHART_IDS 15 与静态页容器断言。注：磁盘真实基线是 **293**（摘要旧写295有2个出入，以磁盘为准，教训3）；本轮净增 11 = test_metrics 6 + test_charts 4 + test_compileall 因新增 metrics.py 多1个参数化编译用例，故终值 **304**（曾在临时补丁脚本未删时短暂显示306，是 compileall 把根目录临时 .py 也参数化计入，删除后稳定304）。
- `metrics.py --selftest` **47项断言全过**；全部生产模块经 test_compileall 参数化编译通过。
- 前端内嵌JS `node --check` 通过；**真实浏览器目视**（用既有 reports/portfolio_equity.csv 实测，source=portfolio：水下曲线/滚动夏普/月度热力三图正常、chips 数值与后端一致、红绿语义正确、控制台零错误；过程中发现并修两处：①日度收益比日期短1导致月度矩阵空/滚动错位 off-by-one；②热力图半宽单元格标签拥挤→改整行宽度+加粗两位小数带符号）。
- **真实64品种 `main.py --once` 冲烟 exit0**：主链与综合分零改动；休眠态 latest_report 零纸面块、不生成 paper_account.txt、chart_data.js `"paper": null` 而 `"tear"` 正常取组合回测出图。
- 规模：生产 44→**45 个 py**、17460→**18178 行**（metrics 592、portfolio 1018、report 1216、backtest 902、charts 1082、config 632）；tests 24 文件/3082 行；运行依赖仍仅 requests/uiautomation/websocket-client，**零新增依赖**。

### 下一站（第30轮）
- **首选 G7 多窗口时序动量 TSMOM(63/126/252)**：现仅有 ret5/ret20 短窗，补 1/3/6 月趋势强度；**先在 tools/factor_eval 影子评估各窗口 IC/RankIC 与边际增量，满足铁律②（确定不更差）才允许并入综合分**，默认只影子不改分。
- G1 纸面引擎进入运行期：PAPER_ENABLED 仍默认 False，连续影子≥4周后与 signal_outcomes 对照，成本后为负必须诚实呈现并回退，永不自动接实盘（G20 门槛）。
- 其余 P2/P3 按融合总纲排队：G19备份/G12价差/G5组合风控/G2插件化/G8只读web/G14一档快照/G13 LLM(无key休眠)/G16浅ML(样本不成熟不上)。
- **三铁律不变**：①不动实时主链与综合分口径；②新能力默认影子/开关、缺省等价旧版、确定不更差才采纳；③生产零新增依赖、每改动合成断言+真实冒烟+负结果诚实。改前端必须 node --check + 真实浏览器目视（教训8）；Edit 间歇故障用带 assert 计数的 Python 补丁脚本（教训5）；物理行数用 Python 数（教训6）；解释器一律 `D:\Python\python.exe`。

## 零.旧、第28轮快速接续摘要（已被第29轮接续，存档）
- **第28轮 G1 纸面交易引擎（二）账户+看板（2026-09-02；生产仍44个py、16832→17326行，tests 274→295全绿约3s/23文件，零新增依赖；commit e53e4fc、tag v0.28.0）**：用户指令="先理解上下文摘要md、继续完成项目、可用本机查找/GitHub借鉴、最后更新摘要"。把第27轮 PaperBroker 撮合内核接入 main 主循环并产出账户文本+看板三图，融合总纲 G1 代码侧全部完成（仅剩运行期连续影子≥4周对照）。
  - **①主循环接入（main.py：import 83行；State 130-142；run_cycle 5.5段 720-733）**：State.__init__ 末尾仅当 PAPER_ENABLED=true 才 PaperBroker(db=self.db)（try 包裹，失败即 None+LOG.warning），并 self.last_paper=None；run_cycle 在第5步存储后、报告渲染前插独立"5.5段"：on_cycle(cycle_time,fut_rows,quotes)→report.write_paper_account(state)→LOG 一行委托/成交/在途/持仓/权益/风险度，整段 try+traceback 不影响主链。休眠=state.paper=None 零实例化零开销，绝不回改综合分/信号/建议/任何现有输出。
  - **②平今/平昨 owner 实时判定（paper_broker.py 701→829行）**：_default_owner_of_ts 懒加载 intraday_backtest.owner_of_dt（按**交易所结算交易日**：夜盘21点后归下一交易日；注意 utils.trade_owner_date 是日切口径、不可混用）；构造器加 owner_fn 可注入（测试零网络零日历），_owner_of 吞掉 owner_fn 异常；任何异常/判不了**保守按平昨 close**（等价第27轮、不虚增平今免费）。开仓落 Position.entry_owner，平仓 _close_leg(pos,ts) 同 owner=today/跨=close，_fill_leg 开仓传 owner/平仓实时算 leg，_liquidate 传 leg_getter，restore 重建 entry_owner；新增 order_status_counts()/positions_view()/pending_view() 三视图；account_summary 增补 float_pnl/n_pending/status/fill_mode；selftest 新增第11节共 25→29 项断言。
  - **③report.py（paper_block/paper_account_text/write_paper_account）**：_wan/_yuan/_pct 格式化（**坑：% 格式不支持千分位逗号，_yuan 必须用 format(x,",.Nf")**）；paper_block=4行紧凑块（休眠返[]）；paper_account_text=账户概览/组合绩效(日度聚合,样本不足诚实标注)/委托状态统计(在途≠拒单)/当前持仓表(含开仓结算交易日列)/在途挂单表(带排队原因)/最近20笔成交/诚实说明，全宽100字符用 utils.pad 对齐；write_paper_account 以 utf-8-sig 落 config.PAPER_ACCOUNT_TXT（文件占用只跳过、休眠不落盘）；render 在横截面块后插入；_DASHBOARD_TABS 在 portfolio_trades.csv 后加 ("paper_account.txt","纸面账户(影子)")（14→15），**_dashboard_html 休眠态(PAPER_ENABLED=False)自动隐藏该页签**（文件不生成、不点开缺失页）。
  - **④charts.py 744→924行（看板 9→12 图）**：paper_payload(state,max_points=1200) 读 db.paper_equity_series(2000)（最近窗口升序）拼9条等长数组+summary、downsample 抽稀、空/脏行/缺方法/异常全返 None 显空态；build_payload 第⑤块独立 try；_PANEL_DOM 加 full 宽 c-paper（chips id=peq-chips）+c-paper-dd+c-paper-risk 三卡；renderPaper=动态权益(金)/静态权益(紫)双线+期初虚线（**权益轴用本地 eq() 两位小数，一位小数 wan() 会把小波动抹成"100.0万"平行线**）、回撤反转轴、风险度+持仓数双轴柱线叠加+100%强平 markLine、空态提示文案；CHART_IDS 加3 id，loadAndRender 成功与 onerror 两条路径都调 renderPaper。
  - **⑤storage.py 998→1014行**：**修 bug**——paper_equity_series 原 ORDER BY id ASC LIMIT N 取的是最旧 N 条，改为子查询"最近 N 条再升序"；新增 paper_order_status_counts()（GROUP BY status 委托状态计数）。
  - **⑥测试 274→295（22→23 个测试文件）**：新建 tests/test_paper_report.py 7例（休眠零输出不落盘/紧凑块≤5行/平仓后空仓/next在途挂单/txt落盘/页签登记位置/页签随 PAPER_ENABLED 显隐）；test_paper_broker +6（同owner平今免费/跨owner平昨收费/owner失效与抛异常保守平昨/强平leg/restore重建owner/账户视图与状态计数）；test_storage +3（最近N升序/空表/状态计数）；test_charts 同步 CHART_IDS(12图)与看板断言（启用态才含纸面页签）。
  - **⑦验证（全绿+双真实冒烟+浏览器双态目视）**：pytest **295 全绿约3s**、paper_broker --selftest **29 断言全过**、内联JS `node --check` 通过；**启用态**进程内开 PAPER_ENABLED+临时库跑两轮真实 run_cycle（next档：首轮挂单19，次轮成交11/持仓11/在途8，真实手续费91元、风险度19.59%、权益快照2点、paper_account.txt 4151字符、实时报告【纸面账户·影子模拟】块正确渲染、三表 filled11/pending8）；**休眠态** `main.py --once --no-launch` exit0（无纸面块、不生成 paper_account.txt、chart_data.js 为 "paper": null、看板无纸面页签、主链零异常）；**真实浏览器**目视启用态三图+9 chips 与全 null 空态（提示文案在、布局不错位、控制台零错误），过程中修掉权益轴精度问题。
  - **⑧关键决定与口径**：PAPER_ENABLED **维持默认 False**（守"先影子≥4周"纪律，用户在 config.json 手动置 true 开启，永不自动接实盘）；快照 n_trades 口径=**累计平仓笔数**（开仓不计，与文本"累计平仓N笔"一致，故纯开仓轮 n_trades=0 是对的）；报告在非交易时段进 state.store.off_reports、交易时段才进 reports（写断言两个 deque 都要查）。
  - **下一站（第29轮）**：G1 代码侧已齐、进入运行期（开启后连续影子≥4周再与 signal_outcomes 对照，成本后为负必须诚实呈现并回退）；新开发按融合总纲 P2 推进——首选 **G3 完整绩效指标包/tear sheet**（新建 metrics.py 纯函数：Sortino/Calmar/Omega/Ulcer/VaR-CVaR/连胜连亏/平均盈亏/profit factor/MAE-MFE/月度收益矩阵/滚动夏普，paper 账户与 backtest/portfolio 统一调用，ECharts 补月度热力/水下曲线/滚动夏普）；或 **G7 多窗口 TSMOM(63/126/252)**（先 tools/factor_eval 评 IC/RankIC 再影子、满足铁律②才并入分）；G20 实盘门槛为远期事项。
## 零.旧、第27轮快速接续摘要（已被第28轮接续，存档）
- **第27轮 G1 纸面交易引擎（一）表+撮合（2026-09-02；生产43→44个py、15938→16832行，tests 255→274全绿约3s，零新增运行依赖）**：用户指令="先理解上下文摘要md、继续完成项目、可用本机查找/GitHub借鉴、最后更新摘要"。按融合总纲6.3本轮=G1第一部分（拆"表+撮合"/"账户+看板"两轮，本轮只做表+撮合，**不接main**）；对标 freqtrade dry-run / vnpy SimNow 的公开标准做法，代码全部自写、纯标准库零依赖，PAPER_ENABLED 默认 False 完全休眠、缺省等价旧版，不动打分/信号/取数/现有输出。
  - **①架构（复用而非另起炉灶）**：新建 `paper_broker.py`（701行，LF无BOM），账户内核**直接组合 portfolio.Portfolio**（三种 sizing、单品种/板块/可用资金0.99/持仓数约束链、逐轮盯市、触发线-安全线两段式强平、真实费率），本模块只做"实时轮询综合分→委托→成交"状态机与持久化；import config/portfolio/backtest.load_fee_schedule/storage.score_band_name，零网络。
  - **②成交两档（与G4回测对齐）**：`close`=信号轮当轮最新价成交；`next`（影子默认、保守）=信号轮只挂 pending、下一轮首个新价才成交（**严格晚于信号**），下一轮锁板/无价/临时资金约束则挂单保留顺延、绝不虚构成交；on_cycle 四阶段=A成交上轮pending（先平后开支持反手）/B本轮信号决策/C盯市后强平/D权益快照，返 summary。
  - **③三阈值迟滞状态机（防抖）**：纯函数 want_position(score,held_dir,entry=4,exit=2)——|分|≥PAPER_ENTRY_SCORE(轻仓线4)才开/反手，持仓后|分|<PAPER_EXIT_SCORE(中性线2)才离场，中间继续 hold；反手=reverse_close+reverse_open 两腿先平后开。**状态机精修（真实冒烟驱动）**：临时约束（持仓上限/可用资金/板块名义/不足1手，RETRYABLE_SKIP 集合）next档保持 pending 顺延而非 rejected；信号转中性（_decide 返空）自动 cancel 遗留挂单；同向旧挂单签名[(action,direction)]相同则不撤不重挂（委托不膨胀，orders 从114降到54）。
  - **④锁板/滑点/费率**：locked_at_quote 相对昨结(prev_settle)用 high/low 整根贴板才拦截（买撞涨停/卖撞跌停），缺昨结/缺幅度放行；apply_slip 成交价内含滑点（买×(1+slip)/卖×(1-slip)）后传给 pf，手续费走 pf.fee_yuan 真实费率表 data/futures_fees.csv（PAPER_USE_REAL_FEES 关则回退 PAPER_FEE_RATE 兜底比例），保证金走 data/futures_margins.csv（缺品种 PAPER_DEFAULT_MARGIN=0.12）。
  - **⑤storage 三表（业务表11→14）+重启恢复**：paper_orders（委托：action∈open/close/reverse_open/reverse_close/liquidate、side、status∈pending/filled/blocked/rejected/cancelled、fill_ts/fill_price/raw_price/order_ref/pos_ref/raw_json）、paper_trades（成交：pos_ref配对、side、slip_yuan/fee_yuan/realized_yuan/leg/forced/order_id/entry_ts/entry_price/margin_rate）、paper_equity（ts UNIQUE每轮一条覆盖幂等）；9方法=insert_paper_order/update_paper_order/insert_paper_trade/insert_paper_equity/paper_open_position_trades(未平仓开仓成交,恢复用)/paper_realized_fees/paper_last_equity/paper_orders_recent/paper_trades_recent/paper_equity_series；table_counts/prune(PAPER_RETENTION_DAYS=3650)接入。PaperBroker.restore() 由三表重建 Position/pf.realized(=平仓net和-未平仓开仓fee)/fees_paid/pending，长期影子可跨进程重启。
  - **⑥config PAPER_* 20项**：在G4段后G10深合并前插入——PAPER_ENABLED=False/PAPER_FILL_MODE="next"/EQUITY0=1e6/ENTRY_SCORE=4.0/EXIT_SCORE=2.0/SIZING=equal_notional/PER_SYMBOL0.15/RISK_PER_TRADE0.01/MAX_SYMBOL_WEIGHT0.30/MAX_SECTOR_WEIGHT0.60/MAX_CONCURRENT12/RISK_LIQUIDATE1.00/RISK_SAFE0.80/DEFAULT_MARGIN0.12/USE_REAL_FEES True/FEE_RATE5e-5/SLIP_RATE1e-4/LIMIT_EPS8e-4/ALLOW_ADD False/RETENTION_DAYS3650/ACCOUNT_TXT(路径受保护)；全大写自动可被 config.json 覆盖，config.template.json 已补19个可调项（路径项不列）并实测全部 applied 无 skipped。
  - **⑦已知边界（诚实标注，第28轮及以后处理）**：本轮统一按"平昨/close"计费，未做实时平今/平昨 owner 判定（回测侧 intraday 有，实时纸面留遗留）；PAPER_ALLOW_ADD 加仓参数预留未启用；pf.skipped 会累计排队中的重复尝试次数（属内部日志，PaperBroker 层这些单仍是 pending 非 rejected，报告需区分"排队尝试"与"确定拒单"）。
  - **改动清单**：新增 paper_broker.py(701)、tests/test_paper_broker.py(18例)；config.py 603→626、storage.py 828→998；config.template.json 补19项；CHANGELOG/VERSION 0.26→0.27、README（轮次表+七.6节+测试规模22文件274例）、tests/README（22文件274+test_paper_broker行）、融合总纲 G1标题/6.3排期/验收三处勾"🟡第27轮完成（一）"。
  - **验证（全绿+真实冒烟）**：pytest **255→274全绿约3s**（新增18例零网络：迟滞全分支/锁板/滑点/close当轮/next晚于信号/无价顺延/反手先平后开/离场/锁板后释放/双边费/强平/资金不足拒单/临时约束排队不膨胀/空输入安全/三表落库+持仓中恢复/平仓后恢复/pending恢复/默认休眠False；测试显式注入MARGIN/FEE/SECTOR表不依赖外部CSV，loose夹具放宽资金）；`paper_broker.py --selftest` 25断言全过；**真实64品种5轮next冒烟**（临时库绝不写生产monitor.db）：19:01挂42→19:06成交12/30临时约束排队（风险度24.8%、占用24.76万、手续费71元）→19:11委托0不重挂→19:16信号归零撤30遗留+12持仓挂平仓单→19:21全12平仓无遗留，orders=54/trades=24（12开12平），真实费率全命中（RB开费2.74/平费2.74）、零兜底保证金品种、持仓中恢复（12仓静态权益逐值一致）与平仓后恢复（999572.7473两边相等）、equity5轮；非交易时段价格不变，净-427元来自双边费+滑点（负结果诚实）；main --once --no-launch exit0、本轮(19:2x-19:3x)日志0个Traceback/ERROR；monitor.db 自动 CREATE TABLE IF NOT EXISTS 补建 paper 三表=14业务表、integrity/quick_check=ok、外键0违规、paper三表均0行（未接主链）；compileall全过；行尾/BOM纪律保持（paper_broker LF无BOM、backtest仍CRLF不动）。
  - **git**：基线 v0.26.0(026a1bc) 之上本轮收尾提交并打 tag **v0.27.0**；临时 _smoke27/_patch27/_dbcheck27 脚本验后即删（.gitignore双保险，注意删除前曾被 test_compileall glob 虚增用例数，删后权威274）。
  - **下一站（第28轮=G1（二）账户+看板，融合总纲6.3）**：①main.State 持有 PaperBroker（受 PAPER_ENABLED 开关，默认影子独立成段不动现有输出），run_cycle 第5步落库后、报告前调 state.paper.on_cycle(cycle_time,fut_rows,quotes)；②reports/paper_account.txt（account_summary：净值/占用/可用/风险度/持仓/挂单/已实现/手续费/排队尝试vs确定拒单）；③report.py 加纸面账户块、charts.py 加纸面净值图（**改前端必须 node --check + 真实浏览器目视 + 全null空态，教训8**）；④连续影子≥4周后与 signal_outcomes 对照，成本后为负必须诚实呈现并回退；⑤评估 PAPER_ENABLED 是否默认开、补实时平今/平昨 owner 判定。之后 P2按需 G3绩效包/G7长窗TSMOM/G19备份/G12价差/G5组合风控/G2插件化。三铁律不变：不动实时主链与综合分口径、默认影子/开关缺省等价旧版、生产零新增依赖、负结果诚实、先paper永不自动接实盘（G20门槛=影子≥3月+证监会2025-12号公告+穿透式测试十项+C4）。
## 零.旧、第26轮快速接续摘要（已被第27轮接续，存档）
- **第26轮 G4 回测严谨性（2026-09-02；生产仍43个py、15560→15938行，tests 238→255全绿约3s，零新增运行依赖）**：用户指令="先理解上下文摘要md、继续完成项目、可用本机查找/GitHub借鉴、最后更新摘要"。按融合总纲6.3本轮=G4；方法为公开标准做法（next-bar fill/trade-level bootstrap），代码自写零依赖，默认值全部等价旧版、close档交易数值逐值不变，只增量展示/留档，不动打分/信号/取数。
  - **①成交时点双档 --fill {close,next_open}（backtest.py 双路径状态机，close路径原样保留）**：prepare_symbol 增 opens（比例后复权本就覆盖开盘价）；close=信号根i以closes[i]成交（旧口径）；next_open=根i决策挂pending单、根i+1以opens[i+1]成交（阶段A先平后开支持反手），次根跳空锁板/无价则计blocked并在决策根重新挂=顺延，末根才出的入场信号无次根→unfilled_entry如实丢弃绝不虚构，末根仍持仓回落末根收盘价平（reason保留到期/反向）；信号层h1/h5/h20与成交时点严格解耦（全量两档9147/8937/8202逐值一致，已验证）。
  - **②交易级 iid bootstrap（纯标准库、可复现）**：quantile_inplace线性插值分位(R type7/numpy linear口径)、_equity_cum_dd逐期复利、bootstrap_trade_stats用局部random.Random(seed)对逐笔净收益有放回重采样1000次，给累计收益与路径最大回撤的P5/中位/P95，总体+多头+空头各一组；<BACKTEST_BOOTSTRAP_MIN_TRADES(20)笔或n_boot=0返None不做假精确；报告注明iid假设在强自相关下区间偏乐观。
  - **③样本内外 --oos-ratio（默认0关）**：split_is_oos按平仓日(缺失回退入场日)升序切前(1-r)IS/后r OOS，总体与多/空并列IS/OOS指标；这是简化版样本外，真正滚动walk-forward仍在backtest_validation（抬头交叉引用、不重复造轮子）。
  - **④冲击成本 --impact-rate（默认0等价旧版）**：单边比例往返两次，_build_trade把成本拆成fee_cost/slip_cost/impact_cost/cost=三者和，trades CSV新增impact_cost列（向后兼容新列）。
  - **⑤backtest_runs留档（storage第11张业务表）**：字段run_ts/kind/fill_mode/cost_mode/n_symbols/n_trades/sample_days/params_json/metrics_json/cumulative/max_dd/sharpe/win_rate+idx_btr_kind；方法insert_backtest_run/backtest_run_history；table_counts/prune(保留BACKTEST_RUNS_RETENTION_DAYS=3650)接入；backtest.archive_run离线写生产库、percentile_at_or_below算"第K条/好于历史X%"挂报告抬头，DB任何异常软降级None绝不拖垮回测（--no-archive关，db_factory供测试注临时库）。
  - **⑥防过拟合交叉引用**：tools/backtest_validation.py build_report改为返回(text,sidecar)、main写txt同名.json（dsr/grid/summaries，allow_nan=False防NaN）；backtest.load_validation_sidecar缺文件/坏JSON/非对象返None，_fmt_validation_ref压成抬头一两句（--no-validation-ref关）。
  - **改动清单**：backtest.py 587→881（opens/双档状态机/4纯函数/archive_run/报告增量块/CSV列/CLI六新参 --fill/--impact-rate/--bootstrap/--seed/--oos-ratio/--no-archive等）、storage.py 780→828、config.py 592→603（G4参数段11项）、tools/backtest_validation.py 695→720（+json/datetime导入）；新增tests/test_backtest_rigor.py 17例；config.template.json补7可调项（list→tuple经config_loader矫正已实测）；CHANGELOG/VERSION 0.25→0.26、README与tests/README规模同步、融合总纲G4四处勾选。
  - **验证（全绿+真实冒烟）**：pytest **238→255全绿约3s**（新增17例零网络：两档成交价/日期/hold精确、末根不虚构、锁板跳过、反手先平后开、close逐值等价、冲击成本往返、bootstrap可复现/区间有序/常数退化/样本不足与关闭守卫、分位已知值、IS-OOS排序比例、sidecar三种坏法、backtest_runs表、archive序号百分位、DB故障软降级）；真实全64品种日线两档对照 **close 1713笔真实费率1713/1713命中、净累计-24.4% vs next_open 1571笔(末根丢弃10)净-79.2%**——保守档更差，正好印证"信号根收盘成交"是温和乐观假设（负结果诚实呈现；两档皆多头优于空头、空头持续亏，与历史各轮一致）；留档2条百分位100%/50%正确；backtest_validation --grid RB,HC 出合法sidecar（DSR0.00/grid2品种）；main --once --no-launch exit0、零Traceback/ERROR；monitor.db integrity/quick_check=ok、外键0违规（11业务表+sqlite_sequence内部表=12对象）；compileall全过；生产文件换行/BOM纪律保持（backtest.py仍CRLF、其余LF、全无BOM）。
  - **git**：基线v0.25.0之上本轮收尾提交并打tag **v0.26.0**。
  - **下一站（第27-28轮，融合总纲6.3）= G1 纸面交易引擎**：拆"表+撮合"/"账户+看板"两轮——新增paper_broker.py实时虚拟成交(close/next两档)、复用portfolio三种sizing与强平状态机、storage加paper_orders/paper_trades/paper_equity三表、reports增paper_account.txt+看板净值、PAPER_ENABLED开关默认影子独立成段不动现有输出，对标freqtrade dry-run/vnpy SimNow，先paper永不接实盘；验收=零网络断言(开/加/反手/锁板跳过/双边费/强平/next严格晚于信号)+--once冒烟+连续影子≥4周对照signal_outcomes。之后P2按需（G3绩效包metrics.py/G7长窗TSMOM63·126·252/G19备份灾备/G12价差/G5组合风控/G2插件化）。三铁律不变：不动实时监控主链与综合分口径、新能力默认影子/开关缺省等价旧版、生产零新增依赖、负结果诚实。
- **第25轮 数据底座一组（2026-09-02；生产43个py/15560行，tests 238全绿约2.7s，零新增运行依赖）**：用户指令="先理解上下文摘要md，然后继续完成项目，可用本机查找任何东西，GitHub有现成代码可借鉴，最后更新上下文摘要"。按融合总纲6.3排期，本轮做立即项G9 + 第25轮数据底座（G10/G11/G6），全部默认行为可回退、只增量不改打分/信号/取数结果。
  - **G9 git工程化（零改运行代码）**：`git init -b main`，仓库级身份（未改全局git config）；基线提交 tag **v0.24.0**（第24轮基线，3a86075）。新增 `.gitignore`（忽略 data/*.db、data/archive、cache、logs、reports全部运行产物(html/txt/csv/js/json/assets)、__pycache__、.pytest_cache、.idea、.env、config.local.json、临时补丁前缀；保留源码/tests/文档与 data 下 futures_fees.csv/futures_margins.csv/费率xlsx，空目录用.gitkeep占位）；`.gitattributes` 统一文本LF（.bat转CRLF、二进制标注），守住"生产.py纯LF无BOM"纪律；requirements.txt 三直接依赖锁==（requests2.34.2/uiautomation2.0.29/websocket-client1.9.1，pytdx仍可选注释），requirements-freeze.txt 为D:/Python完整冻结留档（含dev侧pytest/pandas，头部注明非生产必需）；新增 CHANGELOG.md（0.5→0.24逐轮）与 VERSION。
  - **G10 配置外置（新增 config_loader.py 189行，纯标准库零网络）**：①parse_dotenv/load_dotenv 解析极简.env注入os.environ，**真实环境变量优先不覆盖**，文件缺失零影响；②deep_merge 字典递归深合并不改原dict；③coerce_value 按"默认值类型"矫正（bool先于int、整数浮点3.0接受/3.5拒、tuple↔list、类型不符拒绝）；④apply_overrides 只覆盖config.py已存在的【全大写】可调常量，PROTECTED_NAMES=BASE_DIR/DATA_DIR及 *_FILE/_DB/_DIR/_JS/_HTML/_CSV 路径常量受保护，函数/模块/小写名/未知键/类型不符一律跳过并出报告，绝不抛错。接入点：config.py 顶部BASE_DIR后立刻load .env（早于第99行ALERT_WEBHOOK的environ.get）、文件末尾深合并config.json（暴露CONFIG_OVERRIDE_SOURCE/REPORT）。外置文件路径可用环境变量 FUTURES_MONITOR_CONFIG / FUTURES_MONITOR_ENV 指定。模板：`config.template.json`（扁平结构、值=内置默认、_说明键会被忽略）、`.env.example`。**缺config.json/.env时逐字节等价旧版**（已子进程验证）。
  - **G11 数据源熔断降级链（新增 data_router.py 242行，时钟可注入）**：SourceHealth状态机 CLOSED→连续失败达DATA_ROUTER_FAIL_THRESHOLD(5)→OPEN冷却DATA_ROUTER_COOLDOWN_SEC(300)，冷却到期→HALF_OPEN只放1次试探→record_success回CLOSED/record_failure重新OPEN；DATA_ROUTER_ENABLED=False时只计数永不熔断（等价旧版）。DataRouter按序试多源、熔断源note_skipped跳过、异常或validator判残缺("残缺即弃")降级下一源、全失败抛AllSourcesFailed。HealthRegistry进程级单例 **REGISTRY**（线程安全，record/snapshot/open_sources）。接入（最小侵入、健康时行为不变）：futures_data.fetch_quotes 每批上报 quote_sina 成败、兜底源 quote_em 调用前查 allow()（熔断冷却则跳过，本来也连续失败）；intraday_bars.MinuteCollector.collect 三源分别上报 minute_sina/minute_tdx/minute_em（顺带把原elif改成等价的if not bars）。**决策：主源新浪永远尝试（廉价且是首选），熔断主要保护反复失败的兜底/二三级源，避免向坏源空发请求**。
  - **G6 数据质量监控（新增 data_health.py 181行 + storage第11张表）**：evaluate_quotes纯函数体检=缺数(expected-present)/陈旧价(latest<=0，交易时段内date非今天才算、非交易时段不按日期判)/异常跳变(|chg_pct|≥DATA_HEALTH_JUMP_PCT=0.30)；HealthMonitor跨轮追踪品种连续缺数(DATA_HEALTH_MISS_ALERT_CYCLES=2才告警防抖)与数据源连续全失败，把REGISTRY累计计数折算单轮增量（**首次见基线为(0,0,0)**），产出落表行（每数据源一行+__quotes__聚合行）。storage新增 **data_health表**（ts+source唯一覆盖、stale/jump/state/note、idx、保留DATA_HEALTH_RETENTION_DAYS=365，prune/table_counts同步，现共**11张表**），方法insert_data_health/data_health_recent。main.run_cycle第2.5步：observe→state.db.insert_data_health→连续缺数/熔断复用state.alerts.emit（key限流1800s，只监控不改结果）；report.render在横截面块前加【数据源健康】块（data_health.format_health_block）。新增 **tools/db_archive.py**（159行，零依赖sqlite3/csv，--year按年ATTACH导出SQLite分库或--csv年包，**只导出不删主库**，--selftest通过）。
  - **新增/改动文件清单**：新增根模块 config_loader.py、data_router.py、data_health.py；新增 tools/db_archive.py；新增 tests/test_config_loader.py(17例)、test_data_router.py(14例)、test_data_health.py(10例)；改 config.py(561→592，加G11/G6参数段+G10头尾)、futures_data.py(REGISTRY上报/熔断跳过)、intraday_bars.py(三源上报)、storage.py(740→780，第11表)、main.py(import/State/run_cycle 2.5步)、report.py(健康块)、tests/test_storage.py(+2)、tests/test_tools_selftest.py(+db_archive)；新增 .gitignore/.gitattributes/VERSION/CHANGELOG.md/config.template.json/.env.example/requirements-freeze.txt 与各目录.gitkeep。
  - **验证（全绿+真实冒烟）**：pytest **191→238全绿**（新增47例，约2.7s，全零网络确定性，熔断/时钟均注入）；真实 `D:/Python/python.exe main.py --once --no-launch` exit0、日志0个Traceback/ERROR；data_health落4行=__quotes__ 64/64、minute_sina 320/320、quote_em 17/17、quote_sina 66/66全closed；18:04非交易时段写入 offhours_report.txt（教训：收盘后不走latest_report.txt），【数据源健康】块正确；monitor.db PRAGMA integrity_check/quick_check=ok、foreign_key_check空、11表；compileall递归全过；config.template.json合法、.env.example可解析；实时报告.html结构完好(charts-panel/echarts在)。
  - **git状态**：基线 tag v0.24.0（提交3a86075，85个版本化文件，已排除277MB monitor.db与全部产物）；本轮收尾提交 tag **v0.25.0**。工作流=改前在基线之上、每轮一个可回滚提交。
  - **下一站（第26轮，融合总纲6.3）= G4 回测严谨**：walk-forward滚动、手续费+滑点+冲击成本、样本外、多空分桶、对照基准；之后第27-28轮 G1 纸面交易引擎（订单/持仓/撉合状态机，先paper不接实盘）。再之后 G2/G3/G5（因子IC流水线/特征存储/回测指标）。**不要动已稳定的实时监控主链与综合分口径**；新增能力默认影子/开关、缺省等价旧版。
  - **本轮新立可复用约定**：①配置可调项走config.json、密钥走.env，代码里不再硬编码新阈值；②任何新数据源必须经data_router.REGISTRY上报成败；③取数质量异常只告警/记录，绝不回改打分；④Edit工具在本机仍间歇失败，统一用"补丁.py+assert count==1"改文件，临时脚本用 _patch25/_* 前缀验后即删（.gitignore已双保险）。

## 零.旧、第25轮快速接续摘要（已被第26轮接续，存档）

## 零.旧、第24轮快速接续摘要（已被第25轮接续，存档）
- **第24轮 第三份全网/GitHub对标 + 六文档融合总纲（2026-09-02，纯文档零改码；生产仍39个py/14663行、tests 191全绿约2.3秒）**：用户两步——①"全网搜量化系统组成、看GitHub量化项目、与本项目逐项对比，列出缺什么+修改方案"；②"把新交付2份HTML与统一改进路线图/未完成项落地方案/AI量化对标/GitHub对标清单4份md融合成一份整体md，未实现项按优先级分类，最后更新本摘要"。
  - **第一步产出（真实Chrome file://验证零console错误、已present_files）**：`量化系统对标与改进报告.html`（10个h2/7表/20个gap方案块：十层标准架构、GitHub格局、十维自评、G1-G20差距与文件级改法、排期、不做清单）；`对标能力可视化.html`（十维雷达+价值×成本矩阵两canvas，用本地assets/echarts.min.js）。
  - **十层坐标系**（综合vnpy分层/Qlib四层/Lean/apexphere六层）=①数据②因子信号③策略研究防过拟合④回测⑤组合仓位⑥风控⑦订单执行⑧绩效⑨监控交互⑩工程基建。**十维自评(0-5)**：因子4.5、数据4、策略研究4、组合4、监控4、回测3.5、绩效3、风控3、工程3、**订单执行0.5=唯一塌陷**（无OMS/网关/纸面/实盘，broker字样仅指期货公司保证金率）。
  - **第二步产出 `总体对标与统一改进总纲（融合版）.md`（23.5KB，唯一总入口；6份原始材料保留不删）**：统一 **G1-G20 主编号**、每项括注旧编号（P2-x/WP-Fx/清单⑮等）去重。**未实现按优先级（用户硬性要求）**：**P0**=G9 git工程化（实测无.git，git init+.gitignore忽略data/*.db,cache,logs,reports产物/__pycache__+依赖锁==+CHANGELOG+VERSION，成本1零改码）；**P1**=G1纸面交易PaperBroker(成本3.5/最高价值,新增paper_broker.py实时虚拟成交close/next两档、复用portfolio sizing强平、storage加paper_orders/paper_trades/paper_equity三表、对标freqtrade dry-run/vnpy SimNow)、G4回测严谨(backtest.py L280-292信号当日收盘成交属温和乐观,加--fill next_open对照+固定种子bootstrap1000置信区间+backtest_runs留档表+自动引DSR/PBO)、G10配置外置(=旧P2-1/清单19)、G11数据源熔断降级链(=旧P2-2/清单21,新浪→东财→tdx健康分/熔断/半开)、G6数据质量(data_health表+缺失率/陈旧/跳变+告警+零依赖按年归档)；**P2**=G3绩效包(metrics.py标准库Sortino/Calmar/Omega/Ulcer/VaR/CVaR/连胜连亏/MAE-MFE/月度矩阵)、G7多窗口TSMOM(现仅ret5/20,补63/126/252先tools影子)、G19备份灾备(VACUUM INTO每日留30+任务计划自启+runbook)、G12产业链跨期价差(=旧P2-3/清单15,卷螺差/矿螺利润/油粕比/近远月并入cross_section只监控)、G5组合风控(相关矩阵/组合VaR/原油压力/熔断可配置动作/组合历史净值回测)、G2策略插件化(Strategy基类+注册器,纯重构输出不变)、G14一档tick低频快照(=旧P2-5,5分钟级非逐笔先5-8品种)、G8只读web(标准库http.server,--web默认关)、G15分钟库持续自采(=旧P2-6,minute_bars自09-01起,进行中)；**P3**=G13 LLM第二意见(=旧P2-4/WP-F3/清单22,无key休眠,只复核紧急轮动/≥6.5强信号/背离三类,异步强制JSON,永不改分下单)、G16浅ML(=旧WP-F4,tools训练→JSON→生产标准库推理,硬门槛DSR显著/PBO<0.5/样本外成本后≥现状,可先标准库逻辑回归评分卡台阶)、G17期权遗留(量/额PCR+官方IV交叉校验+精确保证金)、G18 Tushare零依赖支线(不装SDK裸HTTP,T1 trade_cal/T2 fut_wsr最优先均2000积分,ft_mins不买)、G20实盘远期(纸面满3月+证监会2025年12号公告报告+穿透式测试十项+C4,或用认证终端WH8/金字塔免穿透)。
  - **本轮新识别（旧4份md没有）**：G1/G2/G3/G4/G5/G6/G7/G8/G9/G19/G20 共11项；旧路线图P2-1~P2-6/P3/Tushare分别映射到G10-G18。商品CTA学术共识=时序动量TSMOM(多lookback,252日最稳,Bayes-CID2026全模型top10)+横截面动量+曲线carry/roll yield+库存/套保压力（本项目已有横截面/carry/库存，唯缺长窗TSMOM→G7）。GitHub星数量级：freqtrade49-52k/qlib~46k/vnpy42-45k/nautilus22-26k/Lean~19k/hummingbot18k+/backtrader类10-20k/QUANTAXIS类9-11k/TradingAgents·ai-hedge-fund60-100k级。
  - **不做清单已融合保留（附理由防走回头路）**：L2逐笔回放(免费不可得)、DRL实盘(FinRL非平稳/无GPU/样本短)、端到端LLM决策出单(不可复现)、裸K LSTM/Transformer价格预测(FUTURES_NN49.08%低于随机、XGB52%命中但alpha为负)、在线热更新学习、重依赖进requirements、pip装akshare/tushare SDK、付费盘后源替换实时主源、tqsdk程序化批量(1488/年)、Rust/纳秒/HFT重写(5-20分钟场景过度)、现在直接CTP实盘、Flask/FastAPI进生产。
  - **下一站（第24轮更新，详见融合总纲第6.3节排期）**：半天先做 **G9**（零改码安全网）→ 第25轮 G10/G11/G6 数据底座一组 → 第26轮 G4 回测严谨 → 第27-28轮 **G1 纸面交易引擎**（拆"表+撮合"/"账户+看板"两轮，补执行层塌陷）→ P2按需；G15分钟库保持常驻不断采。三铁律（生产零新增依赖/确定不更差才采纳/每改动合成断言+真实冒烟+负结果诚实）不变。
- **第23轮 实时报告与图表看板合并（2026-09-02，真改代码；生产仍39个py、14600→14663行，零新增依赖；tests 189→191用例全绿约2.3秒）**：用户要求"先理解上下文摘要md、把实时报告html与图表看板html合并"。两页都是程序每轮幂等生成（手改产物会被覆盖），故改生成器而非只改产物——**"图表看板"页签由 iframe 套独立页改为同页内嵌渲染，其余13个txt/csv页签 iframe 全保留；独立 图表看板.html 直链保留不删，与内嵌页共用同一片段**。
  - **charts.py（732→744行，重构不重写）**：原 `_PAGE_HTML` 整页模板拆成三段共用片段——`_PANEL_STYLE`（CSS 选择器全加 `#charts-panel` 作用域防污染外层；#head/#gen 改名 .cp-head/#cp-gen）、`_PANEL_DOM`（9图DOM，容器id c-equity…c-out 不变）、`_PANEL_JS`（原平铺全局函数包进 IIFE、内部 charts 字典改名 inst；导出 `window.ChartPanel={activate,reload,resizeAll}`：**首次 activate 才 loadAndRender 懒加载、再次进入只 resize 兜底、reload 仅曾打开过才重渲染省后台开销**；独立页靠 `window.__CHARTS_STANDALONE__=true` 打开即自启；新增 echarts 缺失守卫、empty 空态重建时先清占位并 dispose 旧实例）。`charts_page_html()`=外壳拼三段（独立页行为不变）；**新增 `dashboard_embed_parts()`→(style,dom,js) 纯片段（无 html/head/body 外壳、不自动启动）**。
  - **report.py（968→959行）**：tab 第二项 `("图表看板.html",...)`→`("__charts__","图表看板")` 内嵌标记；head 引一次 assets/echarts.min.js、`</style>` 前注入面板样式、iframe 后加 `<div id="charts-panel">` 面板DOM、主脚本后再开一个 script 注入面板JS；show() 分流（__charts__→隐藏iframe/显示panel/ChartPanel.activate，其余→iframe 换 src）、reloadView() 分流（图表页 ChartPanel.reload、其余重载iframe）；面板容器 `height:calc(100vh-45px);overflow-y:auto`。**片段用 /*__CP_STYLE__*/ 等占位符在 % 格式化之后再 replace 注入，避开片段 JS 中 % 被字符串格式化误伤**；原匿名 return 模板改为 `_dashboard_tmpl` 先格式化再 replace。
  - **关键坑（已处理）**：ECharts 在 display:none 容器 init 宽高为0→先显示 panel 再 activate 懒加载、二次进入 resize 兜底；file:// 下 chart_data.js 仍走动态 script 注入（fetch 不可用约束不变）；echarts/chart_data.js 相对 reports/ 路径不变，sync_echarts_asset 每轮幂等同步照旧。
  - **验证（实跑+真浏览器）**：tests/test_charts.py 14→16用例（片段纯结构/无外壳、IIFE导出、独立页共用片段；report._dashboard_html 集成断言：单echarts引用/9容器/内嵌标记/不再iframe图表页/13页签全保留/占位符清零/面板默认隐藏），**全量 pytest 191 passed 2.3s（=189基线+本轮新增2；注意 test_compileall 会参数化收集根目录所有 py，跑测前须清掉 _ 前缀临时脚本，否则用例数虚高）**；4段内联JS `node --check` 全过；write_dashboard 两次幂等；真实 Chrome file:// 实测——初始txt iframe正常、点图表页签 7 canvas 渲染+chart_data 动态注入成功（gen显数据时间、权益7 chips）、c-sector/c-xs 因该份 chart_data 横截面为 null 按设计显空态（非合并问题，下轮监控自动补）、切回txt再切回图表（resize路径）正常、ChartPanel.reload 重注入正常、独立页直链 standalone 自启同样7canvas、两路径一致，**控制台零页面错误**（仅两条 Chrome 扩展 runtime.lastError 噪音与本页无关）。产物：实时报告.html 6.9KB→27.7KB（内嵌全部图表）、图表看板.html 20.2KB（保留）。
  - **下一站（接续指引）**：不变=P2 工程项（P2-1配置外置、P2-2数据源主备降级链、P2-5 tick快照、P2-3产业链价差并入cross_section）；WP-F4 浅ML 等样本成熟；WP-F3 LLM 无key休眠。运行回归：项目根目录 `D:\Python\python.exe -m pytest`。
- **第22轮 P1-3 看板图表化（2026-09-02，真增代码；生产 38→39 个 py、13794→14600 行，零新增 Python 运行依赖；tests 174→189 用例全绿；本地 ECharts 前端资源）**：用户要求"先理解上下文摘要md、继续完成项目、可用电脑查找/GitHub借鉴、最后更新摘要"。按路线图落地 P1-3——把只能 iframe 嵌 txt 的关键结果图表化，**只做展示层、不改任何打分/信号/回测口径，13 个 txt 页签全部保留并存**。
  - **新增 `charts.py`（732行，纯标准库，IO 与纯函数分离）**：①`parse_equity_csv` 读 portfolio_equity.csv（缺文件/坏表头→None、脏行跳过、`downsample` 确定性等距抽稀≤1200点且首尾必保/并行不错位，**不改原始CSV**）+ summary（期初期末权益/累计收益/最大回撤/平均峰值风险度/峰值持仓）；②`cross_section_payload` 把 state.last_cross_section 收敛为 JSON 安全结构（rows/sectors/top/breadth，float round）；③`calibration_payload` 方向×分档贝叶斯胜率+mult（固定排序、mult=None 保留）；④`outcomes_payload` 从 db.outcome_stats 聚合分周期总/多/空胜率；⑤`factor_payload` 读 factor_eval.json（坏文件 None）。`build_payload` 四块各自 try、缺一块只置 None；`write_chart_data` 落 reports/chart_data.js（**file:// 下 fetch 不可用，沿用 report_status.js 的 window 全局+动态 script 注入**）；`sync_echarts_asset` 把 assets/echarts.min.js 幂等同步 reports/assets/；`python charts.py --rebuild` 可在监控未运行时离线重建。
  - **静态页 `图表看板.html`（无 Python 变量注入，暗色风格 9 图）**：权益曲线（动态/静态权益+期初虚线+统计chips）、回撤（反向轴红面积）、风险度+持仓数双轴（轴上限按数据自适应至少10%，避免被100%强平参考线压成贴底直线）、板块平均xs横条、全品种xs条形+dataZoom+广度chips、三周期meta RankIC分组柱、主周期五档单调性折线（n≥20才画）、方向×分档平滑胜率柱（50%参考线）+sizing乘子双轴、分周期总/多/空胜率；每块缺数据显引导空态；resize 自适应。
  - **接线（最小侵入全软降级）**：config 增 CHARTS_PAGE_HTML/CHART_DATA_JS/ECHARTS_SRC/ECHARTS_DST/CHARTS_EQUITY_MAX_POINTS/FACTOR_EVAL_JSON；report.py 第二位插页签、write_dashboard 幂等写静态页+同步资源、save 每轮 write_chart_data；main.py 启动即写首份（离线CSV/DB先出图、内存态首轮补齐）；**tools/factor_eval.py 抽出 evaluate_all/factor_metrics_json，txt 外新增结构化 sidecar reports/factor_eval.json（txt 口径与数值不变、计算不重复）**。
  - **前端资源**：ECharts 5.5.1（Apache-2.0，jsdelivr 下载 1,030,855 字节，node 解析通过）存 assets/echarts.min.js 随仓库；reports 是可清理运行产物故每轮幂等同步。
  - **验证（实跑）**：新增 tests/test_charts.py 14 用例（抽稀/CSV坏法/截面映射/校准排序/胜率聚合/JSON坏法/全空降级/script转义/落盘结构/资源缺失），全量 **pytest 189 全绿（174+14+compileall新增1）约3秒**；factor_eval 真实重跑 9因子3936配对、txt/json数值一致；main --once --no-launch 真实一轮 exit0、chart_data 五块全填（权益1023点/截面64品种5板块广度49·6·9/校准6行/胜率315·310·176 与 signal_tracking.txt 一致/9因子）；node --check 内联JS（修掉花括号配平、轴刻度重复、标签重叠、参考线压轴等显示问题）；真实浏览器直链页+外层看板点页签两路径逐屏截图、另造全null fixture 验证空态，**控制台全程零报错**。
  - **下一站（接续指引）**：P1 三项（pytest/校准样本/图表化）全部完成。下一阶段=P2 工程项（P2-1配置外置、P2-2数据源主备降级链、P2-5 tick快照自采、P2-3产业链价差并入cross_section）；WP-F4 浅ML 三前置中 pytest✅/ml_samples✅（4.3万行但覆盖率仅约1%）/minute_bars 仍需常驻积累（自09-01起），样本不成熟不提前上模型；WP-F3 LLM 无key永久休眠。
- **第21轮 P1-1 pytest 回归测试体系（2026-09-02，真增代码；生产38个py/13794行一行未改，新增 tests/ 共17个文件约1236行、174个用例；dev侧装 pytest 9.1.1、生产 requirements 零改动；全部零网络、确定性）**：用户要求"先理解上下文摘要md、继续完成项目、可用电脑查找、可借鉴GitHub、最后更新摘要"。按《统一改进路线图》推荐顺序（P0✅→P1-1/P1-2并行→P1-3），P1-2 已于第19轮完成，本轮落地 **P1-1（WP-F4 浅ML 的硬前置）**：把前20轮"验证后即删"的数百条临时合成断言固化为永久 pytest 回归网，一条命令全绿、后续改一处即知是否崩一片。
  - **新增 `tests/`**：`conftest.py` 提供两个夹具——`flat_calendar` monkeypatch trade_calendar/utils 注入"周一~周五交易、无法定节假日、周一~周四有夜盘"的确定性日历（保证调度用例零网络）、`tmp_db` 在 pytest tmp_path 建临时 MonitorDB（绝不读写生产 data/monitor.db），并把项目根与 tools 注入 sys.path；`pytest.ini`（testpaths/markers）、`tests/README.md`（运行方式+纪律+覆盖表）。15 个 test_*.py 共174用例：**test_utils_schedule**(12：next_transition 9类时段翻转、翻转点±1s is_trading_time必反转不变量、next_cycle_time 5/20分钟刻度、trade_owner_date 夜盘归属、review_is_due、三档夜盘分档、clip/norm_cdf/全角pad/sanitize)；**test_cross_section**(稳健z=0.6745(x-中位)/MAD、MAD=0退总体标准差、全相同不崩、XS_Z_CLIP裁剪、板块聚合涨跌家数、多空广度、样本<8不做z、只比较不改score)；**test_risk_gate**(无价/低量/强信号背离/HV极端 veto、HV高/量仓冲突/临近交割/低置信 warn、veto优先于warn、总开关、默认不降级不变量、AUTO_DOWNGRADE降级并留label_before_gate、level_rank)；**test_signal_calibrator**(canonical_factor/dominant_factor含做空负贡献、贝叶斯平滑(20,25)=21/27、乘子触上下限1.2/0.5、四级回退LV_FACTOR→LV_BAND→LV_DIR、小样本不校准、禁用、中性方向不校准、annotate安全)；**test_fundamental**(低库存去库偏多/高库存累库偏空/<15样本None、龙虎榜净多与边际、carry Back正/Contango负、基差、全缺None、缺项按可得权重重归一不超FUND_MAX_SCORE)；**test_factors_sentiment**(词典极性/板块过滤EN·BL/上下文闸门"工厂增产"不计"原油增产"计/否定反转且数值对称、五维强度·不确定·前瞻·相关·事件8分类、facet_tags阈值、NewsFactor同内容去重/可信度折扣)；**test_analyzer_debate**(rating四档边界2/4/6.5与多空对称、build_debate bull/bear拆分、verdict三档、parts基本面·量仓与专支同源去重、机构净空HV)；**test_option_chain**(product_code郑商所无后缀其余_o、pinzhong、parse_leg 8/9元素行权价来源与坏行、PCR五档、build_summary持仓PCR/最大持仓行权价/升序/零认购不除零、locate_atm)；**test_iv_surface**(leg_quote窄价差用中间价/宽价差退最新价标低质/无成交丢弃、Black-76二分反推call·put互逆误差<1e-6、低于内在价值脏价拒反推、put-call平价IV一致、_merge_strike OI加权、平价差>3vol取高质量侧不平均脏值)；**test_intraday_bars**(aggregate_bars OHLCV求和/跨午休缺口不硬拼/尾段零散丢弃/factor=1浅拷贝/坏dt跳过、em_contract_code郑商所3位/em_secid未知交易所返空)；**test_flow_tracker**(增仓上行/增仓下行/减仓回补、最少两快照、同日量回退volume_reset、跨交易日重建基线不把隔夜当放量、无价跳过)；**test_backtest_fees**(side_fee金额费率+按手数固定费相加/open·close·today三腿/缺表与非法守卫、metrics_from_returns胜率累计回撤/常数序列sharpe=0不除零、score_band、technical_score符号与共振叠加)；**test_portfolio**(24：两品种一赚一亏逐bar盯市静态/动态权益/占用/风险度/可用、空头亏损方向、三种sizing等名义·等风险被单品种上限约束·按分档、同时持仓/单品种/板块名义上限、1手买不起是真实约束、无乘数拒开、缺保证金表回退默认率并登记fallback、开平仓费对账net=毛-开-平、强平浮亏最大优先且一路砍到安全线保留持平仓、穿仓全平、健康不强平、SymbolFeed.owner_at无bar时刻bisect取最近已收盘bar、校准乘子仅显式传calibrator才改手数、权益曲线与绩效)；**test_storage**(minute_bars INSERT OR IGNORE幂等/跨换月合约升序衔接、ml_samples INSERT OR REPLACE覆盖写与features_json反序列化、table_counts/integrity_check=ok/外键检查、calibration_pairs空、score_band_name)；**test_tools_selftest**(factor_eval.selftest/build_ml_samples.selftest/backtest_validation._selftest 三个研究工具自带零网络断言纳入pytest)；**test_compileall**(参数化编译全部38个生产.py，语法损坏即变红，另设数量≥30下限防glob路径写错导致"假全绿")。
  - **验证（红/绿双向，非只跑绿）**：`D:\Python\python.exe -m pytest` = **174 passed 约2秒**；**变异测试**——临时把 cross_section 稳健z常数 0.6745 改成 0.5，test_robust_z_median_mad 立即变红（实得-1.0≠期望-1.349），证明用例非空转，还原常数后174全绿（并清理 PowerShell Set-Content 误加的 UTF-8 BOM、恢复与其他生产文件一致的无BOM编码）；真实端到端 `main.py --once --no-launch` 完整一轮 **exit 0**、报告 latest_report.txt 正常、无 Traceback/ERROR（分钟once 64品种×5周期=320任务全 src=sina、拉129472根新增5219根、空/失败0），生产 data/monitor.db `PRAGMA integrity_check=ok`、`quick_check=ok`、10张表、foreign_key_check 无违规。
  - **纪律核对**：生产 requirements.txt 未加 pytest（仅装入 D:\Python 这个dev解释器）、main.py 与常驻链路不 import pytest、174用例全部零网络（调度走注入日历、DB走tmp_path）、生产38个py除"变异→还原"外零改动；文档 README 新增"九、回归测试（开发用，不影响常驻监控）"、《统一改进路线图》3.1节与第0节一页纸总览表 P1-1 均标 ✅。
  - **下一站（接续指引）**：P1-1✅ 与 P1-2✅ 均已就位，WP-F4 浅ML 的"pytest 硬前置"已清除，剩余只是 ml_samples 样本持续积累（第19轮42937行、九因子/情绪/截面快照仅 signals 覆盖期约1%，需常驻跑久、不回填编造）；按路线图推荐顺序，**下一步 = P1-3 看板图表化**（本地 ECharts 前端资源替代 iframe 嵌 txt：portfolio 权益/回撤/风险度曲线、P0 横截面多空热力与板块强弱、P1-2 因子IC条形与分档单调性、胜率校准曲线，数据由现有 CSV/SQLite 注入、无数据显空态）；其后按需推进 P2（①配置外置 config.json/.env、②数据源主备降级链、P2-3板块价差并入cross_section）。WP-F3 LLM 复核仍需用户自备 key、无 key 休眠不做；可选遗留：实时一档 tick 快照、交易所官方IV交叉校验、生意社基差反爬。运行回归：项目根目录 `D:\Python\python.exe -m pytest`（单文件 `pytest tests/test_xxx.py -v`、按名筛 `-k liquidate`）。
- **第20轮 样本外验证与防过拟合（2026-09-02，真增代码，生产33根+tools5=38个py/13794行，零新增依赖，纯标准库）**：用户要求"继续完成项目、可借鉴GitHub、最后更新摘要"。注意编号澄清：AI方案里的 WP-F3 是需自备key的LLM复核（无key休眠、本轮不做），本轮落地的是为 **WP-F4 浅ML 铺路的样本外/防过拟合验证层**（对应 AI方案 E1/E2 与 AFML ch7/11-12）。新增 `tools/backtest_validation.py`（695行，五块方法，公式来自公开论文、代码自写、未拷贝任何 copyleft 仓库）：
  - **① Deflated Sharpe（DSR/PSR，Bailey & López de Prado 2014）**：手写 norm_cdf(erf)/norm_ppf(Acklam逆正态,误差<1.2e-9)/偏度/Pearson峰度；PSR=Φ((SR−SR0)√(T−1)/√(1−g3·SR+(g4−1)/4·SR²))；多重试验期望最大Sharpe SR0=(1/√T)·[(1−γ_E)Φ⁻¹(1−1/N)+γ_E Φ⁻¹(1−1/Ne)]，γ_E=0.5772；Sharpe一律用同频率非年化值。
  - **② CSCV-PBO（Bailey,Borwein,LdP,Zhu 2014）**：时间分S块、枚举C(S,S/2)对半划分（超4000种确定性等距抽样），每折IS按Sharpe选最优列、看其OOS相对秩ω与logit，PBO=logit≤0折占比（IS最优落OOS下半区概率）。
  - **③ PurgedKFold+Embargo（AFML ch7，通用索引版生成器）**：标签窗口[t1[i]]横跨测试折的训练样本purge、折两侧embargo；WP-F4训练ml_samples的强制切分器，禁止随机K折。
  - **④ Walk-forward**：滚动IS窗选参/OOS窗验证，给IS→OOS衰减（IS≈0时不硬算百分比）、OOS跑赢候选中位数比例、最优参数切换率。
  - **⑤ 参数高原vs孤峰**：最优点曼哈顿邻域均值/最优点=plateau_ratio、邻域正收益占比、相邻格粗糙度；**最优点全网格亏损时不给"稳健"假象，直接判"先解决信号方向/成本"（修过负负得正plateau>1的假稳健）**。
  - **真实数据（复用 intraday_backtest 同套信号/撮合/真实费率，对config的18组稳定性网格逐组合回放、按平仓交易日复利聚合成T×N矩阵，无交易日补0并注明）**：全64品种跑出 reports/backtest_validation.txt——组合账户87交易日日收益DSR=0（逐期Sharpe-0.17、偏度-1.02/峰度8.7、N=18时SR0=0.200，即当前默认组合该窗口不盈利、诚实呈现）；PBO<0.2仅8个、全网格全样本Sharpe为负26个、WF样本外Sharpe为正28个；**核心结论=约6个月分钟窗口太短，多数品种IS选优在OOS明显衰减，现在不应挑单点最优参数/急于上ML，应继续积累样本**（与"跑不赢规则不上线"铁律一致）；LH/PP/EG呈参数高原、NR/B/C/J/SR/FG/SA/CJ/RM呈孤峰。
  - **验证**：compileall全过；`--selftest` 合成断言全PASS（Φ/Φ⁻¹互逆与1.959964分位点、对称样本矩、PSR零≈0.5/强>0.99、DSR随试验次数单调变差且N=1无校正、CSCV纯噪声PBO∈(0.2,0.8)/一列真alpha PBO<0.1、PurgedKFold的purge+embargo+覆盖、WF持续占优列每段必中且切换率0、孤峰plateau<0.2/高原>0.9）；全64品种真实网格 exit0。
- **第19轮 WP-F2（P1-2 信号校准与监督学习备料，2026-09-02，真改代码，生产33根+tools4=37个py/13099行，零新增依赖）**：落地《统一改进路线图》WP-F2 三块，延续 WP-F1 铁律（默认只做信息增量、可一键回退、生产纯标准库）：
  - **A3 历史胜率校准（`signal_calibrator.py` 271行，meta-labeling 零依赖版）**：用自有 signal_outcomes 按「方向×综合分档×主导因子」分组做**贝叶斯平滑胜率** (hits+先验0.5×2)/(n+2)，再 `mult=clip(1+(wr-0.5)×2,0.5,1.2)`；四级回退（方向×分档×主导因子→方向×分档→方向→全局），任一层 n<`CALIBRATOR_MIN_N`=20 不给乘子、最终回退1.0。**默认影子模式**：main 4.7b 每轮构建并给每条 row 挂 calib，明细"校准:"行+信号追踪页"三、历史同类胜率校准"只展示（不改综合分/信号/建议）；只有 `portfolio.py --calibrate` 才把乘子乘到手数（Position/成交记录带 calib_mult、报告抬头统计调整次数与平均乘子、回测无九因子时自动回退方向×分档层）。真实数据：做多强信号58.1%→1.16、做多轻仓33.8%→0.68、做空各组n<20显示"积累中"。
  - **B2 因子IC评估（`tools/factor_eval.py` 391行，纯标准库手写 Pearson/Spearman/平均秩）**：九因子拆分 join signal_outcomes，主口径 meta RankIC=Spearman(因子值×方向, 方向收益)，给 IC/RankIC/原始方向RankIC、月度ICIR、5档单调性+多空价差、30m→2h→次日衰减、逐月 walk-forward IS/OOS 同号率，最后**只给建议权重区间（相对当前权重乘数）绝不自动改 analyzer 权重**，出 `reports/factor_eval.txt`。真实结论（样本2026-07起）：新闻消息面/技术共振/基本面/原油联动 meta RankIC 为正，日线动量/机构动向为负（提示人工复核，不自动改）。
  - **B3 triple-barrier 样本（`tools/build_ml_samples.py` 553行 + storage 第9表 `ml_samples`）**：沿用日内回测同口径（信号i收盘确认、i+1开盘入场、入场当根不查、止损优先、跳空开盘成交、同根双触按止损），上轨+2ATR=+1/下轨-1.2ATR=-1/最长48根超时按到期方向收益符号(走平0)；特征严格PIT（技术特征只用≤i，标签路径全>i，`--audit`扰动未来价断言特征不变），就近匹配**同交易时段**（夜段20:00-次日03:00归一开始日、日段09-15、2.5h内、ts<=bar_dt）的九因子/截面稳健z/五维情绪快照；表 UNIQUE(sym,period,bar_dt) INSERT OR REPLACE 可重复跑、保留约10年；另实现 purged_embargo_split（标签窗口横跨测试折+embargo的训练样本剔除）。**全量30m已落 42937 行/64品种（+1=16737/-1=26184/0=16），技术特征100%、九因子/情绪/截面仅 signals 覆盖期约1%（随运行积累、不回填编造）**。
  - **storage 新增**：calibration_pairs/factor_outcome_pairs（outcomes join signals.parts_json，hit/miss/flat为有效样本、expired不进分母）、insert_ml_samples/ml_sample_rows、table_counts/prune 接入第9表。**config 新增 CALIBRATOR_*/ML_SAMPLE_*/FACTOR_EVAL_* 参数块**。
  - **验证**：compileall 全过；A3 纯函数+四级回退+裁剪+Portfolio乘子链路合成断言、B2 `--selftest`（完全单调RankIC=1/无关≈0.017/并列平均秩/分档/ICIR/WF同号率）、B3 `--selftest`（止盈/止损/同根双触/跳空/超时走平/空头对称/入场当根不查/PIT无穿越/embargo 共9类）全 PASS；真实 `main --once` 跑通（明细49处校准行、追踪页第三小节渲染正确）；portfolio RB,HC 对照（默认无校准行结果不变，--calibrate 调整390次开仓、平均乘子0.989、最大回撤2.70%→2.22%）。
- **第18轮 WP-F1（P0 决策增强，2026-09-02，真改代码，生产32→34个py/11180→11670行，零新增依赖）**：落地《统一改进路线图》P0=WP-F1 四块，**铁律：默认只做信息增量、绝不改既有综合分/信号/建议**。①**多空双面卡** `analyzer.build_debate(row)`：把九因子/当日涨跌/量仓/机构净多空/基本面/波动分位拆成 bull/bear 两方强制对列（parts 的"基本面""量仓资金"键与 fundamental/flow 分支去重），verdict 与综合分同向，明细新增"多空: 多[…] vs 空[…] → 多方/空方/均衡"行，价值在暴露反方证据。②**横截面** 新文件 `cross_section.py`：对综合分与当日涨跌幅分别做**稳健z=0.6745(x−中位数)/MAD**（MAD失效退pstdev、全相同不崩、样本<XS_MIN_SAMPLE=8不做z），xs=0.6分z+0.4涨跌z，输出板块强弱榜(含涨/跌家数)/最强最弱Top5/全市场多空广度，`format_block` 渲染【横截面强弱】块，结果存 state.last_cross_section；**只横向比较不回改score**。③**独立风控闸门** 新文件 `risk_gate.py`（与打分解耦的第二道防线）：evaluate 给 pass/warn/veto——veto=无有效价/量<RISK_GATE_MIN_VOLUME(200)/强信号(|分|≥6.5)与当日涨跌反向超2%/HV分位≥0.95；warn=HV≥0.8、信号与量仓资金反向、临近交割/期权到期、强信号低置信；apply_gate 挂 row["risk"]，**默认 RISK_GATE_AUTO_DOWNGRADE=False：veto 只在总表标⛔/明细"风控:"行列因/alerts 走 strong 告警，不改分与建议；置True才降级"暂缓"并保留 label_before_gate**，RISK_GATE_ENABLED 总开关。④**五维情绪** `factors.sentiment_facets(text,variety,cat)`：极性复用_lex_weight(不改主分)+强度/不确定/前瞻(tanh去重计数)+相关性(点名品种=1否则0.35)+事件8分类(货币政策/地缘/供给/需求库存/天气/汇率股市/贸易/资金情绪)，facet_tags 压成消息行角标(如·强0.9·前瞻·货币政策)。**接线**：main run_cycle 4.7段(4.5后/第5步入库前)逐品种 apply_gate + cross_section.rank 存 state；report.render 总表 _gate_mark 标⛔⚠、基本面块后插横截面块、analyzer.detail_lines 加多空卡/风控行/消息角标；alerts._observe_future 开头对 veto 独立 emit(冷却限流)；config 末尾加 SENTIMENT_*/XS_*/RISK_GATE_* 共约20个参数。**验证**：47条零网络合成断言(五维/稳健z/板块聚合/样本不足/MAD=0/风控各规则/默认不改分不变量/自动降级/总开关/多空卡去重)+compileall+三次真实 `main --once --no-launch` exit0，非交易报告 offhours_report.txt 实测横截面1块/多空卡51/风控24(⛔3⚠70)/情绪角标、重复瑕疵0。**关键踩坑**：(a)报告按时段分流——交易时段写 latest_report.txt、非交易(午休/夜盘后)写 offhours_report.txt，午休验证改动必须看 offhours 否则会误判"没生效"；(b)各生产文件换行符混用(LF/CRLF)，补丁脚本多行锚点必须先 `.replace('\r\n','\n')` 归一、写回再按原样还原，否则多行锚点失配而单行锚点正常；(c)补丁脚本三引号块内的 `\n` 会被解释成真换行写进源码导致语法错误，要让源码保留字面反斜杠n需写 `\\n`。

- **第17轮（调度时段边沿触发 + 首轮报告自动打开，2026-09-02，真改代码，生产32个py/11082→11180行）**：用户需求三点——①非交易时段启动后，一到交易时段**立刻**开始交易轮动、其后节奏与该时段一致（开盘前30分钟5分钟、其后20分钟）；②交易→非交易（午休/收盘/凌晨收市）同理立即轮一轮并切回非交易节奏（60s）；③启动后**第一轮真实报告生成时自动用默认浏览器打开 reports/实时报告.html**。**实现（零新增依赖）**：(a) `utils.py` 新增纯函数 `next_transition(now)`：当前在交易时段→返回该时段收盘点 sess.end；非交易→枚举未来每天三个开盘候选 **09:00/13:30/21:00**（is_trade_day/has_night_session 过滤节假日与夜盘，跨零点/周末对称于 current_session）取最近者。**关键补漏：旧 next_session_start 只列 09:00/21:00、不认 13:30 下午开盘（原来午休结束靠"非交易每整分钟一轮"巧合对上），新函数补齐下午开盘**。(b) `main.wait_with_emergency` 改造：进入时记 enter_trading 基线，每次等待时长取 **min(到计划点nxt, 到next_transition翻转点)**；醒来先判 `is_trading_time()!=enter_trading`→返回新状态码 **"edge"**（到点仍返回 True、停止 False、紧急插队逻辑不变且紧急轮后刷新基线防误判）。(c) 主循环 reason=="edge" 时打 WARNING（进入/结束交易时段）并 `continue` 回循环顶部**立即 run_cycle**，跑完用 next_cycle_time(now) 重排——开盘轮后自然落 5/20min 交易刻度、收盘轮后自然落 60s 非交易刻度，实现"切换立即一轮+跟对应时段节奏"。(d) 新增 `main.open_realtime_report_once(state)`：Windows os.startfile（非阻塞）/非Windows webbrowser 兜底，挂在 run_cycle 里 `report.save` 之后且仅 state.cycle==1；State 加 auto_open_report/report_opened（全程只开一次），main 里 `auto_open_report=not args.no_launch`；`config.REPORT_AUTO_OPEN=True` 总开关；**--no-launch 语义扩展为同时抑制"同花顺自动打开"和"浏览器自动弹报告"**（测试/无界面场景）；HTML 缺失/打开失败只告警不影响监控。**验证**：零网络合成断言——next_transition 9 个边界用例（08:30→09:00、11:35午休→13:30、交易中→当前session收盘、15:30→21:00、22:00→次日02:30、凌晨02:00仍属前夜盘→02:30、02:31收后→09:00、周六跨到周一09:00）+ 核心不变量"翻转点前后1秒 is_trading_time 必反转"；wait_with_emergency 四分支（非→交edge/到点True/停止False/交→非edge，monkeypatch注入）；自动打开5项（首轮一次/幂等不重复/--no-launch不开/总开关关不开且置位/文件缺失不抛）。真实冒烟：`main.py --once --no-launch` exit 0 第1轮报告+复盘正常无Traceback；常驻短时实测周三午休 11:51→计划11:52→11:52第2轮→计划11:53 整分节奏正常。规模 main.py 799→868(+69)、utils.py +24、config.py +1。

- **第16轮增补②（AI量化全网/GitHub对标，2026-09-01 深夜，只分析未改代码，新增《AI量化对标与改进方案.md》约15KB）**：按要求全网+GitHub调研AI量化并逐文件核对本项目。**四族裁决**：①多智能体LLM（TradingAgents 69k★/ai-hedge-fund 62k★，UCLA/MIT论文arXiv:2412.20138）只借"多空对立辩论+独立风控否决层+信号→风险→组合三段裁决"的**结构**，不做端到端LLM决策（作者自声明研究用途、非确定性）；②机器学习因子（微软Qlib：Alpha158/LightGBM/PIT防泄漏/IC评估；López de Prado AFML：triple-barrier标签/meta-labeling方向与仓位分离/purged CV+embargo）是最该学的一族；③深度强化学习（FinRL系，PPO/A2C）**裁决不做**（漂亮结果集中在美股、非平稳过拟合公认、本机无GPU、分钟样本自09-01才积累）；④金融NLP（北师大学报2025期货新闻多层次情感、arXiv2026 WTI五维情绪=相关性/极性/强度/不确定性/前瞻性；DeepSeek级API已元/百万token、本地Ollama+Qwen2.5-7B约5-8GB内存CPU可跑）→规则五维先行、LLM只做异步复核配角。**关键反面证据（决定不追DNN热点）**：Stanford期货ML对比=简单趋势夏普最高、模型没超越；FUTURES_NN神经网络二分类49.08%低于随机；XGBoost金银52-55%命中但样本外alpha -29%~-244%；正面样本Bayes-CID2026（41商品30年126因子GBDT样本外夏普2.4）靠的也是时序动量主导+8周期集成+严格purged CV。**本项目AI底子盘点（多数开源AI项目反而没有）**：8张SQLite表=现成样本库、signal_outcomes=现成hit/miss标签、intraday_backtest止盈止损超时=天然triple-barrier标注器、18组稳定性网格=防过拟合文化、portfolio共享资金池+强平=真实资金层；真正缺5块：线性单点打分无对立审查/无横截面/因子权重无评估闭环/情绪只一维/无"研究重生产轻"ML通道。**改进方法（已到文件级，WP-F编排）**：**WP-F1（最高优先、纯标准库零依赖、默认不改综合分）**=A1多空双面卡(analyzer.py:91后加build_debate纯函数，parts正负因子列bull/bear清单+decisive)、A2独立风控闸门(新增risk_gate.py，veto观望/warn降档：锁板/流动性/carry背离/单一因子拉动/板块拥挤/临近交割/HV极端，区别于只提示的risks[])、B1横截面强弱(新增cross_section.py，main收齐fut_rows后对综合分/动量/基本面做稳健z-score(中位数MAD)、板块榜、多空Top，补GitHub清单⑮，portfolio加--cross-section候选)、D1五维情绪(factors.py加sentiment_facets：强度/不确定性/相关性/前瞻性/事件类型，极性×强度×相关、不确定降权)；**WP-F2（用自有DB统计，生产零新增）**=A3 meta-labeling零依赖版(新增signal_calibrator.py，signal_outcomes按方向×分档×主导因子贝叶斯平滑胜率给sizing乘子[0.5,1.2]，n<20不校准、先影子模式)、B2 tools/factor_eval.py算各因子IC/RankIC/ICIR/分档单调性/衰减+walk-forward（只给建议权重不自动改）、B3 tools/build_ml_samples.py复用intraday撮合生成triple-barrier标签落storage第9张表ml_samples(UNIQUE(sym,bar_dt)、PIT+embargo)；**WP-F3（可选、无key休眠）**=D2新增llm_reviewer.py，仅复核紧急轮/≥6.5强信号/词典与技术背离三类小流量，OpenAI兼容HTTP(DeepSeek/本地Ollama)走现有http_client守护线程、强制JSON schema、失败软降级、**永不改综合分永不下单**，key走环境变量FUTURES_MONITOR_LLM_KEY/BASE_URL/MODEL（同webhook先例）；**WP-F4（最低、门槛最严）**=tools/ml_train允许sklearn/lightgbm（同pandas只在tools、不进requirements），只做"多因子融合/meta-label浅模型"不做价格预测，训练产物导出JSON、生产新增ml_inference.py标准库前向推理（线性点积/受限树遍历）、缺模型回退线性parts，**上线硬门槛=样本外真实成本后≥现有线性规则否则不上线**，purged TimeSeriesSplit+embargo强制。**明确不做**：端到端LLM交易、DRL实盘、裸K线LSTM/Transformer价格预测、在线热更新、ML重依赖进生产。影响：F1/F2/F3生产零新依赖、默认行为与现状逐值一致（增强信息+开关，风控闸门仅极端veto且可关）、横截面/情绪毫秒级、LLM异步不阻塞run_cycle、新表与news维度向后兼容；AI不改变第15/16轮"分钟高频成本后不赚钱"的负结果；WP-F1/F2不依赖P2可先行，F4须在P2-②pytest之后；与增补①Tushare路线互补（fut_wsr全历史/ft_limit真实板价提升基本面因子与风控闸门质量）。
- **第16轮增补①（Tushare引入可行性，2026-09-01 深夜，只分析未改代码，新增《Tushare引入可行性分析.md》约11KB）**：GitHub `waditu/tushare`为BSD-3但**开源SDK实质停更于2020-03**（412 commits、696 issues、#1867指pip新包源码未开放且bs4锁死），真正有价值的是Tushare Pro的HTTP JSON服务（api.tushare.pro，**本机POST实测通道可达**：无token返回HTTP200/code40101，用现有http_client即可裸调、无需装SDK）。**积分硬门槛（官方doc_id=108逐行核实）**：注册仅120分，**期货接口全部2000分起步**（fut_basic/期货trade_cal/fut_daily含settle结算价/fut_mapping主力映射/fut_holding(2002起)/fut_wsr仓单(2006起分仓库)/fut_settle结算参数），ft_limit盘前涨跌停价+交易所最低保证金率m_ratio要**5000分**，期货分钟ft_mins是积分外**单独付费2000元/年、实时1000元/月**；2000分=高校学生/教师认证可申请否则捐助约200元、5000分约500元。**能补的短板**：①ft_limit.m_ratio补futures_margins.csv留空的exchange_margin(5000分)；②fut_wsr全历史把库存分位从东财3个月窗口升到多年(2000)；③官方期货trade_cal干掉每年12月手动维护STATIC_HOLIDAY_RANGES(2000)；④fut_daily具体合约+settle+fut_mapping让回测脱离主连比例复权近似、改结算价盯市(2000)；⑤ft_limit真实板价替代FUTURES_LIMIT_MOVE常态档硬编码(5000)；⑥龙虎榜全历史/合约元数据/交易所手续费基准交叉校验/南华指数(2000)。**帮不上**：实时行情(Pro盘后21:05更新无免费实时)、分钟K(收费，继续靠minute_bars自采)、6个新闻源、期权实时T链/IV(opt_daily仅日线且5000分)。**裁决**：方案A(推荐)仿tdx_bars做`tushare_client.py`零依赖可选适配层，token走环境变量、无token/积分不足/断网全软降级、现源全保留为主、只做日频盘后增强，**对现有使用零影响**；方案B(否决)pip install拖pandas/numpy/lxml/bs4全家桶破坏零依赖架构；方案C(坚决否决)替换主源会断实时监控三条腿。落地顺序T1官方日历→T2仓单全历史→T3元数据/主力映射→T4回测具体合约(均2000分内)→T5保证金/板价(5000分)→T6龙虎榜时序等；最小高性价比=T1+T2。定位：日频盘后/历史深度/官方基准的第二信源，永不成唯一源。

- **当前状态（第16轮 WP-E 组合资金账户已完成，2026-09-01 深夜）**：新增 `portfolio.py`（**947行，零新增运行依赖**），把多品种回测从"按交易序列复利近似"升级为**共享一个资金池的组合账户**：统一时间轴逐bar盯市（静态权益=期初+已实现、动态权益=静态+浮盈、保证金占用=Σ价×乘数×手数×公司保证金率、可用=动态权益−占用、风险度=占用/动态权益）、权益曲线、风控强平；分钟信号复用 intraday_backtest、日线信号复用 backtest，**撮合口径与既有两套回测完全一致**（信号i收盘确认i+1开盘成交、入场当根不查止损、同根双触按止损、分钟日终强平、owner判平今、锁板整根封死）。新增 `tools/build_margin_table.py`（184行）从**用户开户公司银河期货**官网保证金比例页（yhqh.com.cn/col71/27539，静态table，取纯字母代码品种基础档+**投机档**）半自动生成 `data/futures_margins.csv`（64品种0缺失：sym/name/exchange/broker_margin/limit_basic/multiplier/as_of/source，exchange_margin 无干净免费源留空不编造）；`config.py` 加 PORTFOLIO_* 参数段（473→493行）；`report.py` 看板加"组合账户回测""组合交易CSV"两页签（11→**13页签**，926→928行）；顺带修复 `backtest.resolve_codes` 主连双零退化（见下）。**生产现 32 个 py、11082 行**（根目录30+tools2）。
  - **三种手数分配（--sizing）**：equal_notional 等名义（默认每品种目标名义15%）/equal_risk 等风险ATR（1%风险预算÷1.2ATR止损定手数）/score 按综合分档（轻仓5%·分批10%·强信号15%）；约束链=策略目标手数→单品种名义上限30%→板块名义上限60%→可用资金×0.99缓冲→同时持仓≤12，每档拦截都计入报告"未开仓原因分布"（不静默丢信号；高价品种如CU单手名义52万、15%权重买不起1手是真实约束非bug）。
  - **强平状态机（本轮修正过两处，勿回退）**：风险度≥强平线(默认1.0)触发后，按浮亏最大优先整仓平，**一路砍到安全线(默认0.8)以下**（不是刚跌破强平线就停，否则下一根反复触发；未触发看强平线、已触发看安全线，safe 构造时 min(safe,liquidate) 保护）；强平后补记同时间戳曲线点（末点为强平后状态、max_dd保留强平前尖峰）。**被砍品种当前时刻可能无bar（无夜盘品种在夜盘时段）**：价格沿用最近收盘价，平今判定走新增 `SymbolFeed.owner_at(t)`（bisect 取≤t 最近bar的结算交易日owner），直接 by_dt[t] 会 KeyError（本轮真实抓到并修复）。
  - **保证金源换源过程（用户指定，勿再用回首版）**：先实测 akshare 本机源码，futures_rule_em 对应东财 GetPZJYInfo（eastmoneyfutures.com 及 www/portal 三域名）当晚均返回 HTML 壳非JSON（接口已变更，不逆向、放弃）；首版用国君期货日历表已弃；**用户明确其开户在银河期货、必须以银河页为准**，两家确有差异（AG 35% vs 国君30%、RB/HC/SS/SP 12% vs 11%、MA/TA/PF/SH 17%、EC 33% 等，实盘以银河为准）。乘数银河页不提供，工具内置"每手报价单位个数"口径并与 futures_fees.csv 交叉校验，唯一口径差异 **JD 鸡蛋**：报价元/500kg、1手=10个报价单位→保证金/名义乘数=10，而费用表按吨=5，用 MULT_KNOWN_DIFF 白名单显式处理（多源核实鸡蛋5吨/手，两口径各自自洽）。
  - **顺带修复的既有公共退化**：`backtest.resolve_codes` 把主连拼成 **RB00**（meta["code"]="RB0" 又 +"0"），而新浪日K接口对双零返回 `var t=(null)`、只认单零 RB0（多组请求实测确认；分钟接口同样单零、生产监控本就用RB0未受影响），导致现有日线回测全部"样本不足"；已改为直接用 meta["code"]、自由输入分支用 isalpha 判断（纯字母补0、具体合约如RB2601原样），修复后 backtest --limit 恢复。
  - **验证**：**37条零网络合成断言全PASS**（两品种一赚一亏逐点对齐 t1动态权益102500/占用10750/风险度、连续下跌强平B浮亏-30000先于A-20000被砍且砍后风险度0.64<0.8、缺表回退0.12并登记fallback_margins、三种sizing手数、板块/可用/持仓上限、1手买不起拒开、平今费开5+平今16.5=21.5、无bar时刻连砍两仓）；compileall全过；真实端到端——**全64品种30m**（140交易日/6099笔/期末758495、-24.15%、夏普-3.64、最大回撤26.87%、平均风险度17.3%、0强平、保证金64/64命中0兜底）、**全64品种日线250日**（341笔/+10.56%/夏普0.84/最大回撤9.32%/多头+14.16万空头-3.59万/0强平）、另用强平线15%压测触发2900次强平验证引擎同步与报告链路；`main.py --once --no-launch` exit 0。**诚实结论与第15轮一致**：分钟高频信号真实成本后不赚钱（EC集运欧线单品种-7.57万、全市场手续费8.32万），日线动量组合口径为正且盈亏几乎全来自多头；默认100万账户同时持仓≤12导致大量信号因持仓上限未开（分钟27203次未开中23821次是持仓数上限），属设计风控、报告如实呈现。CLI：`--all/--codes/--limit/--daily/--days/--period/--sizing/--equity/--per-symbol/--max-concurrent/--risk-liquidate/--risk-safe/--entry/--stop-atr/--target-atr/--no-cost/--margins-file`；固定产物 reports/portfolio_report.txt + portfolio_equity.csv（逐bar：dt/static/float/equity/margin/available/risk/drawdown/npos）+ portfolio_trades.csv（含实际手数/平今/费用/净盈亏/forced强平标记/entry_score/margin_rate）。**下一站=P2 工程项（图表化看板/pytest回归/配置外置/数据源降级链）；遗留可选项：实时一档tick快照自采、交易所官方IV交叉校验、生意社基差反爬。**
- **当前状态（第15轮 WP-D1/D2 日内/平今回测已完成，2026-09-01 晚）**：新增 `intraday_backtest.py`（**712行，零新增依赖**），回放自采 `minute_bars` 分钟库做 vnpy 式 bar 内撮合的日内/平今回测；`config.py` 加 INTRADAY_BT_* 参数段与 64 品种常态涨跌停表 `FUTURES_LIMIT_MOVE`（438→473行）；`report.py` 看板加"日内/平今回测""日内回测交易CSV"两页签（924→926行）。**生产根目录现 29 个 py、9825 行**。核心口径（已被 T1-T15 合成断言锁定，勿回退）：①信号在 bar i 收盘确认、i+1 开盘成交（无未来函数），入场当根不查止损；②止损/止盈预埋停止单，跳空越过以开盘成交、bar 内触及以触发价成交、同根双触保守按止损；③**平今/平昨与日终强平按"交易所结算交易日" owner_of_dt——21:00后夜盘归属 next_trade_day(当天)、凌晨延续归属当日、周五晚归下周一（离线 trade_calendar 自动跳节假日），前晚夜盘开+次日日盘平=平今；绝不沿用 utils.trade_owner_date 的"夜盘开盘日"口径**（早期误用已修）；④日内模式交易日最后一根收盘强平（入场当根即末根也强平，不隔夜），`--swing` 摆动模式允许跨日、`--max-bars` 限持仓根数；⑤锁板=前一交易日收盘×(1±品种常态幅度)、整根封死在板价容差0.8%内才拦截（买单查low≥上限、卖单查high≤下限），v=0不成交，离场锁板顺延计 blocked_exit；⑥费用开 open、平按 leg 走 today/close 真实券商费率，输出每手人民币（乘数取费率表 multiplier）+"若按平昨"对照与平今节省/加收合计。CLI：`--codes`(RB/RB0/中文名/逗号/留空=全64)、`--all --period {1,5,15,30,60} --aggregate-from {0,1,5,15,30}`(细周期现场聚合，1m聚合时做**目标周期边界相位对齐**)、`--lookback/--sig-window/--entry/--stop-atr/--target-atr/--max-bars --swing --fee-rate/--slip-rate/--fees-file/--no-real-fees/--no-cost --limit-move/--no-limit-filter/--no-stable --workers`；固定产物 `reports/intraday_backtest_report.txt` 与 `intraday_backtest_trades.csv`（28字段，均 utf-8-sig）。**验证（首轮+二次复验一致）**：T1-T15 零网络合成断言全 PASS；compileall 全过；DB integrity ok、minute_bars 首轮327360根（夜盘常驻自采会持续增长，二次复验时已33万+，属正常积累）；**二次独立复验（2026-09-01晚另起会话）：重建29项零网络断言全PASS（无未来函数/止损止盈跳空与同根双触按止损/日终强平/平今平昨归属/SS平今优惠/锁板拦截与零量不成交/周期聚合/交易日跨节假日）、5条真实路径(日内/swing/1m聚合5m/5m直读/60m)0错误、main --once 1729行零ERROR、全量重跑64品种10101笔与首轮逐值一致=可复现**；全 64 品种 30m 端到端 **10101 笔**、18 组稳定性网格，日内模式今/昨=10101/0、摆动模式 RB/CU 出现平今157/平昨93；1m直读(3交易日105笔)、1m边界对齐聚合5m（与库内原生5m重叠204根 **h/l/c 100%一致**、开盘价24根有段首tick差异(15根差1跳、最大差4跳、均值0.18)=自采1m段首tick精度差异非算法问题）、--no-cost 路径均实跑通过。**诚实结论**：默认参数(入场±1.5/1.2ATR止损/2ATR止盈/万1滑点)30m 全市场成本后净均收 -0.022%、累计-91.9%，参数网格方向一致（越严越少亏）但无成本后稳定盈利区；LH/PP/AG/FU 窗口内为正、EC集运因费率极高(0.191%)亏最重；本轮交付可信检验工具与**负结果**，不粉饰。**修复的真实缺陷**：minute_bars_for_sym 第三位置参数是 since 不是 limit（误传根数被当时间过滤，已改 limit=关键字）。**下一站=第16轮 WP-E portfolio.py 组合账户+保证金率表（候选 akshare futures_rule_em，落地前先本机实测）。**
- **第14轮状态（WP-D0 + 当晚"数据源二次调研增补"，已完成，2026-09-01 晚）**：按用户要求全面排查更优免费数据/新闻接口并实测对比，只采纳"确定不更差"的两项升级，**零新增强制依赖、28个生产py（9071行）不变数量**：①**新浪1m主源化（核心）**——补测发现新浪 `getFewMinLine&type=1` 一分钟K同样固定1023根、64/64品种零断连、主连RB0与具体合约RB2701/MA610均可取且逐价一致（回溯约2.5交易日），**推翻第14轮"新浪无1分钟"的漏测误判**；`MinuteCollector.collect` 改为全周期(1/5/15/30/60m)统一选源链"新浪主连→通达信(点亮才用)→东财具体合约兜底"，1m不再依赖被IP限流的东财push2his、也不再要求主力合约探测完成；config 的1m LMT 提至1023。②**实时行情东财兜底**——`futures_data.fetch_quotes` 新浪正常时零额外请求，仅对新浪缺失品种用东财 push2 主连快照（secid=市场号.品种小写m，如113.rbm，f111=持仓量）补齐；**实测东财限流会从push2his扩散到push2、且限流边缘/push2delay口会返回HTTP200但f111=1、价格错位的残缺数据，故加 latest>0/prev>0/oi≥100 有效性校验，残缺即弃，宁可不兜底也不污染**。**排查后明确不采纳（勿重复踩坑）**：腾讯免费接口不覆盖国内商品期货（穷举nf_RB0/rbm/rb2701全none_match）；财联社cls老接口下线、新接口要sign签名+指纹对抗（akshare已弃用）；富途live-list/汇通fx678/百度股市通期货新闻=404或需token/风控；金十flash-api备用域缺sub_id而www域正常；tqsdk/Tushare需账号积分付费。**新闻维持现有6源（东财7x24/新浪滚动/华尔街见闻/同花顺/新浪7x24/金十）当晚复测全部健康，不增不减**。验证：21项零网络合成断言全PASS（含残缺拦截、新浪全齐时东财零请求）；main --once 零ERROR，分钟once 320任务**全部src=sina、空/失败0**；minute_bars 由261888增至**327360根=64品种×5周期×1023（1m从0补到65472根/64品种，08-26起）**，零非法OHLC。改动文件 futures_data/intraday_bars/config/main/report/tdx_bars 六个；《数据源选型与通达信替换可行性分析.md》新增第八节。**下一步仍是第15轮 WP-D1/D2（编号未被本次增补占用），且现在1m主连数据已实有，日内回测数据基础更扎实。**
- **第14轮原始状态（WP-D0，已完成，2026-09-01 19:30；注意：本条中"新浪无1分钟/1m=东财"的表述已被上方增补条推翻，以增补条为准）**：分钟K自采库落地。新增 `tdx_bars.py`（200行，通达信可选源）、重写 `intraday_bars.py`（现304行，三源选源+聚合纯函数），storage 第7表→**第8张表 `minute_bars`**（UNIQUE(contract,period,bar_dt)、保留400天），main 启动 once 小回填+后台分钟自采线程（交易5min/非交易30min），config 加 MINUTE_*、report 加分钟库覆盖。**三源实测选型（关键，勿重复踩坑）**：①新浪主连 `getFewMinLine`（现确认 1/5/15/30/60m 全支持，主连代码 RB0 免换月，固定1023根，实测64/64品种全覆盖、零断连、30m回溯6月/60m回溯12.5月、1m回溯约2.5交易日）=**主源**；②东财 `push2his` 当晚对本机 IP 持续 RemoteDisconnected（http/https/主域/镜像/日线全断，连接级异常短退避约3~5s熔断60s自愈），仅作具体合约最后兜底；注意东财 **datacenter-web 基本面域名当晚秒回稳定**，东财要分域名看，不是全站挂；③通达信 pytdx 穷尽实测**不可用**：7709 官方103台仅28台TCP可达且0~120全市场号只有股票无期货，期货所在7727扩展口0台可达、7720/21/29/30全关，且协议层无库存/仓单/龙虎榜/快讯——**东财不能整体替换为通达信**（完整论证见《数据源选型与通达信替换可行性分析.md》）；tdx_bars 启动并发探测（20线程/15s上限，实测1~5s判定），能取期货才点亮、否则零成本降级，pytdx 函数内延迟导入、**不进 requirements 强制依赖**。选源（增补后）：**全周期=新浪主连→tdx→东财**。验证：55项零网络合成断言全PASS；完整回填 minute_bars 现为 **327360根=64品种×5周期×1023**，零非法OHLC、零重复键，二次增量净增0（幂等）。生产根目录现为 **28个py、9071行**；运行依赖仍只 requests/uiautomation/websocket-client。**下一步=第15轮 WP-D1/D2 日内/平今回测（minute_bars 已备好且1m已补齐，主连比例复权序列+aggregate_bars 聚合可直接用）。**
- **第13轮归档（WP-C 基本面数据包，已完成）**：新增 `fundamental_factors.py`（151行，纯函数零网络）与 `fundamental_data.py`（188行，东财直连），改 config/analyzer/storage/main/report，数据库6表→**7表**（新增 `fundamentals` 日频快照表，UNIQUE(sym,trade_date)）。四个子因子方向均为"现货/主力偏紧→正分"：①库存/仓单=东财 RPT_FUTU_STOCKDATA 时序算近约3个月滚动分位(低库存偏多)+周环比去化(去库偏多)，样本<15不算；②龙虎榜=东财 RPT_FUTU_DAILYPOSITION TYPE=2 取主力合约前20席会员多空合计，净多率60%+较昨日边际40%；③期限carry=直接复用第11轮 contracts.term_structure 的 annual_carry（Back近高远低=现货紧偏多，零新增请求）；④基差=生意社当日表（现货升水偏多）。权重 库存.40/龙虎.30/carry.20/基差.10，缺项按可得权重重新归一、满分±FUND_MAX_SCORE=1.5 进综合分。**实测数据源结论（勿重复踩坑）**：东财统一入口稳定（库存覆盖61品种、龙虎榜61、carry64；AP苹果/JD鸡蛋/PK花生/EC集运/LG原木/SC原油无标准仓单时序自动降级）；SHFE官网dailystock在20260824后全404、DCE官网持续412 WAF、CZCE是xlsx、生意社有HW_CHECK的JS-cookie反爬（大概率抓不到，识别即返回None由其他子因子补位，绝不编造）。main 新增 fundamentals_loop 日频后台线程（16点后并发刷库存+基差）+ _prefetch_rank 分析前8线程并发预取龙虎榜（fetcher内日缓存，当天仅首轮请求）；report 期货总表后加【基本面速览】多空各Top5、重点明细加基本面行。固定解释器 `D:\Python\python.exe`（默认 python 是无 requests 沙箱版，验证一律用它）。第12轮WP-B及之前各轮成果均保留不变。
- **第10轮新增能力**：①一次性转换真实券商表为 `data/futures_fees.csv`，64个项目品种全覆盖，明确选择“投机”行（忽略更低的套保行），字段含按金额费率、按手数固定金额、合约乘数、交易日；②`backtest.py` 默认读取真实费率，按金额费与按手数固定费相加，用 `fee_yuan/(成交价×合约乘数)` 把固定元/手折算为收益率，日线多日持仓按“开仓+平仓”计费，不使用平今；③`backtest_trades.csv` 扩展为毛收益、手续费率、滑点率、总成本、净收益、开/平/往返每手人民币费用、乘数、费用模式；④新增 `--fees-file`、`--no-real-fees`，`--no-cost` 同时关闭真实手续费和滑点，缺表自动回退统一比例费率；⑤README、GitHub对标清单同步。
- **手续费关键口径**：Excel同一品种可能有“投机/套保”两行，必须取投机；“按金额”和“按手数”可同时存在（如多数品种6e-7保障基金+固定元/手），必须相加；平今字段只对未来同日开平/分钟回测有用，当前日线回测不用。合约乘数表在 `tools/build_fee_table.py`，已覆盖64品种，关键特殊值：AU=1000克、AG=15千克、SC=1000桶、EC=50点、LG=90立方米、J=100、JM=60、LH=16、PR=15、SH=30、PS=3。
- **最终验证（2026-09-01 09:35~09:36）**：全部生产py `compileall` 通过；合成断言验证RB/AP按金额+按手数公式、固定费折算、净收益=毛收益-手续费-滑点、兜底模式；真实 `backtest.py --codes RB0,MA0 --days 160 --workers 2` exit 0，26/26笔命中真实费率表，净口径 n=26、胜率34.6%、均收+0.28%、累计+4.8%、年化+7.1%、最大回撤17.2%、夏普0.31，毛累计+5.6%，成本拖累累计-0.9个百分点；同数据对比旧统一费率，真实费率累计低约0.04个百分点。最终 `main.py --once --no-launch` exit 0，64/64主力合约、OpenVlab 63品种，当前轮日志 ERROR/Traceback/CRITICAL=0。
- **最新落盘（2026-09-01 09:36）**：`reports/backtest_report.txt` 和 `reports/backtest_trades.csv` 已按真实费率刷新；原始券商xlsx归档到 `data/投资者手续费率查询-2026-08-28.xlsx`；`reports/latest_report.txt` 为09:36全品种轮次；`data/monitor.db` integrity_check=ok，quotes=448、signals=409、news=964、options=560、signal_outcomes=360。
- **下次接续优先级**（⚠️ 第21轮更新：候选②pytest 已完成，见顶部"第21轮"，tests/ 已固化174用例；本条其余内容保留作历史脉络）：WP-A/B/C/D/E 五个工作包已全部完成（第11~16轮）。下一步进入 **P2 工程项**（《GitHub对标与改进清单》/落地方案第6节）：候选=①看板图表化（ECharts权益曲线/回撤，当前iframe嵌txt，**=当前下一站 P1-3**）、②~~pytest 回归~~（✅第21轮已完成 tests/ 174用例）、③配置外置 config.json/.env、④数据源主备降级链。遗留可选项（非P2主线）：实时一档 tick 快照自采表（逼近真实滑点的唯一免费长期路径）、交易所官方IV(option_vol_*)曲面交叉校验、生意社基差反爬绕过（软降级预留位、经济含义大部分由carry覆盖）。能力天花板不变：免费源无历史L2逐笔、远月深虚值不插值、**保证金率为期货公司披露的静态档（非实时、临近交割/长假会上浮，以公司通知为准）、交易所基准档无干净免费源故CSV留空不编造**、东财库存免费窗口仅约3个月。
- **仍须坚持的边界**：回测只回放技术面，不能声称复原历史新闻/机构/实时量仓；分钟K免费窗口固定1023根（1m约2.5交易日、5m约3周、15m约3月、30m约6月、60m约12.5月），**2026-08-26之前的1m、以及各周期免费窗口之外的历史分钟K无法复原**，长期深度靠 minute_bars 常驻自采（自2026-09-01起积累）；当前锁板是收盘贴边+涨跌幅阈值的疑似识别，不是逐笔盘口；真实手续费只到2026-08-28账户费率，后续费率调整需重新运行 `tools/build_fee_table.py`；保证金只是点值估算，不代表交易所/期货公司精确保证金。
- **用户附件说明**：早前重复上传的 `https://aka.doubaocdn.com/s/1jS6ahdTU4` 已确认是“豆包开学季”活动文案，与量化项目无关；本轮有效附件是本地 `C:\Users\Lenovo\Desktop\投资者手续费率查询-2026-08-28.xlsx`。

## 一、项目概况
- 路径：`C:\Users\Lenovo\Desktop\量化\futures_monitor`，Python 期货全品种（64品种+期权）常驻监控程序。
- **【第47轮最新规模，优先以此为准】生产 py 66 个共 29485 行（根45→46含新增 portfolio_risk.py 347行、tools19→20含新增 tools/portfolio_risk_lab.py 241行；上轮64/28897）；tests 35→36文件（+conftest37）/572用例约8s全绿（新增 test_portfolio_risk 19例+2 selftest注册，上轮549）；storage 仍14业务表、看板15图15页签（本轮不动）；当前 tag v0.47.0；**第47轮 G5研究侧①②③：portfolio_risk 相关矩阵/历史&参数VaR/ES/原油压力 + portfolio_risk_lab 四权重风险报告，只读面板、主链/risk_gate/综合分/sizing 零改动、默认8品种双哈希不变。**
- **【第39轮最新规模，优先以此为准】生产 py 57 个共 24604 行（根目录42不变 + tools14→15含本轮新增 tools/factor_regime.py；上轮56/24226）；tests 仍30文件（+conftest31）/440用例约6.8s全绿（test_tools_selftest 注册 factor_regime +1 + compileall 随新生产py+1，上轮438）；storage 仍14张业务表（本轮不碰生产 monitor.db，纯读研究面板 cache/research_panel.db 全64品种61353行）、看板15图15页签（本轮不动）；当前 tag v0.39.0；实时主链/综合分口径零改动（factor_regime 仅研究侧、只读面板、不被任何根/tools生产模块 import）；TSMOM/XSMOM 已归档、carry 待跟踪、5表达式因子 research 不进分；本轮 G29续落地 regime分层/换手/衰减形态（真实定位'长周期动量ret252/tsmom252仅低波regime有效H20低波IC约+0.10、高波转负=动量高波崩溃，信号lag1自相关0.83/月度换手约0.39，近零IC无干净指数/幂律衰减'，只记录不改分）；PAPER_ENABLED 仍默认False休眠。**
- **【第38轮最新规模，优先以此为准】生产 py 56 个共 24226 行（根目录41→42含本轮新增 factor_expr.py + tools13→14含本轮新增 tools/expr_research.py；上轮54/23178）；tests 29→30文件（+conftest31）/438用例约6.7s全绿（新增 test_factor_expr.py 37例 + test_tools_selftest 注册 factor_expr/expr_research +2 + compileall 随2个新生产py+2，上轮397）；storage 仍14张业务表（本轮不碰生产 monitor.db，纯读研究面板 cache/research_panel.db 全64品种61353行）、看板15图15页签（本轮不动）；当前 tag v0.38.0；实时主链/综合分口径零改动（factor_expr 仅被 expr_research 引用、main/analyzer 零 import，旧技术/基本面因子保持过程式原实现）；TSMOM/XSMOM 已归档、carry 待跟踪、5个表达式因子 research 不进分；本轮 G25 落地白名单表达式引擎+因子治理（真实全64品种 parity 303309点 maxAbsDiff=0、表达式对齐实时ret5仅1.1e-16、表达式因子前向|IC|<0.06无稳定预测负结果诚实）；PAPER_ENABLED 仍默认False休眠。**
- **【第37轮最新规模，优先以此为准】生产 py 54 个共 23178 行（根目录41不变 + tools12→13含本轮新增 tools/factor_health.py；上轮53/22603）；tests 仍29文件（+conftest30）/397用例约4.7s全绿（G21续 test_research_panel 14→18、G29 factor_health selftest注册+1、compileall随新py+1，上轮391）；storage 仍14张业务表（本轮不碰生产 monitor.db；研究面板 cache/research_panel.db 已补齐全64品种61353行）、看板15图15页签（本轮不动）；当前 tag v0.37.0（两次 commit：G21续 9dca232 / G29 8e33dae）；实时主链/综合分口径零改动（factor_health 仅研究侧、只读 monitor.db mode=ro、不被 main import）；TSMOM/XSMOM 已归档、carry 待跟踪不进分；本轮 G21续让 tsmom/xsmom/carry 可 --panel 读已复权面板（真实逐值等价 maxAbsDiff=0、缺省网络路径不变），G29 因子体检定位'机构动向次日健康反向IC-0.230/原油联动次日健康+0.276/日频单因子无稳定预测'，只体检不改线上权重；PAPER_ENABLED 仍默认False休眠。**
- **【第36轮最新规模，优先以此为准】生产 py 53 个共 22603 行（根目录41含本轮新增 factors_catalog.py + tools12含本轮新增 panel_builder.py/pit_audit.py；上轮50/21672）；tests 29文件/391用例约4.7s全绿（新增 test_research_panel.py 14用例 + selftest注册+3 + compileall+3，上轮371）；storage 仍14张业务表（本轮不碰生产 monitor.db，研究面板落独立 cache/research_panel.db）、看板15图15页签（本轮不动）；当前 tag v0.36.0；实时主链/综合分口径零改动（三新模块均研究侧、不被 main import）；TSMOM/XSMOM 已归档、carry 为待跟踪候选不进分；本轮 G21 落地特征注册表+标准面板+PIT/训练-服务一致性（真实11品种11154行、parity零不一致）；PAPER_ENABLED 仍默认False休眠。**
- **【第35轮最新规模，优先以此为准】生产 py 50 个共 21672 行（根目录40 + tools10含本轮新增 tools/attribution.py；上轮49/21002）；tests 28文件/371用例约4.6s全绿（新增 test_attribution.py 16用例 + tools selftest/compileall 各+1，上轮353）；storage 仍14张业务表、看板15图15页签（本轮不动）；当前 tag v0.35.0；实时主链/综合分口径零改动（attribution 仅研究侧、只读 monitor.db mode=ro、不被 main import）；TSMOM/XSMOM 已归档、carry 为“长样本成立/近窗边缘”待跟踪候选不进分；本轮 G28 归因定位“技术共振管短周期/原油联动管次日/机构动向次日反向/超额靠板块内选择”，仅复盘不改任何线上权重；PAPER_ENABLED 仍默认False休眠。**
- **【第34轮最新规模，优先以此为准】生产 py 49 个共 21002 行（根目录40含本轮新增 term_history.py + tools9含本轮新增 carry_eval.py；上轮为47/19945）；tests 27文件/353用例约4.6s全绿（新增 test_term_history.py 8用例+carry_eval selftest，上轮342）；storage 仍14张业务表、看板15图15页签（本轮不动）；当前 tag v0.34.0（代码 commit 4a4a095）；实时主链/综合分口径零改动（term_history 不被 main import、carry_eval 仅研究侧）；TSMOM/XSMOM 已归档，carry 截面为'长样本成立/近窗边缘'待跟踪候选、不进分不挂影子；PAPER_ENABLED 仍默认False休眠；逐合约研究缓存 cache/term_history.db（gitignore，已缓存全64品种）。**
- **【第29轮最新规模，优先以此为准】45 个 py 共 18178 行（根目录39+tools6，本轮新增 metrics.py，并改 portfolio/report/backtest/charts/config 五件+2测试文件）；tests 24个 test_*.py/304用例约3.5s全绿；storage 仍14张业务表；图表看板15图（新增G3水下/滚动夏普/月度热力）、实时看板15页签；当前 tag v0.29.0(commit 2920e2e)；PAPER_ENABLED 默认 False 休眠，手动 true 开影子。**
- **【第28轮最新规模，优先以此为准】44 个 py 共 17326 行（根目录38+tools6，数量未增、本轮改 paper_broker/main/report/charts/storage 五件+4测试文件）；tests 23个 test_*.py/295用例约3s全绿；storage 14张业务表；图表看板12图、实时看板15页签（PAPER_ENABLED 休眠时显14、无纸面页签）；当前 tag v0.28.0(commit e53e4fc)；PAPER_ENABLED 默认 False 休眠，手动 true 开影子。**
- **【第26轮最新规模，优先以此为准】43 个 py 共 15938 行（根目录37+tools6，数量未增、改backtest/storage/config/backtest_validation四件）；tests 21个 test_*.py/255用例约3s全绿；storage 现11张业务表（第26轮增 backtest_runs；另sqlite_sequence为AUTOINCREMENT内部表，共12个表对象）；运行依赖仍仅 requests/uiautomation/websocket-client，零新增。以下为第25轮历史口径：**
- 规模：**39 个 py 共 14663 行（根目录34 + tools5；第23轮 charts.py 732→744、report.py 968→959；main.py 907、portfolio.py 991、intraday_backtest.py 712、config.py 561、storage.py 738、futures_data.py 621、charts.py 744、backtest.py 587、report.py 959、option_strategies.py 506、cross_section.py 166、signal_calibrator.py 271、risk_gate.py 178、utils.py 301、iv_surface.py 267；tools/factor_eval.py 435、tools/backtest_validation.py 695、tools/build_ml_samples.py 356 等）**。运行依赖仍仅 requests/uiautomation/websocket-client（requirements.txt）；pytdx 为可选延迟导入；**ECharts 5.5.1 是 assets/ 下的本地前端资源（非 Python 依赖）**。tests/ 回归目录：conftest.py+pytest.ini+README+17个 test_*.py，1605行/191用例（第23轮 test_charts 14→16），`D:\Python\python.exe -m pytest` 约2.3秒全绿，pytest 仅 dev、不进 requirements。
- 运行环境：用户真实解释器为 `D:\Python\python.exe`（已装 requests 2.34.2、websocket-client）；命令行默认 python 是无 requests 的沙箱版，验证代码一律用 `D:\Python\python.exe`；node v20.20.2 可用于看板 JS 验证。
- 启动：双击 `start_monitor.bat`（第6轮已固定用 `D:\Python\python.exe main.py %*`，缺失才回退 PATH 的 python；非0退出15秒自动重启，正常/Ctrl+C/--once 不重启）；`--once` 单轮、`--no-launch` 不开期货通、`--force-review` 立即生成复盘。
- 数据源：新浪7x24/金十（每轮）、新浪行情（10s原油+每轮全品种）、全网扫描4源+全球金融行情（3分钟）、交易可查机构观点（10s）、OpenVlab期权日历（30min）、可选9222端口浏览器直读（30s）；**第6轮新增交易日历：新浪螺纹主连RB0日K（主）+东财指数日K（备）+内置2026法定休市静态表，缓存 cache/trade_dates.txt（trade_calendar.py）。注意新浪旧明文日历 klc_td_sh.txt 已加密失效，勿再用。**；**第14轮新增分钟K自采三源（intraday_bars/tdx_bars，当晚增补后全周期统一）：新浪主连 getFewMinLine（1/5/15/30/60m各1023根、64品种全覆盖）为全周期主源、通达信 pytdx 可选冗余（probe取不到期货自动不启用）、东财 push2his 具体合约最后兜底（限流熔断自愈），落 minute_bars 表常驻自采；实时行情新浪 hq.sinajs 为主、东财 push2 主连快照仅补新浪缺失品种（futures_data._fetch_quotes_em，残缺oi<100即弃）。**

## 二、已完成的需求逐轮记录（第1~16轮详见本节；第17轮调度边沿、第18轮WP-F1、第19轮WP-F2、第20轮防过拟合、第21轮pytest回归体系、**第22轮看板图表化**、第23~29轮、**第30轮TSMOM时序证伪、第31轮XSMOM截面弱正不达标** 的完整记录统一见顶部"零、最新快速接续摘要"，本节不再重复）（前五轮均已验证；第6轮P0、第7/8/9/10轮P1、第11/12/13/14/15/16轮 WP-A/B/C/D0/D1D2/E）

### 第1轮：轮动节奏/复盘/日切/实时更新/置顶
1. 交易时段 9:00-11:30、13:30-15:00、21:00-23:00（`config.SESSIONS`）；每时段开盘前30分钟每5分钟一轮（对齐5分钟刻度），之后每20分钟一轮（对齐20分钟刻度），非交易时段每1分钟。核心 `utils.next_cycle_time()`；`utils.rotation_desc()` 在报告块头标明本轮时间/节奏/下一轮计划。
2. `reports/daily_review.txt`：23:00 后自动生成一次（`report.build_daily_review/write_daily_review`，run_cycle 末尾），汇总当日全部轮动+当日新闻（cache/news_YYYYMMDD.jsonl）；**永久不删、新复盘置顶、同日重生成替换**；新闻跨重启去重；内存无策略时从当日末份交易归档兜底提取期权策略。
3. 次日首次启动 `report.daily_rollover()` 清 latest_report/signals/history/offhours_report/offhours_history 五个文件中昨日及以前内容（跨零点常驻也触发）；daily_review 不动。
4. `reports/实时报告.html` 多页签看板（双击 `查看实时报告.bat`），iframe 加载六个报告，不用关闭重开。
5. reports 下所有文件新轮一律写最前。
6. 落盘鲁棒性：`report._safe_write` 占用重试3次、文件独立写入；修复 signals.csv 被 Excel 锁定导致整轮保存失败的旧 bug；报告 txt 统一 utf-8-sig（BOM）写入、读取透明剥离。

### 第2轮 → 已被第3轮取代（保留结论）
看板不再固定5秒刷新；最终方案见第3轮（状态文件探测、ts 变化才重载）。

### 第3轮：原油急动紧急轮动 + 看板事件驱动
- 原油10s线程中 `OilTracker.detect_jump()`：布伦特/WTI 任一在 OIL_JUMP_WINDOW_SEC(60s) 内涨跌≥OIL_JUMP_REL(0.6%) 触发，冷却 OIL_JUMP_COOLDOWN_SEC(180s)。
- 触发即"插队"跑完整 run_cycle（**全部品种**实时重算，非只能化链；save 同步写全部报告+状态+看板），块头标 [原油急动紧急轮动]、正文 ★紧急触发。
- **计划轮动点不推移**：定时轮后只算一次 nxt=next_cycle_time，紧急轮在内层循环插队、nxt 不变；剩余等待必须用真实 remaining（仅 nxt 已过时兜底5秒，不能用 max(5,remaining)，否则紧急轮后多等）。
- 看板：每轮落盘写极小 `reports/report_status.js`（window.REPORT_STATUS={ts,cycle,kind,emergency,emergency_tag,rotation}）；html 每10秒用动态 `<script>` 探测（file:// 下 script 标签可用、fetch 不行），**ts 变化才重载 iframe**，nextMark 倒计时仅展示；启动时也写一次初始 status。

### 第4轮：全网数据查找（每3分钟，新闻/金融/突发事件）
- 新增 `web_scan.py`：4个文字源（东财7x24 getFastNewsList **必须带 req_trace 毫秒时间戳**；新浪滚动 feed.mix.sina lid=2516/2509，ctime为unix、署名媒体定可信度；华尔街见闻 api-one-wscn lives，display_time；同花顺7x24 tapp，ctime），单点失败互不影响，实测约140条/轮。
- 金融数据走新浪全球行情 hq.sinajs.cn（gbk 编码）：DINIW美元指数/hf_GC黄金/hf_SI白银/hf_HG美铜/gb_ixic,dji,inx美股/s_sh000001,s_sz399001 A股；字段：hf_昨结=f[7]、gb_日涨跌=f[2]、s_日涨跌=f[3]、DINIW无昨结用扫描间差值。scan类与上轮扫描价比、day类用接口日涨跌，达 WEB_MACRO_THRESHOLDS 合成"【金融数据】…"消息（措辞全部取自既有词典），同指标冷却 WEB_MACRO_ALERT_COOLDOWN=300s。
- 可信度分级（真实优先、存疑后排）：SOURCE_CREDIBILITY/AUTH_MEDIA 白名单；存疑词 WEB_DOUBTFUL_WORDS（传闻/据称/网传/未经证实/疑似…）命中 confidence×0.4 并打 doubtful；factors 的 score/trend/top_items 全部乘 n.confidence（默认1.0），报告 Top 消息标"存疑·"前缀。
- 触发：只对**新消息**（WebScanner._seen，首轮只建基线不触发）算 factors._lex_weight(content,None)×confidence（important×1.6），≥WEB_IMPACT_TRIGGER(1.5) 或 important/含 WEB_BREAKING_WORDS 且≥WEB_IMPORTANT_TRIGGER(1.0) 即触发；_affected 按板块词典+品种名命中列直接影响品种。
- **统一紧急事件**：State 用 `kick` 一个事件 + `last_emergency={"src":"oil"/"web",...}` + `emergency_tag`（旧 oil_kick/last_oil_jump 已删除）；oil_loop/web_scan_loop 都置 kick；wait_with_emergency 内 `_build_emergency` 生成 tag/note（[原油急动紧急轮动]/[全网消息紧急轮动]），插队与"nxt 不推移"机制两源完全一致；web_scan_loop 把 items 喂 state.news.add + report.append_daily_news（复盘也覆盖全网消息）。report.save 的 emark 用 state.emergency_tag；status.js 带 emergency_tag，看板徽章通用化。
- 搜索引擎（Bing/百度）实测被反爬/客户端渲染不可用，改用权威站点直连接口（README 已说明）；财联社接口已失效（404/50101），不要再尝试。

### 第5轮：GitHub 同类项目对标 · 不足与缺失清单（只分析，未改业务代码）
- 任务：在 GitHub 检索同类项目，对比本程序不足/缺失并统一列出。
- 方法：GitHub Search API 用 20+ 中/英文关键词检索（期货/期货监控/期权/futures monitor/sentiment/trading agent LLM/基差库存/greeks 等），精读 15 个高相关项目 README；再逐文件核对全部 16 个 py 源码（确认哪些是真缺失）；夜盘时间经上期所官网核实。
- 产出：`GitHub对标与改进清单.md`（23 条，P0/P1/P2 分级 + 实施顺序），**已放入项目根目录**（TEMP 留有同名副本 C:\Users\Lenovo\AppData\Local\Temp\）。
- **P0 六个实际问题（第5轮当时待修、已与源码逐一核对；已于第6轮全部修复，见下）**：
  1. **夜盘时段实质 bug**：`config.SESSIONS` 夜盘一刀切 23:00，但上期所实际 黄金/白银/原油到次日02:30、铜铝锌铅镍锡/不锈钢/国际铜/氧化铝到次日01:00（官网核实）。23:00 后这些品种仍开盘却被判"非交易时段"：信号错进 offhours 两件套、误输出"开盘后校验"的预测走向、23:00 就生成复盘导致夜盘数据不全。修法：按板块/品种分档夜盘结束时间(23:00/01:00/02:30)，is_trading_time 改"该品种是否在交易"，复盘移到全部夜盘结束(02:30后)或日盘15:00后。
  2. `start_monitor.bat` 用 `python` 而非 `D:\Python\python.exe`，双击可能 ModuleNotFoundError。
  3. `utils.is_trading_time` 只 weekday>=5 判休市，法定节假日空转、调休上班日误判休市；需年度交易日历（akshare tool_trade_date_hist_sina 本地缓存）。
  4. 日志 `logging.FileHandler` 无轮转，monitor.log 无限增长；改 RotatingFileHandler/TimedRotatingFileHandler。
  5. 全项目无 `requests.Session`（已核查），每请求新建 TCP/TLS；改模块级 Session+HTTPAdapter 连接池。
  6. 无看门狗/崩溃自重启/心跳告警；bat 加 :retry 循环或 Windows 任务计划失败重启。
- **P1 功能缺失（10 项）**：⑦无主动告警推送（紧急轮/强信号只能盯看板；建议 winsound+飞书/钉钉/Server酱 webhook，冷却复用现有机制）；⑧无结构化数据库（行情只在内存、重启即丢，建议标准库 sqlite3 建 quotes/signals/news/options 四表）；⑨无信号效果追踪（从不回看建议后实际涨跌、无胜率统计）；⑩无回测（×0.28/×1.6/2-4-6.5/1.35 等参数全拍脑袋；主连须合约内算收益再比例后复权）；⑪基本面缺失（成交量 f[14]/持仓量 f[13] 已解析但 analyzer 完全没用；cache/em_cangdan.html 仓单页无代码读取；缺基差/期限结构/库存仓单/龙虎榜，akshare 有现成接口）；⑫技术指标薄（仅 MA/ATR/HV，缺 MACD/RSI/KDJ/布林/量能/缺口与分钟K线多周期共振，MyTT 单文件可搬）；⑬关键词情绪处理不了否定转折（"减产预期落空"误判；轻量加否定反转规则，重量接 LLM 仅复核紧急消息）；⑭期权缺 IV 历史分位/波动率锥/skew/PCR/组合Greeks，策略库可补日历/蝶式/比率；⑮缺板块强弱排行与产业链价差（卷螺差/油粕比/月差）；⑯无资金管理（不按资金/乘数/保证金算手数、无组合风险预算）。
- **P2 工程体验（7 项）**：⑰看板仅 iframe 嵌 txt（5.7KB）无图表/筛选/手机访问，可 Flask+ECharts；⑱无 tests/（前四轮断言均临时脚本验证后即删）、未 git init（缺 .gitignore/LICENSE）；⑲配置耦合在 config.py，应外置 config.json/.env；⑳首轮1-2分钟串行慢，可 ThreadPoolExecutor 并发+本地持久缓存；㉑数据源单点压新浪，应主备降级链（新浪→akshare/efinance→东财）；㉒可选 LLM 综合解读/追问；㉓远期 tqsdk/vnpy 模拟盘闭环。
- **已确认的优势（保持勿丢）**：调度刻度对齐与紧急插队不推移计划点、多源聚合+存疑降权、Black-76+希腊字母+组合策略六项检查、BOM/占用重试/日切落盘鲁棒性、状态文件事件驱动看板、建议标注具体合约与移仓提示。
- 主要对标项目：vnpy/vnpy(45k)、TauricResearch/TradingAgents(102k)、hsliuping/TradingAgents-CN(31k)、bbfamily/abu(18.5k)、QUANTAXIS(11k)、tqsdk-python(5k)、akfamily/akshare、Micro-sheep/efinance、zhouxinhao19/TradingAgent-Future（最直接对标：基差/库存/期限结构+数据源降级链）、culi123/QtGuiSurveillanceMulti、LC9999216/a-share-stock-monitoring（飞书推送）、DrifterXXX/options-arbitrage-system（多周期共振/IV分位/7维评分）、quantskills/skill-commodity-carry-cta（横截面因子/主连接续）、qusong0627/QuantMind、mpquant/MyTT、Homalos/Homalos。
- 第5轮只做分析未改代码；用户随后点名「先修 P0」，即第6轮（见下），P0 六条已全部修复。

### 第6轮：P0 六项修复（夜盘分档/解释器/交易日历/日志轮转/连接池/看门狗）
> 对应改进清单第一批（条目1-6），全部落地并验证；新增 `http_client.py`、`trade_calendar.py` 两个模块。
1. **夜盘分品种分档（P0-1，实质bug）**：`config` 为每个品种标注夜盘结束档（23:00=多数、次日01:00=铜铝锌铅镍锡/不锈钢/国际铜/氧化铝、次日02:30=黄金白银原油），无夜盘品种（鸡蛋/生猪/广期所碳酸锂等）标 None。`utils` 新增 `is_variety_trading(meta, now)` 品种级判定；全局 `is_trading_time/current_session` 按最晚02:30判；调度全部用真实 datetime（SESSIONS 夜盘段为 21:00→次日02:30 的跨日分钟轴），正确处理跨零点、周五夜盘延续到周六凌晨、周一凌晨无夜盘、**节假日前一晚不夜盘（has_night_session 看与下一交易日间隔，仅1天/跨周末3天才有夜盘）**。复盘由 `trade_owner_date`（凌晨归属前一交易日）+ `review_is_due`（该交易日全部夜盘结束后才到期）驱动；`daily_rollover` 改按交易日归属清块，跨零点夜盘块不再被误清。
2. **bat 解释器（P0-2）**：`start_monitor.bat` 固定 `D:\Python\python.exe`（where 校验，缺失回退 python），`%*` 透传；退出码0（正常/Ctrl+C/--once）不重启，非0（含看门狗 os._exit(3)）等15秒 `:retry` 重启并写 logs/restart.log。
3. **交易日历（P0-3）**：新增 `trade_calendar.py`。**新浪旧明文日历 klc_td_sh.txt 实测已加密失效（返回固定密文），东财 push2his 偶发限流**，最终方案「动态+静态」：动态主源新浪螺纹主连 RB0 日K（程序数据层长期在用、实测稳定，日期即期货交易日），备源东财上证指数日K（各重试，任一成功即可），缓存 cache/trade_dates.txt（只留近2年约533条）；静态 `STATIC_HOLIDAY_RANGES` 内置证监会《2026年部分节假日休市安排》全部区间（覆盖日K拿不到的未来日期）。对外 is_trade_day/next_trade_day/prev_trade_day/has_night_session/status_line；周末恒休（调休补班也休）、工作日先看静态表再用动态K线校正、未来工作日兜底为交易日；断网时静态表+周末规则仍可用。**锁必须用 `threading.RLock`（ensure 持锁后会调同样加锁的 refresh，普通 Lock 会自死锁，验证时踩过）。**
4. **日志轮转（P0-4）**：`utils.setup_environment` 改 `RotatingFileHandler`（单文件2MB、保留5份，config.LOG_MAX_BYTES/BACKUP_COUNT），并显式幂等挂到 root logger（先清旧 Stream/Rotating handler 再挂，不依赖 basicConfig 仅首次生效、重复调用不累积句柄）。
5. **连接池（P0-5）**：新增 `http_client.py` 暴露全局 `http = Session()`（HTTPAdapter pool_connections/pool_maxsize、keep-alive、统一 timeout、max_retries=0）；futures_data/oil_data/sina_news/webdata/web_scan/browser_reader 六个数据层全部由裸 `requests.get` 改走 `http.get`（grep 确认无残留）。
6. **看门狗（P0-6）**：main 每轮 run_cycle（含紧急插队轮）开头写 logs/heartbeat.txt（先 cycle+=1 再写，避免显示第0轮）；独立 daemon 看门狗线程每30s检查，距上次心跳>600s 记致命日志并 `os._exit(3)`（非0退出交 bat 重启；用 os._exit 而非 sys.exit，防 C 扩展卡死线程时解释器退不掉）。
- **顺带修复的既有问题（非P0引入但不修无法运行/不正确）**：futures_data.py / analyzer.py / contracts.py 三个文件原有模块 docstring 被提前 `"""` 闭合的语法错误（源码无法编译、此前靠旧 .pyc 运行）；prev_trade_day 会把日K未覆盖的未来工作日误判休市（加 _max_cached 上界保护）；analyzer 旧的 weekday>=5 休市提示改接 trade_calendar。
- **验证**：18个py全部编译/导入通过；84项调度+日历断言、5项 daily_rollover 跨零点归属、23项看板 JS 与 Python next_cycle_time 跨日/周六凌晨时刻对齐，全 0 失败；`main.py --once --no-launch` 完整端到端跑通一整轮（64/64品种、合约探测、期权、预测、报告落盘、心跳、exit 0）；临时测试脚本验证后已全部删除（仍无 tests/，见 P2-18）。

### 第7轮：P1第二批（主动告警、SQLite、信号胜率追踪、量仓因子）
> 对应改进清单第二批（条目7/8/9/11的量仓部分），全部落地并验证；新增 `alerts.py`、`storage.py`、`flow_tracker.py`，零新增第三方依赖。
1. **主动告警（P1-7，alerts.py）**：Windows 本机声音用标准库 `winsound`，非 Windows 自动降级；Webhook 复用 `http_client.http` 全局连接池，支持飞书、钉钉、企业微信群机器人、Server酱、通用 JSON，URL 从环境变量 `FUTURES_MONITOR_WEBHOOK` 读取，类型 `ALERT_WEBHOOK_TYPE=auto` 自动识别；本机静音用 `FUTURES_MONITOR_SOUND=0`。声音/推送均在 daemon 线程，失败只写日志，不阻塞主循环。触发：紧急轮动（300s冷却）、期货分数跨4分档/6.5强档、多空方向翻转、六项全过的期权策略（品种信号1800s冷却）；分档阈值读取 `ALERT_MID_SCORE=4.0`/`ALERT_STRONG_SCORE=6.5`，避免与分析阈值分叉。冷却键为“品种+方向+分档”：重复同向同档被压制，跨档升级或多空翻转可再次提醒；程序启动第一轮只报≥6.5强信号，避免64品种基线刷屏。同一轮多个事件会先聚合，轮末只按最高级别响一次声音、只发送一条“期货监控告警（N条）”汇总 Webhook；独立调用 `emit()` 时仍保持单条告警。Webhook 业务返回中 `code` 或 `errcode` 任一异常都记为发送失败。
2. **SQLite结构化库（P1-8，storage.py）**：标准库 `sqlite3`，WAL、busy_timeout=5000、`threading.RLock`，主循环和后台新闻线程可共用；五张表 `quotes/signals/news/options/signal_outcomes` 及索引。每轮保存64品种行情、期货综合分与因子拆分、新闻权重/可信度、单腿期权与组合策略；数据库路径 `data/monitor.db`，行情/新闻/期权明细默认保留180天（`DB_RETENTION_DAYS`），非中性信号和评估结果长期保留。非交易时段接口重复返回同一行情快照时，`quotes` 先按完整快照签名去重，且只有数据库提交成功后才更新去重签名；`signals` 只保存 `|综合分|>=2` 的非中性可交易信号，中性行仍保留在文本报告和行情表中，避免每轮64品种空转。主链路对 DB 写入做了故障隔离：新闻/行情/信号入库异常只记 warning，报告继续生成。
3. **信号效果追踪（P1-9）**：`|score|>=2` 的期货信号自动建立30分钟、120分钟、1440分钟三个待评估任务；同一品种/方向/分档仍有 pending 时不重复建单。到期后用后续行情计算 `方向收益=方向(多1/空-1)×(评估价/入场价-1)`，正=hit、负=miss、零=flat；评估时区分“品种正在交易但价格没动=打平”和“品种已休市、接口重复给旧价=继续等待”：休市/旧价最多等6小时，长期取不到新行情则记 expired，不硬算失败。胜率统计同时给出 evaluated 有效样本与 expired 过期样本，过期样本不进入胜率分母、不拉低平均方向收益，并防止有效样本为0时除零。看板新增 `reports/signal_tracking.txt` 页签，按周期/分档/多空统计样本、胜率、平均方向收益和最近结果；每日复盘新增“信号效果追踪”章节。
4. **量仓资金因子（P1-11的已做部分，flow_tracker.py）**：按主力连续代码保留最近20轮价格、交易日日期、日累计成交量、持仓量；成交量先转相邻轮次增量并按时间间隔归一，再与近几轮平均增量比较，识别增仓上行/增仓下行/减仓上行（空头回补）/减仓下行（多头减仓）、放量/缩量。增仓新资金权重高于减仓回补，综合贡献裁剪到±1.2（`FLOW_MAX_SCORE`）；`analyzer.analyze_variety(..., flow=None)` 新增“量仓资金”因子，结果行带 volume/open_interest/flow/flow_note，重点品种明细新增“量仓”行。已处理两类基线跳变：新交易日日累计成交量归一时重置，不误判缩量；新浪行情 `date` 表示交易日变化或主力连续换月时，清空该品种历史序列并重新建立基线，避免隔夜量能归零/持仓跳变被误判为异常增缩仓。**基差、仓单/库存、期限结构、龙虎榜仍未做，属于11条剩余部分。**
5. **接入点**：`State` 新增 flow_tracker/db/alerts；新浪新闻与全网扫描新闻均入库；每轮先更新 flow_map 和 quotes，再逐品种分析；报告前评估历史信号、写期货/期权记录和 signal_tracking；报告保存后触发 alerts；退出关闭 DB。`utils.setup_environment` 创建 data 目录；`option_analyzer` 结果补 `underlying_price` 供 options 表记录标的价。
- **第7轮踩坑/修复**：`storage.insert_future_signals` 初次 SQL 是26列但只写25个占位符，模块自测报 `25 values for 26 columns`，已补齐；代码审查发现跨交易日日累计量归零、主力连续换月或交易日切换会被误判缩量/异常增仓，已在 flow_tracker 快照中加入交易日日期并重建基线；`quotes` 原先可能在非交易时段重复写同一快照，已改为提交成功后才更新签名；信号冷却原先只按品种会错误拦截跨档升级/多空翻转，已改为“品种+方向+分档”；Webhook 200 响应内业务码原先要求 code/errcode 同时异常才失败，已改为任一异常即记录；DB 异常与报告渲染解耦，避免磁盘/锁问题导致整轮无报告。
- **第7轮最终验证（2026-08-31 22:23）**：21个py全部 compileall 通过；最终临时 `_p1_verify.py` 覆盖量仓模式、交易日切换重建基线、analyzer接入、SQLite增删查、相同行情去重、只存非中性信号、pending去重、胜率回填、期权入库、五类Webhook payload、飞书/Server酱格式、首轮基线不刷屏、强信号/翻转绕过冷却、新期权策略只提醒一次、报告渲染，共20项断言全部通过（FAILURES:0）后已删除，仓库仍无 tests/。完整命令 `D:\Python\python.exe main.py --once --no-launch` 最终端到端 exit 0：交易日历加载成功、主力合约探测64/64、全网扫描与新闻正常、第1轮报告正常落盘。最终实测库计数：quotes=256、signals=244、news=471、options=320（single/strategy各160）、signal_outcomes=270（hit=32、miss=35、flat=1、pending=202；当前无 expired 残留），`PRAGMA integrity_check=ok`；`signal_tracking.txt` 正常显示30分钟有效样本68、胜率47.1%、平均方向收益-0.16%、待评估202条；`report_status.js` 与 `实时报告.html` 正常刷新，看板含“信号胜率追踪”页签。最新日志中仅有 8月30日历史旧 KeyError（第6轮后已修）和早前 signals.csv 被外部占用的 warning，22:23 本轮无 Traceback/ERROR。

### 第8轮：P1第三批主体（最小回测、技术共振、新闻反转、期权IV深化）
> 对应改进清单条目10/12/13/14主体，新增 `backtest.py`，零新增第三方依赖。
1. **最小日线回测（P1-10）**：`backtest.py` 支持 `--codes RB0,MA0 --days 250 --hold 10` 或全品种；新浪主连按请求窗口截取，疑似换月跳空置0并比例后复权；回放“日线动量+技术共振”纯技术信号，输出非重叠交易的胜率、平均收益、盈亏比、累计/年化、最大回撤、夏普；另列1/5/20日重叠信号衰减但不做复利净值。产物 `reports/backtest_report.txt`、`reports/backtest_signals.csv`，看板新增页签。边界：历史新闻/机构/实时量仓无法复原，未计手续费、滑点、涨跌停不能成交。
2. **技术指标与共振（P1-12日线版）**：`futures_data.compute_indicators` 新增 RSI14、MACD(12,26,9)、KDJ(9,3,3)、BOLL(20,2)、MA60、HV历史分位、10/20/40/60日波动率锥；短（MA5/5日/KDJ）、中（MA20/MACD）、长（MA20/MA60）三票形成裁剪到±1.2的“技术共振”因子，接入综合分、风险提示和明细。30/60分钟K线仍待后续。
3. **新闻否定/转折（P1-13轻量版）**：关键词命中点±16字内出现“并未/没有/未能/落空/证伪/取消/推迟/不及预期”等则反转该词极性；正则内部已经编码方向的“低于预期”不二次反转。例：“OPEC减产预期落空”转利空。
4. **期权IV深化（P1-14主体）**：OpenVlab页面解析真实平值IV、隐波百分位、偏度、偏度百分位；统一 `option_analyzer.implied_vol_profile`，页面缺失时用HV分位+波动率锥代理。裸买要求IV/HV≤1.35且IV分位≤75%，价差≤85%，卖方偏好≥55%；组合策略补齐Gamma，输出Δ/Γ/Vega/Θ并写入SQLite。PCR需完整期权链，当前免费页面没有，保留 `option_chain.pcr` 接入位，缺数据不编造、不否决。
5. **第8轮踩坑/修复**：OpenVlab `atm_iv/prem/rank` 都是按品种名嵌套的字典，必须再取当前品种内层；新浪日K可能无视请求长度返回全历史，回测已强制 `[-days:]`；重叠固定周期信号不计算复利累计和回撤。
6. **第8轮验证**：全部修改文件 py_compile 通过；合成行情断言覆盖RSI/MACD/KDJ/BOLL/共振/HV分位、新闻反转、缓存OpenVlab真实IV分位/偏度解析、回测交易、期权单腿/组合Gamma；真实 `backtest.py --codes RB0,MA0 --days 160 --workers 2` exit 0并写出报告/CSV；最终 `D:\Python\python.exe main.py --once --no-launch` 64品种端到端 exit 0，23:27:51 同步落盘 latest_report.txt、实时报告.html、monitor.db，本轮日志无 Traceback/ERROR。回读结果：58条“技术:”共振明细、40组期权6项检查、80处波动率锥说明、67处组合Greeks、看板含“最小日线回测”；SQLite `PRAGMA integrity_check=ok`，options 表 gamma/greeks_json 已写入，最新轮非空 Gamma 策略记录存在。


### 第11轮：WP-A 期权完整T型链+持仓量PCR、全月份期限结构（零新增依赖）
> 对应《未完成项落地方案.md》工作包A；新增 `option_chain.py`，改 config/contracts/analyzer/option_analyzer/storage/main/report，数据库5表→6表。
1. **数据源（已本机实测，五大所57个期权品种全部成功）**：新浪 `OptionService.getOptionData?type=futures&product={p}&exchange={ex}&pinzhong={pin}`，与现有新浪K线同站、走全局 http_client。product：SHFE/INE/DCE/GFEX=小写sym+"_o"（cu_o/sc_o/m_o/lc_o/si_o），CZCE=小写无后缀（ma/sa/ta）；exchange全小写；pin=小写sym+4位年月（月份来自 webdata.fetch_option_calendar 的六位yymm取后4位，郑商所也用4位）。腿数组 SHFE/INE/GFEX 9元素（含独立行权价位）、DCE/CZCE 8元素（行权价只在代码尾部），统一正则解析；持仓量固定 index5。P_OP 实时快照字段错位不可靠，本轮弃用成交量PCR。
2. **持仓量PCR**：Σ看跌持仓/Σ看涨持仓；build_summary 另给 n_call/n_put、call_oi/put_oi、max_call/put_oi_strike（压力/支撑）、locate_atm 平值行权价、pcr_sentiment 五档（<0.5看涨极占优偏热、0.5~0.7偏乐观、0.7~1.2均衡、1.2~1.5谨慎、>1.5看跌极占优极值，均标注"反向指标需结合趋势"）。option_analyzer 新增 chain_note 进报告期权明细；PCR分位由 storage.pcr_percentile 用 option_chains 近30日样本算（样本<10不显示）。
3. **期限结构**：contracts.term_structure 复用 fetch_ranking 同一次月份行情（补 latest/prev_settle/chg_pct，零额外请求），按日历月排序，算近-远月差/价差%/年化展期收益率（月份差×30.42天）、前2后2均价降噪判 Contango/Back/平水；analyzer.detail_lines 输出一行。**本轮只展示，不进综合分**（基本面因子入分属第13轮WP-C）。
4. **接线**：main.run_cycle 新增3.9步——从 fut_rows 收集 OPTION_VARIETIES 且有 opt_month 的任务，state.opt_chains.warm 6线程并发（TTL30min，命中零请求），chain 挂 row["option_chain"] 并回填 pcr_pct；第5步入库 insert_option_chains。option_analyzer.implied_vol_profile 原 chain.get("pcr") 因 option_chain 提供 pcr 别名直接生效，波动率行自动带 PCR。
5. **口径边界（不得夸大）**：只做持仓量PCR（T链直接给），成交量PCR缺逐腿成交量未做；PCR情绪只呈现、不单独构成交易结论、不进综合分；新浪T链无 INE 低硫燃料油LU（各月份/参数组合实测均空），该品种自动降级无链、期权六项检查照常。
6. **第11轮验证（2026-09-01 14:29）**：①23个py compileall 全过；②合成断言（cu2610/m2609实测片段）覆盖9/8元素解析、行权价代码解析、PCR手算、零持仓不除零、ATM、product/pin规则、Contango/Back识别、旧缓存兼容，全过；③真实链路实拉 CU/M/MA 三所代表+warm并发+缓存命中+坏任务降级+临时DB写入/分位/table_counts，全过；④`main.py --once --no-launch` 64品种端到端 exit 0，日志"期权完整链就绪 39/40"（唯一缺LU系数据源缺口），报告期权链行/期限结构行正确渲染，option_chains 落39行、PRAGMA integrity_check=ok，LU无链时六项检查照常降级；临时脚本已删。

### 第12轮：WP-B 多到期日IV曲面 + 日历价差（零新增依赖）
> 对应《未完成项落地方案.md》工作包B；新增 `iv_surface.py`（267行），改 option_analyzer/option_strategies/main/report/config，storage 无需改。
1. **IV反推（iv_surface.py）**：`implied_vol(price,F,K,T,kind)` 对已有 `option_analyzer.black76` 二分反推（40次迭代，区间[1e-4,5.0]，价格对σ单调）；`leg_quote` 报价质量分级；`_strike_iv_map`/`_merge_strike`（call/put同行权价按OI加权融合，偏差大选窄价差可信侧）；`expiry_smile` 单到期日微笑（ATM、25Δ风险反转RR=IV(25ΔP)−IV(25ΔC)、蝶式Fly、高质量腿门槛）；`build_surface` 多到期日曲面（ATM期限 Contango近低远高/Backwardation近高远低/平坦、moneyness 0.90/0.95/1.00/1.05/1.10 矩阵，缺档None显示--不插值）；`surface_text` 两行紧凑文本。
2. **五层脏报价清洗（免费T链必须，否则IV失真）**：①窄价差(买卖价差比≤IV_MAX_SPREAD_RATIO=0.15)取中间价=高质量q0，宽价差回退last=低质量q1，宽价差无last丢弃；②单腿反推IV>IV_SURFACE_IV_CAP=2.5丢弃；③可反推档位数<IV_SURFACE_MIN_STRIKES=5、或高质量(q0)腿<3档则该到期日不输出，且ATM只从高质量腿中选(IV_SURFACE_NEED_CLEAN_ATM)；④剩余<IV_SURFACE_MIN_DAYS=30天的月份不参与（最后30天T小误差放大；按真实到期日过滤，同为2610、cu剩22天被剔而m剩52天保留）；⑤微笑点IV相对ATM在IV_OUTLIER_RATIO=(0.2,3.5)带外标outlier不参与25Δ/矩阵。
3. **两个关键坑**：①**OpenVlab日历月份键是6位整数(202611)，新浪pinzhong要4位(2611)**，main里必须 `yy,mm=(yymm//100)%100,yymm%100` 归一化（兼容4/6位），否则全品种抓空（首轮"0个月份"即此坑），日历键为sym.upper()；②脏报价（单边挂单ask=0、价差宽达45%、临近到期）会把ATM反推成80%~120%假值、RR±30vol假信号，窄价差腿反推则完全合理（MA30~35%、豆粕14~18%），据此才加的清洗。
4. **日历价差（option_strategies.py 候选9，priority=7：高于铁鹰/蝶式6、低于跨式8与方向价差10）**：取曲面最近两个相邻到期日 exps[0]/exps[1]（已按yy,mm升序），month_gap=远月(yy*12+mm)−近月，1≤gap≤IV_CALENDAR_MAX_MONTH_GAP=2；同行权价K取近月atm_strike；iv_diff=近ATM−远ATM。差≥+IV_CALENDAR_MIN_DIFF=0.03→**卖近买远 long calendar**（正Vega，借方，max_loss=max(净支出,0)，强倾斜时近月权利金反超远月变信用日历）；差≤−0.03→**买近卖远 short/反向日历**（负Vega，净收入，近月到期后远月裸腿按IV_CAL_MARGIN_RATE=0.10名义计保证金、上行风险诚实标max_loss=None理论无上限、强制"仅小仓位"）。**静态盈亏空间硬检查**：假设IV不变、近月到期标的恰好收K（日历最优点），long估算 far_resid=black76(K,K,T_far−T_near,far.iv)−debit，short=credit−far_resid，必须>0否则观望（实测PTA/多晶硅/氧化铝/黄金多个反向日历净收入为正但静态最优为负=IV不变必亏，被正确拦截）。recommend末尾支持 month_label_override/legs_text_override（跨月标签含"/"时不硬加"月份"后缀），新增 `_calendar_legs_text`。
5. **option_analyzer 接线**：`implied_vol_profile` IV优先级改 OpenVlab页面真实 > T链反推(标"T链反推") > HV估计；波动率检查加页面IV vs 链反推ATM 交叉校验（差>IV_CROSS_CHECK_DIFF=0.02标注不否决）；T链反推时历史分位用HV滚动分位代理并明确措辞"HV滚动分位%(代理IV分位)"；analyze_option 返回加 surface_note/surface_matrix/surface_brief，报告在"期权链"行后插曲面摘要+矩阵两行。
6. **main 3.9步重写**：从OpenVlab日历取最近 IV_SURFACE_EXPIRIES=3 个真实挂牌月份（按剩余天数过滤），6线程并发warm各月份链；主力分析月份链仍挂 row["option_chain"]（保持第11轮行为），多月份链组装 row["iv_surface"]=build_surface，**每月份链都进 chain_rows_for_db 入库**（同ts多expiry各一行）。
7. **第12轮验证（2026-09-01 15:35）**：①24个py compileall全过；②两轮合成/回归断言（零网络）全PASS：反推精度<2e-4、put-call parity一致、RR方向/Fly凸性、坏腿/全零/宽价差过滤、ATM脏档规避、全脏与<30天返回None、矩阵缺档--不插值、离群翼点不进矩阵、long/short calendar方向与Vega/保证金、静态空间为负的反向日历不被推荐、极端倾斜静态空间为正才通过、IV剖面三级优先级；③真实 `main.py --once --no-launch` 多轮端到端最终 exit 0：112个月份链/40品种、37个品种产出IV曲面，ATM IV全部落在5%~46%合理区间、远月矩阵平滑（如19/19/20/21/23）；日历策略真实触发（铁矿石卖近买远全通过：净支出0.6点/最大盈利17.6/Δ≈0/Vega+0.5；豆一静态空间正但机构观点不配合→观望；PTA等静态最优为负的反向日历被拦截不推荐）；47条波动率检查中35条走"T链反推IV"；option_chains表同ts下39品种×多expiry各一行（豆粕2611/2612/2701三行PCR齐全），PRAGMA integrity_check=ok；临时脚本/日志已删。
8. **口径边界**：IV为收盘价反推的静态值，盘中实时性最好在夜盘/日盘盘中再抽验；免费源无历史L2逐笔、远月深虚值不插值、缺腿不编造；交易所官方IV交叉校验(option_vol_*)为遗留可选项（第13轮WP-C改做基本面数据包、官方IV未做）；反向日历近月到期后远月裸露、理论风险无上限，仅小仓位。

### 第13轮：WP-C 基本面数据包（库存/仓单+龙虎榜+carry入分+基差，零新增依赖）
> 对应《未完成项落地方案.md》工作包C；新增 `fundamental_factors.py`、`fundamental_data.py`，改 config/analyzer/storage/main/report，数据库6表→7表（fundamentals）。
1. **数据源实测与选型（本机 requests 实测 + 下载 akshare MIT 源码仅借鉴 URL，不引依赖）**：东财数据中心统一入口 `datacenter-web.eastmoney.com/api/data/v1/get`（需 Referer=data.eastmoney.com + 浏览器UA，走全局 http_client）。①品种映射 RPT_FUTU_POSITIONCODE（filter IS_MAINCODE="1"，TRADE_CODE↔品种，广期所/INE 为小写 si/lc/ps/bc/lu/nr，须 upper 后匹配项目全大写 sym、但请求保留东财原始大小写）；②库存/仓单时序 RPT_FUTU_STOCKDATA（filter SECURITY_CODE="CU"，升序取 TRADE_DATE/ON_WARRANT_NUM/ADDCHANGE，实测每品种约71条/近约3个月、CU自2026-05-25起）；③龙虎榜 RPT_FUTU_DAILYPOSITION columns=ALL、filter (SECURITY_CODE="CU2610")(TYPE="2")，**不传 TRADE_DATE + sortColumns=TRADE_DATE desc 即自动返回最新日三行（本日合计/上日合计/总量增减）**，取本日合计=今多空、上日合计=昨多空，SECURITY_CODE=东财原始code+两位年月。
2. **已验证做不通、勿再试**：SHFE 官网 dailystock/pm 在 20260824~0901 全 404、tsite.shfe 连接失败；DCE wbillWeeklyQuotes 持续 412 WAF（补头无效）；CZCE 仓单是 xlsx 零依赖解析繁琐；生意社 sf/sf2/day-*.html 返回 HW_CHECK 的 JS-cookie 反爬混淆页（偶发放行也不稳定）；东财无干净免费基差时序（猜的 RPT_FUTU_BASIS/SPOTPRICE 等全 success=false）。GFEX 官网 loadList POST 可用但仅作备用/交叉验证。**据此：库存+仓单合并用东财时序；龙虎榜用东财TYPE=2；carry复用第11轮term零请求；基差只做尽力抓取生意社+识别HW_CHECK即返回None的预留接入位。**
3. **纯计算 fundamental_factors.py（零网络、便于合成断言）**：inventory_factor（经验分位 `(0.5-pct)*2` 占60% + 周环比去化 tanh 占40%，样本<FUND_INV_MIN_SAMPLES=15 返回None，缺周环比按可得项归一）；rank_factor（净多率=(L-S)/(L+S) tanh(K=6) 占60% + 较昨日净多率变化 tanh(K=30) 占40%，L+S=0返回None）；carry_factor（annual_carry tanh(1/0.15)，接受term dict或数值）；basis_factor（基差率 tanh(1/0.05)）；build_fundamental 按 config 权重 INV.40/RANK.30/CARRY.20/BASIS.10 加权，**缺项权重按可得项重新归一（单因子不被放大/缩小）**，四子项全缺返回None，输出 {score(已×1.5)/raw/parts/sub/note}；note 对仓单注销再注册导致的极端周环比(|wow|>200%)展示封顶为"≥+200%(低基数)"（因子本身已 tanh 饱和不失控）。
4. **网络层 fundamental_data.py**：FundamentalFetcher（RLock 线程安全 + 自然日级缓存）：inventory_map/inventory_series/rank_totals/basis_table；生意社用标准库 HTMLParser（_TableGrabber）提取表格、正则抽品种代码与现货/主力价，基差率口径=现货/主力-1（现货升水为正，与 akshare dom_basis_rate 反向）；任何异常/反爬/空结果都软降级返回 None/[]，绝不影响主监控。
5. **接线**：analyzer.analyze_variety 新增 fund_raw 参数，term 提前到函数开头计算（carry 与主力月份共用），库存/龙虎/基差原料在 flow 后组装为 parts["基本面"]、返回行带 fundamental、detail_lines 加基本面行；storage 新增 fundamentals 表（inventory/inv_pct/inv_wow/inv_n/rank_long/short/net/delta/carry/basis_rate/fund_score/raw_json，UNIQUE(sym,trade_date) INSERT OR REPLACE）+ insert_fundamentals/latest_fundamentals + table_counts；main State 加 fetcher/fund_inv/fund_basis/fund_day，新增 refresh_fundamentals（8线程并发拉64品种库存+基差，启动同步一次、fundamentals_loop 后台每30min检查、16点后日频刷新）与 _prefetch_rank（分析前8线程按主力合约并发预取龙虎榜，日缓存当天仅首轮请求），run_cycle 组装 fund_raw 传入并在第5步入库；report 期货总表后加【基本面速览】偏多/偏空各Top5+数据口径脚注。
6. **第13轮验证（2026-09-01 17:00）**：①26个py compileall 全过；②21条零网络合成断言全PASS（低库存+去库偏多/高库存+累库偏空/样本不足None、净多改善偏正/净空恶化偏负、Back偏多Contango偏空、现货升水偏多、四子项全缺None、仅carry可时归一raw=裸分不缩放、四项全多/全空方向、权重和=1）；③真实 `main.py --once --no-launch` 两轮端到端均 exit 0、无 ERROR/Traceback：库存时序61品种、龙虎榜61、carry64、基差反爬按设计降级，fundamentals 表落64行（inv非空58/rk61/cy64/bs0），fund_score 正31/负30/中性3、范围[-1.20,+1.50]无爆分，报告基本面速览与重点明细渲染正确（沥青低库存1%分位+强Back+50%→+1.15偏多；烧碱/纸浆100%分位+Contango→偏空，经济逻辑自洽）；PRAGMA integrity_check=ok；临时探测/断言/补丁脚本与日志已全部删除。
7. **口径边界（诚实声明）**：东财库存免费窗口仅约3个月，分位是"近3个月滚动分位"而非年度分位（FUND_INV_MIN_SAMPLES=15 兜底）；龙虎榜是前20席会员合计而非全市场持仓；基差源生意社有JS-cookie反爬、大概率缺失，属预留接入位，其"现货紧俏"含义大部分由 carry 覆盖；SHFE/DCE 官网仓单接口当前不通，统一用东财 RPT_FUTU_STOCKDATA 替代，GFEX 官网仅备用；生鲜/指数品种（AP/JD/PK/EC/LG/SC）东财无标准仓单时序，自动只靠 carry+龙虎榜。

### 第14轮：WP-D0 分钟K自采库（初版三源；当晚增补后为"新浪主连全周期+通达信可选+东财兜底"，零新增强制依赖）
> 对应《未完成项落地方案.md》工作包D0；新增 `tdx_bars.py`、重写 `intraday_bars.py`（前半轮东财单源版因 push2his 限流废弃为三源版），改 config/storage/main/report，数据库7表→8表（minute_bars）。完整选型论证见新建《数据源选型与通达信替换可行性分析.md》。
1. **通达信穷尽实测（用户要求全网调研+评估全量替换东财）**：pytdx 1.72（TdxHq_API 标准7709/TdxExHq_API 扩展7727；周期cat 1m=8/5m=0/15m=1/30m=2/60m=3；期货市场号 CZCE28/DCE29/SHFE·INE30/CFFEX47）。①7709 官方103台中28台TCP可达，但逐台验证市场0~120全枚举只有沪深股票、期货列表与K线全空、快照全空；②7727扩展口（商品期货真正通道）0/103可达，7720/21/29/30全关，ExHq走7709协议不通；③本机无通达信客户端（仅同花顺期货通），127.0.0.1无监听；社区文档同样记载7727"10台仅1台可连且数据全None"。**要用通达信期货只能本机常驻登录通达信客户端（违背零GUI依赖架构）或付费，均不采纳。**
2. **东财分域名实测（关键认知）**：当晚 push2his 行情K线域名对本机持续 RemoteDisconnected（18:23~19:10，http/https、主域/1~3镜像、1m/30m/101日线全断，0.2s即拒=IP级限流）；但 push2 clist 域名200正常、**datacenter-web 基本面域名秒回稳定（映射74条0.6s/库存71条0.2s/龙虎榜0.2s）**。结论：东财不能一刀切，基本面接口保留不动，行情口不能作唯一分钟源。
3. **新浪主连实测（升为主源）**：`InnerFuturesNewService.getFewMinLine?type=5/15/30/60`（futures_data.fetch_intraday_kline 已存在，第9轮仅用30m），主连代码 config.VARIETIES[*].code（RB0式，无需合约转换/换月跟随），固定返回1023根、字段 d/o/h/l/c/v/**p(持仓量,无成交额amount记0)**，64/64品种全覆盖零异常；深度 5m≈3周/15m≈3月/30m≈6月/60m≈12.5月，远深于东财；~~唯一缺口=无1分钟~~【当晚增补已推翻：type=1同样1023根可用，见"第14轮增补"节】。
4. **intraday_bars.py（初版267行/增补后304行）**：em_contract_code（CZCE三位年份个位ma610/其余四位）、em_secid（SHFE113/DCE114/CZCE115/INE142/GFEX225）、_parse_line（东财"开-收-高-低"非OHLC顺序）、fetch_sina_minute（主连解析、延迟导入futures_data防循环，增补后支持1m）、MinuteBarFetcher（东财：全局限流+镜像轮换+指数退避+**连接级OSError短退避0.3s快速熔断**，连续8次失败冷却60s）、**MinuteCollector 多源选源**（增补后全周期统一为 sina→tdx→em，不再有"1m=em→tdx"分叉；主力yy/mm未知时新浪主连仍可独立采集全周期）、aggregate_bars（细周期×factor聚合粗周期纯函数，跨休市段不硬拼、尾段不足不编造，供第15轮）。
5. **tdx_bars.py（200行，可选冗余）**：tdx_contract（CZCE三位大写MA610/其余小写四位rb2701）、TdxMinuteSource（RLock串行单连接、20线程并发probe+15s总时限、_try_server验证能取期货才保留连接、断线换服、GFEX公共口不支持自动跳过、未装pytdx永久降级；fetch输出与intraday统一bar dict、src=tdx）。
6. **storage 第8张表 minute_bars**：sym/contract/exchange/period/bar_dt/trade_date/o/h/l/c/v/amount/created_real，**UNIQUE(contract,period,bar_dt) INSERT OR IGNORE**；insert_minute_bars（executemany返回新增数）、minute_bars_for_sym（按sym跨合约时间升序，主连RB0与具体RB2701共存）、minute_bars_coverage（周期→bars/contracts/first/last）、prune 加 minute_bars 400天清理、table_counts 同步。
7. **接线**：config MINUTE_PERIODS=(1,5,30)常驻、BACKFILL=(60,30,15,5,1)、LMT新浪1023、WORKERS=6、TDX_ENABLED=True；main State 加 tdx_minute/minute_collector，collect_minute_bars 改多源（首轮自动probe、任务带主连code、源分布日志、reset_stats），启动序列 contracts.refresh 后 once 小回填，minute_bars_loop 后台线程（backfill全量→交易300s/非交易1800s增量）；report 末尾【分钟K自采库】覆盖展示。
8. **第14轮验证（2026-09-01 19:07~19:20）**：28个py compileall全过；**55项零网络合成断言全PASS**（东财/通达信代码转换、东财行解析开收高低、新浪解析/lmt截断/不支持1m/异常降级、MinuteCollector六类选源分支、tdx不可达probe=False且fetch零成本、聚合跨段/尾段/30m×2、storage建表/去重/跨合约合并/coverage/prune、熔断）；真实 main --once --no-launch exit 0 无ERROR/Traceback（主力64/64、tdx 1s判定不可用、东财1m 4.3s熔断、320任务 sina256/empty64、其余7表与基本面/期权/分析完好）；完整回填 **minute_bars=261888根=64×4×1023精确对齐**（5m至07-31/15m至05-27/30m至02-27/60m至2025-08-13），全库零非法OHLC、零违反唯一键，二次增量净增0；临时探测/断言/补丁脚本共26个已全部删除。
9. **口径边界（诚实声明）**：新浪主连是比例复权连续序列、换月点近似（具体合约真实价格待东财/tdx可用时补，库中按contract区分）；免费源无历史L2逐笔；1m当前依赖东财、其限流期最细只到5m，长期深度靠常驻自采（自2026-09-01起永不丢）；通达信公共服务器可用性随地区/运营商漂移，probe保证环境变化时自适应点亮；库存/仓单/龙虎榜/快讯仍只能靠东财datacenter，不存在通达信全量替换。

### 第14轮增补（2026-09-01 晚）：数据源二次全网/GitHub 调研——新浪1m主源化 + 实时行情东财兜底（零新增强制依赖，不占用第15轮编号）
> 触发：用户要求全面使用本机与全网/GitHub 搜寻更优数据与新闻接口，找到更好的经对比后替换，前提是"替换后不会比替换前差"；方法仍是**本机 requests 真实联网实测为准**（下载 akshare 等 MIT 源码仅借鉴 URL，不引依赖），共跑9组临时探测脚本（用完即删）。
1. **核心发现（纠正第14轮漏测）**：新浪 `InnerFuturesNewService.getFewMinLine&type=1` 一分钟K**实际可用**——固定1023根、64/64品种全覆盖、三轮复测零断连、单请求0.08~0.17s；主连RB0与具体合约RB2701/MA610/CU2610均可取且末根逐价一致；1m回溯约2.5个交易日（5m约3周/15m约3月/30m约6月/60m约12.5月）。第14轮"新浪无1分钟"是只测了5/15/30/60、漏测type=1导致的误判。
2. **候选排查结论（勿重复踩坑）**：①腾讯 ifzq.gtimg.cn/qt.gtimg.cn 穷举 nf_RB0/nf_RB2701/RBM/rbm/rb2701 全 v_pv_none_match——免费接口只覆盖股票与外盘hf_，**不覆盖国内商品期货**，放弃；②东财 push2his 两晚持续 RemoteDisconnected，维持最后兜底，不升主源；③财联社 cls.cn 老接口(nodeapi/telegraphList)下线，v1/roll 与 nodeapi/telegraphs 均返回"访问拦截"HTML，需 sign(SHA1+MD5)+指纹对抗，akshare 已弃用该源——不做逆向、不引入；④富途 news.futunn live-list 404（全局快讯路径已失效，个股资讯需stock_id）、汇通 fx678 网页/api 404（开放平台要token）、百度股市通期货新闻 404/风控页、金十 flash-api 备用域 404/缺sub_id（www.jin10.com/flash_newest.js 现域正常）——均不采纳；⑤tqsdk/Tushare/掘金需注册账号/积分/付费，违背零账号原则不引入；⑥现有6个文字快讯源（东财7x24/新浪滚动/华尔街见闻/同花顺7x24/新浪7x24/金十）当晚复测全部健康，**新闻侧不增不减、维持原样**。
3. **落地改动（6个生产文件，28个py数量不变、净增85行至9071）**：
   - `futures_data.py`：fetch_intraday_kline 放开 period=1（合法周期改1/5/15/30/60，docstring记录纠错）；新增 `_fetch_quotes_em(codes)`（主连code→secid=MINUTE_MARKET[ex].sym小写m，如113.rbm；裸请求不带fltt/invt；字段 f2最新/f3涨跌幅%/f5量/f12码/f13市场号/f14名/f15高/f16低/f17开/f18昨结/f111持仓量；**有效性校验 latest>0 且 prev>0 且 oi≥100，残缺即弃**）；`fetch_quotes` 末尾只对新浪缺失品种调东财补齐、新浪全齐时东财零请求，异常软降级。
   - `intraday_bars.py`：SINA_MIN_PERIODS=(1,5,15,30,60)；MinuteCollector.collect 删除1m分叉，**全周期统一选源链 新浪主连→tdx(点亮)→东财具体合约兜底**，主力yy/mm未知时新浪主连独立可采；文件头docstring重写。
   - `config.py`：MINUTE_PERIODS/BACKFILL/ONCE 的1m LMT 提至1023（INCR仍12），注释更正"新浪无1分钟"并记录腾讯排除、push2快照口特性。
   - `main.py`：collect_minute_bars 删除"p==1且主力未知则continue"；State注释与启动日志措辞同步。
   - `report.py`/`tdx_bars.py`：分钟库行尾与probe日志的"东财补1m"旧措辞改为"全周期新浪主连+通达信/东财兜底"。
4. **东财限流新认知（重要）**：限流会从 push2his **扩散到 push2 快照域名**（数字镜像1/2/3/82.push2同时被断，约数十分钟自愈）；限流边缘与 push2delay 延迟口会返回 **HTTP 200 但内容残缺**（f111=1、f2价格错位、代码大小写异常），不能只靠 try/except，必须做字段合理性校验——这是 _fetch_quotes_em 加 oi≥100 拦截的直接原因；datacenter-web 基本面域名依旧独立稳定。
5. **验证（2026-09-01 晚）**：28个py compileall+import 通过；**21项零网络合成断言全PASS**（type=1放行/type=2拒绝、全周期六类选源分支、1m无合约新浪独立成功、东财字段映射与chg_pct复算、f111="-"与f111=1两类残缺拦截、新浪缺东财补且只请求一次、新浪全齐东财零请求）；真实小链路4品种×(1m/5m)全src=sina、fetch_quotes 64/64 用0.5s；`main --once --no-launch` **零ERROR**：320任务全部sina、空/失败0，minute_bars 261888→**327360根=64×5×1023（1m 65472根/64品种，08-26 10:00起）**，零非法OHLC，综合分/分钟共振/期权/基本面链路完好；9个probe+2个verify+1个dbcheck临时脚本与临时日志全部删除（仍无tests/）。
6. **为什么满足"不会更差"**：1m 从"东财（连续两晚限流、实际0根）→tdx（不可用）"变为"新浪（稳定1023根）→tdx→东财"，旧源全部保留为更后级兜底；实时行情主源新浪不动，东财只在缺失时补且残缺即弃、正常零开销；新闻源一个没动。

### 第15轮：WP-D1/D2 日内/平今回测（vnpy式bar内撮合+交易所交易日平今判定+精确锁板，零新增依赖）
> 对应《未完成项落地方案.md》工作包 D1（平今优惠触发）/D2（bar内撮合升级）；新增 `intraday_backtest.py`（712行），改 config（+INTRADAY_BT_*段、+64品种 FUTURES_LIMIT_MOVE 表）/report（+2看板页签），数据库与常驻监控零改动。撮合规则经搜索核实 vnpy 官方 backtesting（限价单多头 bar.low≤委托价成交、成交价 min(委托价,open)；停止单多头 bar.high≥触发价、成交 max(触发价,open)；同根双触保守按止损；vnpy alpha 也用 pre_close×幅度近似涨跌停）。
1. **数据装载 load_minute_bars**：读 `MonitorDB.minute_bars_for_sym(sym,period,limit=lookback)`（**注意第三位置参数是 since 不是 limit，必须用 limit= 关键字**；返回 dt 为文本字符串、sym 无0后缀、主连 contract=RB0），出口统一转 datetime；`--aggregate-from` 走 `intraday_bars.aggregate_bars(bars,base,factor)` 现场聚合（跨休市段不硬拼）；**1m聚合相位对齐**：自采1m起点未必落在目标周期整边界（如21:13起），裁到首个"分钟%period==0"的 bar 之后再聚合，使段末时间戳与交易所原生粗周期（整5/15/30分）对齐。信号引擎不复用 futures_data.technical_profile（其 HV锥 O(n²) 在分钟6.5万次太慢），只复用底层 _sma/_ema/_rsi/_kdj/_sample_std/clip 自写轻量"三周期共振+波动标准化动量"信号（预热 WARMUP=60、信号窗口 SIG_WINDOW=120、入场阈值 ENTRY=1.5）。
2. **撮合状态机（核心，勿回退）**：信号 bar i 收盘确认、i+1 开盘成交；入场当根不查止损止盈；持仓后每根先判止损后判止盈，跳空越过（多头 open≤stop）以开盘成交并标"止损(跳空)"、bar 内 low≤stop 以触发价成交，止盈对称；同根同时触及止损止盈保守按止损；反向信号或达到 max_bars 下一根开盘离场。**交易日 owner_of_dt（交易所结算交易日口径）**：≥21:00 的夜盘归属 next_trade_day(自然日)、00:00后凌晨归属当日自然日（周末顺延由离线日历跳）、日盘归属当日；日内模式 owners[i+1]≠owners[i]（跨交易日最后一根）按收盘价强平，入场当根恰好是末根也立即强平（不隔夜）；摆动模式 `--swing` 关强平、允许跨日。**平今 leg="today" 当且仅当 entry_owner==exit_owner**。
3. **精确锁板**：幅度取 config.FUTURES_LIMIT_MOVE（主力近月常态档，期货公司2026-09-01披露：RB/HC/SS/SP=5%、CU/AL/ZN等有色9%、NI10%、SN12%、AU14%、AG20%、RU/NR7%、FU/SC/LU14%、EC20%、I9%、J/JM8%、MA9%、CY4%、LC13% 等，兜底 LIMIT_MOVE=7%）；锁板价=前一交易日收盘×(1±幅度)，容差 LIMIT_TICK_EPS=0.0008；**整根 bar 封死**才拦截（买单入场查 low≥板价上限、卖单查 high≤下限），v==0 不成交，入场放弃计次、离场遇锁板顺延到下一根并计 blocked_exits。比日线"收盘贴边"精确得多，但仍是模型近似（免费无历史L2，报告持续标注）。
4. **成本与产出**：复用 backtest.load_fee_schedule/side_fee（开 open、平 today/close，返回名义价值比例+每手元），乘数 multiplier 直接取费率表（AG15/RB10/AU1000等，无需另建表）；滑点单边 SLIP_RATE=1e-4；逐笔 28 字段 CSV（进出场时间/owner归属/leg/持仓bar数与分钟/进出场价/毛/开平和滑点率/净/开平每手元/假设平昨元/平今节省元/每手人民币盈亏/乘数/费用模式/信号分/锁板顺延/离场原因）；报告含全品种汇总（毛/费/净/累计/笔夏普/今昨/锁板）、多空与平今平昨分组、离场原因分布、**入场×止损×止盈 3×2×3=18组稳定性网格**（检验主参数是否孤峰）。
5. **本轮抓到并修复的两个真实设计缺陷**：①最初误用 utils.trade_owner_date（夜盘开盘日口径，服务日切/复盘）判平今，导致"周一晚开仓周二日盘平"被错判平昨——改为交易所结算交易日 owner_of_dt 后正确判为平今；②入场 bar 若恰为交易日最后一根，原"入场当根不检查"会让日内单隔夜——改为该根立即按收盘价强平。另修 minute_bars_for_sym 位置参数误用（since/limit）。
6. **第15轮验证（2026-09-01 晚）**：①29个py compileall 全过；②**T1-T15 零网络合成断言全 PASS**（owner 归属：日盘/21点夜盘/凌晨/周五晚归周一/节假日；信号时序无未来函数；止损止盈触发/跳空/同根双触按止损；日终强平与入场即末根强平；平今vs平昨费率分支；SS"免固定费但仍有极低比例费"today<close；锁板入场拦截/离场顺延/零量不成交；滑点双边；到期离场）；③真实数据：30m 全64品种 10101 笔、58~128交易日/品种、错误0、无成交0，日内今/昨=10101/0；摆动 RB/CU 出现平今157/平昨93验证跨日路径；1m直读 RB 3交易日105笔；1m边界对齐聚合5m 与原生5m重叠204根 h/l/c 100%一致、开盘价24根有段首tick差异(15根差1跳、最大差4跳，自采1m段首tick精度)；--no-cost 可跑；DB integrity ok、minute_bars 327360根不变。临时探测/补丁/断言脚本（_probe15/_patch_*/_test_intraday_bt）全部删除，仍无 tests/。
7. **诚实结论与边界**：默认参数30m全市场成本后净均收 -0.022%、累计-91.9%（高频在万1滑点+真实费下不具成本后优势），18组网格方向一致（入场阈值越高、止损止盈越宽亏损越收窄，最优2.0/2.0/3.0均净0.000%）但无稳定盈利区，**主参数不是孤峰、策略整体不赚钱，如实呈现负结果**；单品种 LH/PP/AG/FU 窗口内为正、EC集运欧线因费率0.191%极高亏最重；**真实成本洞察：平今并非总更省——铜等品种 today 费率是 open 两倍，全市场平今单实际平仓费55458元、若按平昨54000元，反而加收1457.5元**。样本仅约6个月分钟窗口（1m仅3交易日），需随 minute_bars 常驻积累持续复测；回测只回放技术面，不复原历史新闻/机构/L2，不构成投资建议。

### 第16轮：WP-E 组合资金账户 + 银河期货保证金率表（共享资金池逐bar盯市/三种手数/强平状态机/权益曲线，零新增依赖）
> 对应《未完成项落地方案.md》工作包E（替代两套回测里"多品种按交易序列复利近似"的临时口径）；新增 `portfolio.py`（947行）、`tools/build_margin_table.py`（184行）、`data/futures_margins.csv`（64品种），改 config（+PORTFOLIO_*段）/report（+2页签）/backtest（修双零退化），数据库与常驻监控零改动。算法参考 vnpy_portfoliostrategy 的共享账户 calculate_pnl/calculate_cash（MIT，仅借鉴思路零拷贝）。
1. **保证金率表（用户指定银河期货=其开户公司）**：`tools/build_margin_table.py` 主源 `https://www.yhqh.com.cn/col71/27539.html`（"结算时起各品种最新交易保证金比例"，静态table，requests带UA、r.encoding=apparent_encoding、pandas.read_html 跳双层表头，取纯字母代码品种基础档+列4投机档+列3板幅；剔除 cu2609 等近月行与 l_f 月均价行），支持 `--url` 指新期页面/`--html` 离线另存解析；乘数为每手报价单位个数口径（内置 QUOTE_MULTIPLIERS，沿用 build_fee_table 硬编码模式并与 futures_fees.csv 交叉校验），唯一差异 JD 鸡蛋用 MULT_KNOWN_DIFF 白名单（保证金mult=10/费用表按吨5）。产出 utf-8-sig CSV，as_of=20260828、source 标注银河投机档；**pandas 只允许在 tools 维护工具用，生产运行时仍零新增依赖（portfolio 只用标准库csv读表）**。
2. **Portfolio 账户类（核心口径，勿回退）**：静态权益=equity0+已实现盈亏（开/平仓费即时扣）；动态权益=静态+浮盈；占用 margin=Σ 价×mult×手数×broker_margin；可用=动态权益−占用；风险度=占用/动态权益；record(dt,prices) 逐bar记曲线（含回撤、持仓数）；open 返回 (lots,reason)、close 返回成交记录；**decide_lots 约束链=策略目标手数(等名义:equity×per_symbol/单手名义；等风险:风险预算/(1.2ATR×mult)；score分档)→单品种名义30%→板块60%→可用×0.99→同时持仓数，任一拦截都记原因**；缺保证金表品种回退 DEFAULT_MARGIN=0.12 并登记 fallback_margins，报告抬头显式列出（不静默用错数）。
3. **统一时间轴组合回放 run_portfolio**：取全部品种 bar 的 dt 并集排序为统一时间轴，每时刻按字母序确定性处理（结果可复现）；无bar品种沿用最近收盘价盯市；分钟模式复用 intraday_backtest 的 load_minute_bars/ratio_adjusted_bars/prepare_series/build_owner_meta，日线模式复用 backtest.prepare_symbol/technical_score 与 futures_data.fetch_daily_kline；**每品种自己的引擎状态机（pending/pos/blocked）与既有撮合完全同构**：信号i收盘确认i+1开盘成交、入场当根不查止损、同根双触按止损、分钟日终强平、锁板整根封死；组合层在每时刻先撮合各品种、再统一盯市、再跑强平。
4. **强平状态机（本轮两处修正，关键）**：①最初强平循环只对照强平线退出、传入的安全线没生效——改为"未触发看强平线、一旦 rd≥强平线则 triggered 后一路砍到安全线以下"（避免刚跌破100%就停、下一根轻微波动又反复强平），safe>liquidate 时构造自动 min；②真实压测抓到 KeyError：风控要砍一个"当前时刻无bar"的持仓（无夜盘品种在夜盘时段）时，平今判定直接 by_dt[t] 崩——新增 `SymbolFeed.owner_at(t)`（bisect 取≤t 最近bar的owner）、价格沿用最近收盘价；强平后补记同时间戳曲线点。
5. **修复既有公共退化 backtest 双零**：resolve_codes 把 meta["code"]="RB0" 又 +"0" 拼成 RB00，新浪日K `getDailyKLine?symbol=RB00` 返回 `var t=(null)`（只认单零 RB0，RB00/CU00/SS00 多组实测），导致日线回测当时全部"样本不足"；改为直接用 meta["code"]，自由输入分支用 isalpha（纯字母补0、具体合约RB2601原样）。生产监控与分钟回测本就用单零未受影响。
6. **第16轮验证（2026-09-01 深夜）**：①32个py compileall 全过；②**37条零网络合成断言全PASS**（两品种一赚一亏逐点：t1动态权益102500/占用10750/风险度=占用÷动态权益、期末102500；连续下跌B浮亏-30000先于A-20000被砍、砍后风险度0.64<0.8安全线、期末50000；缺表兜底0.12+fallback登记；三种sizing手数15/33/10/15；板块上限45手；1手买不起拒开；平今开5+平今16.5=21.5、净4978.5；无bar时刻连砍A/B且B沿用100平价强平）；③真实端到端：全64品种30m 6099笔/期末758495/-24.15%/夏普-3.64/最大回撤26.87%/平均风险度17.3%/0强平/保证金64/64命中；全64品种日线250日341笔/+10.56%/夏普0.84/回撤9.32%/0强平；强平线压到15%的压力测试触发2900次强平、引擎持续同步可再开仓、报告强平分组与事件完整；main --once --no-launch exit 0；删除全部临时脚本后另起干净进程重跑小样本结果逐值一致（可复现）。
7. **诚实结论与边界**：组合口径证实第15轮负结果不是单序列复利近似造成的假象——分钟高频在万1滑点+真实费+真实公司保证金下全市场仍亏（EC集运欧线单手费贵、单品种-7.57万居首，全市场手续费8.32万），日线动量过去250交易日组合口径+10.56%且几乎全靠多头（多头+14.16万/空头-3.59万）；同时持仓≤12 对64品种分钟信号是主要约束（27203次未开中23821次为持仓数上限），用户可用 --max-concurrent/--per-symbol/--equity 调整；保证金是银河披露的静态投机档，临近交割/长假公司会上浮、交易所基准档未取到，回测占用偏理想，实盘以公司结算单为准；不构成投资建议。

### 第16轮增补（两次全网/GitHub 调研，只分析未改业务代码；生产仍 32 个 py/11082 行，依赖不变）
> 触发：用户先后要求"评估开源 tushare 能否带入优化"与"全网搜 AI 量化+GitHub 量化代码策略对比改进、补进第16轮、更新摘要"；同第5轮性质=只分析轮，产出两份独立 md，不改生产代码、不新增依赖。详细文件级方法分别见两份文档，本节为归档摘要。

**增补① Tushare 引入可行性（独立文档《Tushare引入可行性分析.md》）**
1. 事实：GitHub waditu/tushare（BSD-3）开源 SDK 最后实质提交停在 2020-03、issues 堆积、pip 新包源码未完全开放（#1867）；持续运营的是 Tushare Pro（POST api.tushare.pro，语言无关 HTTP JSON）。本机 2026-09-01 23:39 实测无 token 也返回 HTTP200/code40101=通道可达，现有 http_client 可裸调、**不需要 pip 安装**。
2. 权限（官方权限总表逐行核实）：注册 120 分仅够股票基础；**期货日频接口清一色 2000 分**（fut_basic/期货 trade_cal/fut_daily(含 settle 结算价/oi)/fut_mapping/fut_holding/fut_wsr/fut_settle/南华 index_daily）；ft_limit（盘前每合约 up/down_limit + 最低保证金率 m_ratio）**5000 分**；opt_daily 5000；期货分钟 ft_mins/实时 rt_fut_min 为积分外单独付费（历史 2000 元/年、实时 1000 元/月）。2000 分获取=高校学生/教师认证可申请、否则捐助约 200 元（1 元≈10 分），5000 分约 500 元。
3. 对照结论：高价值补强项=交易所基准保证金率（补 futures_margins.csv 明确留空的 exchange_margin，5000 分）、仓单全历史（东财仅 3 个月→2006 起分仓库，库存分位升级）、官方交易日历（免每年手动补 STATIC_HOLIDAY_RANGES）、具体合约日线+结算价+主力映射（回测脱离主连比例复权/收盘盯市近似）、盘前真实板价（替代 FUTURES_LIMIT_MOVE 常态档硬编码）；中价值=龙虎榜全历史/合约元数据自动同步/交易所手续费基准交叉校验/南华板块指数。**不可替代**=盘中实时（Pro 盘后更新）、分钟 K（收费）、6 新闻源、期权实时链。
4. 引入裁决：**只走方案 A 零依赖可选适配层**（tushare_client.py，token 环境变量、软降级、现源为主、盘后批量），装 SDK（方案 B）与替换主源（方案 C）均否决；T1~T6 落地顺序与"第二信源、永不成唯一源"定位见独立文档。

**增补② AI 量化对标与改进路线 WP-F（独立文档《AI量化对标与改进方案.md》）**
1. 调研范围：四族——多智能体 LLM（TradingAgents/ai-hedge-fund/TradingAgents-CN）、ML 因子平台与方法论（微软 Qlib、López de Prado AFML）、深度强化学习（FinRL/FinRL-X、Qlib-RL）、金融 NLP（FinBERT/中文轻量 LLM/期货多维情绪论文）；并记录现实反面证据（Stanford 趋势 ML、FUTURES_NN 49%、XGBoost 命中但 alpha 为负、Bayes-CID 正面样本的成立条件）。
2. 总判断：不追 DNN/DRL 热点（商品期货上时序/截面动量最稳、本项目分钟样本尚短、生产零依赖）；把 AI 用在五件高确定性事上=分层裁决、横截面、因子评估闭环、情绪多维化、LLM 仅文本复核。
3. **WP-F1（最高优先/纯标准库/默认不改综合分）**：A1 `build_debate(parts)` 多空双面卡（analyzer+report）；A2 新增 `risk_gate.py` 独立风控闸门（veto/warn，检查锁板/流动性/carry 背离/单因子拉动/板块拥挤/临近交割/HV 极端，区别于只提示的 risks[]，config 开关、关闭时与现状逐值一致）；B1 新增 `cross_section.py` 横截面稳健 z-score（中位数/MAD）+板块强弱+多空 Top（main 收齐 rows 后统一算，补清单⑮，portfolio 可选 --cross-section）；D1 `factors.sentiment_facets` 五维情绪（强度/不确定性/相关性/前瞻性/事件类型，极性×强度×相关、不确定降权，news 维度向后兼容）。
4. **WP-F2（自有 DB 统计/生产零新增）**：A3 新增 `signal_calibrator.py`（signal_outcomes 方向×分档×主导因子贝叶斯平滑胜率→sizing 乘子[0.5,1.2]，n<20 回退 1.0、先影子模式）；B2 `tools/factor_eval.py`（IC/RankIC/ICIR/分档单调/衰减/walk-forward，只建议权重不自动改）；B3 `tools/build_ml_samples.py` 复用 intraday_backtest 撮合生成 triple-barrier 标签、storage 增第 9 表 `ml_samples`（UNIQUE(sym,bar_dt)、严格 PIT+embargo）。
5. **WP-F3（可选/无 key 休眠）**：新增 `llm_reviewer.py`，只复核紧急轮/≥6.5 强信号/词典与技术背离三类小流量，OpenAI 兼容协议走现有 http_client、守护线程异步、强制 JSON schema、任何失败软降级、只输出"第二意见"**永不改分永不下单**；key 走环境变量 FUTURES_MONITOR_LLM_KEY/BASE_URL/MODEL。
6. **WP-F4（最低/最严门槛）**：确立"tools/ml_train 重依赖训练→导出 JSON→生产 ml_inference.py 标准库推理→缺模型回退线性 parts"唯一合规通道（sklearn/lightgbm 同 pandas 只在 tools、绝不进 requirements）；只做浅模型多因子融合/meta-label，禁价格预测；强制 purged TimeSeriesSplit+embargo；**样本外真实成本后不优于现有线性规则就不上线**，即便上线也只作小权重"ML融合"因子、默认关。
7. **明确不做（防重复踩坑）**：端到端 LLM 自动交易、DRL 实盘仓位、裸 K 线 LSTM/Transformer 预测价格、在线学习热更新（不可复现）、torch/sklearn 进生产依赖。
8. 影响评估：F1/F2/F3 生产零新依赖、默认输出与现状一致只增信息（风控闸门保守且可关、胜率校准先影子、LLM 无 key 零请求）；计算均毫秒级、LLM 异步不阻塞主循环；新表/新列向后兼容；AI 不改变"分钟高频真实成本后不赚钱"的已验证负结果；F1/F2 不依赖 P2 可先行，F4 须等 P2-②pytest 与样本积累。与增补①互补：tushare 的全历史仓单/真实板价可提升 F1 基本面因子与风控闸门质量。

### 第17轮：调度时段边沿触发 + 首轮报告自动打开（2026-09-02，✅ 合成断言+真实冒烟通过）

**需求**：①非交易时段启动，到点进入交易时段时立即开始交易轮动、轮动间隔跟该时段一致；②交易结束进入非交易时段同理立即轮一轮并切回非交易间隔；③程序启动后第一轮真实报告生成时自动打开实时报告 HTML。

**改动前的真实机制与缺口**：主循环 run_cycle→`next_cycle_time`算下一计划点→`wait_with_emergency` 等到点；非交易时段每 60s 整分一轮，`next_cycle_time` 仅在"1 分钟内将开盘"时把计划点对齐开盘，且其调用的 `next_session_start` **只枚举 09:00/21:00 两个开盘点、不含下午 13:30**——午休结束能在 13:30 跑只是"非交易恰为整分节奏"的巧合，一旦调大非交易间隔就会错过开盘；整体没有显式的时段状态翻转检测，交易→非交易也只靠"收盘后 e+1 分钟"隐式安排。

**实现（全部零新增第三方依赖）**：
1. `utils.py` 新增 `next_transition(now, within_days=12)` 纯函数：在交易时段返回当前 session 的收盘点；非交易则逐日枚举 **09:00、13:30（交易日）与 21:00（当晚有夜盘）** 三个候选，取严格晚于 now 的最近者，节假日/周五晚无夜盘/周六凌晨延续均由 trade_calendar 过滤，与 current_session 对称。
2. `main.wait_with_emergency`：进入记 `enter_trading=is_trading_time()[0]`；等待时长 = min(到 nxt, 到 next_transition)，使等待必在翻转点醒来（翻转点缺失只等 nxt）；唤醒未被 kick 时先比交易状态，翻转→返回 **"edge"**，到 nxt（≤1s）→True，否则继续等；原油/全网紧急插队逻辑保持"nxt 不推移"，且紧急轮后刷新 enter_trading 防止跨点误判。
3. 主循环：`reason=="edge"` 时记录进入/结束交易时段的 WARNING 并立即 continue 跑 run_cycle；该轮 rotation_desc 已按新时段自动描述，跑完 `next_cycle_time` 重排即落入新时段刻度（开盘后 5/20 分钟、收盘后 60 秒）。True/False 分支行为不变。
4. 首轮自动开报告：新增 `open_realtime_report_once(state)`（Windows `os.startfile(REALTIME_HTML)` 非阻塞、其他平台 webbrowser.open file:/// 兜底），钩子在 run_cycle 第5步 `report.save`（其内部会 write_dashboard 刷新实时HTML）之后、仅 cycle==1；State 新增 auto_open_report/report_opened 保证全程一次；`state.auto_open_report = not args.no_launch`；config 加 `REPORT_AUTO_OPEN=True`；--no-launch 帮助与 main 顶部文档同步更新（现在同时管同花顺与浏览器）。任何打开异常只 LOG.warning，绝不阻断监控。

**验证（全绿后才收尾，临时脚本跑完即删）**：next_transition 9 边界 + "翻转点前后 1 秒交易状态必相反"不变量；wait_with_emergency 四分支 monkeypatch 注入；自动打开 5 项（mock os.startfile 不真弹窗）；compileall；`main.py --once --no-launch` exit 0；常驻观察午休非交易整分节奏无回归。**注意**：--once 只跑一轮不进 wait，边沿行为由合成断言保证；真实开盘边沿（如 13:30/21:00）到点可在日志看到"检测到进入交易时段"WARNING。

### 第18轮：WP-F1（P0）多空双面卡 + 横截面强弱 + 独立风控闸门 + 五维情绪（2026-09-02，✅47断言+真实冒烟）

**来源**：执行《统一改进路线图（按优先级融合）.md》的 P0（=《AI量化对标与改进方案》WP-F1），全部零新增第三方依赖、纯标准库实现。**总原则：四层都是"信息增量/独立复核"，默认完全不改变既有综合分、信号 label 与操作 advice，保证与旧版可逐行对照、可一键关闭回退。**

1. **A1 多空双面论证卡**（analyzer.py，+build_debate、返回 dict 加 "debate"、detail_lines 加"多空:"行）：多方/空方分别收集——九类 parts 因子（键名映射短标签，"基本面""量仓资金"与后列分支去重）、当日涨跌 chg、量仓 flow.score+pattern、机构 inst_ratio 净多空率、fundamental 分、HV 分位≥0.8 入空方；verdict 按 score ±0.3 判多方/空方/多空均衡。它不改分，只强制把反方证据摆出来。
2. **B1 横截面相对强弱**（新文件 cross_section.py，139行）：`_robust_z` 中位数+MAD（0.6745 系数，MAD=0 退总体标准差、再退全0，clip±XS_Z_CLIP=3）；`rank(fut_rows)` 对综合分、涨跌幅分别稳健z，xs=XS_SCORE_W 0.6×分z+XS_CHG_W 0.4×涨跌z，样本<XS_MIN_SAMPLE=8 时 robust=False、xs 退回 score/SCORE_MID 归一；按 cat 聚合板块 n/up/down/avg_xs，sector_rank 排序，top_long/short 各5，breadth 多空中性家数+平均涨跌；`format_block` 出4行文本。main 在 4.7 算好存 state.last_cross_section，report 在基本面速览后渲染。
3. **A2 独立风控闸门**（新文件 risk_gate.py，112行）：evaluate(row) 纯判定 → {pass/warn/veto, veto[], warn[], reasons[]}；apply_gate(row) 写 row["risk"] 并在 RISK_GATE_AUTO_DOWNGRADE=True 时把 veto 信号 label 改"暂缓"、原值存 label_before_gate/advice_before_gate。veto 三规则（流动性/强信号反向背离/极端HV），warn 四规则（高HV/量仓背离/临近交割到期/强信号低置信）。main 4.7 逐品种 apply、veto 汇总 LOG.warning；report 总表行内标 ⛔/⚠、明细加"风控:"行；alerts 对 veto 以 level=strong 独立 emit（key=risk-veto:品种，复用 ALERT_SIGNAL_COOLDOWN_SEC 限流与轮末聚合）。任何风控异常都退回 pass、绝不阻断主流程。
4. **D1 五维情绪**（factors.py 末尾 +103行）：INTENSITY/UNCERTAINTY/FORWARD 三组词表 + EVENT_TYPES 8 类；sentiment_facets 纯函数（_distinct_hits 同词去重、tanh 饱和、variety 命中相关性=1、异常整体回中性），facet_tags 按 SENTIMENT_FACET_TAG_MIN=0.25 出角标。analyzer.detail_lines 每条命中消息实时算 facets 追加角标。NewsFactor.score 主分逻辑一字未动。

**配置（config.py 末尾 WP-F1 块，+29行）**：SENTIMENT_INTENSITY/UNCERT/FORWARD_K、FACET_TAG_MIN；XS_SCORE_W=0.6/XS_CHG_W=0.4/Z_CLIP=3/TOP_N=5/MIN_SAMPLE=8；RISK_GATE_ENABLED=True、AUTO_DOWNGRADE=False、MIN_VOLUME=200、DIVERGE_CHG=0.02、HV_EXTREME=0.95/HV_HIGH=0.80、FLOW_CONFLICT/NEAR_DELIVERY/ALERT 布尔。

**验证与踩坑**：47 条零网络合成断言（含"默认配置下 apply_gate 前后 score/label/advice 完全不变"铁律断言、自动降级/总开关对照、MAD=0/空输入/None 安全、多空卡去重）；compileall；三次真实 --once 冒烟 exit0。踩坑：①报告分时段分流，午休/收盘后只写 offhours_report.txt，latest_report.txt 停在最后交易时段轮次——验证非交易改动要看对文件；②生产文件 LF/CRLF 混用，补丁多行锚点先归一换行；③补丁三引号块内 \n 会成真换行，源码要字面 \n 得写 \\n（main 4.7 的 LOG.debug 曾因此语法错误，已修）。

## 三、关键文件职责
- `config.py`：全部可调参数（SESSIONS 夜盘段已延至次日02:30、各品种夜盘结束档 night_end、前30分钟300s/之后1200s/非交易60s、原油急动三参数、全网扫描间隔180s与触发阈值/存疑词/金融阈值、64品种表、日志轮转大小/份数、交易日历缓存路径、输出路径；第7轮新增 DATA_DIR/MONITOR_DB、信号三周期与统计窗口、FLOW_*量仓参数、ALERT_*声音/Webhook/冷却参数；第8轮新增 RSI/MACD/KDJ/BOLL/共振上下限、IV分位买卖阈值、回测默认窗口/持有期/线程/换月跳空阈值；第9轮新增 INTRADAY_* 分钟缓存/线程/共振上限、OPT_SELLER_MARGIN_RATE/OPT_FUTURES_MARGIN_RATE、BACKTEST_FEE_RATE/BACKTEST_SLIP_RATE/BACKTEST_LIMIT_LOCK/BACKTEST_STABLE_*；第14轮新增 MINUTE_*：常驻/回填周期与各周期条数LMT、WORKERS=6、REQ_GAP请求间隔、EM_HOSTS镜像、RETRY/熔断FAIL数与冷却、TDX_ENABLED开关与市场号；**第15轮新增 INTRADAY_BT_*（PERIOD=30/LOOKBACK=1023/WARMUP=60/SIG_WINDOW=120/ENTRY=1.5/ATR_PERIOD=14/STOP_ATR=1.2/TARGET_ATR=2.0/MAX_BARS=48/FLAT_EOD/FEE_RATE=5e-5/SLIP_RATE=1e-4/LIMIT_MOVE兜底0.07/LIMIT_TICK_EPS=0.0008、稳定性网格ENTRIES=(1,1.5,2)×STOPS=(1.2,2)×TARGETS=(1.5,2,3)）与 FUTURES_LIMIT_MOVE 64品种常态涨跌停表；**第16轮新增 PORTFOLIO_*（输出三文件路径、EQUITY0=1_000_000、SIZING=equal_notional、PER_SYMBOL=0.15、RISK_PER_TRADE=0.01、SCORE_WEIGHTS、MAX_SYMBOL_WEIGHT=0.30、MAX_SECTOR_WEIGHT=0.60、RISK_LIQUIDATE=1.00、RISK_SAFE=0.80、DEFAULT_MARGIN=0.12、MAX_CONCURRENT=12、FUTURES_MARGINS_FILE=data/futures_margins.csv；第17轮新增 REPORT_AUTO_OPEN=True（首轮报告浏览器自动打开总开关））**）。
- `utils.py`：is_trading_time / **is_variety_trading（品种级夜盘分档）** / current_session / trade_owner_date（凌晨归属前一交易日）/ review_is_due / next_cycle_time（真实datetime调度）/ **next_transition（下一翻转点：收盘点或09:00/13:30/21:00最近开盘，第17轮边沿触发，补齐旧函数不认13:30的缺口）** / rotation_desc / pad / 日志（setup_environment 已改 RotatingFileHandler 幂等挂载）。
- `trade_calendar.py`（**第6轮新增**）：交易日历。动态=新浪RB0主连日K(主)+东财指数日K(备)缓存 cache/trade_dates.txt；静态=STATIC_HOLIDAY_RANGES 2026法定休市；对外 is_trade_day/next_trade_day/prev_trade_day/has_night_session/status_line/ensure/refresh；锁用 RLock。**每年12月证监会发布次年安排后需把区间补进 STATIC_HOLIDAY_RANGES。**
- `http_client.py`（**第6轮新增**）：进程级全局 `http=Session()` 连接池（HTTPAdapter/keep-alive/统一timeout），各数据层统一走它，禁止再用裸 requests。
- `alerts.py`（**第7轮新增**）：AlertManager 主动告警；winsound声音、飞书/钉钉/企业微信/Server酱/通用Webhook、跨档/翻转/策略/紧急轮动冷却；声音和推送均为守护线程。
- `storage.py`（**第7轮新增/第11/13/14轮加表**）：MonitorDB SQLite**八表**（第14轮 `minute_bars` 分钟K自采库：UNIQUE(contract,period,bar_dt)去重、按sym跨合约查询、保留400天）（第11轮 `option_chains`：UNIQUE(sym,expiry,ts)；第13轮 `fundamentals` 日频基本面快照：ts/trade_date/sym/variety/inventory/inv_pct/inv_wow/inv_n/rank_long/rank_short/rank_net/rank_delta/carry/basis_rate/fund_score/raw_json，UNIQUE(sym,trade_date) INSERT OR REPLACE，日频量小长期保留不prune）、相同行情快照去重、非中性期货信号/新闻/期权入库、信号三周期待评估任务、到期回填 hit/miss/flat/expired、有效样本胜率统计、180天高频明细清理。第13轮新增 `insert_fundamentals`/`latest_fundamentals`（重启读最近交易日快照）；第14轮新增 `insert_minute_bars`（返回新增行数）/`minute_bars_for_sym`（按sym跨合约升序）/`minute_bars_coverage`（周期覆盖），prune 加 minute_bars 400天清理，table_counts 同步 minute_bars。
- `flow_tracker.py`（**第7轮新增**）：FlowTracker 相邻轮次量仓快照，输出增/减仓×涨/跌、放量/缩量与±1.2内的量仓资金分；识别新交易日/换月并清空序列重建基线。
- `option_chain.py`（**第11轮新增，199行**）：新浪商品期权T型报价直连（与行情同站、走全局http_client、零新增依赖）。product_code（SHFE/INE/DCE/GFEX=小写sym+"_o"、CZCE=小写无后缀）、pinzhong（小写sym+4位年月，月份取自 webdata.fetch_option_calendar 的六位yymm后4位）、parse_leg（兼容SHFE/INE/GFEX 9元素含独立行权价与DCE/CZCE 8元素行权价在代码尾部，正则 ^([a-z]+)(\d{3,4})([CP])(\d+)$ 解析，持仓量=index5）、build_summary（calls/puts/总持仓/pcr_oi及pcr别名/最大持仓行权价/情绪档/label）、locate_atm（按标的现价找最近行权价）、pcr_sentiment（PCR_LOW0.7/HIGH1.2/极值0.5/1.5五档）、fetch_chain、OptionChainCache（TTL30min、6线程warm，key=("SYM",yy,mm)，单品种失败不阻断，仿KlineCache范式）。已知缺口：新浪T链无INE低硫燃料油LU期权，自动降级无链。
- `iv_surface.py`（**第12轮新增，267行**）：多到期日IV曲面。implied_vol（Black-76二分反推，40次/区间[1e-4,5]）、leg_quote（窄价差中间价q0/宽价差回退last q1/无成交丢弃）、_merge_strike（call/put按OI加权、偏差大取可信侧）、expiry_smile（ATM/25Δ RR/Fly，ATM须高质量腿、<30天不输出、离群点[0.2×,3.5×]带外剔除）、build_surface（最近3个真实挂牌月份、ATM期限Contango/Back/平坦、5档moneyness矩阵缺档--不插值）、surface_text两行摘要。
- `fundamental_factors.py`（**第13轮新增，151行，纯函数零网络**）：库存/仓单分位+周环比、龙虎榜净多率+边际、carry、基差四个 [-1,1] 子因子与 build_fundamental 缺项权重重归一加权（满分±1.5），全缺返回None。
- `fundamental_data.py`（**第13轮新增，188行**）：FundamentalFetcher 东财直连（品种映射/库存时序/龙虎榜TYPE=2，自然日日缓存+RLock）、生意社基差标准库HTMLParser解析+HW_CHECK反爬识别降级，任何失败软返回None/[]。
- `intraday_bars.py`（**第14轮，当晚增补后304行**）：分钟K三源数据层。fetch_sina_minute 新浪主连（**1/5/15/30/60m全周期**各1023根、主连code免换月、p持仓量无额）、MinuteBarFetcher 东财具体合约（限流/镜像/熔断，最后兜底）、MinuteCollector 选源（**增补后全周期统一=sina→tdx→em，1m不再分叉、无主力合约也能采**）、aggregate_bars 周期聚合纯函数；统一bar dict{dt,o,h,l,c,v,amount,sym,contract,exchange,period,src}。
- `tdx_bars.py`（**第14轮新增，218行，可选源**）：pytdx延迟导入；TDX_FUT_MARKET(CZCE28/DCE29/SHFE·INE30/CFFEX47)、TDX_CATEGORY(1m8/5m0/15m1/30m2/60m3)、tdx_contract(CZCE三位大写/余四位小写)；TdxMinuteSource 并发probe(20线程/15s)验证可取期货才available=True，否则fetch零成本[]，断线换服、RLock串行；公共7709仅股票/7727不可达时自动不启用。
- `futures_data.py`（621行，核心行情数据层）：新浪主连/月份实时行情 `hq.sinajs.cn/list=nf_XX`（GBK解析、40个一批）、日K getDailyKLine、分钟K getFewMinLine（**增补后支持1/5/15/30/60全周期**）、外盘hf_原油黄金；compute_indicators 技术指标；**增补：fetch_quotes 主备降级——新浪为主，仅缺失品种走 `_fetch_quotes_em` 东财push2主连快照（secid=市场号.品种小写m、f111持仓量、oi<100残缺拦截），新浪全齐时东财零请求**。
- `contracts.py`（**第11轮扩展**）：fetch_ranking 每项补 latest/prev_settle/chg_pct（同一次月份行情，期限结构零额外请求）；末尾新增 `_month_index(yy,mm)` 与 `term_structure(info)`：按日历月升序排合约、近-远月差/价差%/年化展期收益率（间隔天数=月份差×30.42，TERM_ANNUAL_DAYS=365）、用前2与后2均价降噪判定正向Contango(近低远高)/反向Back(近高远低)/平水、输出note文本，有效月份<2或无latest返回None（兼容旧缓存）。
- `backtest.py`（**第8轮新增/第9轮重写/第10轮真实手续费化**）：最小日线技术回测，主连比例复权、非重叠交易统计、1/5/20日信号衰减；第9轮加入滑点毛净双口径、疑似锁板入场跳过/离场顺延、3×3参数稳定性、backtest_trades.csv；第10轮默认读取 `data/futures_fees.csv` 的投机账户真实费率，按金额+按手数叠加并按成交价/合约乘数折算，交易CSV拆分手续费、滑点和每手人民币费用，支持 `--fees-file/--no-real-fees/--no-cost`；**第16轮修复 resolve_codes 主连双零退化（RB00→直接用 meta code RB0，新浪日K只认单零，详见第16轮章节），现587行**。
- `intraday_backtest.py`（**第15轮新增，712行**）：基于自采 minute_bars 的日内/平今回测。load_minute_bars（直读或细周期现场聚合+1m边界相位对齐，注意 minute_bars_for_sym 用 limit=关键字）、轻量分钟信号（三周期共振+波动标准化动量，复用 futures_data 底层 _sma/_ema/_rsi/_kdj）、owner_of_dt（交易所结算交易日：21点夜盘归下一交易日/凌晨归当日/周末节假日顺延）、vnpy式bar内撮合（i收盘确认i+1开盘成交、止损止盈预埋单/跳空开盘/同根双触按止损）、日终强平/摆动跨日、精确锁板（FUTURES_LIMIT_MOVE 前收×幅度整根封死拦截/顺延/零量不成交）、真实费率平今平昨+每手人民币+平今对照、18组稳定性网格；CLI 与产物见零节首条；复用 backtest.load_fee_schedule/side_fee/metrics_from_returns 与 intraday_bars.aggregate_bars，零新增依赖。
- `portfolio.py`（**第16轮新增/第41轮 G26续 1018→1191行**）：组合资金账户回测。**第41轮：Portfolio 增可选 risk_sizing(inv_vol/erc，默认None逐字节等价旧版)+risk_gross/set_risk_weights/avg_risk_eff_n，decide_lots 按横截面权重覆盖目标名义、宇宙外回退等名义；纯函数 trailing_risk_weights 只用严格早于t的收盘价稠密对齐估协方差(严格PIT、样本不足返空不抛错)；run_portfolio 增 risk_cfg 按 rebalance 重估注入、_reset_feeds 清引擎状态支持确定性重复回放；CLI 增 --risk-sizing/--risk-window/--risk-rebalance/--risk-min-hist/--risk-gross/--risk-cap/--compare-risk(同宇宙三法影子对照表，基线CSV不变)。**Portfolio（逐bar盯市：静态/动态权益、占用、可用、风险度、权益曲线、decide_lots 约束链返回(lots,reason)、强平状态机 triggered 后砍到安全线、穿仓全平、fallback_margins 登记）、SymbolFeed（by_dt 索引+**owner_at(t) bisect 取最近bar结算交易日，解决无bar时刻强平平今判定**）、load_margins（标准库csv读 data/futures_margins.csv，缺品种回退0.12）、三种 sizing（equal_notional/equal_risk/score）、run_portfolio（统一时间轴字母序确定性回放，分钟复用 intraday_backtest 信号链/日线复用 backtest，撮合口径一致）、绩效（年化/夏普/索提诺/最大回撤/分品种与多空平今强平分组/未开仓原因分布）、CLI（--all/--codes/--limit/--daily/--days/--period/--sizing/--equity/--per-symbol/--max-concurrent/--risk-liquidate/--risk-safe/--margins-file 等），输出 portfolio_report.txt/portfolio_equity.csv/portfolio_trades.csv 到 reports；零新增依赖（pandas 仅 tools 用）。
- `tools/build_margin_table.py`（**第16轮新增，184行，维护工具非生产运行链路**）：银河期货保证金页静态表解析（--url/--html/--date），内置 QUOTE_MULTIPLIERS 每手报价单位乘数、MULT_KNOWN_DIFF 白名单（JD 10 vs 费用表5），与 futures_fees.csv 交叉校验后生成 data/futures_margins.csv。
- `main.py`（**第28轮接纸面**：State 末尾按 PAPER_ENABLED 实例化 self.paper/self.last_paper（默认 None 休眠、失败 try 兜底为 None）；run_cycle 第5步存储后独立 5.5 段调 paper.on_cycle+write_paper_account+LOG，整段 try 隔离不影响主链。其余职责）State 含全部缓存器（第11轮加 `self.opt_chains=option_chain.OptionChainCache()`）；后台线程（原油10s、全网扫描180s、webdata 10s、browser 30s、kline 60s同时刷新日线/30m分钟线、contract 30min、**基本面日频(16点后并发刷库存+基差，第13轮)**、**看门狗30s**、**分钟K自采 minute_bars_loop（第14轮：启动once小回填+backfill全量，之后交易300s/非交易1800s增量，三源选源）**）+ 主循环 run_cycle（写心跳→新闻/新闻入库→行情/量仓/行情入库→期货分析（**分析前8线程并发预取龙虎榜、fund_raw 传入 analyzer，第13轮**）→**3.9期权链并发预热（第11轮单月PCR；第12轮重写为最近3个真实挂牌月份多到期日，主力月挂fut_row["option_chain"]、多月份组装fut_row["iv_surface"]、6位日历键归一4位pinzhong、每月份链均入库）**→期权→策略→历史信号评估/信号期权入库/链快照入库/追踪页→报告落盘→告警→复盘到期判断）+ wait_with_emergency/_build_emergency（**第17轮：等待取min(计划点,next_transition)，交易状态翻转返回"edge"由主循环立即轮动并按新时段节奏重排；open_realtime_report_once 在首轮 report.save 后 os.startfile 自动开实时HTML，全程一次，--no-launch/REPORT_AUTO_OPEN 可关**）。
- `web_scan.py`：全网多源抓取、金融急变合成消息、可信度分级、影响评估触发（第6轮起 HTTP 走 http_client）。
- `report.py`（**第28轮加纸面账户**：paper_block 实时报告4行紧凑块、paper_account_text 全量账户文本(6段+诚实说明,100字符对齐)、write_paper_account 落 reports/paper_account.txt(utf-8-sig,休眠不落盘)、_DASHBOARD_TABS 加纸面页签且休眠隐藏；其余）：render 正文、save 分流落盘、**daily_rollover（第6轮改按 trade_owner_date 交易日归属清块，跨零点夜盘块保留）**、prepend_archive、build/write_daily_review（改由 review_is_due 到期触发；第7轮新增“信号效果追踪”章节）、signal_tracking_text/write_signal_tracking（第7轮看板页签）、看板与 report_status.js、_safe_write。第8轮看板新增“最小日线回测”页签，第9轮新增“回测交易CSV”页签，第15轮追加“日内/平今回测”“日内回测交易CSV”，**第16轮再追加“组合账户回测”“组合交易CSV”两页签（现共13个页签，928行）**；期权表展示IV分位、组合策略展示Δ/Γ/Vega/Θ与保证金点值估算，最大亏损支持“理论无上限”。第22轮 write_dashboard 同时幂等写图表静态页+同步ECharts、save 每轮 write_chart_data；**第23轮看板与图表页合并：图表页签改 `__charts__` 内嵌标记（不再iframe独立页），_dashboard_tmpl 经 charts.dashboard_embed_parts() 占位符注入 style/dom/js，show/reloadView 两路径分流（现959行）**。
- `factors.py`：关键词词典 LEXICON（约70词）/SPECIAL_RULES（15条正则）/上下文闸门 KEYWORD_GATES、NewsFactor（打分×confidence）、局部否定/转折反转；analyzer 综合评分；option_analyzer/option_strategies 期权（第8轮真实IV分位/偏度、HV波动率锥、组合Gamma）；futures_data 第8轮新增RSI/MACD/KDJ/BOLL/日线多周期共振，第9轮新增30m拉取、30m聚合60m、分钟共振与并发预热；**第18轮 factors 末尾新增五维情绪 sentiment_facets/facet_tags（强度/不确定/前瞻/相关性/事件8分类，不改 NewsFactor 主分）**。
- `cross_section.py`（**第18轮新增**）：横截面相对强弱，稳健 z=0.6745(x-中位数)/MAD、板块聚合、多空Top5、市场广度，format_block 供报告；只比较不改分。
- `risk_gate.py`（**第18轮新增**）：独立风控闸门 evaluate/apply_gate，pass/warn/veto，默认只标注+告警不改分，AUTO_DOWNGRADE 时才降级。
- `signal_calibrator.py`（**第19轮新增**）：历史同类信号贝叶斯平滑胜率+四级回退+sizing乘子，默认影子模式只展示。
- `tools/backtest_validation.py`（**第20轮新增，695行**）：DSR/CSCV-PBO/PurgedKFold/Walk-forward/参数高原防过拟合工具箱，研究侧离线只读，`--selftest` 自带合成断言。
- `config_loader.py`（**第25轮 G10 新增，189行，纯标准库**）：.env解析注入（真实环境优先）、dict深合并、按默认类型矫正、apply_overrides只覆盖已存在全大写常量（路径/函数/未知/类型不符安全跳过）；config.py启动先载.env末尾深合并config.json，缺文件等价旧版。
- `data_router.py`（**第25轮 G11 新增，242行，时钟可注入**）：SourceHealth熔断状态机(CLOSED/OPEN/HALF_OPEN)、DataRouter有序主备(异常/残缺降级、全失败AllSourcesFailed)、HealthRegistry单例REGISTRY；futures_data(quote_sina/quote_em)与intraday_bars(minute_sina/tdx/em)上报成败。
- `data_health.py`（**第25轮 G6 新增，181行**）：evaluate_quotes缺数/陈旧/跳变体检、HealthMonitor跨轮连续异常追踪与单轮增量折算、format_health_block报告块；只监控不改结果。
- `tools/db_archive.py`（**第25轮 G6 新增，159行**）：零依赖按年ATTACH导出SQLite分库或CSV年包，只导出不删主库，--selftest。
- storage.py（**第25轮扩到11张表，780行**）：新增 data_health 表(ts+source唯一覆盖/stale/jump/state/note/保留365天)+insert_data_health/data_health_recent，table_counts/prune同步。
- `tools/tsmom_eval.py`（**第30轮新增，622行，离线研究侧、零新增依赖，G7多窗口时序动量IC评估**）：复用 backtest 复权装载与 factor_eval 统计，构造(品种×交易日)pooled面板，出 IC/RankIC/ICIR/期限衰减/分档/互相关/OLS残差边际增量/IS-OOS/分品种一致性六表与自动裁决，写 reports/tsmom_eval.txt+.json，--selftest 10组断言；只给证据不改任何线上权重。
- futures_data.py（**第30轮 G7 增补**）：新增 _lookback_return/_window_std/tsmom_at/tsmom_features/tsmom_series 纯函数（ret{L}长窗累计收益、tsmom{L}=ret÷(窗口日收益std×√252)波动调整z、blend=等权tanh(z)，历史不足/零波动给None）；compute_indicators 在 max_bars=140 截断前用完整序列算长窗、新增8个影子键，旧指标逐字节不变；KlineCache fallback 补键。
- analyzer.py（**第30轮 G7 增补**）：TSMOM_SHADOW 开关下把 tsmom_shadow 挂分析行随 raw_json 落库，绝不进 parts、不改 score（开关两态综合分相等）。
- `tools/xsmom_eval.py`（**第31轮新增、第32轮扩展至977行，离线研究侧、零新增依赖，G7截面动量XSMOM评估**）：复用 backtest 复权装载/futures_data.tsmom_series/factor_eval，构造跨品种同时点面板，非重叠调仓分5档多最强/空最弱构建市场中性组合，出 L×H参数网格/分档单调/两腿拆解/等权·反波动率ivol/IS-OOS/板块条件化/毛净成本/截面裁决门；**第32轮增 sector_scope板块池过滤、long_excess多头超额、三腿ls/lex/long、truncate_dates同源双样本(一次拉2500截最近1023)、conditional_scan×8候选、robust_verdict双样本稳健(含长窗期数增量判据LONG_N_RATIO)、报告第五章、CLI --scope/--leg/--main-days/--long-n-ratio/--no-conditional**，--selftest 14组；真实双样本8候选无一达标（全市场近4年净t0.45、有色2.01系2年同源小样本被否、剔除能化1.48差一点且事后选板块），XSMOM归档边缘效应，只给证据、不改任何线上权重与综合分。
- `tools/attribution.py`（**第35轮 G28 新增，654行，离线研究/复盘侧、零新增依赖、只读 monitor.db（mode=ro 绝不写库）**）：信号事件(parts_json⨝signal_outcomes)的多因子收益归因+BHB板块归因。factor_attribution=带截距OLS加法分解 mean(y)=α+Σβ·mean(x)严格闭合（ols_fit/_ols_detail 给β与t值、零方差/共线列自动剔除、奇异安全）、is_oos_split 时间有序IS/OOS β方向一致性；bhb 为纯函数三效应 AR+SR+IR=组合−基准恒等（events_to_sector_stats 出 wp事件占比/rp方向化均收/rb无方向均涨=y/dir，universe_sector_weights 按 config.VARIETIES 板块只数在考虑集内归一）；factor_curve 逐事件累计、末端闭合=Σy；load_events 只读join、parse_event_row 方向化暴露(part×dir、动态原油键经 factor_eval._canon 归一)；build_report 五章+JSON sidecar(无NaN、allow_nan=False)、write_curve 落CSV；--selftest 8组；只归因、绝不改任何线上权重与综合分。
- `factors_catalog.py`（**第36轮 G21 新增，根模块，195行，纯数据零行为、不被 main import**）：全项目因子唯一登记处。CATALOG=20条（9综合分part live/4基本面子项/4技术指标/tsmom影子/xsmom archived/carry tracking），每条 key/层/方向/贡献界/status/引入轮/live_at/公式；PART_KEYS 九part规范序（测试钉死==config.ATTR_FACTOR_ORDER）；by_key 支持动态原油键归一、part_records/all_keys/validate/catalog_text/selftest。
- `tools/panel_builder.py`（**第36轮 G21 新增，430行，离线研究侧、纯标准库、独立缓存库**）：标准研究面板（品种×交易日×30字段）。asof_before/asof_pick 严格as-of；build_symbol_rows 逐 t 只用 bars[:t+1] 经 futures_data.compute_indicators（与实时同函数=训练服务一致）、基本面严格 trade_date<当日、NULL不编造；PanelStore 落 cache/research_panel.db（research_panel 主键(sym,date)、整品种幂等重建、research_runs manifest）；resolve_items/build_items 联网（复用 backtest/futures_data、失败软降级）、manifest_text；--selftest 7组。
- `tools/pit_audit.py`（**第36轮 G21 新增，291行，PIT/训练-服务一致性通用审计器**）：timestamp_leaks/asof_join_check 时间戳越界、assert_no_future/future_perturb_check 扰动法无未来（含反向泄漏用例）、parity_one/parity_for_symbol 面板vs实时同前缀逐字段、audit_panel_db 缓存结构（日期唯一递增/ret1d复算/无NaN）；--db 零网络审计、--codes 联网parity、--selftest 5组。
- `tools/factor_health.py`（**第37轮 G29 新增，约430行，离线研究/体检侧、纯标准库、只读 monitor.db（mode=ro）+G21面板**）：因子体检卡。事件层 factor_pairs/rolling_ic（窗60步20、连续≥3弱|IC|<0.03/翻转=失效预警）/block_bootstrap_ic（块长20保时序自相关、500次确定性种子、p5/p50/p95/同号率）/factor_event_health（裁决健康/健康(反向)/走弱/失效预警/样本不足，健康要求CI保守边越0.03且同号率≥0.95）/regime_proxy（多空×轻仓分批强信号）；日频层 forward_map（只向未来取）/daily_factor_curve（九因子×H=1..60池化RankIC与Q5-Q1）/fit_exp_halflife（IC指数衰减半衰期τ·ln2，不衰减返None）；复用 attribution.load_events/factor_eval.spearman·quantile_buckets/panel_builder.PanelStore；出 reports/factor_health.txt/.json（无NaN）；--selftest 8组；只体检、绝不改线上权重与综合分。
- **第37轮 G21续 对既有研究工具的增量**：`panel_builder.py` 加 `panel_rows_to_bars/load_adjusted_bars`（面板回读已复权bar、**禁止二次复权**，selftest 7→8组）；`tsmom_eval/xsmom_eval/carry_eval` 各抽 `records_from_adjusted/points_from_adjusted/carry_points_from_adjusted` 纯函数并加 `--panel`（缺省联网路径逐字节不变，面板路径真实重叠点 maxAbsDiff=0）；`factors_catalog.py` 加 `HEALTH_SNAPSHOT/get_health`（G29体检卡回写、validate 钉死只回写已登记因子、selftest 锁机构动向反向）。
- `factor_expr.py`（**第38轮 G25 新增，根模块，约480行，纯标准库、不 import tools、不被 main/analyzer import**）：表达式因子引擎。安全递归下降解析 parse（白名单算子、禁 `__`/`;`、属性点由分词器拒绝、未知算子/错元数/非常量正整数窗口抛 ExprError）；时序 compute_ts/_eval_ts（delay/delta/ts_sum/ts_mean/ts_std/ts_min/ts_max/ts_rank/ts_minmax/decay_linear/corr，尾窗无未来、可嵌套、缺值None）；截面 eval_cs（cross_rank/scale/zscore，同一AST双态）；治理 pearson/spearman/solve/orthogonalize 正交残差/equal·ic·icir_weights/combine；LIBRARY 5个research表达式因子；--selftest 9组。**默认只承载新研究因子，旧因子过程式原实现不动、综合分逐字节不变**。
- `tools/expr_research.py`（**第38轮 G25 新增，约290行，纯离线/零网络/只读 G21 面板**）：series_from_rows（面板列=离线）vs series_from_bars（panel_rows_to_bars回读=实时形状）同表达式 parity；indicator_crosscheck 表达式5日动量对齐面板实时ret5；symbol_factor_ic/forward_return 严格未来 H=1/5/20 RankIC；出 reports/expr_research.txt/.json（无NaN）；--selftest 5组；research 因子不进分。
- `tools/factor_regime.py`（**第39轮 G29续 新增，约360行，纯离线/零网络/只读 G21 面板、不被 main import**）：因子 regime 分层/换手/衰减形态。trend_labels（面板PIT的ret126分牛/熊/震荡±2%）/vol_labels（hv60过去120日 G25引擎ts_rank分低/中/高波）/compute_labels（每品种只算一次缓存）；regime_stratified_ic（trend/vol/牛熊×低高波分桶前向H=5/20 RankIC、桶n<40不给）；factor_persistence（滚动分位lag1/5/20秩自相关+平均|分位变动|=隐含换手）；_ols+fit_decay_shapes（指数lnIC=a−H/τ半衰期τln2 与幂律lnIC=a−βlnH 按log空间R²择优、不衰减/点不足返None）；analyze_factor/run 复用 factor_health.forward_map·daily_factor_curve·fit_exp_halflife、factor_eval.spearman、factor_expr.compute_ts、panel_builder.PanelStore；出 reports/factor_regime.txt/.json（allow_nan=False）；--selftest 6组；只研究、绝不改线上权重与综合分。
- `portfolio_constructor.py`（**第40轮 G26 新增，根模块，约450行，纯标准库、不import tools、不被main/analyzer import；**第41轮起被 portfolio.py import 作风险型sizing求解器（仍不被analyzer/main直接碰）****）：横截面风险型权重分配。covariance/shrink_diagonal(对角收缩保正定)/largest_eigenvalue(幂迭代)/is_psd/_gauss_solve(自含高斯消元)；equal_weights/inverse_vol_weights/risk_parity(ERC=最小化½w'Σw−Σlnw，全Newton解Hessian=Σ+diag(1/w²)+回溯线搜索、**迭代中不归一末尾才归一**，乘性定点会停滞勿回退)/min_variance·quadratic_long_only(长仓GMV，FISTA投影梯度凸最优)；project_capped_simplex(和=1/非负/单票上限二分投影)、target_vol_scale(目标波动+max_gross杠杆上限)、risk_contributions/diversification_ratio/effective_n/gross_exposure/turnover；统一construct；--selftest 10组（ERC=逆波动手算、GMV波动≤等权不变量）。**默认equal等价旧等名义、不改线上sizing**。
- `tools/portfolio_lab.py`（**第40轮 G26 新增，约300行，纯离线/零网络/只读G21面板**）：load_return_map/dense_matrix(近504日覆盖≥95%固定宇宙、稠密对齐)、latest_snapshot(过去126日协方差四方法权重快照+目标波动杠杆)、rolling_proxy(每20日用过去126日严格无未来定权持有)、perf_stats(几何年化/波动/夏普/最大回撤/Calmar/有效N/年化换手)；出 reports/portfolio_lab.txt/.json(无NaN)；--selftest 5组（滚动无未来且GMV波动≤等权、覆盖率筛选、短序列安全）。
- `tools/trade_journal.py`（**第42轮 G30① 新增，710行，纯标准库零网络只读**）：load_trades/load_equity 宽容解析CSV（缺文件返[]、按exit_dt升序）；bucket_table 七维分桶（品种/板块/多空/平仓原因组reason_group/平今平昨/信号强度score_band按|分|多空同档/持仓档hold_band，复用 metrics.trade_stats 出胜率/期望/payoff/PF/连胜连亏+费用占比）；period_pnl 日·ISO周节奏、cumulative_curve；attach_excursions+range_excursion 可选 --bars 按同口径比例复权分钟库盘中h/l闭区间重放 MFE/MAE（多空镜像、覆盖率透明、缺区间安全跳过）；build_report 七段+observations 规则化观察（全胜桶PF=None不误报）；CLI出 reports/trade_journal.txt+.json；空数据安全降级；不被 main import、不改主链/综合分/默认CSV。
- `tools/research_review.py`（**第43轮 G30③ 新增，777行，纯标准库零网络只读、不import任何生产模块**）：SOURCES 台账登记9数据源与刷新命令；load_sidecar 区分 missing/broken、freshness_state 三态、age_label；load_equity_summary（utf-8-sig、跳空记录、期初首行/期末末行/全表回撤）、load_signal_tracking 正则；六段提取 sec_factor_health/sec_attribution/sec_journal/sec_lab/sec_validation（缺字段返{}）；build_actions 规则化待办（WARN/INFO/OK排序，可选产物缺失降INFO）；collect 装载全部、build_report 七段、build_json_payload(allow_nan=False)；CLI 出 reports/research_review.txt+.json；**不 subprocess 重跑、不覆盖主链 reports/daily_review.txt、不被 main import**。
- `experiment_ledger.py`（**第44轮 G27① 新增【根模块】，626行，纯标准库零网络、不被 main/analyzer import**）：统一实验台账，追加式 reports/experiment_runs.jsonl；canonical_hash/config_hash(类型+参数+输入内容身份、排mtime、同配置一致)、json_safe 清洗 NaN、file_fingerprint/build_manifest/data_identity_from_manifest(≤2MB算sha否则size)、LedgerStore(原子os.replace+RLock/坏行宽容/repeat_of串联/同秒-r2/filter)、safe_record(旁路吞错)、环境变量 FUTURES_EXPERIMENT_LEDGER 重定向/关闭；CLI --list/--show/--repeats/--export，无参=13组selftest。
- **第38轮 factors_catalog 增量**：登记5个表达式研究因子（expr_ma_bias5/expr_ma_ratio/expr_trend_per_vol/expr_price_accel/expr_illiq，layer=表达式研究、status=research），CATALOG 20→**25条**，validate 零问题、PART_KEYS 仍9。
- `metrics.py`（**第29轮新增，592行，纯标准库零网络，G3统一绩效口径**）：周期收益类 cumulative/annualized(算术)/cagr(几何)/volatility/sharpe/sortino(全样本下行偏差)/drawdown_series/max_drawdown/calmar/omega/ulcer/value_at_risk·conditional_var(历史法type7分位)/rolling_sharpe(暖机None)/monthly_returns(自然月复利)；逐笔类 trade_stats(胜率/PF/payoff/连胜连亏/期望/best·worst)；持仓过程 excursion(多空对称MFE/MAE)+mae_mfe_summary；工具 clean_returns/quantile_linear/returns_from_equity/daily_last_equity(日内多轮收敛日度)；一键 tear_sheet，样本不足键值为 None 绝不抛；--selftest 47项手算断言。**接入方（均纯增量、旧口径不变）**：portfolio.performance() 增补 calmar/omega/ulcer/var95/cvar95/monthly/profit_factor/连胜连亏/mae_mfe 且 Position 盯市累计 MFE/MAE；report.paper_account_text 加风险调整/连续性/月度行；backtest `_fmt_g3_extended` 总体段加一行；charts.tear_payload 供看板⑥三图（优先纸面否则组合回测CSV）。
- `paper_broker.py`（**第27轮新增/第28轮 701→829行/第41轮 830→840行（增 risk_sizing/risk_gross 能力位透传+set_risk_weights 透传，默认None逐字节等价旧版、实时权重源未接线），纯标准库零网络**：第28轮加平今/平昨结算交易日 owner 判定（owner_fn 可注入、判不了保守平昨、开仓记 entry_owner/强平/restore 全带 leg）、order_status_counts/positions_view/pending_view 三视图、account_summary 增补 float_pnl/n_pending/status/fill_mode、selftest 29断言；其余**第27轮**：纸面交易经纪=实时综合分到虚拟委托/成交的状态机。纯函数 want_position(三阈值迟滞返want_dir/action)/locked_at_quote(相对昨结整根贴板锁板,缺数据放行)/apply_slip(买加卖减)/sector_map；类 PaperBroker 组合 portfolio.Portfolio，on_cycle(ts,fut_rows,quotes)四阶段(A成交pending先平后开/B信号决策/C强平/D权益快照)、close/next两档(next严格晚于信号,锁板无价临时约束顺延,信号转中性撤遗留,同向不重挂)、_fill_leg(滑点入价后调pf.open/pf.close并落trade+更新order)、restore(三表重建持仓/已实现/挂单)、account_summary(供第28轮报告)、main(--selftest)25断言；成交价内含滑点、手续费/保证金复用真实CSV。
- storage.py（**第27轮扩到14张表/第28轮 998→1014行：修 paper_equity_series 为最近N条升序、新增 paper_order_status_counts；第27轮：新增 paper_orders(委托全生命周期)/paper_trades(开平仓成交,pos_ref配对)/paper_equity(ts UNIQUE每轮覆盖)三表+9方法(insert/update order、insert trade/equity、paper_open_position_trades未平仓开仓成交供恢复、paper_realized_fees/paper_last_equity/orders_recent/trades_recent/equity_series)，table_counts/prune(PAPER_RETENTION_DAYS3650)同步；MonitorDB 启动 CREATE TABLE IF NOT EXISTS 向后兼容给老库自动补建空表。
- `charts.py`（**第22轮新增/第23轮拆片段/第28轮 744→924行、9→12图，纯标准库**：第28轮加 paper_payload（读 paper_equity 最近2000快照升序、9数组+summary、抽稀1200、空脏显None）、renderPaper 三图（权益双线两位小数/回撤反转/风险度+持仓双轴+强平线）、DOM 三卡与 CHART_IDS、loadAndRender 双路径渲染；其余：P1-3 看板图表化的数据层+静态页。parse_equity_csv/downsample（权益CSV解析+确定性抽稀）、cross_section_payload/calibration_payload/outcomes_payload/factor_payload 四个纯函数映射、build_payload 四块独立try软降级、payload_to_js（window.CHART_DATA，allow_nan=False+转义</）、write_chart_data 落 chart_data.js、sync_echarts_asset/ensure_charts_page 幂等同步本地ECharts与静态页；**第23轮模板拆为 _PANEL_STYLE/_PANEL_DOM/_PANEL_JS 三段（#charts-panel作用域、IIFE 导出 window.ChartPanel 的 activate/reload/resizeAll、独立页靠 __CHARTS_STANDALONE__ 自启），charts_page_html 拼独立页、dashboard_embed_parts 供实时看板内嵌，两页共用同一片段**；CLI `--rebuild` 离线重建。**只展示既有产物、不改任何口径**。
- `assets/echarts.min.js`（**第22轮新增**）：ECharts 5.5.1（Apache-2.0，1.03MB）本地前端资源，运行时幂等同步到 reports/assets/，离线 file:// 可看。
- `tests/`（**第21轮新增、逐轮扩充，第28轮=23个 test_*.py/295用例约3s全绿，零网络确定性**；第28轮新增 test_paper_report.py 7例、扩充 paper_broker/storage/charts 用例；早期**第21-23轮：`conftest.py`（flat_calendar 注入确定性交易日历、tmp_db 临时库两夹具+sys.path）、`pytest.ini`、`tests/README.md`；17 个 `test_*.py`——**test_charts.py 第22轮14用例（抽稀首尾对齐/CSV缺文件坏表头脏行/截面映射/校准排序与mult None/outcomes聚合与db异常/factor JSON三种坏法/build_payload全量与全空/script转义/落盘与静态页结构/资源缺失安全），第23轮再增2用例（片段纯结构无外壳+IIFE导出、实时看板内嵌集成断言）共16用例**；test_compileall 参数化编译全部39个生产py（数量下限防假全绿）。运行：根目录 `D:\Python\python.exe -m pytest`；pytest 仅 dev、不进 requirements、main 不依赖。
- 其余 contracts/oil_data/sina_news/webdata/browser_reader/ths_app 数据与外部程序（数据层第6轮均已接 http_client）。
- 分析管线现状（analyzer.analyze_variety）：综合分[-10,+10]=新闻(±4)+原油联动(±5×oil_w)+机构动向(±2,≥3家)+日线动量(±4.5,MA/ATR/HV/ret5/20)+技术共振(±1.2，第8轮，短/中/长三组日线指标投票)+分钟共振(±0.4，第9轮，30m短中/60m中长四票，缺失降级为0)+盘中动量(±1.5)+量仓资金(±1.2，第7轮)+**基本面(±1.5，第13轮：库存.40/龙虎.30/carry.20/基差.10缺项归一)**；分级 |分|<2观望、2~4轻仓、4~6.5分批、≥6.5强信号；止损1.2×ATR/目标2×ATR。

## 四、reports 目录文件语义
| 文件 | 语义 |
| --- | --- |
| latest_report.txt | 交易时段滚动最近5轮，最新在前（第6轮起按品种级夜盘分档判定，贵金属/有色夜盘不再错分到 offhours） |
| signals.csv | 交易时段最近5轮信号流水，最新在前，utf-8-sig |
| signal_tracking.txt | **第7轮新增**：信号胜率追踪，30分钟/2小时/次日分周期、分分档、分多空统计，看板页签加载 |
| backtest_report.txt / backtest_signals.csv / backtest_trades.csv | **第8轮新增、第9/10轮扩展**：手动运行 backtest.py 后的最小日线回测报告、逐信号1/5/20日结果、逐笔非重叠交易（毛收益/真实手续费/滑点/净收益/每手费用/锁板顺延/退出原因），看板两个页签加载 |
| intraday_backtest_report.txt / intraday_backtest_trades.csv | **第15轮新增**：手动运行 intraday_backtest.py 后的日内/平今回测报告（全品种汇总/多空与平今平昨分组/离场原因/18组参数稳定性/平今vs平昨每手人民币对照）与逐笔28字段交易（进出场时间、交易所交易日owner、平今平昨leg、持仓分钟、毛净、开平每手费用、锁板顺延、离场原因），均 utf-8-sig，看板两个页签加载 |
| paper_account.txt | **第28轮新增（PAPER_ENABLED=true 才生成，休眠无此文件、看板页签也隐藏）**：纸面影子账户逐轮快照文本——账户概览(动态/静态权益/浮动/已实现/手续费/保证金/风险度)、组合绩效(日度年化,样本不足诚实标注)、委托状态统计、当前持仓(含开仓结算交易日)、在途挂单(带排队原因)、最近20笔成交、诚实说明(影子≥4周对照/永不自动实盘)，utf-8-sig |
| portfolio_report.txt / portfolio_equity.csv / portfolio_trades.csv | **第16轮新增/第41轮 G26续**：手动运行 portfolio.py 后的组合资金账户回测（第41轮：加 --risk-sizing 时抬头标注风险型口径，加 --compare-risk 时报告末尾附【同宇宙影子对照】表=等名义/逆波动/ERC 三法绩效并列；**equity/trades CSV 永远写基线等名义、默认口径逐字节不变**）——组合级净值/年化/夏普/索提诺/回撤/平均与峰值风险度/强平次数/未开仓原因分布/分品种与多空平今强平分组报告；逐bar权益曲线（dt,static,float,equity,margin,available,risk,drawdown,npos）；含实际手数/平今leg/开平仓费/净盈亏/forced强平标记/entry_score/margin_rate 的逐笔成交；看板“组合账户回测”“组合交易CSV”两页签加载；每次运行覆盖（最近一次模式即看板内容，与 backtest/intraday 固定名模式一致） |
| ../data/futures_fees.csv | **第10轮新增**：`tools/build_fee_table.py` 从券商xlsx转换的64品种投机费率；原始xlsx同目录归档，回测运行时只读CSV |
| ../data/futures_margins.csv | **第16轮新增**：`tools/build_margin_table.py` 从银河期货保证金比例页解析的64品种公司保证金率(投机档 broker_margin)+基础板幅 limit_basic+每手报价单位乘数 multiplier，as_of/source/note 齐全，exchange_margin(交易所基准档)无干净免费源留空；portfolio 运行时标准库只读，公司调整时手动重跑工具 |
| ../data/monitor.db | **第7轮新增、第11/13/14轮加表**：SQLite库，八张表 quotes/signals/news/options/signal_outcomes/option_chains/fundamentals/minute_bars；行情/新闻/链快照保留180天，信号结果、基本面日频快照与分钟K长期/400天保留 |
| history_report.txt | 交易时段当日归档，置顶，按交易日归属清旧（跨零点夜盘块保留到该交易日结束） |
| offhours_report.txt / offhours_history.txt | 非交易时段两件套，规则同上 |
| daily_review.txt | 每日复盘，永久保留，最新在前（第6轮起由 review_is_due 在该交易日全部夜盘结束后才生成，数据完整） |
| 实时报告.html | 多页签看板（**第23轮起"图表看板"页签同页内嵌（第28轮12图）；第28轮加"纸面账户(影子)"页签共15个（休眠态自动隐藏=14），其余 txt/csv 页签仍iframe**），探测 report_status.js、有新报告才重载当前页签（图表页走 ChartPanel.reload 重渲染） |
| 图表看板.html / chart_data.js / assets/echarts.min.js | 第22轮新增、**第23轮改为片段拼装（独立直链保留）**：本地 ECharts 图表页（**第28轮12图**：组合权益/回撤/风险度、截面板块与全品种强弱、因子IC与单调性、胜率校准、**⑤纸面账户影子净值/纸面回撤/纸面风险度+持仓数（paper:null 时三图显空态提示）**）；chart_data.js 每轮由 charts.write_chart_data 注入 window.CHART_DATA（勿手改）；echarts 从项目 assets/ 幂等同步，删了下轮自动补；独立页与实时看板内嵌共用 _PANEL_STYLE/DOM/JS 三段片段 |
| factor_eval.json | **第22轮新增**：tools/factor_eval.py 的结构化 sidecar（与 factor_eval.txt 同一次计算，供图表页消费；手动跑工具后刷新） |
| research_panel.db / research_panel_manifest.txt（cache/，gitignore） | **第36轮 G21/第37轮G21续**：panel_builder 的标准研究面板（全64品种61353行、品种×交易日×30字段、已比例复权、独立缓存不碰生产库），研究工具 `--panel` 直读、**禁止二次复权** |
| factor_health.txt / .json（gitignore） | **第37轮 G29**：factor_health.py 因子体检卡（事件层RankIC/滚动IC失效预警/块bootstrap CI/regime + 日频层IC期限半衰期），结论同步 factors_catalog.HEALTH_SNAPSHOT，只体检不改权重 |
| expr_research.txt / .json（gitignore） | **第38轮 G25**：expr_research.py 表达式因子研究台结果——面板列vs bar回读同表达式全64品种 parity（303309点maxAbsDiff=0）、表达式动量对齐实时ret5（1.1e-16）、5个research表达式因子对未来1/5/20日的逐品种/池化RankIC与截面cross_rank演示；research不进综合分 |
| factor_regime.txt / .json（gitignore） | **第39轮 G29续**：factor_regime.py 因子regime/换手/衰减形态——牛熊震荡×高中低波PIT分桶前向RankIC、1/5/20日秩自相关与隐含换手、指数vs幂律IC衰减R²择优；只研究不改权重 |
| portfolio_lab.txt / .json（gitignore） | **第40轮 G26**：portfolio_lab.py 组合构建实验台——等权/逆波动/ERC/GMV 最新权重快照（年化波动/有效N/分散化度/风险贡献/目标波动杠杆）+每20日滚动样本外代理回测（年化/波动/夏普/回撤/换手，equal基线、严格无未来）；默认不改线上sizing |
| trade_journal.txt / .json（gitignore） | **第42轮 G30①**：trade_journal.py 交易复盘——七维分桶（胜率/期望/盈亏比/PF/费用/连胜连亏）、日周盈亏节奏、--bars盘中MFE/MAE与由盈转亏比例、最佳最差单、规则化观察；只读不改主链，空数据安全降级 |
| research_review.txt / .json（gitignore） | **第43轮 G30③**：research_review.py 聚合各研究 sidecar 的**一键研究复盘**——七段简报（新鲜度/信号/G29因子/G28归因/journal/组合风险/防过拟合）+规则化待办WARN/INFO；只读不重跑、缺损陈旧降级；区别于主链 daily_review.txt（实时轮动复盘） |
| experiment_runs.jsonl（gitignore，append-only运行日志） | **第44轮 G27①**：根模块 experiment_ledger.py 维护的**统一实验台账**——4宿主自动旁路追加，每行=类型/规范化参数/config_hash/输入内容指纹/关键指标/产物/结论/复现命令/版本；同配置 hash 一致、重跑 repeat_of 串联看漂移；experiment_ledger.py --list/--repeats/--show/--export 查询；不覆盖任何既有产物、不入库 |
| 量化系统对标与改进报告.html | **第24轮新增（手写文档，非 main 产物、不会被程序覆盖）**：全网+GitHub 十层对标全文——十层标准架构、GitHub 项目格局、十维自评、G1-G20 差距与文件级改法、排期、不做清单；真实浏览器验证零报错 |
| 对标能力可视化.html | **第24轮新增（手写文档）**：十维能力雷达 + GAP 价值×成本优先级矩阵两 canvas，引用本地 assets/echarts.min.js；其数据在融合总纲附录A/B 有 markdown 表 |
| 总体对标与统一改进总纲（融合版）.md | **第24轮新增（六文档融合后的唯一总入口，原始6份保留不删）**：G1-G20 统一编号（括注旧 P2-x/WP-Fx/清单号），未实现项按 P0(G9)/P1(G1,G4,G10,G11,G6)/P2(G2,G3,G5,G7,G8,G12,G14,G15,G19)/P3(G13,G16,G17,G18,G20) 分类，含不做清单、保持强项、纪律回退表、轮次排期、参考来源；后续接续路线以它为准 |
| report_status.js | 看板专用极小状态文件，每轮落盘更新（勿删、无需查看） |
| logs/heartbeat.txt | **第6轮新增**：每轮写入心跳时间戳+轮次，看门狗据此判活（勿删） |
| logs/restart.log | **第6轮新增**：bat 崩溃自动重启的记录 |
| cache/trade_dates.txt | **第6轮新增**：交易日历动态缓存（每行一个交易日，近2年，自动刷新） |
| cache/ | news_YYYYMMDD.jsonl 当日新闻；另有 em_cangdan.html/em_ccpm.html 仓单页缓存但**无代码读取（P1-11）** |

## 五、验证记录
- **第44轮（G27① 统一实验台账，2026-09-03）**：生产62 py compileall 全过；新增 test_experiment_ledger 22例零网络+selftest注册1+compileall自动1，**全量 pytest 486→510 全绿 0失败0错误0跳过（cache/r44_junit.xml）**、模块13组自测过；4宿主钩子真实登记成功、同配置重跑 config_hash 逐字一致(fc30b0d5ce/3fe4f997a1)且 repeat_of 串联；**默认8品种 equity/trades 与 cache/base 双 sha256 逐字节一致**（portfolio.py 主链零影响、普通回测不触发钩子）；grep 证明 main/analyzer 零 import 台账；真实 main --once exit0、stderr 空零 ERROR/Traceback（cache/r44_main_*.log）。
- **第45轮（G27②③ WF稳定性+成本曲面/容量，2026-09-03）**：生产63 py compileall 全过；新增 test_wf_cost_lab 14例零网络零DB+selftest注册1+compileall自动1，**全量 pytest 510→526 全绿 0失败0错误0跳过（cache/r45_junit.xml）**、wf_cost_lab 8组自测过；真实 RB/MA/I/TA 跑通（RB/I漂移基准已亏安全垫0、MA双轴∞、TA滑点4倍垫、容量数量级）；**默认8品种 equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 与 cache/base 双 sha256 逐字节一致**；grep 证明 main/analyzer 零 import wf_cost_lab；真实 main --once exit0、stderr 0行零异常（cache/r45_main_*.log）。
- **第46轮（G19 在线热备/自启导出/灾备，2026-09-03）**：生产64 py compileall 全过；新增 test_db_backup 22例 tmp零网络零生产库+selftest注册1+compileall自动1，**全量 pytest 526→549 全绿 0失败0错误0跳过（cache/r46_junit.xml）**、db_backup 13组自测过；真实 monitor.db 359.8MB/14表467695行在线热备成功（约10s、源/副本quick_check=ok）、tmp 演练备份-改坏-恢复闭环与坏备份拒绝；**默认8品种 equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 双 sha256 逐字节一致**；真实 main --once exit0、stderr 0行零异常、main --version 秒退0.46.0（cache/r46_main_*.log）。
- **第47轮（G5 组合风险研究侧①②③，2026-09-03）**：生产66 py compileall 全过；新增 test_portfolio_risk 19例零网络零面板+portfolio_risk/lab 两个 selftest，**全量 pytest 549→572 全绿 0失败0错误0跳过（cache/r47_junit.xml）**；真实61品种126日窗风险报告落 reports/portfolio_risk_lab.txt|.json（等权历史VaR95=1.06%/erc0.66%/gmv0.40%、原油-5%等权-0.61%）；**默认8品种 equity=c4da4cdf61f3bcdc/trades=50dcc800d326f8e9 双 sha256 逐字节一致**；main/analyzer/portfolio/risk_gate grep 零引用、本轮未改 main 故未跑 main --once。
- **第43轮（G30③ 研究侧一键复盘编排器，2026-09-03）**：生产61 py compileall 全过；新增 test_research_review 15例零网络+selftest注册1+compileall自动1后全量 pytest **469→486 全绿约7.3s 0失败0错误0跳过**（cache/r43_junit.xml）；**默认8品种宇宙查明=I,MA,RB,SA,TA,AU,AG,CU，其 equity/trades CSV 与 cache/base 基线双哈希逐字节一致**（git 仅新增 tools+两tests，主链零改动）；真实9 sidecar 秒级聚合出 reports/research_review.txt/.json、空目录降级成稿验证通过；真实 main --once 43s exit0 无 ERROR/Traceback。
- **第42轮（G30① 交易复盘journal，2026-09-03）**：生产60 py compileall 全过；新增 test_trade_journal 17例零网络+selftest注册1+compileall自动1后全量 pytest **450→469 全绿约8.9s 0失败0错误0跳过**（--junitxml解析，cache/r42_junit.xml）；**默认8品种组合回测 equity/trades CSV 与 cache/base_portfolio_*.csv 哈希逐字节一致**（主链零改动）；真实16品种30m回测4.1s+journal --bars 1.8s exit0、MFE/MAE覆盖率100%（2563笔）；真实 main --once 42.6s exit0 无 ERROR/Traceback。
- **第41轮（G26续 风险型sizing接入组合共享内核，2026-09-03）**：生产59 py compileall 全过；**默认组合回测（8品种30m）portfolio_equity.csv/portfolio_trades.csv 与改动前留存基线哈希逐字节一致**（报告仅标题行变化）；新增 test_portfolio 6例后全量 pytest **444→450 全绿约8s 0失败0错误0跳过**（--junitxml 解析）；风险型单次 --risk-sizing erc 与 --compare-risk 真实跑通（8品种三法同995笔证只改手数；16品种宽样本风险型回撤10.50%→3.3%）；真实 main --once 48s exit0、日志无 ERROR/Traceback，主链/综合分零改动；paper_broker 默认构造逐字节等价（能力位默认None）。
- **第38轮（G25 表达式因子引擎+因子治理，2026-09-03）**：生产56 py compileall 全过；factor_expr selftest 9组、expr_research selftest 5组、factors_catalog selftest 全过（白名单安全解析/21危险反向/时序截面算子手算/无未来扰动/OLS恢复β/IC·ICIR加权/面板bar parity=0/表达式==实时ret5/前向严格向未来）；新增 tests/test_factor_expr.py 37例、test_tools_selftest +2，全量 **pytest 397→438 全绿约6.7s/30文件、0失败0错误0跳过**；真实全64面板 expr_research exit0：面板列vs bar回读同表达式303309点 maxAbsDiff=0、表达式动量对齐实时ret5 61033点仅1.1e-16、5个research因子前向H1/5/20 |IC|均<0.06无稳定预测（负结果诚实）；全树grep确认仅expr_research引用factor_expr、main/analyzer零import；真实 main --once 一轮 exit0、ERROR/Traceback=0，主链与综合分零改动、旧因子过程式实现逐字节不变。
- **第37轮（G21续 研究工具读面板 + G29 因子体检，2026-09-03）**：生产54 py compileall 全过；panel_builder selftest 8组、tsmom/xsmom/carry/factor_health/factors_catalog `--selftest` 全过（正负IC裁决/噪声不误判/构造失效触发/块自助确定性/前向无未来/IC半衰期拟合）；test_research_panel 14→18、test_tools_selftest +1，全量 **pytest 391→397 全绿约4.7s/29文件、0失败0错误**；真实联网等价：八品种同1023日，xsmom6096/tsmom5616 重叠点面板vs网络 maxAbsDiff=0、零None不一致，carry --panel 四品种 exit0；面板补齐全64品种61353行0失败；真实 factor_health 事件层+日频层 exit0 出 factor_health.txt/.json（机构动向次日IC-0.230健康反向、原油联动+0.276健康）；真实 main --once 一轮 exit0、ERROR/Traceback=0，主链与综合分零改动。
- **第36轮（G21 研究面板+特征注册表+PIT/parity，2026-09-03）**：生产53 py compileall 全过；factors_catalog/panel_builder/pit_audit 三模块 `--selftest` 全过（注册表一致/严格asof边界/暖机ret1d/扰动无未来+反向泄漏/训练服务parity/PanelStore幂等/结构审计）；新增 tests/test_research_panel.py 14例、test_tools_selftest +3；全量 pytest 371→391 全绿；真实联网建11品种11154行0失败、pit_audit 结构审计全过、RB/MA/CU 各25时点parity零不一致、RB真实幂等重建不翻倍；真实 main --once exit0、1899行ERROR/Traceback=0。
- **第35轮（G28 因子收益归因+BHB板块归因，2026-09-03）**：生产50 py compileall 全过；attribution `--selftest` 8组全过（方向化暴露/OLS精确恢复与奇异安全/加法闭合/零方差空样本/BHB两板块手算+200组随机fuzz恒等/板块rb无方向/累计曲线闭合/IS-OOS有序/端到端报告与JSON安全）；新增 tests/test_attribution.py 16例、test_tools_selftest +1，全量 **pytest 353→371 全绿约4.6s/28文件、0失败0错误**；真实 monitor.db 三周期(30分370/2时368/次日235)归因 exit0、两恒等式闭合误差≈1e-19、数字可复现；真实 `main.py --once --no-launch` exit0、ERROR/Traceback=0、分钟K 320任务空/失败0、数据源全成，主链与综合分零改动（本轮仅新增离线工具+config常量簇）。
- 第1轮：调度边界21时刻、置顶、日切、BOM往返、占用重试、复盘去重/兜底共40+断言；看板 JS node 跑23个边界时刻与 Python 刻度一致。
- 第3轮：急动检测（+0.7%触发/+0.2%不触发/急跌/冷却）、紧急轮置顶标记、状态文件、看板"同ts不刷新/新ts恰好一次"共18断言。
- 第4轮：四源真实抓取140条/9项全球行情、突发触发(权重8.2)、存疑降权(8.20→1.28)并排后、弱消息不触发、重要消息降门槛、oil/web 两类插队均在6.01s准点回计划点、块头与状态标记，共20+断言；全部临时测试脚本已删除。
- 已清理 logs 下旧调试转储与 __pycache__；仅保留 logs/monitor.log。
- 第5轮（非代码验证，是核查）：逐文件 grep/通读确认——无 requests.Session、无 sqlite/回测/推送/winsound/邮件/webhook、无 tests/、量仓字段解析未使用、仓单缓存未接管线、看板无图表；夜盘时间经上期所官网与东财期货页双源核实。
- **第6轮（P0修复验证）**：①18个py全部 py_compile + import 通过（顺带修复3个文件原有 docstring 语法错误）；②84项 Python 断言（17全局时刻×预期、品种级三档夜盘、周六/周一凌晨、next_cycle_time 刻度、trade_owner/review_is_due、真实联网交易日历含国庆休市与节前无夜盘、日切归属、RotatingFileHandler 幂等、Session 实连）；③5项 daily_rollover 跨零点块保留/次日清空；④node 跑23个跨日/周六凌晨时刻，看板 JS nextMark 与 Python next_cycle_time 全部一致；⑤`main.py --once --no-launch` 端到端一整轮（64/64品种、合约探测、期权、非交易时段分流、心跳落盘、exit 0）。临时测试脚本验证后已删，仓库仍无 tests/（P2-18）。
- **第7轮（P1第二批验证）**：21个py全部 compileall 通过；最终临时 `_p1_verify.py` 共20项断言全部通过并已删除，覆盖 SQLite 建表/写入、相同行情去重、只存非中性信号、新闻、期货信号三任务、期权单腿+策略、30分钟回填、有效/过期胜率、量仓模式与交易日切换、analyzer接入、五类告警 payload、冷却状态机、追踪文本渲染。完整 `D:\Python\python.exe main.py --once --no-launch` 最终一轮 exit 0，64/64主力合约探测与报告/看板/数据库均落盘；最终库内 quotes=256、signals=244、news=471、options=320、signal_outcomes=270（hit32/miss35/flat1/pending202），SQLite integrity_check=ok；signal_tracking.txt 正常展示有效样本胜率；临时脚本已删除。
- **第8轮（P1第三批最终验证，2026-08-31 23:27）**：22个py compileall 与合成断言先通过；真实双品种回测 `backtest.py --codes RB0,MA0 --days 160 --workers 2` exit 0；最终全量 `D:\Python\python.exe main.py --once --no-launch` exit 0，64/64主力合约探测完成，OpenVlab期权日历63品种，报告/看板/数据库均在23:27:51落盘，本轮日志未检出 Traceback/ERROR。latest_report.txt 回读到58条技术共振明细、40组期权6项检查、IV分位表头、80处波动率锥、67处组合Greeks；实时报告.html 含“最小日线回测”；monitor.db integrity_check=ok，最新轮 quotes=320、signals=302、news=629、options=400、signal_outcomes=276，options.gamma 非空240条且 greeks_json 同步含 delta/gamma/vega/theta_day。
- **第9轮（P1第四批最终验证，2026-09-01 00:06）**：22个生产py compileall exit 0；合成断言覆盖分钟聚合/共振、analyzer接入、成本=毛-净、锁板、8类期权策略、storage margin；真实RB0/MA0回测exit 0并生成26行trades；最终全量main exit 0、64/64合约、OpenVlab 63品种、当前轮日志ERROR/Traceback=0。latest_report含98处分钟共振、54处30m明细、5组蝶式、1组备兑和保证金估算；HTML含回测交易CSV；monitor.db integrity=ok，quotes=384、signals=356、news=765、options=480、signal_outcomes=309。
- **第10轮（真实券商手续费，2026-09-01 09:36）**：转换银河期货20260828费率表，64品种全覆盖且只取投机行；生产代码compileall通过；合成断言验证按金额+按手数叠加、固定费用/乘数折算、净收益恒等式和兜底费率；真实RB0/MA0回测26/26笔命中真实费率，净累计+4.8%、毛累计+5.6%、成本拖累-0.9pct；全量main exit 0、64/64合约、OpenVlab 63品种、当前轮ERROR/Traceback/CRITICAL=0；monitor.db integrity=ok，quotes=448、signals=409、news=964、options=560、signal_outcomes=360。
- **第11轮（WP-A，2026-09-01 14:29）**：23个py compileall过；合成断言覆盖9/8元素解析/PCR手算/期限结构；真实main exit 0、期权链就绪39/40（缺LU系数据源）、option_chains落39行。
- **第12轮（WP-B，2026-09-01 15:35）**：24个py compileall过；两轮零网络合成/回归断言全PASS（反推精度/parity/skew/凸性/五层清洗/矩阵缺档/日历方向Vega保证金/静态空间拦截/IV三级优先级）；真实main --once --no-launch exit 0，112月份链/40品种、37个IV曲面且ATM全在5%~46%合理区间、远月矩阵平滑；日历策略真实触发（铁矿石long calendar全通过、静态最优为负的反向日历被拦截）；option_chains同ts多expiry各一行（豆粕3个月份），integrity_check=ok；临时脚本日志已删。
- **第13轮（WP-C，2026-09-01 17:00）**：26个py compileall过；21条零网络合成断言全PASS（库存分位/去库方向、龙虎净多/边际、carry方向、基差方向、缺项归一不缩放、全缺/样本不足None、权重和=1）；真实main --once --no-launch 两轮 exit 0 无ERROR/Traceback，库存61/龙虎61/carry64/基差反爬降级，fundamentals表落64行、fund_score正31负30中性3范围[-1.20,+1.50]，报告基本面速览+明细渲染正确、integrity_check=ok；临时脚本日志已删。

- **第14轮（WP-D0，2026-09-01 19:20）**：28个py compileall过；55项零网络合成断言全PASS（三源代码转换/解析/六类选源分支/tdx降级/周期聚合/建表去重/熔断）；真实 main --once --no-launch exit 0 无ERROR（主力64/64、tdx 1s判定不可用、东财1m 4.3s快速熔断、320任务新浪256成功/64个1m熔断跳过、既有7表与基本面期权链路完好）；完整回填 minute_bars=261888根（64品种×5/15/30/60m×1023），回溯5m至07-31/15m至05-27/30m至02-27/60m至2025-08-13，零非法OHLC、零违反唯一键、二次增量净增0（幂等）；26个临时探测/断言/补丁脚本已删。
- **第14轮增补（数据源二次调研，2026-09-01 晚）**：28个py compileall+import过；21项零网络合成断言全PASS（type=1放行/非法周期拒绝、全周期统一选源六分支、1m无合约新浪独立成功、东财字段映射/chg_pct复算、f111="-"与f111=1两类残缺拦截、新浪缺东财补且仅一次请求、新浪全齐东财零请求）；真实小链路4品种×(1/5m)全src=sina、fetch_quotes 64/64仅0.5s；main --once --no-launch 零ERROR，320任务全部sina/空失败0，minute_bars 261888→327360根（1m补到65472根/64品种，08-26起），零非法OHLC，综合分/分钟共振/期权/基本面完好；9个probe+2个verify+1个dbcheck临时脚本与临时日志已删。
- **第15轮（WP-D1/D2，2026-09-01 晚）**：29个py compileall过；T1-T15 零网络合成断言全PASS（交易所交易日owner归属/无未来函数/止损止盈/跳空/同根双触按止损/日终与入场即末根强平/平今平昨费率/SS极低比例平今/锁板拦截顺延/零量/滑点/到期）；真实数据多路径端到端：30m全64品种10101笔/错误0/无成交0、日内今昨=10101/0，摆动RB/CU平今157平昨93，1m直读RB 3交易日105笔，1m边界对齐聚合5m与原生5m重叠204根h/l/c 100%一致（开盘价24根有段首tick差异、15根差1跳、最大差4跳=自采1m段首精度），--no-cost可跑；全市场默认参数净均收-0.022%、18组网格方向一致无孤峰；DB integrity ok、minute_bars 327360根不变；_probe15/_patch_*/_test_intraday_bt 等临时脚本全删（仍无tests/）。**二次独立复验（2026-09-01晚）**：重建29项零网络断言全PASS；日内30m/swing(RB平今69/平昨51)/1m边界聚合5m/5m直读/60m五条真实路径0错误；交叉验证204根h/l/c 100%一致、开盘180根全等(余24根段首tick差、15根差1跳、最大4跳、均值0.18)；main --once 1729行零ERROR；全量重跑10101笔逐值复现；复验临时脚本验后即删，生产py仍29个9825行。
- **第16轮（WP-E，2026-09-01 深夜）**：32个py compileall过；**37条零网络合成断言全PASS**（一赚一亏逐点权益/占用/风险度、连续下跌强平顺序与砍后风险度、缺表兜底登记、三种s
- **第20轮（样本外防过拟合，2026-09-02）**：38个py compileall过；backtest_validation `--selftest` 全PASS（正态函数互逆/矩、PSR、DSR多重试验单调、CSCV噪声≈0.5·真alpha≈0、PurgedKFold purge+embargo、WF、高原vs孤峰）；全64品种18组参数真实网格 exit0 出 backtest_validation.txt。
- **第19轮（WP-F2，2026-09-02）**：37个py compileall过；A3校准器+B2因子IC+B3 triple-barrier 三套 `--selftest`/合成断言全PASS（贝叶斯平滑/四级回退/乘子裁剪、单调RankIC=1/无关≈0、止盈止损跳空超时双触/PIT无穿越/embarg
- **第21轮（P1-1 pytest回归体系，2026-09-02）**：生产38个py零改动；新建 tests/ 后 `D:\Python\python.exe -m pytest` = **174 passed 约2秒**（15个test文件+conftest）；**红绿双向验证**——变异测试把 cross_section 稳健z常数0.6745改0.5，test_robust_z_median_mad 如期变红（-1.0≠-1.349），还原后174全绿（并清除PowerShell误加BOM恢复无BOM）；test_compileall 参数化编译全部38个生产py通过且设数量≥30下限防假全绿；真实 `main.py --once --no-launch` 完整一轮 exit0、无Traceback/ERROR（分钟once 320任务全sina、新增5219根、空/失败0）；生产 data/monitor.db integrity_check=ok、quick_check=ok、10表、foreign_key_check 无违规；requirements.txt 未改动（pytest 9.1.1 仅装 dev 解释器 D:\Python）。o）；真实 main --once 与 portfolio --calibrate 对照冒烟通过；ml_samples 全量落42937行幂等去重验证通过。izing手数、单品种/板块/可用/持仓数四类上限、1手买不起拒开、平今费、无bar时刻连砍与owner_at二分）；真实端到端全64品种30m 6099笔/期末758495/-24.15%/夏普-3.64/回撤26.87%/平均风险度17.3%/0强平/保证金64-64命中0兜底，全64品种日线250日341笔/+10.56%/夏普0.84/回撤9.32%/0强平，强平线15%压测触发2900次强平验证引擎同步与报告链路；main --once --no-launch exit 0；删临时脚本后干净进程小样本逐值复现；修复 backtest RB00 双零退化（修复后日线回测恢复、83/83笔手续费命中）；_probe16/_patch16/_fix16/_view16/_t16/_dbg16 共17个临时脚本与运行日志全删（仍无tests/）。
- **第22轮（P1-3 看板图表化，2026-09-02）**：39个py compileall过；新增 test_charts.py 14用例后全量 **pytest 189 passed 约3秒**；factor_eval 真实DB重跑 9因子3936配对点、txt/json 数值一致；真实 `main.py --once --no-launch` exit0、一轮64品种后 chart_data.js 五块全填充且胜率数字与 signal_tracking.txt 一致（315/310/176）；node --check 页面内联JS语法；真实浏览器打开直链页与外层看板页签两路径逐屏截图目视9图、全null空态fixture同样目视，控制台零报错（过程中修掉 series 花括号配平、回撤轴刻度重复、风险度轴被100%参考线压贴底、x轴标签自动隐藏/重叠、markLine标签贴边五类显示问题）。
- **第28轮（G1（二）账户+看板，2026-09-02）**：生产44 py（test_compileall 47例覆盖编译）；pytest **274→295 全绿约3s/23文件**；paper_broker --selftest **29断言全过**；charts 内联JS `node --check` 通过；**启用态真实64品种两轮集成**（进程内开 PAPER_ENABLED+临时库：next档首轮挂19→次轮成交11/持仓11/在途8，真实手续费91元零兜底、风险度19.59%、权益快照2点、paper_account.txt 4151字符、实时报告纸面块与chart_data paper块逐字段核验、三表 filled11/pending8）；**休眠态 main --once --no-launch exit0**（无纸面块/不生成txt/"paper": null/无纸面页签/主链零异常，另即时验证默认开关下 State.paper=None）；**真实浏览器双态目视**（启用态三canvas+9chips、全null空态提示、控制台零错误），并修权益轴一位小数抹平小波动(100.0万→100.00万)、paper_equity_series 误取最旧N条两处问题。

- **第29轮（G3 完整绩效指标包/tear sheet，2026-09-02）**：生产45 py（新增 metrics.py，test_compileall 全过）；metrics `--selftest` **47项手算断言全过**；pytest **293→304 全绿约3.5s/24文件**（新增 test_metrics 6 + test_charts 4，同步 CHART_IDS/容器清单到15）；charts 内联JS `node --check` 通过；**真实浏览器目视**（既有 portfolio_equity.csv 驱动三图：水下/滚动夏普/月度热力正常、chips数值对、红绿语义对、控制台零错误；修掉日度收益 off-by-one 致月度空、热力图标签拥挤两处）；**真实64品种 main --once exit0**（主链零改动、休眠零纸面块/不生成paper_account.txt/paper:null 而 tear 正常取组合回测出图）；生产44→45py、17460→18178行、零新依赖；tag v0.29.0/commit 2920e2e。

## 六、待办/注意
- **【第44轮已完成】G27① 统一实验台账**（根模块 experiment_ledger.py 追加式JSONL，config_hash排mtime同配置一致/repeat_of漂移/原子写/坏行宽容/safe_record旁路/环境变量隔离，4宿主挂钩子、conftest测试隔离、.gitignore忽略*.jsonl，pytest→510、默认8品种双哈希不变、main零异常）；**【第45轮已完成】G27②③**（tools/wf_cost_lab.py 复用backtest_validation网格/WF，wf_stability稳定·一般·漂移评级、fee×slip 5×5净收益曲面+成本安全垫、分钟量换手/参与率容量数量级精确待G14，pytest→526、默认8品种双哈希不变、main零异常、末尾登记台账）；**至此 G27 ①台账(44)②WF③成本曲面/容量(45)三项全部落地**，仅余"精确容量待G14一档盘口、曲面可扩多参数/多周期"等增强项。
- **【第43轮已完成】G30③ 一键研究复盘编排器**（tools/research_review.py 不重跑只聚合6sidecar+equity+信号文本，七段+规则待办，缺损陈旧降级、BOM兼容，命名 research_review 隔离主链 daily_review.txt；tag v0.43.0/5d3fa06，pytest 486、默认8品种CSV哈希一致、--once 43s零异常。**续尾：G30②纸面-回测一致性待G14盘口、看板 research_review 页签**）。
- **【第42轮已完成】G30① 交易复盘journal**（tools/trade_journal.py 七维分桶+日周节奏+--bars盘中MFE/MAE一键txt/json，复用metrics.trade_stats同口径、空数据安全降级、只读不改主链；真实2563笔定位极短单PF0.57失血/信号强度单调/59%亏损单曾浮盈；tag v0.42.0/202ac51，pytest 469、默认CSV哈希一致、--once 42.6s零异常。**续尾：G30②纸面-回测一致性待G14盘口、③daily_review全链路编排**）。
- **【第41轮已完成】G26续 风险型sizing接入组合共享内核**（portfolio.Portfolio 增 risk_sizing(inv_vol/erc)/risk_gross，trailing_risk_weights 严格PIT只用早于t的bar估协方差调portfolio_constructor、样本不足安全回退等名义；run_portfolio risk_cfg 按rebalance注入、_reset_feeds 确定性重复回放；CLI --risk-sizing/--compare-risk 同宇宙影子对照（基线CSV逐字节不变）；paper_broker 预留能力位但实时权重源未接线；config 增 PRS_* 簇默认全关、GMV不接入；真实30m 16品种回撤10.50%→3.3%/风险度15.2%→5.0%（降险非增收益、不默认开）；tag v0.41.0/12755ec，pytest 450、默认回测CSV哈希一致、真实--once 48s零异常）。
- **【第40轮已完成】G26 组合构建器**（新增根模块 portfolio_constructor.py：等权/逆波动/ERC风险平价[全Newton、迭代中不归一]/长仓GMV[FISTA+capped-simplex]/目标波动缩放+杠杆上限/风险贡献·有效N·换手诊断，纯标准库只用协方差不预测收益、默认equal等价旧等名义；新增 tools/portfolio_lab.py 固定宇宙61品种快照+每20日滚动样本外代理严格无未来；config增PC_*簇；真实定位'ERC降波动25%/降回撤32%夏普0.42→0.53风险最均衡、逆波动换手0.27最省、GMV降波动48%但有效N仅11/换手1.77/夏普反降过集中'，只出证据不改线上；tag v0.40.0，pytest 444、selftest 10+5组、真实--once冲烟零异常；生产57→59py）。
- **【第39轮已完成】G29续 因子regime分层+换手稳定性+幂律/指数衰减形态**（新增 tools/factor_regime.py：ret126牛熊震荡×hv60过去120日分位PIT分桶前向RankIC、因子滚动分位1/5/20日秩自相关与隐含换手、IC(H)同时拟合指数/幂律按R²择优，复用G25引擎ts_rank与factor_health日频层、纯标准库只读面板不接main；config增REGIME_*簇；真实全64品种定位'长周期动量ret252/tsmom252低波regime有效(H20低波IC约+0.10、熊低波+0.11)、高波转负(动量高波崩溃)，信号lag1自相关0.72~0.85/月度换手约0.39，除ret5外近零IC两形态都不贴合'，regime条件结构只记录、须双样本/事件层互证才议进分；tag v0.39.0，pytest 440、selftest 6组、真实--once冲烟零异常；生产56→57py）。
- **【第38轮已完成】G25 纯标准库表达式因子引擎 + 因子治理**（新增根模块 factor_expr.py：白名单DSL递归下降、时序delay/delta/ts_*/rank/minmax/decay/corr无未来+截面cross_rank/scale/zscore同一AST双态、禁eval/属性/dunder、OLS正交残差/等权·IC·ICIR加权；新增 tools/expr_research.py 纯离线读G21面板，全64品种面板列vs bar回读同表达式303309点 parity maxAbsDiff=0、表达式动量对齐实时ret5仅1.1e-16、5个research因子前向|IC|<0.06无稳定预测负结果诚实；5因子登记factors_catalog（20→25、research不进分）；main/analyzer零import、旧因子过程式实现与综合分逐字节不变；tag v0.38.0，pytest 438、各selftest、真实--once冲烟零异常；旧因子表达式改写与自动挖掘留G25续且受红线门控）。
- **【第37轮已完成】G21续（研究工具统一读标准面板）+ G29 因子体检**（panel_builder 加 panel_rows_to_bars/load_adjusted_bars 回读层、tsmom/xsmom/carry 加 --panel 读已复权面板且实证禁二次复权、真实重叠点逐值等价 maxAbsDiff=0、面板补齐全64品种61353行；新增 tools/factor_health.py 事件层滚动IC/块bootstrap CI/失效预警/regime代理+日频层IC期限半衰期，体检卡回写 factors_catalog；真实定位机构动向次日健康反向IC-0.230、原油联动次日健康+0.276、日频单因子无稳定预测；两次 commit 9dca232/8e33dae，tag v0.37.0，pytest 397、各selftest、真实--once冲烟零异常；研究侧不碰生产表/不接main/不改综合分；机构动向反向的处置须更长样本双样本，本轮只记录不动 analyzer）。
- **【第36轮已完成】G21 标准研究面板+特征注册表+PIT/训练-服务一致性**（新增 factors_catalog.py + tools/panel_builder.py + tools/pit_audit.py：因子唯一登记、品种×交易日标准面板落独立缓存库、严格PIT as-of、扰动法无未来、实时/离线parity；真实11品种11154行/parity零不一致/幂等重建；tag v0.36.0，pytest 391、三模块selftest、真实--once冲烟零异常；研究侧不碰生产表/不接main/不改综合分；既有研究工具切换读面板与全64品种常驻增量留后续）。
- **【第35轮已完成】G28 因子收益归因+BHB板块归因**（新增 tools/attribution.py：OLS加法归因与BHB三效应双恒等闭合、累计归因曲线、IS/OOS β一致性；真实数据定位技术共振管短周期/原油联动管次日/机构动向次日反向/超额靠板块内选择；tag v0.35.0，pytest 371、selftest 8组、真实--once冲烟零异常；研究侧只读、不改任何线上权重；接入纸面/组合逐笔归因与看板归因页签留后续）。
- **【第29轮已完成】G3 完整绩效指标包/tear sheet**（新增 metrics.py 统一绩效口径47项自检；portfolio/report/backtest 增补G3指标且旧值不变；看板⑥水下曲线/滚动60日夏普/月度热力三图，优先纸面否则组合回测；tag v0.29.0/2920e2e，pytest 304、node --check+浏览器目视+真实--once冲烟全过）。
- **【第28轮已完成】G1 纸面交易引擎（二）账户+看板**（PaperBroker 接 main 5.5段默认休眠、平今/平昨结算交易日owner判定、paper_account.txt+实时紧凑块+看板三图与页签、storage快照查询修正；tag v0.28.0/e53e4fc，pytest 295、selftest 29、双冒烟+浏览器双态目视；**PAPER_ENABLED 维持默认False，开启方式=config.json 置 true 重启**；G1 仅剩运行期连续影子≥4周后与 signal_outcomes 对照）
- **【第27轮已完成】G1 纸面交易引擎（一）表+撮合**（新增 paper_broker.py 复用 Portfolio 内核、close/next两档、迟滞/锁板/滑点/真实费率/顺延撤单状态机、storage 三表与重启恢复、config PAPER_*20项默认休眠；本轮 tag v0.27.0，pytest 274全绿、真实64品种5轮撮合+--once冒烟exit0、14表 integrity ok）。**下一站第28轮=G1（二）账户+看板：main.State 持有 PaperBroker 并在 run_cycle 第5步后 on_cycle（受PAPER_ENABLED开关、默认影子独立成段）、reports/paper_account.txt、report纸面账户块、charts纸面净值图（改前端过node --check+真实浏览器目视,教训8）、连续影子≥4周对照 signal_outcomes、评估默认开、补实时平今/平昨owner判定**。
- **【第26轮已完成】G4 回测严谨性**（--fill close/next_open双档+固定种子bootstrap置信区间+IS/OOS分段+冲击成本单列+backtest_runs逐次留档历史百分位+validation sidecar交叉引用；本轮tag v0.26.0，pytest 255全绿、真实全64品种两档对照与--once冒烟exit0；G9/G10/G11/G6已于第25轮完成）。**下一站第27-28轮=G1纸面交易引擎（paper_broker实时虚拟成交close/next两档、复用portfolio sizing与强平、paper_orders/paper_trades/paper_equity三表、paper_account.txt+看板净值、PAPER_ENABLED默认影子，先paper不接实盘，拆两轮，融合总纲6.3）；再后 P2按需 G3绩效包/G7长窗TSMOM/G19备份/G12价差/G5组合风控/G2插件化**。约束：不动实时监控主链与综合分口径，新能力默认影子/开关、缺省等价旧版。
- **改进路线**：~~第一批 P0~~ **第6轮完成**；~~第二批 P1（告警/SQLite/信号追踪/量仓）~~ **第7轮完成**；~~第三批 P1主体（最小技术回测、RSI/MACD/KDJ日线共振、否定/转折、IV分位/锥/偏度/组合Gamma）~~ **第8轮完成**；~~第四批 P1续（30/60分钟共振、回测成本/锁板/参数稳定性、蝶式/比率/备兑/保护性认沽、保证金点值）~~ **第9轮完成主体**。P1剩余边界：~~PCR需要完整期权链~~ **第11轮已完成持仓量PCR（成交量PCR待交易所日行情备用源）**、~~期限结构~~ **第11轮已组装展示、carry 入综合分第13轮已完成**、~~日历价差/IV曲面~~ **第12轮已完成多到期日IV曲面+日历/反向日历（交易所官方IV交叉校验为遗留可选项、未做）**；~~条目11的基差/库存/仓单/龙虎榜~~ **第13轮WP-C已完成（东财库存仓单+龙虎榜+carry入综合分，生意社基差软降级预留；交易所官方IV曲面交叉校验为遗留可选项）**；**第15轮 WP-D1/D2 日内/平今回测已完成（intraday_backtest.py：vnpy式bar内撮合、交易所交易日判平今、精确锁板、真实费率每手人民币+平今对照、18组稳定性网格，全64品种10101笔验证）**；**第16轮 WP-E 组合资金账户已完成（portfolio.py 947行：共享资金池逐bar盯市/三种手数/强平状态机/权益曲线 + 银河期货保证金率表，37断言+全64品种分钟/日线验证，并修复 backtest 主连双零退化）——至此 WP-A/B/C/D/E 五个工作包全部完成**。**第16轮增补已新增 AI 量化工作包路线 WP-F（详见《AI量化对标与改进方案.md》，与 P2 并列）**：~~WP-F1（多空双面卡 build_debate/独立风控闸门 risk_gate.py/横截面 cross_section.py/五维情绪 sentiment_facets）~~ **第18轮已完成（默认只增量标注不改综合分，veto 仅告警，RISK_GATE_AUTO_DOWNGRADE 可选自动降级）**；WP-F2=信号胜率校准 signal_calibrator.py + tools/factor_eval.py 因子IC评估 + tools/build_ml_samples.py 落第9表 ml_samples（triple-barrier/PIT）；WP-F3=可选 llm_reviewer.py（无key休眠、只复核不改分）；WP-F4=tools 浅ML训练→JSON→生产标准库推理（样本外成本后跑不赢规则不上线）；明确不做端到端LLM/DRL/裸价格深度预测/ML进生产依赖。WP-F1/F2 不依赖 P2 可先行，F4 须在 P2-②pytest 之后。另有《Tushare引入可行性分析.md》给出可选第二信源路线（期货接口2000分起、零依赖适配层、只做盘后增强）。
**下一站（第47轮更新，编号以《总体对标与统一改进总纲（融合版）.md》3.5 为准）**：G19备份灾备✅46、**G5①②③相关/VaR/原油压力✅47（研究侧）**；**第48轮可选**：①**G5④ risk_gate 熔断可配置动作**（顺本轮VaR接动作层、默认仍只标注）；②**G26续二** 全64多gross网格影子/换手成本；③**G5⑤** 风险平价sizing影子接入（默认equal对照）；④多品种组合历史净值回测（替代backtest复利近似）；⑤G28续逐笔归因/看板页签。
**下一站（第35轮更新（旧，存档），编号以《总体对标与统一改进总纲（融合版）.md》3.5 为准）**：G28 已落地（因子+BHB归因；接入纸面/组合逐笔归因、看板“归因”页签为后续可选）；**第36轮可选**：①P1 **G21 标准研究面板+特征注册表+PIT/训练-服务一致性校验（地基）**；②**G22 续** tradable_mask涨跌停掩码/季节交割日历/常驻增量采集；③**G23 续**跟踪 carry（近窗含roll t 稳定≥1.5 才谈影子）；④本轮发现机构动向次日反向(IC−0.203)，**G29 因子体检（IC衰减半衰期/滚动失效预警）优先级上升**；旧排队保留：G24微结构/季节因子、G25表达式引擎(→G16)、G26组合构建(宜在G2后)、G27实验台账、G30交易journal、G1纸面影子对照(PAPER默认False)、G19备份、G5组合风控、G2插件化、G8只读web、G14一档快照、G13无key休眠；守三铁律与'不引重依赖/LLM因子不自动上线'红线。
**（历史路线，第24轮记）** **下一站（第24轮更新，编号以《总体对标与统一改进总纲（融合版）.md》为准）**：WP-A~E（11-16轮）、WP-F1（18）、WP-F2（19）、防过拟合（20）、pytest（21）、看板图表化（22）、两页合并（23）均已完成。第24轮完成第三份对标与六文档融合、**未改生产代码**。下一步按 G 编号推进：**P0 半天做 G9 git 工程化**（实测无 .git；git init 打基线 tag+.gitignore 忽略 data/*.db、cache、logs、reports 产物、__pycache__+pip freeze 锁三依赖==版本+CHANGELOG+VERSION，零改运行代码、pytest 仍191）；随后 **P1**：G10 配置外置（旧P2-1，config.json深合并+.env环境变量+template，缺省与现状逐字节一致）、G11 数据源主备熔断降级链（旧P2-2，新浪→东财分域→tdx→OpenVlab，健康分/熔断/半开，零网络mock断言）、G6 数据质量监控（data_health表+缺失率/陈旧价/跳变+复用alerts告警+零依赖按年SQLite分库归档）；再 G4 回测严谨（--fill close/next_open并列先影子+固定种子bootstrap1000置信区间+backtest_runs留档表+自动引DSR/PBO）；**最高价值 G1 纸面交易 PaperBroker**（新增paper_broker.py，实时虚拟成交close/next两档、复用portfolio三sizing与强平、storage加paper_orders/paper_trades/paper_equity三表、paper_account.txt+净值图、PAPER_ENABLED默认影子独立成段，拆两轮）。P2 按需：G3绩效包/G7多窗口TSMOM(先tools/factor_eval影子评估63/126/252再进分)/G19备份/G12产业链价差(旧P2-3,并入cross_section只监控不下单)/G5组合风控/G2策略插件化(第一步纯重构输出不变)/G14一档快照(旧P2-5)/G8只读web(标准库,默认关)；G15分钟库保持常驻自采（旧P2-6，minute_bars 自09-01，是G16样本根本，不买tqsdk年包）。研究线 G16 浅ML（旧WP-F4）门槛不变：pytest✅/ml_samples 42937行但跨度短，样本不成熟不提前上模型；上模型强制 tools/ml_train（LightGBM/逻辑回归，PurgedKFold+embargo、DSR/PBO把关）→导出JSON→生产 ml_inference.py 标准库推理、样本外真实成本后跑赢线性规则才允许小权重上线（可先做标准库逻辑回归/评分卡台阶）。G13 LLM 复核（旧WP-F3）需用户自备key、无key完全休眠，按需再做。G20 实盘须 G1 纸面影子满3月且指标过关+证监会2025-12号公告程序化报告+穿透式测试十项+C4等级，或用认证终端WH8/金字塔承载执行，在此之前不做任何自动实盘。
- **交易日历维护提醒**：静态休市表 STATIC_HOLIDAY_RANGES 目前只到 2026 年；每年12月证监会发布次年安排后需补区间，否则次年新节假日期间会按工作日误判（动态日K会在节假日发生后自动校正历史，但节前夜盘判断依赖静态表）。
- **本机写入权限（环境事实）**：会话初期向项目目录及 D 盘根写入一度被三通道（Write/PowerShell/Python）全部拒绝（疑似 Windows 受控文件夹访问）；经 `interaction.authorize_folder` 授权 futures_monitor 后**已恢复可写**（上下文摘要.md 与改进清单均成功落盘）。若后续再遇 PermissionError，优先重新申请目录授权或临时落 `%TEMP%`（C:\Users\Lenovo\AppData\Local\Temp，始终可写）再复制。
- 看盘不要用 Excel 长期打开 signals.csv（占用时程序跳过该文件、下轮补写），实时看 html 看板。
- history/offhours_history 为当日归档跨天自清；长期历史只在 daily_review.txt。
- 改时段/步长/阈值只需改 config.py（看板时段参数自动注入同步）。
- **回测口径**：backtest.py 只验证技术面，不能声称回放历史新闻/机构/实时量仓；分钟K免费窗口固定1023根（1m约2.5交易日/5m约3周/15m约3月/30m约6月/60m约12.5月），窗口之外的历史分钟K无法复原，第15轮日内回测只用 minute_bars 库内实有区间（自2026-09-01常驻自采、1m自08-26起）；主连必须比例复权；1/5/20日固定持有样本重叠，只做均值/胜率，不做复利；非重叠交易默认已扣真实券商手续费+滑点，报告同时给毛/净口径；日线多日持仓按开仓+平仓，不用平今；**日线锁板仍是收盘贴边+涨跌幅阈值的“疑似”识别，第15轮日内回测已升级为前收×品种常态幅度的整根封死精确锁板（FUTURES_LIMIT_MOVE），但两者都不是逐笔成交簿回放（免费无历史L2）**。日内回测平今按交易所结算交易日（非自然日、非夜盘开盘日）判定，详见第15轮章节；**第16轮 portfolio.py 组合账户与上述两套回测共用同一撮合口径，但资金层面是多品种共享一个资金池（按银河公司保证金率算占用/风险度/强平），替代旧的“按交易序列复利近似”，详见第16轮章节。**
- **OpenVlab嵌套字典**：`page["atm_iv"][品种名]`、`page["prem"][品种名]`、`page["rank"][品种名]`，不能直接在外层取。
- 新增数据源时：单源 try 包裹互不影响；消息 dict 必须带 source/time/content/important，建议补 confidence/doubtful；数据层建议做主备降级（P2-21）。
- **教训1（参数空间穷举）**：第14轮"新浪无1分钟"的结论源于只测了 type=5/15/30/60、漏测 type=1；凡断言"某源不支持某粒度/某字段"，必须把该接口的参数空间（周期/粒度/代码变体/主连与具体合约）穷举实测后再下结论，后续轮次发现反常（如东财1m持续拿不到而新浪5m很稳）应回头补测。
- **教训2（备用源也要防脏数据）**：东财限流期会返回 HTTP 200 但字段残缺的响应（f111=1、价格错位、push2delay延迟口尤甚），try/except 拦不住；任何备用/兜底源入库前必须做字段合理性校验（价格/昨结为正、持仓量达量级），宁可不兜底也不允许脏数据污染主链路。
- **教训3（跨会话接续先核磁盘）**：摘要/交接叙事可能滞后于磁盘真实状态（本次交接叙事停在第14轮，但磁盘上第15轮代码与四份文档实际已完整）；接续前先 ls/读关键文件核对真实进度、以磁盘为准，既不盲目重复开发、也不凭叙事假设"未完成"，先探查再决定是"新开发"还是"复验+补漏收尾"。
- **教训4（行数统计口径）**：PowerShell `(Get-Content 文件).Count` 对 LF 行尾或含中文文件统计行数偏小（main.py 显示745、Python物理行实为799）；核对代码规模一律用 `D:\Python\python.exe` 物理行口径 `sum(1 for _ in open(f,encoding='utf-8'))`，与摘要"29个py/9825行"对齐以此为准。
- **教训5（本环境编辑通道）**：Edit 工具在本机会间歇性报 "Native execution failed"（config.py/backtest.py/report.py 均中招，重试无效）；可靠通道是写一次性 Python 补丁脚本（每个替换锚点 assert count==1 防误伤，跑完即删）。PowerShell 不支持 bash heredoc（<<'EOF' 直接报错）、双引号嵌套的 `python -c "..."` 易因中文/引号崩，复杂 Python 一律落成临时 .py 文件用 `D:\Python\python.exe` 执行；临时脚本统一 `_probe/_patch/_fix/_view/_t/_dbg +轮次` 前缀，验后即删。
- **教训6（统一时间轴回放的"无bar时刻"与风控状态机）**：多品种组合回放取 dt 并集后，"某品种当前时刻无bar"（无夜盘品种在夜盘、各所收盘时刻错开）是常态而非异常——盯市要沿用最近收盘价、平今/费用/强平等所有按 t 取该品种数据的路径都必须能回退到"≤t 最近一根bar"（owner_at bisect），不能假设 t 一定在 by_dt 里；合成断言必须专门构造"被砍品种当前无bar"的场景，正常分散仓位（风险度低、0强平）覆盖不到强平路径，需刻意压低强平线做压测。另：风控要分清**触发线与停止线**（风险度≥强平线才启动、启动后砍到安全线才停），只对照一条线会在阈值附近反复触发。
- **教训7（接口代码变体要实测，勿凭命名推断）**：新浪期货日K主连只认单零 RB0、对双零 RB00 返回 `var t=(null)`（分钟接口同），而 backtest 曾把 meta["code"]="RB0" 再 +"0" 拼成 RB00 导致日线回测静默全空；同类教训还有第14轮新浪1m漏测。凡"某代码/某粒度取不到数据"，先把代码变体（RB/RB0/RB00/RB2601）与参数空间穷举实测，再怀疑数据源本身。
- **教训8（前端改动必须过 node --check + 真实浏览器目视双关）**：ECharts option 的 series 数组花括号配平错误（少一个 }）Python 侧完全无感、静态页照常写出，只有 node --check 内联JS能抓出语法错；而轴刻度重复、markLine 把坐标轴拉到0~100%压贴真实曲线、category 轴自动隐藏重叠标签、标签贴边裁切这类**显示层问题语法全对、只有真实浏览器截图才看得到**。凡改看板/JS：先抽内联脚本 node --check，再 file:// 打开逐屏截图，并用全 null fixture 验证空态，同时看浏览器 console 零报错。
- **教训9（工具显示可能失真，关键文件以磁盘字节为准）**：本会话 Read 工具对 上下文摘要.md 持续返回与磁盘不一致的另一套结构（章节/行数看似相同但内容不同）；发现信号是补丁锚点 count=0 且 Grep 搜不到关键词。处置：立即改用 `D:\Python\python.exe` 直读磁盘（splitlines 逐行打印）核对，以磁盘为准重写补丁，不凭 Read 显示内容下锚点；PowerShell 控制台对 UTF-8 中文显示乱码（GBK）不等于文件损坏，用 Python 回读验证即可。
