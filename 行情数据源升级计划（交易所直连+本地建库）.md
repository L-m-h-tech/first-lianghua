# 行情数据源升级计划：交易所直连 + 本地K线建库

> 生成日期：2026-09-11
> 背景：第118轮后，新浪 stock2（日线+分钟K）被 WAF 456 封锁禁用，当前日线=天勤主源+代理池兜底、分钟K=代理池+天勤兜底，链路可用但对爬虫源依赖仍深。本轮做全网调研 + GitHub 开源项目调研 + 本机实测，给出下一步计划。
> 调研方法：2 路 Agent（全网免费源现状 + GitHub 项目采集/存储方案）+ 本机 curl 实测五大交易所官网接口。

---

## 〇、一句话结论

**日线从"爬新浪"改为"直连五大交易所官网盘后文件"（免费、官方、无封锁，本机已验证 3/5 直接可用），分钟K以天勤为主源（近期滚动）+ 掘金量化做 2017 年至今的长历史一次性初始化，并配套本地 SQLite 增量建库（QUANTAXIS Diff 模式），彻底摆脱对已被封锁的新浪/东财爬虫链路的日常依赖。**

---

## 一、现状问题回顾（数据收集痛点）

| 数据 | 当前链路 | 问题 |
| --- | --- | --- |
| 日线 | 新浪（禁用）→ 天勤 TqSdk → 免费代理池 | 新浪 stock2 被 WAF 456 IP 级封锁（连交易时段也封）；天勤免费版每个周期限最近 8000 根；代理池质量参差 |
| 分钟K | 新浪（禁用）→ 代理池 → 天勤 | 同上，且免费代理不稳定、出口 IP 不可控 |
| 实时行情 | 新浪 hq.sinajs → 东财 → 天勤 | 新浪实时暂正常，但同域名族封锁风险仍在 |
| 历史深度 | 无本地长历史库 | 天勤免费版拿不到 8000 根以前的数据；换月后历史丢失 |

核心痛点：**第三方爬虫源（新浪/东财）不可控、会被 IP 级封锁；官方 CTP/SimNow 通道当前网络不可达；系统没有自己的长期历史数据库，导致"历史深度"与"数据主权"两个短板。**

---

## 二、全网调研结论（2025-2026 现状）

### 2.1 日K线：交易所官网盘后文件 = 最优免费源（本机实测）

| 交易所 | 接口 | 本机实测（2026-09-11） | 历史深度 |
| --- | --- | --- | --- |
| 上期所 SHFE | `https://www.shfe.com.cn/data/tradedata/future/dailydata/kx{YYYYMMDD}.dat`（JSON） | **HTTP 200，123KB，0.85s** | 全历史交易日 |
| 上期能源 INE | `https://www.ine.cn/data/tradedata/future/dailydata/kx{YYYYMMDD}.dat` | 与 SHFE 同构，待冒烟 | 2018 开市起 |
| 郑商所 CZCE | `https://www.czce.com.cn/cn/DFSStaticFiles/Future/{YYYY}/{YYYYMMDD}/FutureDataDaily.txt`（TXT，`\|` 分隔） | **HTTP 200（自动跳 HTTPS），37KB，0.89s** | 1990 年代起 |
| 中金所 CFFEX | `http://www.cffex.com.cn/sj/hqsj/rtj/{YYYYMM}/{DD}/{YYYYMMDD}_1.csv`（CSV）；或整月 zip | **HTTP 200，56KB，0.51s** | 全历史 |
| 广期所 GFEX | `http://www.gfex.com.cn/u/interfacesWebTiDayQuotes/loadList`（POST `trade_date`+`trade_type="0"`） | **HTTP 200**（需补 trade_type 参数） | 2021 开市起 |
| 大商所 DCE | `http://www.dce.com.cn/dcereport/publicweb/dailystat/dayQuotes`（POST JSON） | **HTTP 412**（JS 挑战型 WAF，curl 直连被拦） | 全历史 |

- **DCE 对策**：DCE 是唯一被反爬拦的交易所。三条路：① 天勤 TqSdk 已覆盖 DCE 日线（当前主源就有）；② akshare `get_futures_daily()` 内部对 DCE 有专门处理（社区可用，但本机直连被 412 拦，需 curl_cffi 浏览器指纹 + cookie 会话）；③ DCE 官网有 Excel 导出需过 JS 挑战，暂不强求。**结论：DCE 日线继续用天勤，不投入对抗。**
- 交易所文件**只有日线**（盘后发布，含结算价/持仓/成交额全字段），**官方不提供分钟数据**（行业共识）。

### 2.2 分钟K：免费长历史的两个互补源

| 源 | 免费 | 历史深度 | 现状 |
| --- | --- | --- | --- |
| **掘金量化 myquant** | 免费（基础行情函数） | 60s 起 **2017-01-01**；tick 2022 起 | 官方稳定，需登录终端/SDK；一次全量初始化 + 之后增量 |
| **天勤 TqSdk** | 免费访客模式 | 每周期最近 8000 根（60m≈3年、15m≈1年、1m≈2-3周） | 已接入，作近期滚动主源 |
| 东财 push2his（分钟） | 免费 | 合约上市以来 | **2024-2026 大量 TLS 指纹封锁**，不建议做日常主源 |
| 新浪 stock2 分钟 | 免费 | 1-2 年 | **456 封锁普遍存在**（akshare issue #1901 等），已禁用 |
| 交易所官方 | — | — | 无分钟数据 |

### 2.3 封禁现状判断：新浪/东财封锁是普遍现象，无"一劳永逸绕法"

- 新浪 stock2 456：akshare issue #1901（2021 年起反复出现）确认是 WAF 反爬，官方无解。
- 东财 push2his：2024-2025 大量 issue（#6764/#7005/#7228 等），akshare 只能加指数退避重试缓解。
- 社区主流对策（按推荐度）：**换官方/CTP 通道（本计划主路）→ 降频+缓存+增量 → curl_cffi 伪装指纹 → 家庭宽带 PPPoE 重拨/手机热点换 IP → 国内云中转**。代理池质量差，只适合低频兜底（当前正是这么用的）。

### 2.4 GitHub 开源项目调研（他们怎么收数据）

| 项目 | 上游源 | 存储 | 增量 | 可借鉴点 |
| --- | --- | --- | --- | --- |
| **QUANTAXIS**（11k★） | pytdx 通达信 + 东财 + 天勤主连 | MongoDB（可选 ClickHouse） | **QASU Diff 机制：只下载新增** | 增量更新调度、多源并行下载、主连获取 |
| **akshare**（15k★） | 新浪/东财/交易所 | 无内置（返回 DataFrame） | 无，需自写 | **交易所日线封装现成**（`get_futures_daily`） |
| **vnpy**（10k★） | CTP / 天勤 / tushare / RQData | SQLite 等 7 种适配器 | `download_bar_data(start)` 无断点记忆 | 数据库适配器分层 |
| **hikyuu**（3.5k★） | 通达信数据目录 | **HDF5**（单文件高性能） | 自研 | 大历史库性能方案 |
| **pytdx** | 通达信协议（exhq 支持期货） | — | — | 免费全量历史的通道（但公共口期货不稳，此前已实测否决） |
| **tqsdk** | 天勤服务器 | 内存，无持久化 | — | KQ.m@ 主连自动换月 |

社区最成熟可抄的存储/增量模式：**"首次全量 + 每日增量 + upsert 防重"**（QUANTAXIS Diff + vnpy datamanager 结合），存储轻量用 SQLite（本项目 monitor.db 已在用），大历史可选 HDF5。

---

## 三、目标架构（建议）

```
┌─ 日线（盘后）：五大交易所官网文件为主（SHFE/INE/CZCE/CFFEX/GFEX）
│                └ DCE → 天勤 TqSdk（已覆盖）
│                └ 失败 → 代理池兜底（保留现状）
│
├─ 分钟K（近期滚动）：天勤 TqSdk 为主（8000 根内）
│                └ 失败 → 代理池 → （可选掘金量化）
│
├─ 分钟K（长历史）：掘金量化一次性初始化（2017 起），写入本地库
│
├─ 实时行情：新浪 hq.sinajs → 东财 → 天勤（现状保留，不再动）
│
└─ 本地历史库：SQLite（monitor.db 扩展或独立 futures_kline.db）
    · 表：daily_kline / minute_kline / main_contract_mapping
    · 更新：每交易日收盘后增量（query 最新日期 → 只拉之后）
    · 主连：天勤 KQ.m@ 自动换月，或按持仓量排名自算（akshare 思路）
```

**关键收益**：① 日线日常不再依赖被封锁的新浪/东财；② 数据存本地，历史不丢、换月可回看；③ 天勤/代理池降为兜底而非日常主源，失败时系统更稳。

---

## 四、实施计划（分阶段，每阶段可独立验证）

### 阶段 1：交易所日线采集模块（新增 `exchange_daily.py`）【高优先级】
1. 实现 6 个交易所的拉取函数（SHFE/INE 同构 JSON、CZCE TXT、CFFEX CSV、GFEX POST、DCE 走天勤）。
2. 复用现有 `http_client.py` 全局连接池/重试/熔断；交易所接口无登录无封锁，注意**低频礼貌**（交易所不封但应克制，日线盘后一天一次即可）。
3. 交易日历：用现有 `trade_calendar.py`，只拉最近一个交易日的文件；历史补数据按日历倒推。
4. 输出统一 OHLCV+结算价+持仓结构，与 `futures_data.fetch_daily_kline` 现有返回格式对齐（**尽量做适配层，不改调用方**）。
5. 冒烟验证：9/10 与 9/11 五个交易所全品种拉取，与天勤当前值交叉核对（RB0/CU0/TA0 等对账）。

### 阶段 2：日线主源切换（`futures_data.py` 改链路）【高优先级】
1. `fetch_daily_kline` 主源改：先查本地库（有当日数据直接返回）→ 交易所文件 → 天勤 → 代理池。
2. 保留第 117/118 轮全部状态机/熔断/失败缓存机制不动（作为交易所源失败时的兜底路径）。
3. `SINA_DAILY_DISABLED` 保持 True，代码保留（新 IP 后可选恢复，但不再是主源）。
4. 验证：pytest 全绿 + 主分析报告日线数值与天勤一致。

### 阶段 3：分钟K主源稳定化（天勤强化）【中优先级】
1. 核查天勤免费访客模式当前分钟K覆盖是否满足全部 64 品种（第 118 轮已验证部分，补全品种遍历）。
2. `tqsdk_minute_kline` 增加 1m 周期（现有 5/15/30/60m，1m 是新浪没有的、东财又被封的周期，天勤可补）。
3. 本地分钟K滚动落库：每交易时段结束时把当天分钟K upsert 进 SQLite，解决"换月即丢历史"。
4. 验证：64 品种 × 1/5/15/30/60m 全部可拉；落库后重启可回读。

### 阶段 4：本地历史库 + 增量更新（新增 `kline_store.py`）【中优先级】
1. SQLite 表设计（参照 vnpy bar 表 + QUANTAXIS Diff）：
   - `daily_kline(symbol, date, open, high, low, close, settle, volume, open_interest, source, UNIQUE(symbol,date))`
   - `minute_kline(symbol, period, dt, ohlcv, UNIQUE(symbol,period,dt))`
   - `main_contract(symbol, date, contract_no)`（主连映射，换月记录）
2. 更新策略：每交易日收盘后 cron（现有 `run_cycle`/定时机制里挂一个任务）→ 查各表最新日期 → 只拉增量 → `INSERT OR REPLACE` 防重。
3. 首次初始化：日线用交易所文件按日历倒推全量；分钟K用掘金量化（阶段 5）或先只存天勤能拿到的 8000 根。
4. 验证：连续 3 个交易日增量运行不重复、不丢数；重启系统后分析直接读本地库。

### 阶段 5（可选）：掘金量化长历史分钟K初始化【低优先级，视需求】
1. 评估 myquant 免费额度与本系统是否匹配（需登录掘金终端/SDK，与项目"零重依赖"风格冲突，需权衡——**建议做成独立 tools/ 脚本，不进生产链路**）。
2. 一次性拉 2017 年至今全品种 60m/日线，写入阶段 4 的库，之后天勤负责近期滚动。
3. 如不需要 2017 年前历史，可跳过本阶段（天勤 8000 根 + 每日落库已满足监控/回测基本盘）。

---

## 五、验证清单（每阶段验收标准）

- [ ] 阶段1：9/10 五所日线全品种拉取成功，与天勤/代理池值对账一致（误差=0）
- [ ] 阶段2：pytest 全绿；`fetch_daily_kline('RB0')` 走交易所源，失败自动降天勤
- [ ] 阶段3：64 品种分钟K全覆盖（含 1m）；落库可回读
- [ ] 阶段4：连续 3 日增量无误；重启后读本地库不依赖网络
- [ ] 全程：不引入任何重量级依赖（pandas 仍只允许 tools/）；保持现有 WAF 状态机/熔断机制完整

---

## 六、风险与对策

| 风险 | 对策 |
| --- | --- |
| 交易所接口地址未来变更（SHFE 2025-06 已迁过一次地址） | 常量集中放 config/cons；变更时按 akshare 源码同步 |
| DCE 反爬升级 | 不硬刚，DCE 走天勤（现状已覆盖），监测 akshare 是否有新解 |
| 天勤免费版 8000 根限制影响长历史 | 阶段4 每日落库滚动积累；长历史缺口用掘金（阶段5）补 |
| 掘金/新依赖与"零重依赖"风格冲突 | 独立 tools/ 脚本隔离，不进生产链路；不满足可跳过 |
| 交易所文件格式（CZCE 千分位逗号、null 值等） | 解析层做清洗，与天勤值交叉校验兜底 |

---

## 七、遗留/可选

- 本机 SimNow/openctp CTP 仍不可达（180.168.146.187 超时）——若未来网络环境允许（如公司网/期货公司网），`simnow_source.py` 已就绪，可实时 tick 积累，补上"实时采集自行积累历史"这条官方通道。
- 新浪日线/分钟K代码保留但禁用；若新 IP 解封，可作第 4/5 源校验用，不再做主源。
