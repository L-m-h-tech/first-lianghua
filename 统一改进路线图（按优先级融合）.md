# 期货监控系统 · 统一改进路线图（按优先级融合版）

> 融合来源：《未完成项落地方案.md》（WP-A~E，第 11~16 轮）+《AI量化对标与改进方案.md》（WP-F1~F4，第 16 轮增补）+ 上下文摘要中的 P2 工程项与 Tushare 可行性结论。
> 融合日期：2026-09-02；性质：**这是一份按优先级重排的"下一步做什么"总路线图**，两份原始文档保留不删，本文件是它们的统一入口。
>
> **三条不可违背的总原则**（16 轮一贯，融合后继续生效）：
> 1. **生产运行链路零新增第三方依赖**（仅 requests/uiautomation/websocket-client；pandas/sklearn/lightgbm 等重依赖只允许出现在 `tools/` 研究侧）；GitHub 代码一律"读源码、用标准库 + `http_client.http` 重写"，可选适配器 try-import、缺失自动降级，绝不进 requirements 主依赖。
> 2. **确定不更差才采纳**：任何改动默认不改变现有输出（开关默认保守、先影子模式、可一键回退）；ML/策略类必须样本外、真实成本后 ≥ 现状才允许上线。
> 3. **每个改动配零网络合成断言 + 真实 `main.py --once --no-launch` 冒烟 + 负结果诚实呈现**；不编造数据，拿不到就留空并标注。

---

## 0. 一页纸优先级总览（先看这张表）

> 来源标记：**[基]**=原未完成项方案遗留；**[AI]**=AI 量化对标新增；**[工]**=P2 工程项；**[外]**=需外部条件的可选支线。
> 状态：✅=已完成（第 1 节归档）；⏳=进行中/持续积累；🔜=待做。

| 优先级 | 工作项 | 来源 | 生产新依赖 | 关键产出 | 前置条件 |
| --- | --- | --- | --- | --- | --- |
| **基线** | WP-A 期权链+PCR / WP-B IV曲面 / WP-C 基本面 / WP-D 分钟库+日内回测 / WP-E 组合账户 | [基] | 无 | ✅ 第11~16轮全部落地（详见第1节） | — |
| **P0 最高** | **WP-F1 规则化决策升级 + 横截面**（多空双面卡/独立风控闸门/横截面强弱/五维情绪） | [AI] | **无（纯标准库）** | risk_gate.py、cross_section.py；改 analyzer/factors/report/main/config | 无，可立即开工 |
| **P1-1** | ✅ **pytest 回归测试体系**（把历轮临时合成断言固化进 tests/，第21轮完成：15文件174用例全绿） | [工] | 仅 dev 侧 pytest（未进 requirements） | tests/ 目录、`pytest` 一条命令回归 | ✅ 已完成；P3 模型硬前置已就位 |
| **P1-2** | **WP-F2 数据驱动校准与 ML 样本资产**（信号胜率校准 sizing / 因子 IC 评估 / triple-barrier 样本表） | [AI] | 生产无；tools 可用 numpy | signal_calibrator.py、tools/factor_eval.py、tools/build_ml_samples.py、DB 第 9 表 ml_samples | signal_outcomes/minute_bars 持续积累（已在跑） |
| **P1-3** | ✅ **看板图表化**（本地 ECharts：权益/回撤/风险度、截面强弱、因子IC、胜率校准，第22轮完成） | [工] | 无（前端本地 ECharts，零 Python 依赖） | charts.py、assets/echarts.min.js；改 report/main/config、factor_eval 出 JSON sidecar | ✅ 已完成（14 新用例，全量 189 绿） |
| **P2-1** | 配置外置 config.json/.env（含 LLM key、未来 tushare token、webhook 统一管理） | [工] | 无 | config 加载层 | 无 |
| **P2-2** | 数据源主备自动降级链（新浪/东财/通达信/OpenVlab 分域名健康度与熔断） | [工][基] | 无 | 统一 fetcher 编排 | 无 |
| **P2-3** | 板块价差/产业链套利监控（**与 P0 横截面模块合并实现**） | [工][AI] | 无 | cross_section.py 扩展 | P0 B1 |
| **P2-4** | **WP-F3 LLM 情绪/信号复核适配层**（可选、无 key 休眠、只复核不改分） | [AI] | 无（走现有 http_client） | llm_reviewer.py | 用户自备 key（可选） |
| **P2-5** | 实时一档 tick 快照自采表（逼近真实滑点的唯一免费长期路径） | [基] | 无 | storage tick_snapshots 表 + 落库线程 | 无 |
| **P2-6** | 分钟库持续自采回填（P2-D0，⏳已在跑，越跑越长） | [基] | 无 | minute_bars 表（已 32.7 万根） | — |
| **P3 研究** | **WP-F4 浅 ML 多因子融合**（tools 训练→导出 JSON→生产标准库推理，跑不赢规则不上线） | [AI] | 仅 tools 装 sklearn/lightgbm | tools/ml_train/、ml_inference.py、模型 JSON | **必须先有 P1-1 pytest + P1-2 样本集 + 足够样本积累** |
| **P3 研究** | 期权遗留：成交量/成交额 PCR 备用源、交易所官方 IV 交叉校验 | [基] | 无 | option_chain/iv_surface 扩展 | 交易所源联调 |
| **可选支线** | **Tushare 零依赖适配层**（第二信源：官方日历/全历史仓单/交易所保证金/真实板价） | [外] | 无（裸 HTTP） | tushare_client.py + T1~T6 | **需 2000/5000 积分**（学生认证或捐助），详见第 6 节 |
| **永不做** | L2 逐笔回放、tqsdk 程序化批量、DRL 实盘、端到端 LLM 决策、裸价格深度网络预测、在线热更新、ML 重依赖进生产、pip 装 akshare/tushare SDK、用付费/盘后源替换实时主源 | [基][AI] | — | — | 见第 7 节 |

**推荐推进顺序**：P0（WP-F1，立即）→ P1-1 pytest 与 P1-2 校准/样本 并行 → P1-3 图表化 → P2 各项（按需）→ 样本与测试成熟后再评估 P3 模型；可选支线（Tushare/LLM）有外部条件时随时插入，不阻塞主线。

---

## 1. 已完成能力基线（WP-A~E，第 11~16 轮，后续工作的地基）

> 这些已不是"待办"，压缩归档；完整过程见《上下文摘要.md》与《未完成项落地方案.md》。后续模块直接复用其产出，不重复造轮子。

| 工作包 | 轮次 | 核心产出（文件） | 关键结论/可复用资产 |
| --- | --- | --- | --- |
| WP-A 期权链+PCR | 11 | option_chain.py；改 option_analyzer/storage/report/main/analyzer/contracts/config | 新浪 T 链全腿报价、持仓量 PCR；OptionChainCache(TTL30min)；成交量 PCR 待备用源；LU 新浪无源已降级 |
| WP-B IV 曲面+日历价差 | 12 | iv_surface.py(~260行)；改 option_strategies/option_analyzer/main/report/config；option_chains 表 | 多到期日 IV 曲面、25Δ RR/蝶式、日历/反向日历候选 + 静态盈亏空间硬检查；**免费脏报价五层清洗** |
| WP-C 基本面数据包 | 13 | fundamental_data.py(188)+fundamental_factors.py(151)；fundamentals 第 7 表 | 东财库存仓单（仅近 3 个月窗口）+龙虎榜 TYPE=2+期限 carry+基差软降级；缺项按可得权重重归一、不编造 |
| WP-D 分钟库+日内回测 | 14/15 | intraday_bars.py、tdx_bars.py、minute_bars 第 8 表；intraday_backtest.py(649)；config FUTURES_LIMIT_MOVE | 新浪主连分钟为主+东财补1m+通达信可选冗余；vnpy 式 bar 内撮合、平今/平昨按**交易所结算交易日**、精确锁板；**成本后负结果诚实呈现** |
| WP-E 组合资金账户 | 16 | portfolio.py(947)、tools/build_margin_table.py(184)、data/futures_margins.csv；report 13 页签 | 共享资金池逐 bar 盯市、三种 sizing、板块/名义/可用/持仓数约束链、强平状态机、权益曲线；37 断言+全 64 品种端到端 |

**后续必须继承的 5 个关键坑（踩过、勿重踩）**：
1. OpenVlab 期权月份键是 6 位整数（202611），新浪 T 链 pinzhong 要 4 位（2611），必须 `(yymm//100)%100, yymm%100` 归一，否则全品种抓空。
2. 免费 T 链脏报价（临近到期、ask=0、价差 45%）会反推出 80%~120% 假 IV，必须走五层清洗（报价质量分级/IV>250% 丢弃/ATM 需高质量腿/剩余<30 天剔除/离群点带外剔除，缺档 `--` 不插值）。
3. 东财要**分域名评估**：datacenter-web 基本面秒回时，push2his 行情口可能持续断连；行情不可单一压东财。交易所官网 SHFE dailystock 404、DCE 412 WAF、生意社 HW_CHECK 反爬均已验证做不通，别重试。
4. 平今/日终强平按**交易所结算交易日**（21:00 夜盘归下一交易日），不沿用 utils 的"夜盘开盘日"服务日切口径。
5. 新浪日 K 只认单零主连（RB0），双零 RB00 返回 `var t=(null)`；保证金以**用户开户的银河期货**投机档为准（与国君档确有差异，如 AG 35% vs 30%）。

**AI 化底子盘点（多数开源 AI 项目反而没有，P1/P3 直接用）**：8 张 SQLite 表=现成样本库；signal_outcomes 三周期 hit/miss=现成监督标签；intraday_backtest 止盈/止损/超时=天然 triple-barrier 标注器；18 组稳定性网格=防过拟合文化；portfolio 共享资金池+强平=真实资金层；analyzer 9 类因子 parts 线性融合（analyzer.py:46-91）。

---

## 2. P0 最高优先：WP-F1 规则化决策升级 + 横截面（零依赖、可立即开工）

> 为什么排第一：全部纯标准库实现、**默认不改变现有综合分**（只增信息层 + 保守开关）、是 P1-2/P2-3/P3 的结构基础，且直接补上 AI 对标中确定性最高的四块短板。对应多智能体框架（TradingAgents/ai-hedge-fund）值得借鉴的"多空对立审查 + 独立风控否决"结构，用规则实现、不引入 LLM 不确定性。

### 2.1 A1 多空双面论证卡（bull/bear debate 规则化）
- **现状**：`analyzer.analyze_variety` 把 parts 正负值直接 `sum` 成一个分（analyzer.py:91），因子分歧被加总抵消，报告只看到结论。
- **改法**：score 算出后**不动 score**，新增纯函数 `build_debate(parts, notes)`，输出 `{"bull":[正向因子名+值+理由], "bear":[反向...], "decisive": 绝对值差最大因子}`；重点品种明细加一行"多: 技术+2.1/基本面+1.1｜空: 新闻-1.8｜决定项=技术"。
- **文件**：analyzer.py（新纯函数）、report.py 明细渲染。
- **验证**：合成 parts 断言多/空分组、全中性不出卡、decisive 正确。**默认行为：纯展示，综合分与告警不变。**

### 2.2 A2 独立风控闸门（Risk Manager 规则化，区别于现有 risks 文字提示）
- **现状**：`risks[]` 只提示不影响建议；硬约束只存在于离线 portfolio。
- **改法**：新增 `risk_gate.py` 纯函数 `risk_gate(row, ctx) -> {"verdict": pass/warn/veto, "level", "reasons":[]}`，检查项全部可计算：锁板/接近涨跌停、流动性（量/仓低于品种分位）、方向与期限 carry 背离、综合分仅由单一因子拉动、板块拥挤（依赖 2.3 截面结果）、临近交割（现有 dd<45 提示升级为硬降级）、HV 极端分位。**veto=降级观望不下手数，warn=减档**；main 在拿到 row 后、告警/报告前应用。
- **文件**：新增 risk_gate.py；改 analyzer/main/report/alerts（被 veto 不推"可交易"告警）；config 加 RISK_GATE_* 开关与保守阈值。
- **验证**：逐检查项多/空/边界合成断言；veto 不出交易建议、warn 降档；**开关关闭时与现状逐值一致（回归保护）**。默认仅极端情形 veto。

### 2.3 B1 横截面强弱（商品 CTA 文献最稳、本项目最明确的缺失块）
- **现状**：64 品种每轮各自时序打分，从不横向比较（GitHub 清单条目⑮一直未做）。学术证据：商品期货上时序+截面双动量是最稳预测信号（Bayes-CID 2026 41 商品 GBDT 样本外夏普 2.4 亦由时序动量主导）。
- **改法**：新增 `cross_section.py`，main 收齐全部 fut_rows 后统一计算：对综合分/日线动量/基本面分做**稳健横截面 z-score（中位数/MAD，防单点拉爆）与分位**，按板块聚合板块均值，输出全市场多空 Top/Bottom、板块强弱榜、板块内排名、"同板块多空打架"拥挤提示（喂给 2.2 风控闸门）；report 期货总表后加【横截面强弱】块；portfolio 加 `--cross-section` 候选过滤（默认关，回测对比后再定）。
- **文件**：新增 cross_section.py；改 main/report/config、portfolio 可选。
- **验证**：已知分布断言 z-score/分位/板块聚合/极值稳健；缺值品种不参与不报错。**默认：展示层立即生效，组合过滤默认关。**

### 2.4 D1 五维情绪（金融 NLP 论文维度，规则先行零依赖）
- **现状**：factors.py 约 70 词词典+15 正则+局部否定，只输出一维极性。
- **改法**：新增纯函数 `sentiment_facets(text)`，在不改极性主分前提下加五维（对齐北师大学报期货多层次情感、WTI 五维情绪论文）：**intensity 强度**（程度副词"大幅/骤/飙"）、**uncertainty 不确定性**（"或/据称/传闻/有望"，与现有 doubtful 衔接）、**relevance 相关性**（是否点名品种/板块 0/0.5/1）、**forwardness 前瞻性**（"预期/拟/明年" vs "已/落地"）、**event_type 事件类**（供给/需求/库存/宏观/政策/地缘/汇率）。聚合：情绪分=极性×强度×相关性，不确定性高则降权，前瞻/事实分开计数。
- **文件**：factors.py（补词典与纯函数）、storage.news 加维度列或 raw_json（兼容旧行）、report 维度角标。
- **验证**：逐维造句断言（"传闻拟大幅减产"=前瞻+不确定+高强度+供给类）；旧一维调用路径兼容。

**WP-F1 验收总范式**：compileall 全过 → 4 模块零网络合成断言全过 → `main.py --once --no-launch` exit 0 且关闭开关时输出与当前逐值一致 → 临时脚本删除 → 更新 README/摘要。

---

## 3. P1 高优先

### 3.1 P1-1 pytest 回归测试体系 [工]（P3 模型与后续一切改动的硬前置） ✅ 已完成（第21轮 2026-09-02：tests/ 共15个测试文件174用例，pytest 9.1.1 全绿约2秒；生产 requirements 未动）
- **现状**：历轮断言都是临时 `_t*.py` 跑完即删（第 16 轮 37 条、全项目累计数百条口径），没有固化回归。
- **改法**：建 `tests/`，把已验证口径按模块固化为 pytest 用例（analyzer 打分边界、intraday_backtest 撮合十五组、portfolio 37 条、iv_surface 清洗、fundamental 归一、risk_gate/cross_section 新增项）；零网络、确定性、字母序回放；`pytest -q` 一条命令全绿。仅 dev 环境装 pytest，**生产 requirements 不动**。
- **验证**：刻意制造一个错误断言确认能红、修复后全绿。
- **落地记录（第21轮）**：`tests/conftest.py` 提供 `flat_calendar`（注入确定性交易日历，零网络）与 `tmp_db`（临时 SQLite，不碰生产库）两个夹具；15 个 `test_*.py` 覆盖 utils 调度时段/cross_section 稳健z/risk_gate 三档/signal_calibrator 四级回退/fundamental 缺项归一/factors 五维情绪与否定/analyzer 评级与双面卡/option_chain/iv_surface/intraday_bars/flow_tracker/backtest 费用/portfolio 盯市手数强平/storage 去重/三 tools 自测/全量编译；变异测试（改坏 0.6745 常量）确认对应用例变红、还原后 174 全绿；`main --once --no-launch` 冒烟退出码0、生产库 integrity/quick_check=ok。运行：项目根目录 `D:\Python\python.exe -m pytest`，详见 `tests/README.md`。

### 3.2 P1-2 WP-F2 数据驱动校准与 ML 样本资产 [AI]（生产零新增依赖） ✅ 已完成（第19轮 2026-09-02：signal_calibrator 影子校准 + tools/factor_eval + tools/build_ml_samples/第9表 ml_samples 42937行）
**A3 方向与仓位分离——历史胜率校准 sizing（meta-labeling 零依赖版）**
- 现状：portfolio 手数档位只看当下综合分；signal_outcomes 已积累"分档/因子组合→hit 率"却没用于 sizing。
- 改法：新增 `signal_calibrator.py`，按"方向×综合分档×主导因子"做**贝叶斯平滑胜率/平均方向收益** `(hits+α)/(n+α+β)`，输出置信乘子 `conf_mult∈[0.5,1.2]`；portfolio 的 sizing 乘该乘子，实时侧仅展示"历史同类胜率 x%（n 笔）"；**n<20 不校准返回 1.0**，先**影子模式**（只展示不下发）观察分布后再启用，可 `--no-calibrate` 关。
- 验证：合成 hit/miss 断言平滑公式、小样本回退、乘子裁剪；真实 DB 影子跑只统计不改交易。

**B2 因子 IC/RankIC 评估闭环（学 Qlib Analyser，替代拍脑袋权重）**
- 改法：`tools/factor_eval.py`（研究侧允许 numpy/pandas）：从 signals（含因子拆分）+signal_outcomes 算每因子对 30m/2h/1d 远期收益的 IC、RankIC、ICIR、分档单调性、多空价差、半衰期衰减，walk-forward（前 K 月统计后 1 月验证滚动），产出 reports/factor_eval.txt，**只给"建议权重区间"供人工调 config，不自动改权重**。
- 验证：构造完全单调/完全无关两组合成数据断言 RankIC≈1/≈0；断言标签窗口严格晚于特征（无泄漏单测）。定期人工跑、不进常驻链路。

**B3 Triple-Barrier 样本集（为 P3 备料，学 López de Prado AFML）**
- 改法：`tools/build_ml_samples.py` 复用 intraday_backtest 撮合口径离线生成路径依赖标签（+k·ATR 止盈=1 / -k·ATR 止损=-1 / 最长 M 根超时=sign），同时落特征快照（9 因子+截面 z+五维情绪），写 **storage 第 9 张表 `ml_samples`**（UNIQUE(sym,bar_dt)，长期保留）。严格 PIT：特征 ≤t、标签路径 >t、跨标签窗口 embargo。
- 验证：先触止盈/先触止损/横盘超时/跳空穿越四类合成路径断言标签；断言无特征穿越。

### 3.3 P1-3 看板图表化 [工] ✅ 已完成（第22轮 2026-09-02：新增 charts.py(732行)+本地 ECharts5.5.1，tests/test_charts.py 14 用例，全量 189 绿）
- **现状**：report.py 13 页签，权益/交易/IC 等仍是 iframe 嵌 txt。
- **改法**：引入本地 ECharts（前端资源、非 Python 依赖）画：portfolio 权益/回撤/风险度曲线、P0 截面多空热力图与板块强弱、P1-2 因子 IC 条形与分档单调性、信号胜率校准曲线。数据由现有 CSV/SQLite 注入。
- **验证**：--once 生成后浏览器逐页签目视核对数值与 txt 一致；无数据时显示空态不报错。
- **落地记录（第22轮）**：
  - 新增 `charts.py`（纯标准库）：`parse_equity_csv`（权益CSV解析+确定性等距抽稀≤1200点，不改原始CSV）、`cross_section_payload`（state.last_cross_section→JSON安全结构）、`calibration_payload`（方向×分档贝叶斯胜率+sizing乘子）、`outcomes_payload`（signal_outcomes 分周期总/多/空胜率）、`factor_payload`（读 factor_eval.json）；四块各自独立 try，缺一块只置 None；`build_payload/write_chart_data` 每轮落 `reports/chart_data.js`（window.CHART_DATA 全局，file:// 下动态 script 注入，沿用 report_status.js 模式）；`python charts.py --rebuild` 可在监控未运行时从 DB+CSV 离线重建。
  - 前端：`assets/echarts.min.js`（5.5.1 Apache-2.0，1.03MB，随仓库）运行时幂等同步到 `reports/assets/`；静态页 `reports/图表看板.html`（暗色风格，8+1 个图：权益曲线/回撤/风险度+持仓、板块强弱/全品种xs+广度、三周期RankIC分组柱/主周期分档单调性、校准胜率柱+乘子线/分周期胜率）；外层实时看板新增第二个页签，**13 个 txt 页签全部保留并存核对**；缺数据显引导空态、控制台零报错。
  - `tools/factor_eval.py` 重构出 `evaluate_all/factor_metrics_json`，txt 报告外新增结构化 sidecar `reports/factor_eval.json`（txt 口径不变、计算不重复）；report.save 每轮注入、main 启动即写首份（离线CSV/DB先出图，内存态首轮补齐）。
  - 验证：node --check 内联JS语法；`main --once --no-launch` 一轮真实 64 品种数据五块全填充（截面64品种5板块、校准6行、胜率315/310/176 与 signal_tracking.txt 一致、9因子IC）；真实浏览器逐图目视+全空态fixture目视，控制台全程零错误。

---

## 4. P2 中优先（按需推进）

### 4.1 P2-1 配置外置 [工]
新增 config.json/.env 加载层，把 webhook、LLM key（P2-4）、未来 tushare token（第 6 节）、可调参数从代码分离；**key/token 只走环境变量，不写死不入库不进日志**（沿用 alerts 的 FUTURES_MONITOR_WEBHOOK 先例）；缺配置回退默认值。零网络单测覆盖加载/缺项/非法值。

### 4.2 P2-2 数据源主备自动降级链 [工][基]
统一 fetcher 编排：新浪→东财→通达信(可选)→OpenVlab 分域名健康度统计、连续失败熔断与定时重探（通达信期货 7727 口 probe 点亮才用）；把第 14 轮"分域名评估、不把单点压一家"的经验固化为自动机制。零网络用 mock 注入断言主备切换/熔断/恢复。

### 4.3 P2-3 板块价差/产业链套利 [工][AI]
**并入 P0 的 cross_section.py 实现**：在截面强弱之上加产业链对（煤焦钢矿、油化工链、油脂油料）的价差/比价 z-score 与极端分位提示；只呈现与告警提示，不自动交易。合成产业链价格序列断言方向与分位。

### 4.4 P2-4 WP-F3 LLM 情绪/信号复核适配层 [AI]（可选、无 key 完全休眠）
- **定位**：LLM 只做"第二意见"，对应 P2-22 预留；**永不改综合分、永不下单、不做端到端决策**（TradingAgents 作者自声明研究用途、输出随 temperature 漂移）。
- **改法**：新增 `llm_reviewer.py`，**只**对三类小流量调用：①触发紧急轮动的新闻；②|综合分|≥6.5 强信号的新闻簇；③词典情绪与技术/基本面明显背离。OpenAI 兼容协议（DeepSeek 级云端约元/百万 token，或本地 Ollama+Qwen2.5-7B，约 5~8GB 内存 CPU 可跑）走现有 http_client，**守护线程异步、不阻塞 run_cycle**；强制 JSON schema（方向/强度1-5/涉及品种/不确定性/一句话理由/与词典是否一致）；超时/无 key/坏 JSON 全软降级为"无复核"。key 走环境变量 FUTURES_MONITOR_LLM_KEY/BASE_URL/MODEL。
- **验证**：mock HTTP 断言 schema 解析、字段越界裁剪、超时/坏 JSON/无 key 三条降级路径、异常绝不抛进主循环、主分不被复核修改。**默认：无环境变量即零请求零开销。**

### 4.5 P2-5 实时一档 tick 快照自采表 [基]
免费可历史回放的 L2 逐笔不存在（天花板，见第 7 节），唯一逼近真实滑点的免费路径是自己采：用现有 10s/60s 轮询把一档买卖价量快照持续落 `tick_snapshots` 表，事后统计滑点/盘口深度。零新依赖；断言 upsert 去重、断网续传不丢不重。

### 4.6 P2-6 分钟库持续自采 [基]（⏊进行中）
minute_bars 表已建（32.7 万根，自 2026-09-01 起），kline 60s 线程持续 upsert；这是 P1-2 样本、P3 模型、分钟回测稳定性的根本，**只需保持常驻不断采**；历史缺口靠新浪 1023 根窗口滚动补齐，1m 靠东财/通达信补，不买 tqsdk 年包。

---

## 5. P3 研究侧（低优先，水到渠成，门槛最严）

### 5.1 WP-F4 浅 ML 多因子融合 [AI]（"研究重、生产轻"唯一合规通道）
- **架构规则**：`tools/ml_train/` 内才允许 scikit-learn/lightgbm（与 pandas 同等待遇，**不进 requirements、不进生产**）；训练产物只导出极简 JSON（线性权重/受限树分裂阈值/特征列表/版本/训练区间/样本外指标）；生产新增 `ml_inference.py` 用**标准库前向推理**（线性点积或深度受限决策树遍历），缺模型文件/版本不符→自动回退现有线性 parts 打分并登记 fallback。
- **模型边界**：只做"多因子融合/meta-label 概率"（给定特征预测 triple-barrier 为正的概率），**不做价格预测、不做 DNN**；限定 LightGBM/逻辑回归等可解释浅模型；强制 Purged TimeSeriesSplit+embargo（禁随机 K 折）。
- **上线硬门槛（写死）**：样本外、真实手续费+滑点+公司保证金后，净值与稳定性必须 **≥ 现有线性规则**，否则不上线（学术反面证据：Stanford 简单趋势跑赢 ML、FUTURES_NN 49% 低于随机、XGBoost 52% 命中但 alpha 为负）。即便通过也只作小权重"ML融合"因子或喂给 A3 校准器，默认关、不端到端接管。
- **前置**：P1-1 pytest + P1-2 的 ml_samples 样本集 + minute_bars 足够样本（分钟样本自 2026-09-01 积累，需耐心，不提前上模型）。
- **验证**：sklearn 训练→导出→标准库推理逐值一致（容差 1e-9）；缺文件回退断言；样本外成本后不达标时断言"不上线"分支。

### 5.2 期权遗留增强 [基]
成交量/成交额 PCR（批量 P_OP_ 快照或交易所日行情备用源）；交易所官方 IV（akshare option_vol_shfe/gfex 的 URL，零依赖重写）与自算 IV 交叉校验、偏差>2vol 标注。均为边际增强，低优先。

---

## 6. 可选外部增强支线（需外部条件，不阻塞主线）

### 6.1 Tushare 零依赖适配层 [外]（详见《Tushare引入可行性分析.md》）
- **结论**：GitHub waditu/tushare 的开源 SDK 实质停更于 2020-03 且拖 pandas/lxml/bs4 全家桶，**不 pip 安装**；真正有价值的是 Tushare Pro 的 HTTP JSON（api.tushare.pro，本机已实测通道可达，现有 http_client 可裸调）。**只做零依赖可选适配层** `tushare_client.py`（token 环境变量、无 token/积分不足/断网全软降级、现源全保留为主、盘后批量），定位"日频盘后/历史深度/官方基准"第二信源，永不成唯一源。
- **积分门槛**：注册 120 分不够期货；期货日频接口清一色 **2000 分**（高校学生/教师认证可申请，否则捐助约 200 元）；ft_limit 盘前真实板价+交易所最低保证金率 m_ratio 要 **5000 分**；期货分钟 ft_mins 积分外单独付费（2000 元/年），**不买、继续靠自采**。
- **落地顺序（有积分后）**：

| 批次 | 内容 | 接口 | 积分 | 补本项目哪个短板 |
| --- | --- | --- | --- | --- |
| T1 | 官方期货交易日历最高优先级源，删掉每年手动维护 STATIC_HOLIDAY_RANGES | trade_cal | 2000 | 交易日历 |
| T2 | 仓单全历史回填+日增，库存分位从东财 3 个月升到多年 | fut_wsr(2006起) | 2000 | WP-C 基本面口径短板 |
| T3 | 合约元数据（乘数/最后交易日）同步、主力映射交叉校验 | fut_basic/fut_mapping | 2000 | 减硬编码、冷启动 |
| T4 | 回测具体合约拼接+结算价盯市，替代主连比例复权/收盘盯市近似 | fut_daily(含settle) | 2000 | WP-D/E 回测保真 |
| T5 | 交易所基准保证金补 futures_margins.csv 留空列、盘前真实板价替代常态档 | ft_limit/fut_settle | **5000** | WP-E 保证金/锁板 |
| T6 | 龙虎榜全历史时序、交易所手续费基准交叉校验、南华板块指数 | fut_holding/fut_settle/index_daily | 2000 | 因子/截面增强 |
- **最小高性价比=T1+T2**（2000 分档内消掉两个白纸黑字的短板）；与 WP-F1/F2 互补：全历史仓单提升基本面因子质量、真实板价提升风控闸门锁板判定。

---

## 7. 明确不做清单（天花板 + AI 反面证据，防止重复投入）

**A. 免费数据/工程天花板（《未完成项方案》第 7 节，诚实声明、不夸大）**
1. 历史逐笔/L2 五档免费不可得：分钟回测成交是 bar 内规则假设（开盘成交/高低触及/参与率限量），不是真实队列；实时盘口只能靠自采（P2-5）。
2. 分钟历史深度有限：长期库最终靠常驻自采，主连拼接处是比例复权近似。
3. tqsdk 程序化回测/批量下载是专业版（1488/年），免费版仅实时/有限历史，适配器缺授权必须降级，不硬依赖。
4. 基本面日频多源口径有差异，数值标注来源；龙虎榜是席位聚合≠机构全部持仓；保证金率为估算、需定期重跑工具。
5. IV 曲面在低流动性远月/深虚值腿不可信，反推前必须过滤、缺腿不插值。

**B. AI 量化不做项（《AI 量化方案》裁决，附证据）**
6. **不做深度强化学习实盘**（FinRL 系）：漂亮结果集中在美股、非平稳/过拟合公认、本机无 GPU、分钟样本太短。
7. **不做端到端 LLM 自动交易/自动出单**：非确定性、不可复现、不可回测；LLM 仅做 P2-4 的文本复核配角。
8. **不做裸 K 线 LSTM/Transformer 价格预测**：证据为准确率 49% 低于随机、命中方向但成本后 alpha 为负。
9. **不做在线学习/热更新**：破坏可复现性；模型必须定版、带训练区间与样本外指标。
10. **不把 torch/sklearn/lightgbm/pandas 装进生产 requirements**：研究重依赖只在 tools，生产标准库推理。
11. **不 `pip install akshare/tushare SDK`**：只搬 URL/算法、零依赖重写或裸 HTTP；**不用付费/盘后源替换新浪/东财实时主源**（Pro 盘后更新、无免费实时，替换会断盘中监控三条腿）。

---

## 8. 统一实施纪律与影响评估

**每个工作项的固定流程**（16 轮沿用，融合后不变）：
1. 先写/改纯函数，compileall 全过；
2. 零网络合成断言（fixture 用真实响应片段/构造边界用例），临时脚本跑完即删，成熟口径沉淀进 P1-1 的 tests/；
3. 真实 `python main.py --once --no-launch` exit 0、无 ERROR/Traceback，DB `PRAGMA integrity_check=ok`；
4. 策略/模型类必须给真实成本后结果，负结果照实写，不粉饰；
5. 同步更新 README、本路线图勾选状态、上下文摘要。

**对现有使用的影响（统一承诺）**：

| 工作项 | 生产依赖 | 默认行为 | 回退方式 |
| --- | --- | --- | --- |
| P0 WP-F1 | 零新增 | 综合分不变，只增多空卡/截面/五维信息；风控闸门仅极端 veto | config 开关关闭即与现状逐值一致 |
| P1-1 pytest | 仅 dev | 不影响运行 | — |
| P1-2 校准/IC/样本 | 生产零新增（tools 可 numpy） | 胜率校准先影子模式；IC/样本为离线工具 | --no-calibrate；不跑工具即无影响 |
| P1-3 图表化 ✅第22轮 | 零（本地 ECharts 前端资源） | txt 与图表并存（13 txt 页签全保留） | 删 charts 接线即回纯 txt |
| P2 工程项 | 零新增 | 缺配置/缺 key/源故障全部软降级 | 回退默认值/旧源 |
| P2-4 LLM | 零（现有 http） | 无 key 零请求休眠 | 移除环境变量即停用 |
| P3 ML | 仅 tools | 默认关、缺模型 JSON 回退线性打分 | 删模型文件即完全回退 |
| Tushare 支线 | 零（裸 HTTP） | 无 token 完全等价现状 | 不设 token 即休眠 |

**性能**：截面/五维/风控为每轮毫秒~十毫秒级纯计算；胜率校准读本地 SQLite；LLM/Tushare 均守护线程或盘后异步，不阻塞 run_cycle 主循环。**兼容性**：新表/新列一律向后兼容（INSERT OR REPLACE、raw_json 兜底旧行、UNIQUE 去重）。

---

## 9. 参考资料与可复用开源清单（两份文档合并）

### 9.1 工程/数据/回测类（宽松许可，只搬 URL/算法、不引依赖）
| 仓库（License） | 搬什么 |
| --- | --- |
| akfamily/akshare（MIT） | 新浪期权链解析、交易所期权日行情/官方 IV URL、基差/仓单/库存/龙虎榜/展期收益 URL 与表格解析 |
| vnpy/vnpy_ctastrategy、vnpy_portfoliostrategy（MIT） | bar 内撮合 cross_limit_order、平今分别计费、逐笔盈亏、逐日权益、多合约共享资金账户（WP-D/E 已移植） |
| shinnytech/tqsdk-python（Apache-2.0，免费版） | 可选分钟/tick 适配器（程序化批量付费，不依赖） |
| vollib/py_vollib（MIT） | IV 反推迭代思路（已自写二分，零依赖） |
| zhouxinhao19/TradingAgent-Future | 基本面因子组织与数据源降级链 |

### 9.2 AI 量化类（借结构/方法论，不追热点）
| 来源 | 取什么 / 戒什么 |
| --- | --- |
| TauricResearch/TradingAgents（69k★，arXiv:2412.20138）、virattt/ai-hedge-fund（62k★） | 取"多空对立辩论 + 独立风控否决 + 信号→风险→组合三段裁决"的**结构**（P0 规则化）；戒端到端 LLM 决策 |
| microsoft/qlib | 取 Alpha158 因子组织、PIT 防泄漏、IC/RankIC 评估、滚动训练、模型集成（P1-2/P3） |
| López de Prado《Advances in Financial ML》 | triple-barrier 标签、meta-labeling、purged K-fold+embargo、CPCV、样本权重、SHAP 选特征（P1-2/P3） |
| AI4Finance FinRL/FinRL-X | 仅参考 S/A/T/R 分层思想；DRL 实盘**不做** |
| 金融 NLP：北师大学报 2025 期货多层次情感、arXiv2026 WTI 五维情绪、FinBERT/中文轻量 LLM | 五维情绪维度（P0 D1 规则版）；LLM 仅复核（P2-4） |
| 反面证据：Stanford 期货趋势 ML、lrud/FUTURES_NN、regime-aware XGBoost、Bayes-CID 2026 | 商品期货时序/截面动量最稳、DNN 常跑不赢简单趋势、ML 有效需严格防泄漏+样本外成本后评估 |

### 9.3 关联文档
- 《未完成项落地方案.md》——WP-A~E 完整落地过程与本机实测原始证据（附录 A/B）
- 《AI量化对标与改进方案.md》——四族调研全文、A~E 层文件级改法、工作包论证
- 《Tushare引入可行性分析.md》——积分权限逐行核实、T1~T6 与三种引入方式裁决
- 《数据源选型与通达信替换可行性分析.md》《GitHub对标与改进清单.md》——选源与 23 条工程对标
- 《上下文摘要.md》——16 轮全部决策、代码锚点、教训与当前状态（接续入口）
