# Tushare 引入可行性分析（GitHub 开源调研 × 本项目逐模块对照）

> 结论日期：2026-09-01 深夜（本机，重庆/当前运营商网络；官方文档 + GitHub 仓库 + 本机 requests 实测，非纸面推断）
> 触发背景：按要求上 GitHub 找开源 tushare，评估能否带入 futures_monitor、能优化哪些模块、是否影响现有使用。
> 对照基线：本项目第 1~16 轮全部成果（32 个 py / 11082 行，生产运行依赖仅 requests/uiautomation/websocket-client，刻意零重依赖）。

---

## 〇、一句话结论

**tushare 真正值钱的是 Tushare Pro 的"日频盘后/全历史"数据，不是 GitHub 上那个开源 SDK；它补的是本项目"历史深度与官方基准参数"的短板，补不了"实时盘中监控"。建议按 `tdx_bars.py` 同款"可选适配层 + 零依赖裸 HTTP + 无 token 自动降级"方式引入，只做增强源、不替换任何现有主源——这样对现有使用零影响；不建议 `pip install tushare`，更不能用它替换新浪/东财主链路。**

硬门槛先摆这里：**期货类接口全部 2000 积分起步**（注册只给 120；高校学生/教师认证可申请 2000；否则捐助约 200 元）；交易所盘前保证金/涨跌停价 `ft_limit` 要 **5000 积分**；期货分钟线 `ft_mins` 是积分之外**单独付费**（历史 2000 元/年、实时 1000 元/月）。

---

## 一、GitHub 上的 tushare 到底是什么（调研事实）

### 1.1 它是"两层东西"，别混为一谈

| 层 | 是什么 | 现状（2026-09 实测/实查） |
| --- | --- | --- |
| 开源 SDK 层 | GitHub `waditu/tushare`，BSD-3-Clause 协议 | 412 commits、22 contributors、被 2.5k 项目依赖；**最后实质代码提交停在 2020-03（6 年前）**，696 个 issue 未关；2026-05 仍有 issue（#1867）指出 pip 新版包的源码并未在该 repo 完整开放、且把 `bs4` 锁死在旧版本与现代环境冲突——**SDK 层"开源但实质停更"** |
| 数据服务层 | Tushare Pro，`POST http://api.tushare.pro`，语言无关的 HTTP JSON API | 持续运营、接口持续增加；**靠 token + 积分制授权**；Python SDK 只是它的一层薄包装，不装包用 requests 也能直接调 |

关键认知：**本项目完全不需要安装那个停更的 SDK**。Pro 接口就是一个 POST：

```python
# 本机实测（2026-09-01 23:39，D:\Python\python.exe，项目现有 requests）
# POST http://api.tushare.pro  body={"api_name":"trade_cal","token":"",...}
# HTTP 200，返回 {"code":40101,"msg":"您上传Token！"} —— 通道可达、无 token 时是标准 JSON 报错
```

即：**网络通道本机通畅、调用方式与项目现有"直连东财 datacenter / 新浪接口 + 全局 http_client"的模式完全同构**，token 只是授权参数。这与第 13 轮"下载 akshare MIT 源码仅借鉴 URL、不引依赖"是同一个打法。

### 1.2 为什么不建议 `pip install tushare`（SDK 依赖重量）

开源 SDK 的运行依赖是 pandas / numpy / lxml / beautifulsoup4 / simplejson / requests 一整套（README 只写 pandas，setup.py 实际更多）。本项目**刻意**保持生产链路零重依赖（pandas 只允许出现在 `tools/` 维护脚本里，生产代码连 CSV 都用标准库读）。为一个 HTTP 薄包装把 pandas 全家桶装进 `D:\Python` 运行环境、还背上一个 6 年停更、有依赖锁冲突前科的包，**收益为零、风险为正**，不采纳。直接裸调 HTTP API 即可拿到完全相同的数据。

### 1.3 积分与频次（官方权限总表 doc_id=108 / 134，逐行核实）

| 积分档 | 获取方式 | 频次 | 能调什么 |
| --- | --- | --- | --- |
| 120 | 注册 100 + 完善资料 20（随手可得） | 较低 | 仅股票基础信息、股票**非复权**日线、`trade_cal`（**股票**日历）等；**所有期货接口都不够** |
| **2000** | 高校学生/教师认证可申请赠送；否则捐助约 200 元（1 元≈10 分）；写文章/提 bug 等社区贡献也可攒 | 200 次/分钟、单接口日上限 | 约 60% 接口，**全部期货日频接口都在这一档** |
| 5000 | 捐助约 500 元 | 500 次/分钟 | 约 90% 接口，含 `ft_limit`（盘前涨跌停+最低保证金率）、`opt_daily` 期权日线 |
| 积分外单独付费 | 期货分钟历史 2000 元/年；期货实时分钟 1000 元/月 | 500 次/分钟 | `ft_mins` / `rt_fut_min`，**不在任何积分档内** |

> 积分是"门槛"不是"消耗品"：达到后调用不扣分。规则官方注明"未来可能调整积分"。学生/教师赠送渠道以 Tushare 申请页面实际要求为准。

### 1.4 期货/期权接口权限清单（官方文档逐项核实）

| 接口 | 数据内容 | 历史起点 | 最低积分 | 单次上限 |
| --- | --- | --- | --- | --- |
| `fut_basic` | 期货合约列表（乘数、交易单位、上市日、最后交易日、交割日） | 全部历史 | 2000 | 10000 |
| `trade_cal`（exchange=各期货所） | **官方期货交易日历** | 1996 | 2000 | — |
| `fut_daily` | 具体合约日线：开高低收、**结算价 settle、昨结算、成交量/额、持仓量、持仓变化** | 1996 | 2000 | 2000 |
| `fut_mapping` | 主力/连续合约 ↔ 具体月份合约映射 | — | 2000 | 2000 |
| `fut_holding` | 每日成交持仓排名（**分会员明细**，上期所含 INE） | 2002 | 2000 | 2000 |
| `fut_wsr` | 仓单日报（**分仓库/厂库明细**） | 2006 | 2000 | 1000 |
| `fut_settle` | 结算参数：结算价、**交易所手续费率/手续费**、交割费、**买/卖套保保证金率** | 2012 | 2000 | — |
| `ft_limit` | **盘前每合约涨跌停价 + 最低交易保证金率 m_ratio** | 2005 | **5000** | 4000 |
| `index_daily` | 南华期货指数（板块/品种指数） | 10 年+ | 2000 | — |
| `opt_basic` / `opt_daily` | 期权合约列表 / 期权日线行情 | — | 2000 / **5000** | — |
| `ft_mins` / `rt_fut_min` | 期货 1/5/15/30/60m 历史 / 实时分钟 | — | **单独付费** | 8000 行 |

---

## 二、逐模块对照：能优化本项目的哪些地方（核心清单）

> 列：项目现状（截至第 16 轮）→ tushare 能给什么 → 优化价值 → 积分门槛 → 是否动现有链路。

### 2.1 高价值：精准命中本项目"自己承认的短板"

| # | 项目模块/现状痛点（摘要原文有据） | tushare 对应 | 优化价值 | 门槛 | 对现链路 |
| --- | --- | --- | --- | --- | --- |
| 1 | **交易所基准保证金率"无干净免费源、`futures_margins.csv` 的 exchange_margin 列留空不编造"**；现只有银河期货**公司档**（公司档=交易所档+加点，且临近交割/长假会上浮） | `ft_limit` 的 **m_ratio=交易所最低交易保证金率**、逐合约逐日；`fut_settle` 另给套保档 | **直接补掉这个明确留空字段**，且能拿到"临时提保/扩板"的逐日变化，组合回测 `portfolio.py` 的占用/风险度可做"交易所基准 vs 公司实际"双口径 | **5000** | 纯增量，不改主链路 |
| 2 | **东财库存/仓单 `RPT_FUTU_STOCKDATA` 免费窗口仅约 3 个月**，基本面因子只能算"近 3 个月滚动分位"（摘要反复声明这是口径短板，非年度分位） | `fut_wsr` 仓单 **2006 年起、分仓库明细、总量不限**，可一次性回填全历史 | **把库存分位从 3 个月窗口升级为年度/多年分位**，并能拆仓库/厂库、看注册仓单结构；这是基本面因子质量的实质性提升 | 2000 | 增强 `fundamental_data`，东财保留为盘中/最近源 |
| 3 | **交易日历靠"新浪 RB0 日 K 推断 + 东财备用 + 内置 2026 静态节假日表"拼凑，每年 12 月必须手动把次年休市区间补进 `STATIC_HOLIDAY_RANGES`，漏补就会在节前夜盘判断上出错** | `trade_cal`（期货所）官方日历，1996 起、定期更新 | **干掉每年一次的手动维护**，节前夜盘/调休判断直接以官方日历为准；现有三源可全部降级为兜底 | 2000 | 给 `trade_calendar.py` 增加一个最高优先级源，旧源全保留 |
| 4 | **日线回测用新浪主连"比例复权连续序列"，换月点是近似；且新浪日 K 没有结算价，`portfolio`/回测只能用收盘价盯市**（期货真实盯市按结算价） | `fut_daily` 给**具体合约**真实 OHLC + **settle 结算价** + 昨结算 + 持仓量；`fut_mapping` 给官方主力映射 | 可构建"具体合约拼接的主力连续"替代比例复权近似；回测/组合账户可改用**结算价盯市**（更贴近期货真实盈亏与追保逻辑）；`change1/change2`（对昨结算的涨跌）也与交易所口径一致 | 2000 | 只服务回测/研究侧（backtest/portfolio），**不碰盘中监控** |
| 5 | **主力合约靠 `contracts.py` 运行时拼月份代码+行情探测**（要先探测完成才能采具体合约，第 14 轮前 1m 甚至被这一步卡过） | `fut_mapping` 官方主力/连续映射，一次拉全市场 | 主力切换有了权威依据，可与现有探测结果交叉校验、探测失败时兜底；冷启动不必等探测 | 2000 | 增强 `contracts`，现有探测保留 |
| 6 | **精确锁板用 `config.FUTURES_LIMIT_MOVE` 硬编码的 64 品种"常态档"**，无法反映某日某合约临时扩板/提保；锁板价=前收×常态幅度是模型近似 | `ft_limit` 每交易日**盘前直接给每个合约的 up_limit/down_limit 真实价格** | 日内回测 `intraday_backtest` 的锁板拦截可从"常态幅度估算"升级为"当日真实板价"，并自动覆盖临时扩板；这是回测保真度提升 | **5000** | 只增强回测，不影响运行 |

### 2.2 中价值：锦上添花 / 交叉校验

| # | 项目现状 | tushare 对应 | 价值 | 门槛 |
| --- | --- | --- | --- | --- |
| 7 | 龙虎榜因子只取东财 TYPE=2 **最新日"今合计/上合计"两行**（净多率+一日边际） | `fut_holding` **2002 年起分会员、分多空/成交的完整排名明细** | 龙虎榜因子可做时序分位、头部会员集中度、多空增减趋势，而不只是两日对比 | 2000 |
| 8 | 合约乘数/交易单位等元数据**硬编码**在 `build_fee_table`/`build_margin_table`/config 多处，新品种上市要手动加（JD 鸡蛋口径差异还得白名单特判） | `fut_basic` 官方合约要素（乘数、单位、上市/最后交易日） | 元数据单一可信源、自动同步新品种；最后交易日可服务换月/交割提醒 | 2000 |
| 9 | 真实手续费只到 **2026-08-28 银河账户费率表**，公司调整要手动重跑工具 | `fut_settle` 的 **交易所**手续费率/手续费（2012 起逐日） | 不是替代公司费率（公司档=交易所+加收，实盘仍以银河为准），但可做**基准交叉校验**、并在交易所调费时及时提示重跑 `build_fee_table` | 2000 |
| 10 | 无板块/品种指数对标 | `index_daily` 南华期货指数（10 年+） | 可加"品种 vs 板块指数"的相对强弱，属于 P2 板块强弱排行的一块新拼图（对应改进清单条目⑮） | 2000 |

### 2.3 帮不上 / 不该换的（避免误用，同样重要）

| 模块 | 项目现状 | 为什么 tushare 不替代 |
| --- | --- | --- |
| **盘中实时行情** | 新浪 `hq.sinajs` 主 + 东财 push2 备，每轮 64 品种，10s 原油线程 | Pro 期货行情是**盘后数据**（`fut_daily` 每交易日 21:05 后更新），**没有免费实时**；实时分钟 `rt_fut_min` 要 1000 元/月。实时主链路绝不能换，换了监控就瞎了 |
| **分钟 K** | 新浪主连 1/5/15/30/60m 各 1023 根 + `minute_bars` 常驻自采（已 32.7 万根且持续增长） | `ft_mins` 历史包 **2000 元/年**、实时包 1000 元/月；项目分钟数据免费且在持续积累，**为回溯 2026-08-26 之前的历史分钟而买年包，是唯一可能的付费场景**，但这是花钱的事、应单独决策，不属于"开源免费带入" |
| **新闻快讯/情绪** | 东财7x24/新浪滚动/华尔街见闻/同花顺/新浪7x24/金十 共 6 源，当晚复测全健康 | Pro 资讯类以股票财经媒体为主，无期货专项实时快讯优势；第 14 轮已确立"新闻侧不增不减"，不动 |
| **期权实时 T 型链 / IV 曲面 / 希腊字母** | 新浪 T 链实时持仓 PCR + OpenVlab 实时 IV + 自研 Black-76 反推/微笑/曲面/日历价差 | `opt_basic` 只是合约清单（可做元数据备用），`opt_daily` 是**日线且要 5000 分**，没有实时盘口/买卖价/持仓结构，撑不起 IV 曲面，**不替代现有期权链路** |
| **外盘原油/全球指数/宏观** | 新浪全球行情 `hf_`/`gb_`/`DINIW`，原油 10s 急动检测 | Pro 对应为外汇/海外日线，非实时、不对口，不替代 |
| **技术指标/回测引擎/组合账户/告警/看板** | 全部自研、零依赖、合成断言锁定 | tushare 只是数据，不含这些；无影响也无可替代 |

---

## 三、"是否会影响使用"——三种引入方式的影响评估（直接回答）

### 方案 A（推荐）：零依赖可选适配层，只做增强源

仿照项目已有的 `tdx_bars.py`（pytdx 延迟导入、probe 不亮就零成本跳过）和 `option_chain.OptionChainCache`（TTL 缓存、单品种失败不阻断）范式，新增一个 `tushare_client.py`：

- **不 `pip install tushare`**，用现有 `http_client.http` 直接 POST `api.tushare.pro`（本机已实测通道可达）；
- token 从环境变量读（沿用 `alerts.py` 的 `FUTURES_MONITOR_WEBHOOK` 先例，如 `TUSHARE_TOKEN`），**不写死、不提交**；
- 无 token / 积分不足（识别返回 code）/ 断网 / 限频 → 全部软降级返回 None/[]，记一条日志，**现有任何功能不受影响**；
- tushare 数据一律落 SQLite 新表或增强既有表/CSV，定位为"第二信源 + 历史回填 + 交叉校验"，**现有新浪/东财源全部保留为主**；
- 日常只在盘后做日频增量（按 `trade_date` 一次拉全市场，避免逐品种），2000 分档 200 次/分钟绰绰有余。

**影响结论：没配 token 时程序行为与现在 100% 一致；配了 token 只多不少、只增强不替换；任何 tushare 侧故障都被软降级隔离，不会传染主监控。许可证 BSD-3 且我们不拷贝 SDK 代码、只调公开 HTTP API，无合规问题。**

### 方案 B（不推荐）：把 tushare SDK 装进生产依赖

影响：pandas/numpy/lxml/beautifulsoup4/simplejson 全部进入生产运行链路，破坏项目坚持了 16 轮的"生产零重依赖、pandas 仅限 tools"架构；`D:\Python` 环境要装一串包；SDK 6 年停更、有依赖锁死冲突的现存 issue（#1867）。**用重量级依赖换一个 HTTP 薄包装，纯亏，不采纳。**

### 方案 C（错误、明确否决）：用 tushare 替换新浪/东财主源

Pro 是盘后数据、分钟线收费、无免费实时、无期货快讯。替换会**直接断掉盘中实时监控、分钟自采、新闻情绪三条主腿**，与第 14 轮"通达信不能全量替换东财"是同一类错误，且更严重。**绝不这么做。**

### 3.1 残余风险与约束（即使走方案 A 也要认）

1. **账号与积分是硬前提**：至少 2000 分（学生/教师认证可申请，否则约 200 元）；想补交易所保证金/真实板价（#1/#6）要 5000 分（约 500 元）。没积分之前方案 A 等于空转，不会有任何增强。
2. **它是商业社区服务，不是 SLA 服务**：官方文档自注"积分未来可能调整"，接口/价格/权限可能变 → 决定了它只能是增强层，**永远不能成为唯一源**（与项目"不把单点压在一家"的一贯原则一致）。
3. **只增强日频/盘后/历史，不增强盘中**：对 9:00-次日 02:30 的实时轮动、原油急动插队、实时告警没有任何帮助，别期待错地方。
4. **频次纪律**：按交易日批量拉全市场（单次 2000~10000 行），本地缓存 + 增量，不逐品种高频请求，避免触发限频。
5. **口径核对**：tushare 交易所档与银河公司档不同（公司档更高），实盘保证金/手续费仍以银河结算单为准，tushare 数据用于基准、回测与交叉校验，报告措辞需保持项目一贯的"诚实声明"风格。
6. **token 安全**：只走环境变量/本地不入库配置（正好契合 P2 的"配置外置 config.json/.env"），不进 git、不进日志。

---

## 四、如果落地：推荐顺序（沿用项目"确定不更差才采纳"铁律）

> 前提：先注册并拿到 ≥2000 分（若有高校学生/教师身份走认证通道；否则评估 200 元捐助是否值得）。每一步都保留现源为主、tushare 为增强，配零网络合成断言 + 一条真实小链路验证（沿用项目既有验证范式），验不过就不并入。

| 批次 | 内容 | 用到接口 | 积分 | 对应项目收益 |
| --- | --- | --- | --- | --- |
| T1 | 官方期货交易日历作为最高优先级源，三旧源降为兜底；删掉"每年手动补静态表"的维护负担 | `trade_cal` | 2000 | 痛点 #3 |
| T2 | 仓单全历史一次性回填 + 日频增量，库存分位升级为年度/多年分位（东财 3 个月窗口保留为最近值源） | `fut_wsr` | 2000 | 痛点 #2（基本面因子质变） |
| T3 | 合约元数据（乘数/单位/上市/最后交易日）同步，收敛多处硬编码；主力映射交叉校验/兜底 | `fut_basic` `fut_mapping` | 2000 | #5 #8 |
| T4 | 回测侧升级：具体合约拼接主力连续 + 结算价盯市，替代比例复权/收盘盯市近似（只动 backtest/portfolio，可开关对比） | `fut_daily` `fut_mapping` | 2000 | 痛点 #4 |
| T5（需 5000 分） | 交易所基准保证金率补 `exchange_margin` 留空列；盘前真实板价替代常态档硬编码，回测锁板保真、识别临时提保扩板 | `ft_limit` `fut_settle` | 5000 | 痛点 #1 #6（最对口但门槛更高） |
| T6（可选） | 龙虎榜时序分位、交易所手续费基准交叉校验、南华指数板块强弱 | `fut_holding` `fut_settle` `index_daily` | 2000 | #7 #9 #10 |
| 不做 | 分钟线付费包、实时行情替代、期权链替代、新闻替代 | — | — | 见 2.3 |

**最小可见收益路径**：只做到 T1+T2（2000 分档内），就能消掉"每年手动维护交易日历"和"库存只有 3 个月分位"两个项目白纸黑字记下的短板，且全程不碰实时主链路、不新增强制依赖——这是性价比最高的一刀。T5（交易所保证金/真实板价）价值同样很实，但卡在 5000 分，值得但要单独决定是否投入。

---

## 五、一页纸总览

- **能不能带进来**：能，但带的是 **Pro 的 HTTP 数据**（不是停更的开源 SDK），且应做成**零依赖、无 token 自动降级的可选增强层**。
- **优化什么**：①官方交易日历免维护；②仓单全历史把库存分位从 3 个月拉到多年；③交易所基准保证金率补留空字段（5000 分）；④具体合约日线+结算价+主力映射，让回测脱离主连比例复权近似；⑤盘前真实涨跌停价让锁板/提保建模更准（5000 分）；⑥龙虎榜全历史、合约元数据、手续费基准交叉校验、南华指数。
- **优化不了什么**：实时行情、分钟 K（收费）、新闻快讯、期权实时链/IV——这些现有免费自研链路反而更合适，不动。
- **是否影响使用**：走方案 A（推荐）对现有程序**零影响**（无 token 完全等价于现在，有 token 只增强）；走方案 B（装 SDK）会污染零依赖架构，不推荐；走方案 C（替换主源）会断实时监控，否决。
- **成本**：2000 分（学生/教师可申请，否则约 200 元）解锁全部日频期货接口；5000 分（约 500 元）才能拿交易所保证金/真实板价；分钟线另付 2000 元/年，不建议为之外购、继续靠 minute_bars 自采。
- **定位**：tushare 是本项目"日频盘后/历史深度/官方基准参数"的**第二信源与回测增强源**，不是、也不应成为实时监控的主源。

---

### 附：主要事实来源

- GitHub 仓库：https://github.com/waditu/tushare （BSD-3-Clause；提交历史/Issues/#1867）
- 官方权限总表：https://www.tushare.pro/document/1?doc_id=108 ；期货频次 https://tushare.pro/document/2?doc_id=134
- 期货日线 fut_daily：https://tushare.pro/document/2?doc_id=138
- 仓单 fut_wsr：https://tushare.pro/document/2?doc_id=140 ；结算参数 fut_settle：https://tushare.pro/document/2?doc_id=141
- 持仓排名 fut_holding：https://tushare.pro/document/2?doc_id=139 ；主力映射 fut_mapping：https://tushare.pro/document/2?doc_id=189
- 盘前涨跌停/保证金 ft_limit：https://tushare.pro/document/2?doc_id=368
- 分钟数据包定价：https://tushare.pro/document/1?doc_id=290 ；ft_mins：https://tushare.pro/wctapi/documents/313.md
- 积分获取：https://tushare.pro/document/1?doc_id=13.
- 本机一手实测：2026-09-01 23:39，`POST http://api.tushare.pro` 无 token 返回 HTTP 200 / `code 40101 您上传Token`，证明通道与裸 HTTP 调用方式可行。
