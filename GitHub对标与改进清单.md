# GitHub 同类项目对标 · 不足与缺失清单（2026-09-01）

> 方法：在 GitHub 用「期货/期货监控/期货量化/期权/futures monitor/financial news sentiment/trading agent LLM/
> futures dashboard/基差库存/option greeks」等 20+ 关键词检索，精读 15 个高相关项目 README，
> 再逐文件核对本项目全部 16 个 py（约 3200 行）后形成。每条均标注了代码事实与对标项目。

## 〇、对标参考项目（按相关度）

| 项目 | 星数 | 对本项目的参考价值 |
| --- | --- | --- |
| vnpy/vnpy | 45k | 事件驱动、CTP、回测、风控、组合管理的行业标杆 |
| TauricResearch/TradingAgents | 102k | LLM 多智能体（分析师辩论→风控→CIO）金融决策框架 |
| hsliuping/TradingAgents-CN | 31k | 上述框架中文版，A 股/商品适配 |
| bbfamily/abu | 18.5k | 股票/期权/期货/币一体化量化（回测+机器学习） |
| yutiansut/QUANTAXIS | 11k | 数据/回测/模拟/可视化/多账户本地方案 |
| shinnytech/tqsdk-python | 5k | 期货实时行情/历史数据/实盘，数据层可参考 |
| akfamily/akshare、Micro-sheep/efinance | — | 免费金融数据库：基差、仓单、龙虎榜、交易日历、分钟K线都有现成接口 |
| zhouxinhao19/TradingAgent-Future | 新 | **最直接对标**：6 所 80+ 品种，技术面/基本面/持仓/基差/库存/期限结构/新闻情绪多维分析，数据源降级链 AKShare→Tushare→BaoStock |
| culi123/QtGuiSurveillanceMulti | 54 | 基于 CTP+Qt 的期货实时监控（多账户盈亏汇总） |
| LC9999216/a-share-stock-monitoring | — | 监控+预警条件+**飞书推送**+冷却管理器，架构图清晰 |
| DrifterXXX/options-arbitrage-system (StatusHub) | — | 期货期权信号仪表盘：多周期共振、N 形结构、IV 分位、策略 7 维评分、Flask+Chart.js |
| chjm-ai/stock-monitor-skill | 46 | 涨跌幅/量能异动/金叉死叉/RSI/缺口/动态止盈预警 |
| quantskills/skill-commodity-carry-cta | — | 商品横截面因子：carry/基差动量/库存/时序+横截面动量、主连接续回测 |
| qusong0627/QuantMind | 1.1k | AI 原生平台：Parquet+DuckDB 存储、RankIC 回填、数据漂移告警、7x24 舆情 |
| mpquant/MyTT | 2.8k | 单文件实现通达信/文华全部常见技术指标（MACD/RSI/KDJ/BOLL） |
| Homalos/Homalos | 20 | 事件驱动期货系统：FastAPI+Vue3 Web 管理界面、通知中心、任务调度 |

---

## 一、P0 现有程序的实际问题（✅ 第6轮已全部修复，2026-08-31）

> 修复总览：6 条 P0 已全部落地并通过验证（84 项调度/日历断言 + 5 项日切归属 + 23 项看板 JS 对齐 + 一次完整 --once 端到端）。
> 新增 `http_client.py`（全局 Session 连接池）、`trade_calendar.py`（交易日历）两个模块；详见每条末尾「→ 第6轮修复」。

### 1. 夜盘时段一刀切到 23:00，漏掉最长 3.5 小时真实交易【实质 bug】✅ 已修
- 代码事实：`config.SESSIONS = [(9,0,11,30),(13,30,15,0),(21,0,23,0)]`，全部品种夜盘统一 23:00 结束。
- 交易所实际（上期所官网交易时间表）：
  - **21:00–次日 02:30**：黄金、白银、原油（及期权）
  - **21:00–次日 01:00**：铜、铝、锌、铅、镍、锡、不锈钢、国际铜、氧化铝（及期权）
  - 21:00–23:00：螺纹/热卷/橡胶/燃料油/沥青/纸浆及大商所、郑商所、广期所全部品种
- 后果：23:00 后黄金白银原油有色仍在开盘，程序却判为「非交易时段」——
  信号不进 latest_report.txt/signals.csv（进了 offhours 两件套）、输出的是「预测走向·开盘后校验」
  （但这些品种当时正在开盘）、且 **23:00 就生成当日复盘，夜盘还没走完，复盘数据不完整**。
- 修法：SESSIONS 按板块/品种设不同夜盘结束时间（23:00 / 01:00 / 02:30）；
  「是否交易时段」改为「该品种是否在交易」；每日复盘改到全部夜盘结束（02:30 后）或日盘 15:00 收盘后。
- → **第6轮修复**：`config` 夜盘按品种分三档（23:00 / 次日01:00 / 次日02:30），无夜盘品种（鸡蛋/生猪/广期所等）单独标记；`utils` 新增品种级 `is_variety_trading`，全局 `is_trading_time` 按最晚 02:30 判定；调度全部改用真实 datetime（正确处理跨零点、周五夜盘到周六凌晨、周一凌晨无夜盘）；复盘改由 `trade_owner_date/review_is_due` 在「该交易日全部夜盘结束后」才生成；日切 `daily_rollover` 改按交易日归属，跨零点夜盘块不再被误清。

### 2. start_monitor.bat 的解释器可能是错的 ✅ 已修
- 代码事实：bat 里写的是 `python main.py`；但《上下文摘要》明确记录「命令行默认 python 是无 requests 的沙箱版，
  验证一律用 D:\Python\python.exe」。双击 bat 很可能直接 ModuleNotFoundError: requests。
- 修法：bat 改为 `D:\Python\python.exe main.py %*`（或先 call 激活对应环境）。
- → **第6轮修复**：bat 固定 `D:\Python\python.exe`（where 校验，缺失才回退 PATH 中的 python），`%*` 透传全部参数；正常退出/Ctrl+C/`--once`（退出码0）不再重启，仅非0退出（含看门狗 os._exit(3)）等15秒后 `:retry` 重启，重启记录写 logs/restart.log。

### 3. 交易日历只认周末，法定节假日/调休必误判 ✅ 已修
- 代码事实：`utils.is_trading_time` 仅 `weekday()>=5` 判休市。
- 后果：国庆/春节等工作日休市会被当交易日空转出无效信号；调休上班的周六周日反被当休市。
- 修法：接入交易日历（akshare `tool_trade_date_hist_sina()` 年度缓存到本地 json，或手动维护年度日历表）。
- → **第6轮修复**：新增 `trade_calendar.py`（不依赖 akshare/pandas）。**注：原拟用的新浪 klc_td_sh.txt 明文日历实测已改为加密密文失效**，故采用「动态+静态」双保险——动态主源为新浪螺纹主连 RB0 日K（与监控同市场、程序数据层长期在用），备源东财上证指数日K，日期即交易日并缓存 cache/trade_dates.txt；静态内置证监会发布的 2026 全年法定休市区间（覆盖日K拿不到的未来日期）；断网时退化为「静态表+周末规则」。提供 is_trade_day/next_trade_day/prev_trade_day/has_night_session（节假日前一晚自动判无夜盘）；调休补班周末仍判休市。

### 4. 日志无轮转，常驻数月后文件无限膨胀 ✅ 已修
- 代码事实：`utils.setup_environment` 用 `logging.FileHandler` 单文件写 logs/monitor.log，无 maxBytes/backupCount。
- 修法：换 `RotatingFileHandler`（如 10MB×5 份）或 `TimedRotatingFileHandler`（按天保留 30 天）。
- → **第6轮修复**：`utils.setup_environment` 改用 `RotatingFileHandler`（单文件 2MB、保留 5 份，config.LOG_MAX_BYTES/BACKUP_COUNT 可调），并改为显式、幂等挂载到 root logger（不依赖 basicConfig「仅首次生效」、重复调用不累积句柄）。

### 5. HTTP 连接不复用，每轮新建大量 TCP/TLS 连接 ✅ 已修
- 代码事实：全项目无 `requests.Session`（已逐文件核查），64 品种分批行情、4 个新闻源、9 项全球行情、
  机构/期权接口每次都 `requests.get` 新建连接，慢且易触发限流。
- 修法：各数据模块用模块级 `requests.Session()` + `HTTPAdapter(pool_connections, pool_maxsize)` 复用连接。
- → **第6轮修复**：新增 `http_client.py`，提供进程级全局 `Session`（HTTPAdapter 连接池、keep-alive、统一超时/重试开关）；futures_data/oil_data/sina_news/webdata/web_scan/browser_reader 六个数据层全部改走该 Session，已 grep 确认无残留裸 `requests.get/post`。

### 6. 无看门狗/崩溃自重启 ✅ 已修
- 代码事实：主循环 run_cycle 有 try，但启动阶段异常、磁盘写满、后台线程函数外异常都会让常驻程序永久停摆，
  且没有任何「程序已死」的告警。
- 修法：bat 加 `:retry` 循环重启（崩溃后等 10s 重拉）；或注册 Windows 任务计划「失败后重启」；
  更稳妥是每轮写心跳时间戳，独立看门狗脚本检测超时后报警/重启。
- → **第6轮修复**：双保险——① 每轮 run_cycle（含紧急轮）开头写 logs/heartbeat.txt 心跳时间戳，main 内独立看门狗线程发现 600 秒无心跳即 `os._exit(3)`（非0退出，能被 bat 捕获，规避线程被 C 扩展卡死时 sys.exit 无效的问题）；② bat `:retry` 循环在非0退出后等 15 秒自动重启并记 restart.log。

---

## 二、P1 重要功能缺失（对标后最值得补）

### 7. 没有任何主动告警/推送 —— 监控程序的最大短板 ✅ 已完成（P1第二批）
- **落地（第7轮，2026-08-31）**：新增 `alerts.py`，零新增依赖。Windows 本机声音用标准库 `winsound`，Webhook 复用 `http_client.http` 全局连接池；支持飞书/钉钉/企业微信群机器人/Server酱/通用 JSON，URL 经环境变量 `FUTURES_MONITOR_WEBHOOK` 配置并自动识别类型。声音和推送均在守护线程执行，失败只记日志，不阻塞主循环。触发覆盖紧急轮动、期货跨4分/6.5强档、多空翻转、六项全过的期权策略；紧急冷却300秒、品种信号冷却1800秒；启动第一轮只报强信号，避免64品种基线刷屏。
- 现状：紧急轮动、强信号只能靠人盯 html 看板，人离开电脑就错过（原油急动插队轮的价值大打折扣）。
- 对标：LC9999216 的核心就是触发条件→冷却管理→飞书 webhook；Homalos 内置「通知中心」。
- 建议（由易到难，可叠加）：
  1. 本机：winsound 声音 + 任务栏闪烁/弹窗（零依赖，紧急轮动必响）；
  2. 手机：飞书/钉钉/企业微信群机器人 webhook、Server 酱（推微信）、邮件 SMTP，任选其一（一个 post 请求即可）；
  3. 触发条件复用现有成果：综合分跨档（如越过 ±4/±6.5）、紧急轮动、期权策略新出现、价格/涨跌幅破阈值；
     冷却机制可直接复用 OIL_JUMP_COOLDOWN 的同款写法，避免连发轰炸。

### 8. 没有结构化数据库，无法统计、无法回看 ✅ 已完成（P1第二批）
- **落地（第7轮，2026-08-31）**：新增 `storage.py`，使用 Python 标准库 `sqlite3`（WAL + busy_timeout + RLock，主循环和后台新闻线程可共用），建立 `quotes/signals/news/options/signal_outcomes` 五张表及索引。每轮结构化保存64品种行情、期货综合分与因子拆分、新闻权重/可信度、单腿期权与组合策略；`data/monitor.db` 为数据库文件，行情/新闻默认保留180天，信号与结果长期保留。
- 现状：行情只在内存 deque（重启即丢），落盘只有 txt/csv/jsonl 文本，想查「螺纹上周三轮次分数序列」无法程序化检索。
- 对标：StatusHub/Homalos/QuantMind 均用 SQLite（或 DuckDB/Parquet）。
- 建议：Python 标准库自带 sqlite3，零新增依赖，建 4 张表：
  quotes(时间,品种,价格,涨跌,量,仓)、signals(时间,轮次,品种,综合分,各因子分,方向,止损目标)、
  news(时间,源,内容,权重,可信度)、options(时间,品种,策略,腿,IV,Greeks)。
  这是第 9、10、17 条的共同地基。

### 9. 没有信号效果追踪，建议对错从不复盘 ✅ 已完成（P1第二批）
- **落地（第7轮，2026-08-31）**：`|score|>=2` 的期货信号自动建立30分钟、2小时、约24小时三个待评估任务；同一品种/方向/分档仍有 pending 任务时不重复建单。到期后用后续行情计算“信号方向×后续涨跌幅”，状态分为 hit/miss/flat/expired。`reports/signal_tracking.txt` 与看板新页签按周期、分档、多空方向输出样本数、胜率、平均方向收益和最近结果；每日复盘新增“信号效果追踪”章节。休市或接口旧价导致到期仍无新成交价时，最多等待6小时后记 expired，不硬算成失败。
- 现状：daily_review 只做「首轮 vs 末轮分数」文字对比，从不验证「当时建议做多的品种后来到底涨没涨」。
- 建议：信号落库后，自动回看每条信号 30 分钟/2 小时/次日的实际涨跌，复盘里输出：
  各分数档胜率、平均盈亏比、多空分别命中率、失效最多的板块。比完整回测轻量，但能让权重调整有依据，
  形成「出信号→跟踪→统计→调参」闭环（QuantMind 的 RankIC 回填是同一思路）。

### 10. 没有回测，全部权重和阈值都是拍脑袋定的 ✅ 最小版+真实化（P1第三/四批）
- **落地（第8/9/10轮，2026-09-01）**：新增 `backtest.py`，零新增运行依赖。按请求窗口截取新浪主连日线，对疑似换月跳空置0并比例后复权；回放“日线动量+技术共振”纯技术信号，输出非重叠交易的胜率、平均收益、盈亏比、累计/年化、最大回撤、夏普，并另列1/5/20日重叠信号衰减。第9轮补齐滑点万1（CLI可调/可关闭）、收盘贴最高/最低且涨跌幅≥7%的疑似锁涨跌停过滤（入场跳过、离场顺延）、`backtest_trades.csv` 逐笔毛/净收益，以及持有5/10/20日×入场阈值1.5/2.0/2.5的3×3参数稳定性扫描。第10轮接入用户银河期货2026-08-28真实手续费表，转换为`data/futures_fees.csv`，64品种全覆盖；按金额费率与按手数固定金额相加，用合约乘数把每手费用折算为收益率，日线多日持仓按开仓+平仓计费，交易CSV拆分手续费/滑点/每手人民币费用，缺失时回退统一比例费率。历史新闻/机构/实时量仓/分钟K线仍无法逐日复原，平今优惠触发、逐笔盘口成交、组合资金权重曲线仍待更完整回测引擎。
- 现状：综合分公式里的 ×0.28、×1.6、tanh 系数、2/4/6.5 分级、期权 1.35/1.5 等参数从未经历史验证，
  无法回答「这套规则历史上胜率多少、最大回撤多少」。
- 对标：vnpy/abu/qf-lib/skill-commodity-carry-cta 都把回测当核心。
- 建议：做最小可用回测——用历史日线（新浪/akshare 可拉数年）回放技术因子+新闻因子部分，
  输出分档胜率、盈亏比、年化、最大回撤、夏普；注意商品期货主连必须做**合约内算收益再链接（比例后复权）**，
  直接拼主连价格会注入虚假收益（carry-cta 特别强调的坑）。

### 11. 基本面维度缺失：量仓解析了没用、基差/库存/期限结构/龙虎榜全缺 ✅ 已完成（量仓第7轮；库存/仓单/龙虎榜/期限carry/基差第13轮 WP-C）
- **量仓部分落地（第7轮，2026-08-31）**：新增 `flow_tracker.py`，按主力连续代码保存相邻轮次价格、日累计成交量、持仓量快照，识别“增仓上行/增仓下行/减仓上行（空头回补）/减仓下行（多头减仓）”和放量/缩量；成交量先转相邻轮次增量并按时间归一，再与近几轮平均增量比较，新交易日日累计量归零不会误判缩量。因子裁剪在 ±1.2，已接入 `analyzer.py` 的“量仓资金”和重点品种明细。
- **基本面其余部分落地（第13轮 WP-C，2026-09-01）**：新增 `fundamental_data.py`（东财数据中心直连，零新增依赖）+`fundamental_factors.py`（纯函数）。①库存/仓单：东财 RPT_FUTU_STOCKDATA 注册仓单时序算近约3个月滚动分位（低库存偏多）+周环比去化（去库偏多），样本<15不计算；②龙虎榜：东财 RPT_FUTU_DAILYPOSITION TYPE=2 取主力合约前20席会员多空合计，净多率60%+较昨日边际40%；③期限carry：复用第11轮 contracts.term_structure 的年化展期收益率（Back近高远低=现货紧偏多，零新增请求）；④基差：生意社当日表现货升水偏多，该站有JS-cookie反爬、抓不到即软降级。权重.40/.30/.20/.10，缺项按可得权重重归一、四子项全缺不计，加权后×1.5作为“基本面”因子进综合分；main 日频后台线程+分析前8线程并发预取，storage 新增 fundamentals 第7张表，report 加基本面速览多空榜。**实测做不通的源（勿重复踩坑）**：SHFE官网仓单20260824后全404、DCE官网持续412 WAF、CZCE是xlsx、东财无免费基差时序，故库存/仓单/龙虎统一走东财datacenter、基差只留生意社软降级预留位；生鲜/指数品种（AP/JD/PK/EC/LG/SC）无标准仓单自动只靠carry+龙虎榜。
- 现状：
  - `futures_data._parse_quote` 已解析成交量 f[14]、持仓量 f[13]，但 analyzer **完全没用**
    （量价背离、增仓上行/减仓下行、持仓排名变化都没有）；
  - cache 里存了 em_cangdan.html（仓单页）但没有任何代码读取分析（已逐文件核查）；
  - 无基差、月差/期限结构（近远月、contango/backwardation）、库存仓单趋势、会员持仓龙虎榜。
- 对标：TradingAgent-Future 把「持仓、基差、库存、期限结构」列为与技术面/情绪并列的核心维度；
  carry-cta 用 carry/基差动量/库存做横截面轮动；这些是商品期货与股票最大的不同，是基本面派的主战场。
- 建议：akshare 有现成免费接口（现货价、仓单日报、大商所/郑商所会员持仓排名、期限结构），
  先做零成本的两条：① 成交量/持仓量异动因子（数据已有）；② 仓单/库存周变化方向因子。

### 12. 技术指标太薄，无多周期共振 ✅ 已完成日线+30/60分钟版（P1第三/四批）
- **落地（第8/9轮，2026-08-31）**：`futures_data.py` 新增 RSI14、MACD(12,26,9)、KDJ(9,3,3)、BOLL(20,2)、MA60；日线短/中/长三票共振形成±1.2的“技术共振”。第9轮实测并接入新浪 `getFewMinLine(type=30)`：30分钟看短/中周期，两根连续30m本地聚合60m看 中/长周期，形成裁剪到±0.4的“分钟共振”因子；分钟级与综合方向背离时进入风险提示。KlineCache 10分钟缓存并在每轮前6线程并发预热，失败自动降级为0且不阻断报告。
- 现状：只有 MA5/10/20、ATR14、HV20/60、5/20 日收益；「盘中动量」只是 10/30 分钟价差。
- 缺：MACD、RSI、KDJ、布林带、均线多空排列、放量突破、跳空缺口；无分钟 K 线，无多周期。
- 对标：StatusHub 做 15m/30m/60m/2h/4h/日多周期信号矩阵 + **跨周期共振**（多周期同向才出强信号，
  是过滤假突破的有效手段）；stock-monitor-skill/MyTT 提供全部常见指标（MyTT 单文件可直接搬）。
- 建议：先加 RSI/MACD/KDJ + 量能异动（改动集中在 futures_data.compute_indicators），
  再拉 30/60 分钟 K 线做「日线定方向、分钟线找入场」的共振过滤。

### 13. 新闻情绪是硬关键词匹配，存在误判且维护成本高 ✅ 轻量否定/转折规则已完成（P1第三批）
- **落地（第8轮，2026-08-31）**：`factors.py` 在每个关键词命中点±16字窗口内识别“并未/没有/未能/落空/证伪/取消/推迟/不及预期”等转折词，命中则反转该关键词极性；正则规则内部已编码方向的“低于预期”等不做二次反转。LLM事件抽取的重量路线仍保留为后续。
- 现状：约 70 词 + 15 正则 + 上下文闸门，做得算细，但无法处理否定/转折/复杂因果
  （如「减产预期落空」「并未如预期减产」会被误判为利多），新事件要手工加词。
- 两条路线：
  - 轻量：加否定/反转规则（窗口内出现「落空/证伪/不及预期/并未/未能」则极性反转或清零），成本极低；
  - 重量：接 LLM 做事件抽取与利多/利空/强度判定（本地 Ollama 或云端 API），
    TradingAgents-CN(31k)/TradingAgents(102k) 代表的就是这个方向；可先只对「触发紧急轮动的消息」
    多调一次 LLM 复核，过滤关键词误触发。

### 14. 期权分析深度可加强 ✅ 已完成（第8/9轮IV分位/锥/组合Greeks；第11轮持仓PCR+完整T链；第12轮多到期日IV曲面+日历价差）
- **落地（第8/9轮，2026-08-31）**：OpenVlab页面解析真实平值IV、隐波百分位、偏度和偏度百分位；页面缺失时用滚动HV分位和10/20/40/60日波动率锥代理。单腿与组合策略的隐波检查同时使用IV/HV和IV分位；组合输出Δ/Γ/Vega/Θ并写入SQLite。第9轮新增买入蝶式(1:2:1)、看涨/看跌1:2比率价差、备兑看涨、保护性认沽；腿结构支持数量与期货腿，输出保守保证金点值估算，比率结构明确标注裸露尾部无限风险。当前免费页面仍无完整期权链，PCR只保留 `option_chain.pcr` 接入位、缺数据不编造；日历价差需要两个到期日IV曲面，交易所精确保证金和组合优惠仍待完整合约/账户数据。
- **落地（第11轮，2026-09-01）**：新增 `option_chain.py` 直连新浪商品期权T型报价（五大所57个期权品种实测、零新增依赖），持仓量PCR/ATM/最大持仓行权价/情绪档/30日分位全部落地，快照落 option_chains 表；成交量PCR缺逐腿成交量，留待交易所期权日行情备用源；INE低硫燃料油LU新浪无源自动降级。
- **落地（第12轮，2026-09-01）**：新增 `iv_surface.py`，对最近3个真实挂牌月份（剩余≥30天）的T链逐腿 Black-76 二分反推IV，补齐原缺的**多到期日IV曲面**——ATM IV期限结构(Contango/Backwardation)、25Δ风险反转skew(RR)与蝶式凸性(Fly)、5档moneyness×多月份曲面矩阵（缺档不插值）；IV取数改三级优先级（OpenVlab页面真实 > T链反推 > HV估计）并做页面vs链反推交叉校验。针对免费T链脏报价（单边挂单/宽价差/临近到期误差）落地五层清洗（窄价差质量分级/IV上限/ATM须高质量腿/30天门槛/离群带剔除）。**策略库补齐日历价差**：卖近买远 long calendar（正Vega、风险=净支出）与买近卖远反向日历（负Vega、远月裸露诚实标理论风险无上限），|近-远ATM IV差|≥3vol且静态盈亏空间为正才推荐；实测静态最优为负的反向日历会被正确拦截。交易所官方IV(option_vol_*)交叉校验留待第13轮WP-C；交易所精确保证金/组合优惠仍待完整账户数据。
- 现状：Black-76 + 希腊字母 + 六项检查 + 5 种组合策略，个人项目里已属少见；但：
  - IV 用 HV×1.05 估计，「贵不贵」只看 IV/HV≤1.35 的固定倍数，**没有 IV 历史分位/波动率锥**
    （同样 1.3 倍，在 IV 历史 5% 分位和 95% 分位意义完全不同）；
  - ~~缺波动率倾斜 skew、认沽认购比 PCR~~ **第11/12轮已补25Δ RR/Fly skew 与持仓量PCR**；组合层 Greeks 敞口汇总、卖方精确保证金占用仍待（当前为点值估算）；
  - ~~策略库可补日历价差、蝶式、比率价差、备兑/保护性认沽~~ **均已补齐（蝶式/比率/备兑/保护第9轮，日历/反向日历第12轮）**。
- 对标：StatusHub 的 IV 分位直方图 + 策略 7 维评分（Theta/Vega/历史胜率/幅度/效率/Delta/IV）。

### 15. 缺板块/跨品种横向视角
- 现状：64 品种各自独立打分、报告按交易所罗列，没有板块强弱排名、板块共振确认，
  也没有跨品种价差/套利信号（卷螺差、矿螺利润、油粕比、炼厂利润、内外盘比价、近远月价差）。
- 建议：加板块聚合分与全市场分数排行（哪个板块最强/最弱）、同板块方向一致性确认；
  选 2~3 个你熟悉的产业链做价差监控即可，不必求全。

### 16. 没有仓位/资金管理模块
- 现状：建议写「≤20% 仓位」但不结合账户资金、合约乘数、最小变动价位、保证金比例，
  给不出「该下几手」；无组合层风险预算、板块集中度、单日亏损熔断。
- 建议：输入账户资金后，按 ATR 反推每品种手数（风险百分比法，如每笔风险=资金 1%），
  并汇总保证金占用/板块敞口。

---

## 三、P2 工程化与体验（锦上添花）

### 17. 看板只是 txt 的 iframe，没有任何图表
- 现状：实时报告.html 仅 5.7KB，6 个页签全是 iframe 嵌纯文本，无 K 线、无分数榜、无热力图，
  无品种搜索/筛选/自选、无历史信号回看、无浏览器内声音。
- 对标：StatusHub（Flask+Chart.js 三个仪表盘）、Homalos（FastAPI+Vue3）。
- 建议：基于第 8 条的 SQLite，用 Flask/FastAPI + ECharts 做：全品种综合分条形榜（红绿色阶）、
  板块热力图、单品种分数/价格时序小图、IV 分位、信号历史表；顺带支持手机浏览器访问（现在 file:// 只能本机看）。

### 18. 无回归测试套件、无 git 工程化
- 现状：前四轮的 40+/18/20+ 断言都是临时脚本、验证完即删（仓库内无 tests/），现在改任何代码都没有安全网；
  项目也不是 git 仓库（无 .git/、.gitignore、LICENSE、版本号/CHANGELOG）。
- 建议：把调度边界、急动检测、触发阈值、期权六项检查、BOM 往返这些纯函数用 pytest 固化为 tests/；
  git init 并加 .gitignore（忽略 reports/cache/logs/__pycache__）、LICENSE、requirements 锁版本。

### 19. 配置与代码耦合
- 现状：所有阈值在 config.py，改配置=改代码；将来加 webhook/账户资金/自选品种更挤。
- 对标：LC9999216 用 config.json + .env + 模板。
- 建议：用户级配置外置 config.json（带 config.template.json）、密钥走 .env，config.py 只留结构与默认值。

### 20. 首轮 1~2 分钟太慢，无本地持久缓存
- 现状：64 品种主力合约探测 + 日线预取全部串行；重启后全部重来。
- 建议：ThreadPoolExecutor 并发（限频 5~8 路）；合约月份/日线落本地缓存文件（带日期），
  重启先读缓存秒出第一轮，后台再刷新。

### 21. 数据源单点风险，无降级链
- 现状：全链路压在新浪；已有财联社接口失效、搜索引擎反爬的教训。
- 对标：TradingAgent-Future 的 AKShare→Tushare→BaoStock 自动降级。
- 建议：数据层包一层「主源失败自动切备源」（新浪→akshare/efinance→东财），单源异常不影响出报告。

### 22. 无 AI 综合解读 / 自然语言追问
- 现状：报告是模板拼接；消息面与技术面矛盾时只给一句固定风险提示。
- 建议（可选）：对 Top 品种让 LLM 生成「看多逻辑/风险点/失效条件」综述；
  或做一个问答入口（「为什么今天螺纹偏空」「昨晚哪些消息推高了纯碱」），数据来自第 8 条数据库。

### 23. （可选远期）无模拟盘/实盘闭环
- 信号止于「建议」。若将来想验证执行，可走 tqsdk/vnpy 的模拟盘（仿真撮合）先跑，
  统计真实滑点/成交情况，不必直接实盘。

---

## 四、你已经比多数开源项目做得好的地方（保持，别改丢）

1. **调度严谨**：5/20 分钟固定刻度对齐、紧急轮「插队但计划点不推移」、真实 remaining 等待，
   比绝大多数开源监控项目的「sleep 固定间隔」严谨得多；
2. **多源聚合 + 可信度分级 + 存疑降权后排**，舆情处理成熟度高于一般关键词项目；
3. **期权深度**：Black-76 + 希腊字母 + 组合策略 + 六项严格检查，个人项目里罕见；
4. **落盘鲁棒性**：BOM 透明处理、文件占用重试、单文件失败不拖垮整轮、日切、永久复盘，细节扎实；
5. **事件驱动看板**：极小状态文件探测、ts 变化才重载，file:// 下规避 fetch 限制的设计很巧妙；
6. 购买建议标注**具体合约月份、移仓提示、期权月份顺延**，贴近实战。

---

## 五、建议实施顺序

| 批次 | 内容 | 对应条目 | 价值/成本 |
| --- | --- | --- | --- |
| ~~第一批（修 bug）~~ ✅已完成(第6轮) | ~~夜盘分品种收盘时间、bat 解释器、交易日历、日志轮转、Session 连接池、看门狗~~ | 1-6 | 2026-08-31 全部修复并验证 |
| ~~第二批（补短板）~~ ✅已完成(第7轮) | ~~声音+webhook 推送、SQLite 落库、信号胜率追踪、量仓因子~~ | 7/8/9/11 | 2026-08-31 落地：新增 alerts/storage/flow_tracker，零新增依赖；量仓只覆盖成交量/持仓量，基差库存仍待后续 |
| ~~第三批（提质量）~~ ✅已完成(第8/9轮) | 最小回测、RSI/MACD+日线/30/60分钟共振、情绪否定规则、IV分位、蝶式/比率/备兑/保护性认沽、回测成本/锁板/参数扫描 | 10/12/13/14 | 2026-08-31 落地 |
| ~~WP-A/B（期权深化）~~ ✅已完成(第11/12轮) | 完整T链+持仓量PCR、全月份期限结构展示、多到期日IV曲面(skew/期限/矩阵)+日历/反向日历价差 | 14 | 2026-09-01 落地，零新增依赖；成交量PCR/官方IV校验/精确保证金留后续 |
| ~~WP-C（基本面）~~ ✅已完成(第13轮) | 东财库存/仓单分位+周环比、龙虎榜前20席净多与边际、期限carry入综合分、生意社基差软降级，fundamentals第7张表+基本面速览榜 | 11 | 2026-09-01 落地，新增2模块339行+5处接线，零新增依赖；官方IV曲面交叉校验留后续 |
| 第四批（体验） | 图表化看板、pytest 回归、配置外置、并发提速+缓存、数据源降级 | 17-21 | 长期可维护 |
| 按需 | 板块价差、资金管理、AI 解读、模拟盘 | 15/16/22/23 | 看个人精力 |
