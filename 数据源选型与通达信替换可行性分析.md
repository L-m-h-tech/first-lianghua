# 数据源选型与「东财能否全部替换为通达信」可行性分析

> 结论日期：2026-09-01 晚（本机，重庆/当前运营商网络，真实联网实测，非纸面推断）
> 触发背景：第 14 轮 WP-D0 分钟 K 线落地时，东财行情域名 push2his 出现持续断连；按要求全网调研通达信（pytdx）接口并评估能否整体替换东财。

## 一、结论（先说答案）

1. **东财接口不能整体替换为通达信，当前环境下分钟行情也不能以通达信为主源。** 原因有两层，任一都足以否决：
   - **通道层（本机实测）**：通达信免费公共行情服务器，标准行情口 7709 官方 103 台中 28 台 TCP 可达，但**逐台验证全部只同步沪深股票、没有任何商品期货**；商品期货真正所在的扩展行情口 7727 **0/103 可达**，7720/7721/7729/7730 也全部关闭。即"连得上的没有期货，有期货的口连不上"。
   - **能力层（协议事实）**：通达信行情协议只有 K 线/五档快照/分笔/证券列表，**根本没有**期货库存仓单、龙虎榜会员持仓、7×24 新闻快讯、交易日历——这些恰是项目第 4/6/13 轮依赖东财 datacenter 的核心数据，通达信无从替代。
2. **东财不是"全站不行"，要分域名看**：出问题的只是行情 K 线域名 `push2his`（按本机 IP 临时限流/断连）；数据中心域名 `datacenter-web`（库存/仓单/龙虎榜）实测秒回、稳定，二者互不影响，继续保留。
3. **分钟 K 的最优解是"新浪主连为主 + 东财补 1 分钟 + 通达信可选冗余"**，已按此落地（详见第四节）。新浪主连分钟 K 实测 64/64 品种全覆盖、1023 根/次、30m 回溯约 6 个月、60m 回溯约 12.5 个月，且零断连，综合优于东财 push2his。
4. **通达信没有被丢弃**：已落地为可选适配模块 `tdx_bars.py`，程序启动自动探测，只有确认能取到期货分钟 K 才点亮，否则零成本跳过；未安装 pytdx 也不影响运行。一旦未来环境变化（见第六节）会自动受益，无需改代码。

## 二、通达信实测过程与证据（2026-09-01 18:40–19:10）

### 2.1 标准行情口 7709（pytdx TdxHq_API）

- 对 pytdx 内置官方主机全表 **103 台**做 TCP 连通扫描：**28 台 7709 可达**（最快 60.191.117.167，65ms）。
- 逐台连接后验证数据范围：
  - 市场 0（深圳股票）有数据；市场 1（上海股票）多数公共服务器甚至都没同步；
  - **市场号 0~120 全枚举**，期货所在的 28 郑商所/29 大商所/30 上期所/47 中金所，`get_security_list` 合约列表全空；
  - 直接按代码在 0~120 全市场拉 `rb2701 / m2701 / MA610 / sc2610 / si2611 / IF2609` 的日 K/分钟 K，**全部返回空**；
  - 实时快照 `get_security_quotes` 对期货代码同样为空。
- 结论：免费 7709 公共服务器面向股票行情，**不同步国内商品期货**。

### 2.2 扩展行情口 7727（pytdx TdxExHq_API，商品期货/外盘/港股通道）

- 同一批 103 台扫描 7727：**0 台可达**；补扫 7720/7721/7729/7730/443/80，除 80/443 网页口外行情扩展口全关。
- 用 TdxExHq_API 反向尝试走 7709：协议不通（超时/拒绝）。
- 本机无通达信金融终端在运行（仅有同花顺期货通，是项目既有的 uiautomation 程序），127.0.0.1 的 7709/7727 无本地监听。
- 社区旁证：sickate/pytdx（2026-08 更新）文档自述"扩展行情 7727：10 台服务器仅 1 台可连接，但数据接口均返回 None"；多篇实践文章指出公共 7727 长期不稳定，期货行情通常需要**本机常驻登录通达信客户端做本地代理**或付费/券商网关。

### 2.3 要用通达信期货数据，只剩这三条路（本轮均不采纳，理由附后）

| 路径 | 可行性 | 不采纳原因 |
| --- | --- | --- |
| 本机 7×24 常驻登录"通达信金融终端"，pytdx 连本地 7709/7727 | 技术可行 | 引入 GUI 常驻依赖，违背项目 13 轮坚持的"零新增运行时依赖、无人值守、崩溃看门狗自愈"架构；且本机当前用的是同花顺期货通，不是通达信 |
| 付费/券商 OpenAPI 通达信网关 | 可行但收费 | 项目坚持免费数据源 |
| 等公共服务器恢复期货同步 | 不可控 | 无 SLA，不能作为长期稳定方案的前提 |

## 三、项目现有东财接口 × 通达信逐项替换可行性

| # | 数据 | 现用东财接口 | 通达信对应能力 | 能否替换 |
| --- | --- | --- | --- | --- |
| 1 | 分钟/日 K 线（具体合约） | `push2his.eastmoney.com` kline（1/5/15/30/60m、日） | 协议支持 K 线，但公共口取不到期货 | **当前不能**；已用新浪主连替代其主源角色，东财保留补 1m |
| 2 | 期货库存/仓单时序（WP-C） | `datacenter-web` `RPT_FUTU_STOCKDATA`（61 品种，约 3 个月窗口） | **无**（行情协议不含基本面） | **永远不能**，保留东财（实测 0.2s 稳定） |
| 3 | 龙虎榜前 20 席多空持仓（WP-C） | `datacenter-web` `RPT_FUTU_DAILYPOSITION` | **无** | **永远不能**，保留东财（实测 0.2s 稳定） |
| 4 | 期货品种代码映射（WP-C） | `datacenter-web` `RPT_FUTU_POSITIONCODE`（74 条） | **无** | **永远不能**，保留东财 |
| 5 | 7×24 全球快讯（第 4 轮四文字源之一） | `np-listapi.eastmoney.com` | **无** | **不能**，保留东财 |
| 6 | 交易日历备源（第 6 轮，主源为新浪 RB0） | `push2his` 上证指数日 K | 7709 有上证指数 K，理论可做备源 | 无必要（主源已是新浪，东财仅兜底） |
| 7 | 实时行情/五档/持仓量快照 | 项目主用新浪 hq.sinajs（第 1 轮起） | 7709 股票可用、期货不可用 | 期货**不能**，维持新浪 |

**小结**：7 项里 4 项（#2/#3/#4/#5）通达信在协议层就不存在对应数据，#1/#7 当前通道取不到期货，仅 #6 理论可做但无收益。**"全部替换为通达信"不成立，且会直接断掉基本面与新闻两条腿。**

## 四、分钟 K 最终选型（已落地，第 14 轮 WP-D0）

### 4.1 三源实测对照

| 源 | 粒度 | 历史深度（本机实测） | 覆盖 | 稳定性（本机实测） | 最终角色 |
| --- | --- | --- | --- | --- | --- |
| **新浪主连** `getFewMinLine` | 5/15/30/60m（**无 1m**） | 固定 1023 根：5m≈1 月、15m≈3 月、30m≈6 月、60m≈12.5 月 | **64/64 品种** | 零断连、0.1–0.6s/品种 | **主源** |
| 东财 `push2his` | **1**/5/15/30/60m、日，具体合约 | 1m 仅约 1–2 日、其余约 1 月 | 全品种（需合约转换） | 18:23–19:10 持续 RemoteDisconnected（IP 级限流，熔断自愈） | 仅补新浪没有的 **1m** + 具体合约兜底 |
| 通达信 pytdx | 1/5/15/30/60m | 理论深（TCP 二进制） | 公共口无期货 | 7709 仅股票、7727 不可达 | **可选冗余**，probe 通过才启用 |
| tqsdk 免费版 | 分钟/tick | 最后 8000 根/近 1 年 | 全品种 | 需注册账号，程序化批量下载属专业版（收费） | 不引入（坚持零账号零依赖） |

### 4.2 选源逻辑（`intraday_bars.MinuteCollector`）

- **1 分钟**：新浪无此周期 → 东财具体合约 → 通达信（若点亮）；
- **5/15/30/60 分钟**：新浪主连（免换月、深、稳）→ 通达信具体合约（若点亮）→ 东财具体合约兜底；
- 任一源成功即返回并记录源名；全失败只计数、不阻断主监控。

### 4.3 工程防护

- 东财：低并发 + 全局限流 + 镜像子域轮换 + 连接级异常短退避快速熔断（约 3–5s 进入 60s 冷却，不再逐任务空耗 66s），任何失败软降级 `[]`；
- 通达信：启动并发探测（20 线程、15s 总时限，实测约 1–5s 判定完毕），不可用则 `available=False`，之后 `fetch` 零成本返回 `[]`；pytdx 函数内延迟导入，**不进 requirements 强制依赖**；
- 新浪：复用全局 `http_client` 连接池与既有重试，主连代码直接取自 `config.VARIETIES[*].code`，无需等主力合约探测完成即可采集。

## 五、落地结果与验证（2026-09-01 19:07–19:20）

- 新增 `tdx_bars.py`（通达信可选适配），重写 `intraday_bars.py`（三源），`storage` 第 8 张表 `minute_bars`，`main` 启动 once 小回填 + 后台常驻自采线程，`config.MINUTE_*`，`report` 分钟库覆盖展示。
- 零网络合成断言 **55 项全过**（代码转换/三源选源/降级/聚合/建表去重/熔断）。
- `main --once --no-launch` 端到端 **零 ERROR**：主力探测 64/64；分钟 once 320 任务，新浪 256 成功、东财 1m 64 任务熔断跳过；既有 7 表与基本面/期权/分析链路完好。
- 完整回填后 `minute_bars` **261888 根 = 64 品种 × 4 周期 × 1023 根，精确对齐**；零非法 OHLC、零违反唯一键；二次增量采集净增 0（upsert 幂等）。
- 回溯深度：5m 至 2026-07-31、15m 至 2026-05-27、30m 至 2026-02-27、60m 至 2025-08-13。

## 六、什么情况下重新评估"通达信为主源"

满足任一条件，可把 `tdx_bars` 的选源顺序提前（改 `MinuteCollector.collect` 即可，模块已就绪）：

1. 本机安装并常驻登录通达信金融终端，确认 127.0.0.1:7709/7727 可取期货分钟 K；
2. 更换网络环境/运营商后，公共 7727 恢复可达且 `get_instrument_bars` 返回期货数据（程序启动 probe 会自动发现并点亮，无需改代码）；
3. 接入券商/付费通达信网关（有明确 SLA 后再评估成本）。

即便如此，**库存/仓单/龙虎榜/快讯仍必须保留东财 datacenter**，通达信只可能接管"行情"这一层，不存在"全部替换"。

## 七、能力天花板（诚实声明，沿用前 13 轮口径）

- 新浪主连是**比例复权的连续序列**，换月点价格为近似；具体合约真实价格由东财/通达信在可用时补充（库中以 `contract` 区分：主连 `RB0` 与具体合约 `RB2701` 按 `sym` 共存）。
- 免费源无历史 L2 逐笔；1 分钟粒度当前依赖东财，东财持续限流期间最细只到 5m；分钟库长期深度最终靠程序 7×24 常驻自采滚动积累（自 2026-09-01 起永不丢）。
- 通达信公共服务器可用性随地区/运营商/时间漂移，本文结论基于 2026-09-01 本机实测；`tdx_bars` 的自动探测保证结论变化时程序行为自适应。

---

## 八、2026-09-01 晚二次全网/GitHub 调研与数据源增补（在第七节之后的修订）

### 8.1 调研范围与方法

- 全网 + GitHub（akshare、efinance、Ashare、mootdx、tqsdk 等开源项目的接口字典）逐个排查"更好的免费期货数据/新闻接口"；**一律以本机 requests 真实联网实测为准**，纸面可用但本机不通的不采纳。
- 采纳铁律：只做"确定不更差"的替换/增补——新源为主、旧源降级为兜底；候选不如现源或需逆向/账号/新增强制依赖的，一律不动。

### 8.2 候选实测对照（2026-09-01 20:00–22:00，本机）

| 候选 | 用途 | 实测结果 | 决策 |
| --- | --- | --- | --- |
| **新浪 `getFewMinLine&type=1`** | 一分钟K | **固定1023根、64/64品种零断连、0.1s级；主连RB0与具体合约RB2701/MA610均可取；主连末根与主力合约逐价一致；回溯约2.5个交易日** | **采纳：1m 主源由东财切换为新浪主连（纠正第14轮"新浪无1分钟"的漏测误判）** |
| 腾讯 ifzq/qt（`nf_RB0`/`rbm`/`rb2701` 等穷举） | 期货行情/分钟K | 全部 `v_pv_none_match`/param error——免费接口只覆盖股票与外盘 hf_，**不覆盖国内商品期货** | 不采纳 |
| 东财 `push2his` kline | 分钟K兜底 | 两晚持续 RemoteDisconnected（IP级限流） | 维持最后兜底，不升主源 |
| 东财 `push2` ulist 主连快照 | 实时行情备用 | 主连secid=市场号.品种小写m（113.rbm/115.mam/142.scm/225.sim），f111=持仓量；交易时段0.2s正确；**限流会扩散到该域名，且限流边缘/push2delay 会返回 f111=1、价格错位的残缺200响应** | **采纳为新浪实时行情缺失兜底，加 oi≥100 等有效性校验，残缺即弃；不引入 push2delay** |
| 财联社电报 cls.cn | 文字快讯 | 老接口下线；nodeapi/v1 均返回访问拦截页，需 sign 签名+指纹对抗（akshare 已弃用） | 不采纳（不稳定逆向） |
| 富途 live-list / 汇通 fx678 / 百度股市通期货新闻 / 金十 flash-api | 文字快讯 | 404、需 token/sub_id 或风控页；现用金十 www 域名正常 | 不采纳，现有6快讯源维持 |
| tqsdk/Tushare/掘金等 | 全量行情 | 需注册账号/积分/付费，程序化批量下载收费 | 不引入（坚持零账号零强制依赖） |

### 8.3 最终选源（取代 4.2）

- **分钟K 全周期统一链路**（`MinuteCollector.collect`，1/5/15/30/60m 不再分叉）：新浪主连（type=1/5/15/30/60，免换月、稳、1023根）→ 通达信具体合约（probe 点亮才用）→ 东财具体合约兜底；主力合约未知时新浪主连仍可独立完成全周期采集。
- **实时行情**：新浪 `hq.sinajs` 为主（正常时东财零请求）；仅对新浪缺失品种用东财 push2 主连快照补齐，残缺/断连软降级为空，绝不污染。
- **文字快讯**：东财7x24/新浪滚动/华尔街见闻/同花顺7x24/新浪7x24/金十 共6源，当晚复测全部健康，维持不变。

### 8.4 落地与验证（2026-09-01 晚）

- 改动文件：`futures_data.py`（放开 type=1；新增 `_fetch_quotes_em` 兜底+残缺拦截，`fetch_quotes` 主备）、`intraday_bars.py`（全周期统一选源）、`config.py`（MINUTE 注释与 1m LMT=1023）、`main.py`（1m 不再依赖主力合约探测）、`report.py`/`tdx_bars.py`（措辞同步）；**零新增强制依赖**。
- 零网络合成断言 **21 项全过**（周期校验/全周期选源链/东财字段映射/残缺拦截/新浪全齐时东财零请求）。
- `main --once --no-launch` 端到端 **零 ERROR**：分钟 once 320 任务**全部 src=sina、空/失败0**；`minute_bars` 由 261888 根增至 **327360 根 = 64品种×5周期×1023（1m 从0根补到65472根/64品种，08-26起）**，零非法 OHLC；既有7表、基本面、期权、综合分链路完好。
- 能力天花板更新：免费源 1m 窗口仅约2.5个交易日（东财1m限流时为0），长期1m深度仍靠常驻自采（自2026-09-01起滚动积累、永不丢）；主连为比例复权序列的口径不变。
