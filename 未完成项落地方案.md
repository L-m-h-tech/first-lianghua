# 七项未完成能力 · 具体落地方案（2026-09-01）

> 对应《上下文摘要.md》第10轮后 P1 剩余边界与回测引擎缺口。本文每个接口都标注了**实测状态**：
> - ✅本机实测 = 2026-09-01 用 curl 在本机真实请求成功；
> - 📄官方文档 = 接口存在、字段以官方文档为准，落地时需再跑一次联调；
> - 💰付费天花板 = 免费源做不到、必须付费或实盘柜台的部分，明确不硬做。
>
> 总原则（与前十轮一致）：**运行时零新增第三方依赖**。GitHub 现成代码一律采用"读其源码、用标准库 + `http_client.http` 重写"的方式套用；akshare/tqsdk 只作为**可选适配器**（try-import，缺失自动降级），绝不进入 requirements.txt 主依赖。

---

## 0. 总览：7 个问题归并为 5 个工作包

| 原未完成项 | 工作包 | 数据从哪来（是否新依赖） | 新增/改动文件 | 优先级 |
| --- | --- | --- | --- | --- |
| ① PCR 认沽/认购比 | WP-A 期权完整链 | 新浪期权 T 链 ✅本机实测（零依赖，同源） | 新增 `option_chain.py`；改 `option_analyzer.py`/`storage.py`/`report.py`/`main.py`/`analyzer.py`/`contracts.py`/`config.py` | ✅ **第11轮已完成（持仓量PCR；成交量PCR待交易所日行情备用源；LU新浪无源已降级）** |
| ② 日历价差/IV 曲面 | WP-B IV 曲面 | WP-A 全链价格 + 现有 Black-76 反推（零依赖）；官方 IV 校验📄 | 新增 `iv_surface.py`；改 `option_strategies.py`/`option_analyzer.py`/`main.py`/`report.py`/`config.py` | ✅ **第12轮已完成（多到期日IV曲面+日历/反向日历；官方IV交叉校验留待WP-C交易所源；免费T链脏报价已做五层清洗）** |
| ③ 基差/库存/仓单/期限结构/龙虎榜 ✅第13轮 | WP-C 基本面数据包 | 期限结构=现有term零新增；库存/仓单/龙虎榜=东财datacenter直连✅实测；基差=生意社(反爬,软降级预留位) | 新增 `fundamental_data.py`(188行)+`fundamental_factors.py`(151行)；改 config/analyzer/storage/main/report；DB加 fundamentals 第7表 | ✅ 完成 |
| ④ 平今优惠触发 | WP-D 日内回测 | 分钟 K：新浪主连全周期为主+东财/通达信兜底+常驻自采 | `intraday_bars.py`/`storage.minute_bars` 第14轮就绪；**第15轮新增 `intraday_backtest.py`（649行），平今/平昨按交易所结算交易日判定，真实费率+每手人民币+平今对照** | ✅ 第15轮完成 |
| ⑤ 逐笔盘口成交 | WP-D 撮合升级 | 免费无历史 L2（天花板不变）；bar 内撮合移植 vnpy | **第15轮：vnpy式bar内撮合已落地（下一根开盘成交/止损止盈预埋单/同根双触按止损/跳空开盘成交/整根封死锁板拦截/零量不成交）；L2逐笔队列仍为不可逾越天花板，未来靠常驻tick快照积累** | ✅ bar内部分第15轮完成（L2除外） |
| ⑥ 长期分钟历史回放 | WP-D 分钟数据库 | 三源互补（东财/tqsdk/自采落库） | `storage.py` 加 `minute_bars` 表；`main.py` kline 线程增量落库 | P2-D0（先做） |
| ⑦ 组合资金权重曲线 | WP-E 组合账户 | 保证金率表：**银河期货官网（用户开户公司）投机档**半自动解析 | 新增 `portfolio.py`(947行)、`tools/build_margin_table.py`(184行)、`data/futures_margins.csv`；改 config/report/backtest | ✅ **第16轮已完成（共享资金池逐bar盯市+三种手数+强平状态机+权益曲线，37条合成断言+全64品种分钟/日线端到端验证）** |

**建议实施顺序**：A（接口已实测通，1~2 轮）→ C 的期限结构（现成）→ B → C 其余 → D0 分钟自采库 → D1/D2 日内回测 → E。

---

## 1. WP-A：完整期权链 + PCR（解决①）—— ✅ 第11轮已落地（2026-09-01 端到端验证通过）

### 1.1 数据源（✅ 2026-09-01 本机实测通过，零新依赖）

新浪商品期权 T 型报价，与项目现有新浪 K 线**同一站点、同一套 headers、同一个 `http_client.http`**：

```
http://stock.finance.sina.com.cn/futures/api/openapi.php/OptionService.getOptionData
   ?type=futures&product={product}&exchange={ex}&pinzhong={品种字母+4位年月}
```

- `product/exchange` 映射（实测 cu、m 均成功）：

| 交易所 | product 规则 | 示例 |
| --- | --- | --- |
| 上期所 SHFE / 能源 INE | 品种小写 + `_o` | `cu_o`、`au_o`、`ru_o`、`sc_o`（INE 待联调确认后缀） |
| 大商所 DCE | 品种小写 + `_o` | `m_o`、`i_o`、`c_o` |
| 郑商所 CZCE | 品种小写，**无后缀** | `ma`、`cf`、`sr`、`ta` |
| 中金所 CFFEX | 品种小写，无后缀 | `io`、`mo`、`ho` |
| 广期所 GFEX | 待联调（预计 `lc_o`/`si_o` 风格） | 落地时逐个验证 |

- `pinzhong` = 标的品种字母 + 期权到期年月，如 `cu2610`、`m2609`。**到期月份不用自己猜**：直接复用 `webdata` 已解析的 OpenVlab 期权日历（`contracts.ContractCache.refresh(exp_cal=...)` 里已有 `{sym: {yymm: {exp_date}}}`），只对真实挂牌月份发请求。
- 返回 JSON：`result.data.up` = 全部看涨腿，`result.data.down` = 全部看跌腿。每行字段：

```
[买量, 买价, 最新价, 卖价, 卖量, 持仓量, 涨跌%, (行权价), 合约代码]
```

  - SHFE 8 字段、独立带行权价；**DCE 7 字段、没有独立行权价位**（实测 `["0","0.000","0.500","0.500","1243","5514",0,"m2609P2500"]`），行权价必须从代码尾部正则 `([CP])(\d+)$` 提取。解析器按"长度 7/8 双兼容、行权价一律优先从代码解析"写。
- 单腿实时快照（补**成交量**用）：`https://hq.sinajs.cn/etag.php?list=P_OP_m2609P2500,P_OP_m2609C2500`，可批量，字段含成交量；期权日 K：`FutureOptionAllService.getOptionDayline?symbol=m2609P2500`（jsonp，解析方式同 `fetch_daily_kline`）。

### 1.2 PCR 三个口径

```
持仓量 PCR  = Σ看跌腿[5] / Σ看涨腿[5]          # T链直接算，最稳，机构布局意图
成交量 PCR  = ΣP成交量 / ΣC成交量             # 需批量 P_OP_ 快照或当日 option_hist
成交额 PCR  = Σ(P最新价×成交量×乘数) / Σ(C…)   # 同上，主力资金意图
```

三口径同时输出，持仓 PCR 为主；另存近 N 日 PCR 序列算历史分位（>1 偏情绪底部、极值反转是期权情绪因子的通用用法，只做呈现不做单因子结论）。

### 1.3 落地步骤与接入点

1. 新增 `option_chain.py`：
   - `fetch_chain(sym, ex, yymm) -> {"calls":[...], "puts":[...], "pcr_oi":.., "pcr_vol":..}`，走 `http_client.http`，单品种 try 包裹失败不影响主流程（沿用项目数据层铁律）。
   - 带 `OptionChainCache`（仿 `KlineCache`，TTL 与 OpenVlab 一致 30min，线程锁）。
2. `option_analyzer.implied_vol_profile()` **已经预留了接入位**：第 96~97 行 `chain = page.get("option_chain") or {}; pcr = chain.get("pcr")`。把真实 chain 通过 `fut_row["chain"]` 传进来即可，`analyze_option` 里 `PCR={pcr}` 的展示行（第 153 行）自动生效，无需改展示逻辑。
3. `storage.py`：`options` 表新增 `chain_json`（全链快照）或新建 `option_chains` 表（sym/date/expiry/strike/cp/oi/vol/iv），为 IV 曲面与 PCR 分位积累历史。
4. `main.py`：期权分析前并发拉取有期权品种的链（仿 kline 60s 线程 + `ThreadPoolExecutor` 预热模式，项目里已有现成并发范式）。
5. `report.py`：期权块加"PCR 三口径 + 分位 + 全链行权价档位数"。

### 1.4 可直接套用的 GitHub 代码

- **akfamily/akshare（MIT）** → `akshare/option/option_sina.py`：新浪商品期权链解析（up/down、合约代码、T 型表组织方式），以及 `option_current_em`（东财期权链，作为备用源）。只搬 URL 构造与字段解析，不引入 pandas。
- 备选源（主源失败降级）：交易所官方期权日行情 `option_hist_dce/czce/shfe/gfex`（大商所/郑商所官网 XLS、上期所 sk 接口），URL 同样可从 akshare 同名源码里直接取。

### 1.5 验收

合成 fixture（用本次实测的 cu/m 真实 JSON 片段）断言：7/8 字段双解析、C/P 分组正确、PCR=手算值、缺腿不除零；`main.py --once --no-launch` 后至少 10 个期权品种写出非空 PCR；断网时降级为"链缺失"而不是整轮失败。

---

## 2. WP-B：多到期日 IV 曲面 + 日历价差（解决②）—— ✅ 第12轮已落地（2026-09-01 端到端验证通过）

### 2.1 数据与算法（零新依赖）

- **曲面网格**：对同一品种的多个挂牌月份（OpenVlab 日历已有）分别跑 WP-A，得到「到期日 × 行权价」的全腿最新价矩阵——这就是 IV 曲面的原料，不需要任何新数据源。
- **IV 反推**：项目已有 `option_analyzer.black76(F,K,T,sigma,kind)` 且价格对 σ 单调。新增 20 行二分/牛顿求解：

```python
def implied_vol(price, F, K, T, kind):  # 二分法，sigma∈[1e-4, 5]，迭代40次到1e-5
    lo, hi = 1e-4, 5.0
    for _ in range(40):
        mid = (lo + hi) / 2
        if black76(F, K, T, mid, kind) > price: hi = mid
        else: lo = mid
    return (lo + hi) / 2
```

  （参考 py_vollib `black_scholes.implied_volatility` 的迭代收敛思路，MIT，数学方法无版权问题；自写零依赖。）
- 输出三层结构：
  1. **波动率微笑/skew**：同一到期日、不同行权价的 IV；ATM、25Δ 风险反转 `IV(25ΔP)-IV(25ΔC)`、蝶式宽度。
  2. **期限结构**：各到期日 ATM IV；近月-远月差（contango/backwardation of vol）。
  3. **曲面矩阵**：行=到期日、列=moneyness(K/F 分档)，值=IV，写报告/看板。
- **官方 IV 校验/补缺（📄）**：akshare `option_vol_shfe`、`option_vol_gfex` 直接给交易所计算的合约隐波（上期所/广期所官网），作为自算 IV 的交叉校验；偏差 >2 个 vol 点时标注"自算/官方不一致"。

### 2.2 日历价差策略（补进 option_strategies.py）

- 规则：近月 IV 显著高于远月（>锥 90 分位差）→ 卖近买远（long calendar）；反向 → 卖远买近；两腿 Δ 中性、组合 Greeks 直接复用第 8/9 轮已有的组合 Δ/Γ/Vega/Θ 框架（`option_strategies` 已支持多腿与保证金点值）。
- 风控：同标的同数量、到期日差 1~2 个月、Vega 为正/负方向与观点一致、最大亏损=净权利金支出。

### 2.3 接入点与验收

- 新增 `iv_surface.py`（反推 + 微笑 + 期限结构 + 曲面）；`option_analyzer` 的 profile 增加 `term/skew_calc`；`option_strategies` 增加 `calendar_spread()`；`storage.options` 增加每腿 IV 列。
- 验收：用 cu2610 真实链断言"平价关系附近 call/put 反推 IV 一致（put-call parity，差<1vol）"、ATM IV 与交易所官方值偏差容忍内；构造"近月 IV=40%/远月=20%"合成数据断言日历方向正确。

### 2.4 ✅ 第12轮落地记录（2026-09-01，实测）

**实际产出**：新增 `iv_surface.py`（约260行）；改 `option_analyzer.py`（IV 三级优先级+交叉校验+曲面展示）、`option_strategies.py`（候选9日历/反向日历+静态盈亏空间检查+override机制）、`main.py`（3.9步多到期日链并发预热与曲面组装）、`report.py`（曲面摘要+矩阵两行、跨月标签）、`config.py`（IV_*/IV_CALENDAR_* 参数段）；`storage.py` 无需改（`option_chains` 的 UNIQUE(sym,expiry,ts) 天然支持同 ts 多 expiry 各一行）。

**两个关键坑（后续轮次务必注意）**：
1. **OpenVlab 日历月份键是 6 位整数（202611=2026年11月），新浪 T 链 pinzhong 要 4 位（2611）**。必须 `yy,mm=(yymm//100)%100, yymm%100` 归一化（同时兼容4/6位），否则全品种抓空（首轮端到端"0个月份"即此坑）。日历键为 `sym.upper()`。
2. **免费 T 链脏报价会让反推 IV 严重失真**：临近到期 T 小、单边挂单（ask=0）、买卖价差宽达45%，价格微小误差被放大成 80%~120% 的假 ATM IV 与 ±30vol 假 skew。实测窄价差（≤15%）腿反推完全合理（MA 30~35%、豆粕14~18%）。据此落地**五层清洗**：
   - ① `leg_quote` 报价质量分级：窄价差取中间价=高质量(q0)，宽价差回退最近成交价=低质量(q1)，宽价差且无成交直接丢弃；
   - ② 单腿反推 IV>250%（`IV_SURFACE_IV_CAP`）丢弃；
   - ③ ATM 必须由高质量腿构成、高质量腿<3 档则该到期日不输出曲面（`IV_SURFACE_NEED_CLEAN_ATM`）；
   - ④ 剩余<30天（`IV_SURFACE_MIN_DAYS`/`IV_CALENDAR_NEAR_MIN_DAYS`，从21天上调到30）的月份不参与——最后30天时间价值小、误差放大，且按**真实到期日**过滤（同为2610，cu剩22天被剔、m剩52天保留）；
   - ⑤ 微笑点 IV 相对 ATM 在 [0.2×,3.5×]（`IV_OUTLIER_RATIO`）带外标 outlier，不参与25Δ与矩阵，缺档显示 `--` 不插值。
   - call/put 同行权价按持仓量加权融合，两侧反推偏差>3vol（`IV_PARITY_DIFF`）取窄价差可信侧并在摘要标注档数。

**对原方案的两处增强（实盘必要性）**：
- 日历价差除方向/到期跨度/流动性外，新增**静态盈亏空间硬检查**：假设 IV 不变、近月到期标的恰好收在同行权价（日历最优点）时估算盈利必须为正，否则时间价值损耗后无利可图、直接观望。实测中多个"买近卖远"反向日历（PTA/多晶硅/氧化铝/黄金）净收入为正但静态最优点为负（注定 IV 不变则亏损），被该检查正确拦截，避免"建议执行一个静态必亏结构"。
- 反向日历（买近卖远、净收入）近月到期后远月裸露，理论上行风险无上限（`max_loss=None` 诚实标注），远月裸腿按 `IV_CAL_MARGIN_RATE=10%` 名义估算保守保证金点值，并在明细强制"仅可小仓位"。

**验证结果**：合成断言（零网络，反推精度<2e-4、put-call parity、RR/Fly 方向、坏腿/全零/宽价差/ATM脏档/离群点过滤、全脏与<30天返回None、矩阵缺档--、long/short calendar 方向与Vega/保证金、静态空间拦截、IV剖面三级优先级）全部 PASS；真实 `main.py --once --no-launch` exit 0，收盘静态价下 112 个月份链/40 品种、37 个品种产出 IV 曲面，ATM IV 全部落在 5%~46% 合理区间、远月矩阵平滑；日历策略真实触发（铁矿石卖近买远 long calendar 全通过：净支出0.6点/最大盈利17.6点/Δ≈0/Vega+0.5；豆一静态空间为正但机构观点不配合→观望）；`option_chains` 表同 ts 下 39 品种×多 expiry 各一行（豆粕 2611/2612/2701 三行 PCR 齐全）。**口径限制**：IV 为收盘价反推的静态值，盘中实时性建议在夜盘/日盘盘中再抽验；免费源无历史 L2、远月深虚值不插值。

---

## 3. WP-C：基本面数据包（解决③：基差/库存/仓单/期限结构/龙虎榜）—— ✅ 第13轮已落地（2026-09-01 端到端验证通过）

### 3.1 期限结构：**不用新数据源，现成能力只差组装** —— ✅ 展示部分第11轮已完成（`contracts.term_structure`，因子入综合分仍在 WP-C）

`contracts.fetch_ranking(sym)` 已经在拉"未来 N 个月份全部合约"的最新价/成交量/持仓量（`futures_data.fetch_quotes` 支持任意月份，主循环每 30min 已在跑）。直接加：

```python
def term_structure(sym):  # 返回 [(yymm, 收盘, 持仓, 相邻月差, 年化展期收益率)]
    rows = ContractCache.get(sym)["list"]          # 已缓存，零额外请求
    rows = sorted(rows, key=lambda r: r["yy"]*100+r["mm"])
    # 月差 = P近 - P远；年化展期收益率 = (P近/P远 - 1) * 365/间隔天数
    # 升水(contango)/贴水(back)结构、近远月斜率、月差极端分位
```

输出：期限结构形状（正向/反向市场）、近远月价差、年化展期收益率（carry 因子，横截面选品种用）。这是 5 个基本面里**唯一完全不依赖外部新源**的，建议最先做。

### 3.2 其余四项：仿 akshare 直连公开源（零依赖路线）

akshare 本质是"公开网页/接口 + 解析"，全部是日频数据，新增一个日级后台线程（每天收盘后跑 1~2 次即可），用 `http_client` 直连，URL 与解析逐行参考 akshare 源码（MIT）：

| 数据 | 首选接口（akshare 名，📄文档） | 底层来源 | 关键字段 | 备用 |
| --- | --- | --- | --- | --- |
| 现货价+基差 | `futures_spot_price(date)` | 生意社等公开报价 | 品种/现货价/主力价/基差/基差率 | 奇货可查 `variety_data(variety,date)`：spot/basis/basis_rate |
| 基差历史序列 | `spot_price_qh(symbol)` | 期现对比页 | 日期/期货收盘/现货价（可算基差时序与分位） | — |
| 展期收益率 | `get_roll_yield` / `get_roll_yield_bar` | 四所行情计算 | 主力/次主力年化 roll yield | 3.1 自算 |
| 注册仓单 | `get_receipt(start,end)`；分所 `futures_warehouse_receipt_shfe/dce/czce` | 三所官网每日仓单 | 品种/日期/仓单量/增减 | 99 期货 |
| 社会库存 | `futures_inventory_99(symbol)` / `futures_inventory_em(symbol)` | 99 期货/东财 | 日期/库存/增减 | 生意社库存 |
| 龙虎榜（会员持仓） | `futures_{shfe,dce,czce,cffex}_position_rank`、`get_rank_sum`（前 5/10/15/20 汇总） | 四所官网每日排名 | 多空前 20 席位持仓、净持仓、变化 | 奇货可查 `variety_profit_loss`/龙虎比 |

- 新增 `fundamental_data.py`：每个源一个 `fetch_xxx()`，单源 try 隔离、主备降级（P2-21 的降级链在这里落地）；日频结果缓存到 `cache/fund_YYYYMMDD.json` 并入库。
- 新增 `fundamental_factors.py`：把原始数据转成裁剪后的因子分（与现有量仓因子同量级，建议各 ±0.8）：
  - 基差因子：基差率历史分位（深度贴水=现货紧张偏多）；
  - 仓单/库存因子：周环比、年同比、库存分位（累库偏空、去库偏多）；
  - 期限结构因子：3.1 的 carry（反向市场偏多）；
  - 龙虎榜因子：前 20 净多空持仓及日变化（机构方向）。
- 接入：`analyzer.analyze_variety(..., fund=None)` 加进综合分（同步在 `config.py` 设 `FUND_*` 权重与裁剪上限，风格对齐 FLOW_MAX_SCORE）；`storage.py` 加 `fundamentals` 表（sym/date/基差/库存/仓单/净持仓/各因子值）；`main.py` 加 1 个收盘后台线程（如每天 15:30、02:45 各一次），**不进 20 分钟主轮、不拖慢实时链路**。

### 3.3 可选依赖路线（不推荐作为主路径，写明权衡）

若接受重依赖，`pip install akshare`（带 pandas/lxml/requests 等一串依赖）可直接拿到全部上表数据，开发量最小；但与项目"运行依赖仅 requests/uiautomation/websocket-client"的既定原则冲突。折中：写 `fundamental_akshare_adapter.py`，`try: import akshare` 成功就用、失败走自写直连，两条路输出同一 dict 结构，requirements 不写死。

### 3.4 可套用代码与验收

- 参考：**akfamily/akshare（MIT）** `akshare/futures/` 下 `futures_spot_price.py`、`futures_warehouse_receipt.py`、`futures_inventory_*.py`、`futures_position_rank.py`（URL/参数/表格解析直接搬）；对标清单已记录的 **zhouxinhao19/TradingAgent-Future**（基差/库存/期限结构 + 数据源降级链组织方式）。
- 验收：每个源至少 3 个品种真实拉通并与官网当日数值核对一致；某源挂掉时其余源与主循环不受影响；因子方向用"已知去库上涨行情"的历史片段回验符号。

### 3.5 ✅ 第13轮落地记录（2026-09-01，本机实测 + 端到端验证）

**最终数据源选型（与 3.2 规划的差异，均经本机 requests 实测，勿重复踩坑）**：

- **库存/仓单（合并为一个源）**：东财数据中心统一入口 `https://datacenter-web.eastmoney.com/api/data/v1/get`（需 Referer=`https://data.eastmoney.com/` + 浏览器 UA，走全局 http_client）。品种映射 `reportName=RPT_FUTU_POSITIONCODE`（filter `(IS_MAINCODE="1")`，TRADE_CODE↔品种，广期所/INE 为小写 si/lc/ps/bc/lu/nr，匹配项目 sym 用 upper、请求保留原始大小写）；时序 `RPT_FUTU_STOCKDATA`（filter `(SECURITY_CODE="CU")`，字段 TRADE_DATE/ON_WARRANT_NUM/ADDCHANGE，sortColumns=TRADE_DATE 升序、pageSize=400）。**实测每品种约 71 条、仅近约 3 个月（CU 自 2026-05-25 起）→ 分位诚实标注为"近3个月滚动分位"，FUND_INV_MIN_SAMPLES=15 兜底。**
- **龙虎榜**：同一入口 `RPT_FUTU_DAILYPOSITION`、columns=ALL、filter `(SECURITY_CODE="CU2610")(TYPE="2")`；**不传 TRADE_DATE + sortColumns=TRADE_DATE desc 即自动返回最新交易日三行**（本日合计/上日合计/总量增减），取"本日合计"=今前20席多空、"上日合计"=昨多空，最省请求。SECURITY_CODE=东财原始品种code+两位年月（CU2610 / si2611）。
- **期限 carry**：直接复用第11轮 `contracts.term_structure` 的 annual_carry，**零新增请求**（本轮才把它接入综合分）。
- **基差（软降级预留位）**：生意社 `https://www.100ppi.com/sf/day-{date}.html`，标准库 HTMLParser 解析、基差率口径=现货/主力−1（现货升水为正）。该站有 HW_CHECK 的 JS-cookie 反爬、大概率返回混淆挑战页，识别到 HW_CHECK/无表格即返回 None，由库存/龙虎/carry 按可得权重重归一补位，**绝不编造**。

**已验证做不通、不要再试的源**：①SHFE 官网 dailystock.dat/pm.dat 在 20260824~0901 全部 404、tsite.shfe.com.cn 连接失败（真实日期确为 09-01，东财/GFEX 当日数据可证非时间错位）；②DCE 官网 wbillWeeklyQuotes 持续 412 WAF（补 Content-Type/Referer/X-Requested-With 无效）；③CZCE 仓单是 xlsx，零依赖解析繁琐；④东财无干净免费基差时序（猜的 RPT_FUTU_BASIS/SPOTPRICE/POSITIONRANK/HOLDPOSITION 全 success=false）。GFEX 官网 `interfacesWebTdWbillWeeklyQuotes/loadList` POST 可用，仅作备用/交叉验证。

**因子设计（fundamental_factors.py，纯函数零网络）**：方向统一"现货/主力偏紧→正分"。库存=分位项 `(0.5-pct)*2`(60%)+周环比去化 tanh(40%)，样本<15 返回 None；龙虎榜=净多率 tanh(K=6)(60%)+较昨日边际 tanh(K=30)(40%)；carry=annual_carry tanh(1/0.15)；基差=基差率 tanh(1/0.05)。权重 库存.40/龙虎.30/carry.20/基差.10，**缺项按可得权重重新归一（单因子不被放大缩小）**，四子项全缺返回 None，加权后 ×FUND_MAX_SCORE=1.5 进综合分。仓单注销再注册导致的极端周环比（实测见 +9433%）因子已 tanh 饱和、展示文本封顶"≥+200%(低基数)"。

**接线与落地**：analyzer 提前算 term、parts 加"基本面"、返回行带 fundamental、明细加行；storage 加 `fundamentals` 表（UNIQUE(sym,trade_date)、INSERT OR REPLACE、日频长期保留）+ insert/latest_fundamentals；main 加 fundamentals_loop 日频后台线程（16点后8线程并发刷库存+基差，启动同步一次）与 _prefetch_rank（分析前8线程按主力合约并发预取龙虎榜，fetcher 自然日日缓存、当天仅首轮请求），不拖慢实时链路；report 期货总表后加【基本面速览】多空各 Top5 + 口径脚注。config 新增完整 FUND_* 参数段。

**验收结果（对照 3.4）**：26 个 py compileall 全过；21 条零网络合成断言全 PASS（各子因子方向、样本不足/全缺 None、缺项归一不缩放、权重和=1）；真实 `main.py --once --no-launch` 两轮 exit 0、无 ERROR/Traceback——库存时序 61 品种、龙虎榜 61、carry 64、基差反爬按设计降级；fundamentals 表落 64 行（库存非空 58/龙虎 61/carry 64/基差 0），fund_score 正31/负30/中性3、范围 [-1.20,+1.50] 无爆分；经济逻辑自洽（沥青库存1%分位+强Back年化+50%→+1.15偏多；烧碱/纸浆库存100%分位+Contango→偏空）；PRAGMA integrity_check=ok。生鲜/指数品种 AP/JD/PK/EC/LG/SC 东财无标准仓单时序，自动只靠 carry+龙虎榜。

---

## 4. WP-D：分钟/Tick 数据层 + 日内回测（解决④平今、⑤盘口、⑥长期分钟）

### 4.1 分钟历史数据：三源互补（先认清各自天花板）

| 源 | 能拿到什么 | 成本/限制 | 实测状态 | 角色 |
| --- | --- | --- | --- | --- |
| 东财 push2his | 1/5/15/30/60 分钟 K，字段 `时间,开,收,高,低,量,额` | 免费无 token，**secid 必须是具体合约**（无主力连续），市场号 113 上期/114 大商（115 郑商/116 中金/142 能源/147 广期落地联调），klt=1/5/15/30/60、101 日 | ✅本机实测 RB2610/M2609 全周期成功；1 分钟返回该合约上市以来全部（实测 267 根） | 主源（零依赖） |
| tqsdk 免费版 | 每 K 线序列**最后 8000 根**（官方文档）；vnpy-tqsdk-edb 免费口径=日线任意区间、**分钟线最近 1 年**；实时 tick 含一档买卖量 | 免费注册账号；**程序化回测/批量下载是专业版功能（1488/年）**；免费版可模拟运行 | 📄官方文档 | 可选适配器，补近 1 年分钟 |
| **常驻自采** | 程序本就 7×24 常驻、kline 线程每 60s 拉分钟 K | 零成本、越跑越长；只能积累"今天起之后"的数据 | 现有线程已在拉、只是没落库 | **长期库的根本解** |

> **2026-09-01 晚实测修正（第14轮落地时，详见《数据源选型与通达信替换可行性分析.md》）**：
> ① 东财行情域名 `push2his` 当晚对本机 IP 持续 RemoteDisconnected（http/https、主域/镜像、1m/日线均断），而 `datacenter-web` 基本面域名秒回——东财需分域名评估，行情口不可作唯一分钟源；
> ② 新浪主连 `getFewMinLine` 实测 64/64 品种全覆盖、固定 1023 根（5m≈1月/15m≈3月/30m≈6月/60m≈12.5月）、零断连，**升为分钟主源**（唯一缺口是无 1m）；东财降为"补 1m + 具体合约兜底"，保留熔断；
> ③ 通达信 pytdx 穷尽实测：7709 公共口 103 台仅 28 台可达且全部只有股票（0~120 全市场无期货），期货所在 7727 扩展口 0 台可达；且协议层无库存/仓单/龙虎榜/快讯，**不能替换东财**；落地为 `tdx_bars.py` 可选冗余（probe 点亮才用、零成本降级、延迟导入不进 requirements）；
> ④ tqsdk 仍不引入（需账号、程序化批量下载收费）。最终选源：1m=东财→通达信；5/15/30/60m=新浪主连→通达信→东财。

东财请求模板（✅实测）：

```
https://push2his.eastmoney.com/api/qt/stock/kline/get
  ?secid=113.RB2610&klt=30&fqt=0&lmt=2000&end=20500101
  &fields1=f1,f2,f3,f4,f5&fields2=f51,f52,f53,f54,f55,f56,f57
# klines[i] = "2026-09-01 14:00,开,收,高,低,量,额"（注意是 开-收-高-低，不是OHLC顺序）
```

历史主连分钟回测的拼接：用 `contracts` 历史主力月份序列，逐具体合约拉分钟 K，在主力切换点做与日线 `ratio_adjusted_bars` 相同的比例复权（该函数已写好，直接复用）。

**落地**：新增 `intraday_bars.py`（东财主、tqsdk 可选、新浪现有 30m 兜底的三级降级 + 本地缓存）；`storage.py` 新增 `minute_bars(sym, contract, dt, period, o,h,l,c,v,oi)` 表，唯一键去重；`main.py` 的 kline 60s 线程每次拉到新分钟 K 就 upsert 入库——**从落地当天起自动滚出一份永不丢的自有分钟库**。

### 4.2 平今优惠（④）：数据有了就只差"同日开平路径"

- 现状：`backtest.side_fee(fee_row, price, leg)` **已经实现 `leg="today"`**（第 74~75 行已读 CSV 的 `today_amt_rate/today_per_lot`），缺的是"识别这笔平仓是不是今天开的"。
- 日内回测用带时间戳的分钟 bar 驱动后，持仓对象记录 `entry_dt`：
  - 平仓 bar 的交易日 == entry 交易日 → `leg="today"`（用平今费）；
  - 跨交易日 → `leg="close"`（现有逻辑）；
  - 上期所/能源平今收单边、其他所平今通常更贵或免收，费率表已分行，直接按表走。
- 新增 `intraday_backtest.py`（或 `backtest.py --freq 30m`）：分钟 bar 按时间排序回放，信号引擎复用 `technical_profile`（第 9 轮已验证可吃分钟序列），交易结构在现有 `position` 上加 `entry_date/contract`。

### 4.3 逐笔盘口（⑤）：诚实的天花板 + 可做到的上限

- **免费、可历史回放的逐笔/L2 五档不存在**：新浪 `nf_` 快照只有买一/卖一价（f6/f7），新浪期权 T 链给一档买卖量但无历史；tqsdk 免费实时 tick 含一档量、但历史 tick 仅保留最近约 8000 个且批量回测属专业版；交易所 L2/CTP 逐笔要柜台或付费。**因此不承诺"逐笔成交回放"，报告里继续标注为疑似/假设。**
- 可落地的 bar 内撮合（比现在"收盘贴边"精确得多），算法直接移植 **vnpy_ctastrategy/backtesting.py（MIT）**：
  - 下一根 bar 开盘价成交（`cross_limit_order` 规则：多头限价 ≥ bar.low 成交、成交价 `min(委托价, open)`；空头对称）；
  - 分钟 bar 内 high/low 触及止损/目标才成交，触及涨跌停价锁单（用该合约涨跌幅×昨结算精确上下限，替代日线贴边启发式）；
  - 成交量约束：bar 成交量为 0 不成交、委托量超过 bar 量 × 参与率阈值则部分成交（这是"排队顺序"能免费近似的极限，明确标注为模型假设而非真实队列）。
- 实时侧：用现有 10s/60s 轮询把**一档买卖价量快照**持续落 `tick_snapshots` 表，积累自有盘口库，用于事后滑点统计（这是未来唯一能逼近真实盘口的免费路径：自己采）。

### 4.4 可套用代码

- **vnpy/vnpy_ctastrategy（MIT）** `backtesting.py`：`cross_limit_order`（bar 内撮合）、`calculate_commission`（按开平/平今/多空分别计费的重写范式）、`calculate_pnl`（逐笔盈亏）、`update_daily_close`（逐日权益）。只移植算法函数，不引入 vnpy/event 引擎依赖。
- **shinnytech/tqsdk-python（Apache-2.0，免费版）**：`get_kline_serial`/`get_tick_serial`，作为可选数据源。
- 分钟主力拼接复权直接复用本项目 `backtest.ratio_adjusted_bars`。

### 4.5 验收

东财源对 RB/M 各拉 30m 分钟与新浪现有 30m 交叉比对收盘价一致；合成"当日开、当日平"用例断言走 today 费率、隔夜单走 close；合成锁涨停分钟序列断言买单不成交、卖单可成交；自采库重启后 upsert 不重复。

### 4.6 第15轮 WP-D1/D2 落地记录（2026-09-01，✅ 端到端验证通过）

**新增** `intraday_backtest.py`（649行，零新增依赖）；`config.py` 加 INTRADAY_BT_* 参数段与 64 品种常态涨跌停表 `FUTURES_LIMIT_MOVE`（取期货公司2026-09-01披露的主力合约档）；`report.py` 看板加"日内/平今回测""日内回测交易CSV"两页签。

- **数据**：读自采 `minute_bars`（主连，1/5/15/30/60m 各1023根）；`--aggregate-from 1` 可从1m现场聚合，并做**目标周期边界相位对齐**（自采1m起点如21:13不对齐整5分时裁到首个整边界后，聚合段末与原生粗周期时间戳一致）；交叉验证 RB：重叠204根中 h/l/c **100%一致**、开盘价24根有段首tick差异（180根完全一致、其中15根差1跳、最大差4跳、均值0.18，自采1m段首tick精度差异，非算法问题）。
- **交易日口径（关键修正）**：平今/平昨与日终强平按**交易所结算交易日**（21:00夜盘归属下一交易日、凌晨延续归属当日、周五晚归属下周一，由离线 `trade_calendar.next_trade_day` 推节假日），**不沿用** utils 里服务日切的"夜盘开盘日"口径；前晚夜盘开+次日日盘平=平今，已合成断言。
- **撮合**：信号 bar 收盘确认、下一根 bar 开盘成交（无未来函数），入场当根不查止损；止损/止盈预埋停止单，跳空越过以开盘成交、bar内触及以触发价成交、同根双触保守按止损；日内模式交易日最后一根收盘强平（入场当根即末根也强平，不隔夜），摆动模式 `--swing` 允许跨日、最长 `--max-bars`；锁板=前一交易日收盘×(1±品种常态幅度)、整根封死在板价才拦截并顺延，v=0不成交。
- **费用**：开 open、平按 leg 走 today/close 真实券商费率，输出开/平每手人民币（乘数取费率表 multiplier）、"若按平昨"对照额与平今节省/加收合计；滑点单边比例。
- **验证**：15组（T1-T15）零网络合成断言全过（交易日归属/无未来函数/止损止盈/跳空/双触/日终强平/平今平昨/锁板拦截顺延/零量/到期/滑点/SS平今优惠）；compileall 通过；DB integrity ok、分钟库327360根不受影响；全64品种30m端到端 10101 笔交易、18组稳定性网格。
- **诚实结论**：默认参数（入场±1.5、1.2×ATR止损、2.0×ATR止盈、万1滑点）下30m全市场成本后净均收 -0.022%，参数网格方向一致（更严阈值/更宽止损止盈亏损收窄）但无成本后稳定盈利区——**本轮交付的是可信的检验工具与负结果，不粉饰策略有效**；LH/PP/AG/FU 单品种窗口内为正，样本仅约6个月、需随分钟库积累持续复测。1m窗口仅约3个交易日，主序列用5/15/30/60m。
- **仍未做（留给后续）**：实时一档 tick 快照自采表（逼近真实滑点的唯一免费长期路径）；L2逐笔队列（免费天花板，不承诺）。

---

## 5. WP-E：组合资金账户与权益曲线（解决⑦）—— ✅ 第16轮已落地（2026-09-01 端到端验证通过）

### 5.1 新增 `portfolio.py`（零依赖，算法移植 vnpy_portfoliostrategy）

统一账户对象，逐 bar（日或分钟）结算，所有品种共享一个资金池：

```python
class Portfolio:
    equity0          # 初始权益（参数，如100万）
    positions = {sym: {dir, lots, entry_price, entry_date, contract}}
    def on_bar(dt, prices):
        # 1. 浮动盈亏 = Σ dir*(price-entry)*multiplier*lots   （multiplier 已在 fees 表）
        # 2. 保证金占用 = Σ price*multiplier*lots*margin_rate（新增 futures_margins.csv）
        # 3. 可用资金 = 静态权益 + 浮盈 - 保证金占用 - 冻结手续费
        # 4. 风险度 = 保证金占用 / 动态权益；超过强平线按规则砍仓（先砍浮亏最大/保证金占比最高）
        # 5. 记录 dt: 权益/可用/占用/风险度/回撤 -> 权益曲线序列
```

- **手数分配**三种可选（config 切换）：等名义（每品种分配等额名义本金）、等风险（`风险预算/(ATR×乘数)` 定手数，与现有 1.2×ATR 止损天然配套）、按综合分分档加权（|分|越高权重越大，单品种上限 + 板块上限）。
- **强平逻辑**：风险度 ≥ 维持线（如 100%）按优先级强制减仓直到回到安全线；记录强平事件，交易 CSV 标注。
- 保证金率：新增 `data/futures_margins.csv`（sym/交易所保证金率/公司加收/乘数），维护方式完全照搬第 10 轮 `tools/build_fee_table.py` 模式——半自动从交易所参数页（akshare `futures_settle_*`/`futures_contract_info_*` 有 URL）转换、手工校对；**明确标注是估算，不代表期货公司实时保证金**（与现有口径声明一致）。

### 5.2 与回测/监控的关系

- 回测侧：`intraday_backtest`/`backtest` 的交易信号喂给 Portfolio，输出**组合级**净值/年化/回撤/夏普/风险度序列（替代现在报告里"多品种按交易序列复利近似"的临时口径，该口径在 `build_report` 第 460 行已自陈"实盘组合同时持仓时需另做资金权重曲线"）。
- 监控侧（可选，后置）：同一套 Portfolio 类用于"模拟持仓盘"，实时算建议组合的占用与风险度。

### 5.3 可套用代码

- **vnpy/vnpy_portfoliostrategy（MIT）** `backtesting.py`：多合约共享账户的 `calculate_pnl`/逐日盯市、组合净值统计；**vnpy_ctastrategy/backtesting.py** 的 `calculate_cash`（保证金占用、平仓释放、可用资金）。
- 绩效指标（夏普/索提诺/最大回撤/胜率/盈亏比）本项目 `metrics_from_returns` 已有，扩展成资金加权即可。

### 5.4 验收

合成两品种一赚一亏，手算权益/占用/风险度与程序逐分对齐；构造连续跌停使风险度破线，断言强平顺序与释放资金正确；保证金 CSV 缺失时回退"点值估算"并显式标注（不允许静默用错数）。

### 5.5 ✅ 第16轮落地记录（2026-09-01，实测）

- **保证金源（用户指定其开户公司）**：银河期货官网"结算时起各品种最新交易保证金比例"静态表（yhqh.com.cn/col71），取纯字母代码品种基础档+**投机档**，64品种0缺失生成 `data/futures_margins.csv`（另存板幅、乘数、as_of、source）。先做过的国君期货日历表弃用（两家确有差异，如实盘以银河为准：AG 35% vs 30%、RB/HC/SS/SP 12% vs 11% 等）；akshare futures_rule_em 对应的东财 GetPZJYInfo 接口当晚三域名均返回 HTML 壳（接口已变更），不逆向、记录为做不通。乘数银河页不提供，工具内置"每手报价单位个数"口径并与 futures_fees.csv 交叉校验，唯一口径差异 JD 鸡蛋（报价元/500kg→保证金乘数10、费用表按吨5）用白名单显式处理。
- **`portfolio.py`（947行，零新增运行依赖）**：Portfolio 类逐bar盯市（静态权益=期初+已实现、动态权益=静态+浮盈、占用=Σ价×乘数×手数×公司保证金率、风险度=占用/动态权益）；统一时间轴字母序确定性回放，分钟复用 intraday_backtest 的信号/owner/复权函数、日线复用 backtest，**撮合口径与既有两套回测完全一致**（信号i收盘确认i+1开盘成交、入场当根不查止损、同根双触按止损、分钟日终强平、锁板整根封死）。
- **手数分配**：等名义（默认每品种15%）/等风险ATR（1%风险预算÷1.2ATR止损）/按综合分档（轻仓5%·分批10%·强信号15%）；约束链=策略目标手数→单品种名义30%→板块60%→可用资金×0.99→同时持仓≤12，每档拦截都计入"未开仓原因分布"（不静默丢信号）。
- **强平状态机（本轮修正）**：风险度≥强平线(默认100%)触发后，按浮亏最大优先整仓平，**一路砍到安全线(默认80%)以下**（而非刚跌破强平线就停，避免下一根反复触发）；强平后补记同时间戳曲线点；修复"被砍品种当前时刻无bar（无夜盘品种在夜盘）"时平今判定 KeyError——新增 SymbolFeed.owner_at 二分取最近bar的结算交易日。
- **验收**：37条零网络合成断言全过（两品种一赚一亏逐点对齐、连续下跌强平顺序与砍后风险度、缺表回退0.12并登记 fallback、三种sizing手数、板块/可用/持仓上限、平今费、无bar强平）；真实端到端——全64品种30m（140交易日/6099笔/期末758495、-24.15%、平均风险度17.3%、0强平）、全64品种日线250日（341笔/+10.56%/夏普0.84/0强平）；另用强平线15%压测触发2900次强平验证引擎同步与报告链路。结论方向与第15轮一致：分钟高频信号在真实成本后不赚钱（EC集运欧线单品种-7.57万、手续费8.32万），日线动量组合口径为正、且盈亏几乎全来自多头。
- **顺带修复既有公共退化**：`backtest.resolve_codes` 把主连拼成 RB00（meta code "RB0" 又 +"0"），而新浪日K接口对双零返回 `var t=(null)`、只认单零 RB0（多组请求实测），导致日线回测全部"样本不足"；已改为直接用 meta code、自由输入按纯字母补0，修复后日线回测恢复。
- 看板 report.py 由11页签增至13页签（组合账户回测/组合交易CSV）；config.py 新增 PORTFOLIO_* 参数段；main.py --once --no-launch 冒烟 exit 0。

---

## 6. 实施顺序、轮次与产出建议

| 轮次 | 内容 | 依赖 | 预计新增代码量 |
| --- | --- | --- | --- |
| 第11轮 | WP-A 期权链+PCR（接口已实测通）+ WP-C 期限结构（纯组装现成数据） | 无 | ~300 行，1 新模块 + 4 处接线 |
| 第12轮 ✅ | WP-B IV 曲面 + 日历价差（已落地，实测~260行新模块+5处接线） | 11轮 | ~260 行 |
| 第13轮 ✅ | WP-C 基本面数据包（东财库存仓单+龙虎榜TYPE=2+carry入分+生意社基差软降级，已落地，实测新增2模块339行+5处接线） | 无 | 实际~590行(含接线) |
| 第14轮 ✅ | WP-D0 分钟库（已落地，实测选型调整为**新浪主连为主+东财补1m+通达信可选冗余**，详见《数据源选型与通达信替换可行性分析.md》；新增 tdx_bars.py、重写 intraday_bars.py、第8张表 minute_bars、回填261888根） | 无 | 实际~700行(2新模块+接线) |
| 第15轮 ✅ | WP-D1/D2 日内回测：平今路径 + vnpy 式 bar 撮合 + 精确锁板（已落地，intraday_backtest.py 649行） | 14轮 | 实际~650行 |
| 第16轮 ✅ | WP-E 组合账户/权益曲线/强平 + 银河保证金表工具（已落地，portfolio.py 947行+build_margin_table.py 184行+37断言，修复backtest双零退化、看板+2页签） | 15轮 | 实际~1150行(含工具/接线) |

每轮沿用既有纪律：compileall 全过 → 合成断言（fixture 用本次实测真实响应片段）→ 真实 `main.py --once --no-launch` exit 0 → 临时验证脚本删除 → 同步更新 README、本方案勾选状态、上下文摘要。

## 7. 能力天花板（必须在报告/代码注释里诚实声明，不夸大）

1. **历史逐笔/L2 盘口免费不可得**：分钟回测的成交是 bar 内规则假设（开盘成交/高低价触及/参与率限量），不是真实逐笔队列；实时盘口只能靠程序自采积累。
2. **分钟历史深度有限**：东财按具体合约给其上市以来数据、tqsdk 免费 8000 根/近 1 年，长期分钟库最终靠常驻自采；主连分钟拼接处的复权是比例近似。
3. **tqsdk 程序化回测/批量下载是专业版**，免费版只做实时/模拟与有限历史，适配器必须检测授权、缺了就降级，不能在主链路硬依赖。
4. **基本面日频数据有口径差异**：生意社/99期货/交易所口径不同，基差/库存数值标注来源；龙虎榜是席位聚合、不等于机构全部持仓。
5. **保证金率是估算**：交易所基准 + 公司加收随时调整，`futures_margins.csv` 与费率表一样需要定期重跑工具更新。
6. IV 曲面在流动性差的远月/深虚值腿上报价不可信（买一卖一为 0），反推前必须过滤，缺腿不插值编造。

---

## 附录 A：2026-09-01 本机实测记录（curl 原始证据摘要）

1. 新浪铜期权链（SHFE，8 字段）：
   `getOptionData?product=cu_o&exchange=shfe&pinzhong=cu2610` → `result.data.up/down` 全行权价腿，示例 `["2","12068","13576","13662","2","633",7.07,"96000","cu2610C96000"]`（第 6 值=持仓量 633）。
2. 新浪豆粕期权链（DCE，7 字段，行权价仅在代码中）：
   `product=m_o&exchange=dce&pinzhong=m2609` → down 示例 `["0","0","0.5","0.5","1243","5514",0,"m2609P2500"]`。
3. 东财分钟 K（具体合约，无主力连续）：
   `secid=113.RB2610` klt=1/5/15/30/60 均返回 klines；顺序为 `时间,开,收,高,低,量,额`；`113.rbm/rb888` 等主力代码返回 data:null；市场 114=M2609 验证通过；klt=101 日线、102 周、103 月。

## 附录 B：可直接套用的开源代码清单（均为宽松许可）

| 仓库（License） | 具体文件/函数 | 搬什么 |
| --- | --- | --- |
| akfamily/akshare（MIT） | `option/option_sina.py` | 新浪期权链 up/down 解析、合约代码组织 |
| 同上 | `option/option_dce.py`、`option_shfe.py`、`option_vol_*.py` | 交易所期权日行情/官方 IV 备用源 URL |
| 同上 | `futures/futures_spot_price.py`、`spot_price_qh`、`futures_warehouse_receipt*.py`、`futures_inventory_99/em.py`、`futures_*_position_rank.py`、`get_roll_yield` | 基差/仓单/库存/龙虎榜/展期收益的请求 URL 与表格解析 |
| vnpy/vnpy_ctastrategy（MIT） | `backtesting.py`: `cross_limit_order`、`calculate_commission`、`calculate_pnl`、`update_daily_close` | bar 内撮合、平今分别计费、逐笔盈亏、逐日权益 |
| vnpy/vnpy_portfoliostrategy（MIT） | `backtesting.py` | 多合约共享资金账户、组合净值 |
| shinnytech/tqsdk-python（Apache-2.0，免费版） | `get_kline_serial`/`get_tick_serial` | 可选分钟/tick 数据源适配器 |
| zhouxinhao19/TradingAgent-Future | 基差/库存/期限结构模块 | 基本面因子组织与数据源降级链（第5轮对标已记录） |
| vollib/py_vollib（MIT） | `implied_volatility` | IV 数值反推迭代思路（建议自写二分，零依赖） |
